# type(scope): summary   — imperative, lowercase, ≤72 chars, no trailing period
#
# Body: WHAT changed and especially WHY (wrap ~72 cols). Bullets fine.
#
# Footer: BREAKING CHANGE: <desc>  ·  Refs #<issue>  ·  trailers
#
# ── Conventional Commits ──────────────────────────────────────────────────
#   type:  feat | fix | docs | refactor | perf | test | build | ci | chore | revert
#   scope: optional area — pnl · brief · risk · db · constants · scanner · ui
#   e.g.   feat(pnl): add Tier B true day-over-day P&L
#          fix(brief): split macro-blocked names out of the funnel caption
#          docs(architecture): record the fragility gauge
#
# ── DRISHTA rules ──────────────────────────────────────────────────────────
#   • Threshold/gate changes (stock_analyzer/constants.py) are investment-policy
#     decisions — name the constant + old→new value in the body.
#   • Claude-authored commits end with:
#       Co-Authored-By: Ajay with <claude model> <ajay.x.ku@accenture.com>
#     (use the actual model from the session — Sonnet 5, Opus 4.8, etc.)
#
# ── Enforced by .claude/hooks/pre_tool_checks.py (it will BLOCK the commit) ──
#   • Touching constants / a gate / a scoring formula / a DB-write path
#     (db.py, cron_runner.py, system_health.py, …) requires:
#       Review = Opus reviewer (<resolved model>): SHIP|FIX-FIRST, N blocking; <notes>
#     e.g. Review = Opus reviewer (Opus 5): SHIP, N blocking; sentinels verified
#     Copy the model from the reviewer's own MODEL: line — don't guess a version.
#     NOTE: the example above deliberately says "N blocking", not a digit. `-F`
#     does NOT strip these # lines (git's cleanup=whitespace), so a real digit
#     here would make a template-seeded message satisfy the gate by itself.
#
#   • Any `feat(` commit requires BOTH provenance trailers:
#       Design = planner (Opus 5): <verdict>   |   Design = lead — <why no planner>
#       Build  = implementer (Sonnet 5)        |   Build  = lead — <why no implementer>
#     "lead" is an accepted answer — the point is a deliberate, recorded statement.
