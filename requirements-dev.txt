# Dev/test-only dependencies — NOT installed on Streamlit Cloud or Railway.
# Install alongside requirements.txt for local test runs / CI:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0,<6.0
