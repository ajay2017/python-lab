[pytest]
testpaths = tests
python_files = test_*.py
markers =
    fast: pure-logic tests whose module never transitively imports
        stock_analyzer.bundle_loader (see scripts/analyze_test_deps.py and
        docs/test-dependency-graph.md). Use `pytest -m fast` for quick
        iteration; the full suite is still what CI/the commit hook run.
