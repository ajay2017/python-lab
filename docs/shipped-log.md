# Shipped feature log — DRISHTA

Full changelog of shipped features, moved out of [CLAUDE.md](../CLAUDE.md) on 2026-07-22 to stop it from being injected into every session's context (it had grown to ~76KB, ~86% of that file). Nothing was deleted — this is a verbatim relocation of CLAUDE.md's old "Recently shipped" list.

**Use this file for:** the Definition-of-Done check ("did we already ship something like this?" — grep this file before starting new work), or when you need the full history behind a feature. It is not auto-loaded — `Read`/`Grep` it on demand.

Cross-reference: many entries here have a fuller, dedicated memory file (see `MEMORY.md` in the Claude auto-memory directory) — check there too for anything tagged `memory project_*`.

---

**Recently shipped (do not re-chase):**
- **Sector Gaps pointer + widened diversifier-sector coverage (F-222; 2026-07-30; `stock_analyzer/portfolio.py`, `app.py`, `docs/requirements.md`; plan `C:\Users\ajay.x.ku\.claude\plans\hashed-wiggling-key.md`; memory `project_sector_diversification_gaps`).** User reviewing 🥧 Portfolio Overview noticed the Sector Exposure chart (F-07) only ever shows sectors currently held — no visibility into sectors at 0%, a real diversification blind spot. Investigation found the gap half-solved already: `diversification_recommendations()` computed zero-holding "ADD" recs, but only for 5 of the app's 13 curated thematic sector labels (Healthcare/Energy/Defense/Financials/Clean Energy), and those ADD cards only ever rendered on 📡 Signals & Advice's Diversification Advisor tab (F-13a) — nowhere near where the user was actually looking. Two changes: (1) a new "🧭 Sector Gaps" awareness-pointer section on Portfolio Overview, reading the already-published `_div_recs_cache` (no recompute) and listing `ADD`-type recs with a nav button to the full Advisor — deliberately a pointer, not a duplicate card, per single-surface-priority; (2) widened `_DIVERSIFYING_SECTORS`/`_SECTOR_PROFILES` coverage to all 13 labels, a policy call made with the user in the same session — **added** Communications (corr 0.20), EV & Auto (corr 0.40), Enterprise Tech (corr 0.45) as genuine diversifiers; **scored but kept excluded** Semiconductors and Cybersecurity (corr 0.65 each, same AI/compute/SaaS cycle as the already-excluded AI & Cloud/AI & Data cluster) — plus new candidate rosters/discovery-bucket mappings so the 3 newly-included sectors can actually surface an ADD rec. Also fixed a misleading pre-existing caption ("Avg correlation to your existing book" → "Typical correlation to a growth/tech-heavy portfolio" — the number was always a static table value, never computed from the user's actual holdings). No new `constants.py` entries. A separate "true benchmark vs. S&P 500/GICS sector weights" idea was explicitly discussed and **parked** (no such data exists in the codebase; a bigger, separate build) — not part of this ship. Opus pre-ship code review: **SHIP, 0 blocking** (verified the 3 new sectors' candidate pools resolve non-empty, no ticker/sector-label collisions, no new hardcoded gate threshold — the `_SECTOR_PROFILES` corr values are display-only metadata, and the `_DIVERSIFYING_SECTORS` list-membership is the only real policy gate).
- **Cross-feature reduce-call coordination fix — Orphan Conviction + Grow Today/Buy Candidates add-to-winner (F-194, F-26c; 2026-07-30; commit `be752a5`; `stock_analyzer/decision_bucket.py`, `stock_analyzer/daily_briefing.py`, `app.py`, `tests/test_decision_bucket.py`, `tests/test_daily_briefing.py`, `docs/architecture.md`, `docs/requirements.md`; plan `C:\Users\ajay.x.ku\.claude\plans\mossy-purring-eclipse.md`; memory `project_investor_mirror`, `feedback_cross_feature_coordination`).** Found via a live incident: My Edge's "🌱 Orphan Conviction" flagged BKNG as a high-conviction, undersized "size up" candidate with zero awareness of the Daily Brief's Act Today reduce calls. The user bought 5 more shares on that cue, which itself pushed BKNG's weight over the earnings-overweight-trim threshold, so Act Today then asked to trim the same position back down days later — two advisors giving opposite directives on the same ticker in the same week with no cross-check between them. **The canonical fix infrastructure already existed** for an identical purpose: `decision_bucket.reduce_call_items()`/`_reduce_calls`, already correctly consumed by the Analysis page ("Under a Reduce/Exit call — not a place to add") and Overview's Opportunity Signals lane — Orphan Conviction simply never read it. **User explicitly asked to survey for more instances of the same root cause before shipping a one-off fix** (rather than "just fixing this one off"); the survey found two more independent instances live on Home itself: `_grow_today()`'s `add_positions` bucket and `_buy_candidates()`'s own separate `add_winner` block (`daily_briefing.py`) both cross-checked ONLY `act_today` via a naive `.get("ticker")` loop, never `review_list` — so a **review-origin** reduce call (earnings-overweight-trim and weak-large-position trim are BOTH review-origin `TRIM_TO_TARGET` items) never suppressed either add-to-winner surface, meaning a same-day "add to X" could sit right next to a same-week "trim X" with no check at all. **Fix:** two new pure functions in `decision_bucket.py` — `all_flagged_tickers(act_today, review_list)` (the canonical broad "any card, either lane" set, promoted from an inline computation `build_daily_briefing()` already used for the 2026-07-29 audit H6 risk-off exclusion, now refactored to call the shared helper) and `suppress_orphans_under_reduce_call(orphans, reduce_calls)` (partitions Orphan Conviction candidates into actionable/suppressed — suppressed names render a visible "⛔ excluded" banner naming the active reduce call, never a silent filter, per the CLAUDE.md UI-suppression rule). `_grow_today()` and `_buy_candidates()` both gained an optional `review_list` parameter and now build `_act_blocked` via the shared helper; My Edge's two Investor Mirror call sites (the eager top-of-page publish block feeding `_mirror_orphans`, and the Conviction Alignment tab's own render) both filter through the new suppression function against `st.session_state["_reduce_calls"]`. Filtering at the publish site also fixes a second, independently-found contradiction on the Analysis page: its "🪞 Investor Mirror — Orphan Conviction... sizing opportunity" banner reads `_mirror_orphans` unconditionally, with no reduce-call check of its own, and could show right next to the page's own "Under a Reduce/Exit call" banner for the same ticker. **Opus pre-commit review: FIX-FIRST (1 blocking) → fixed → SHIP, 0 blocking.** The blocking finding: the new `docs/requirements.md` row was mistakenly labeled F-26b, colliding with an existing F-26b (the offense/defense layout row) — renumbered to F-26c. Non-blocking, also fixed: a misleading "None — high-conviction names are well-sized" caption when ALL orphans were suppressed rather than genuinely well-sized (now "None currently actionable — see excluded names below" in that case). Non-blocking, deliberately left as-is: Orphan Conviction suppresses on the NARROW reduce-only `_reduce_calls` set while Grow Today/Buy Candidates suppress on the BROAD `all_flagged_tickers` set (a WATCH-only card also blocks an add there, per H6) — judged defensible since Orphan's suppression banner needs a genuine reduce-call reason object to render, and the actual BKNG incident (a TRIM_TO_TARGET reduce) is fully caught by the narrow set. 9 new tests in `test_decision_bucket.py`, 4 new/extended in `test_daily_briefing.py`; full suite (2903 tests) passing, zero regressions. No new `constants.py` entries — this is a pure ticker-membership coordination fix, not a threshold change. **General lesson for this codebase (recorded in memory): a "size up"/"add" recommendation surface must check the canonical `decision_bucket` reduce-call helpers, not invent its own narrower or absent exclusion set — this was the third independently-duplicated instance of exactly that mistake found in one sitting.**
- **Thesis Red Team Agent — Phase 3: Daily Brief "Thesis Under Pressure" annotation (F-196; 2026-07-28; commit `0aa1349`; `app.py`, `stock_analyzer/constants.py`, `docs/architecture.md`; plan `docs/plans/thesis-red-team-agent.md`; memory `project_agentic_intelligence_roadmap`).** Closes the Thesis Red Team Agent feature — all 3 phases now shipped. New annotation at the end of 🏠 Home's Daily Brief (after the Buy Candidates overflow section, before Evening Debrief): for any held ticker where today's erosion score meets `THESIS_EROSION_BRIEF_MIN` (50) and its baseline was below that floor, OR the score jumped `THESIS_EROSION_BRIEF_JUMP` (15)+ points since baseline. **Baseline = the most recent PRIOR trading day with an actual scored `thesis_erosion_cache` row** — walked back up to a new constant `THESIS_EROSION_BASELINE_LOOKBACK_DAYS` (10) calendar days — deliberately NOT literal calendar-yesterday, since a cache row only exists on a day the user visited the Red Team tab. No row found in that window ⇒ first-ever observation, gated on the absolute floor alone. **Pure cache read, never triggers a compute** (that only happens on the Red Team tab itself), so the annotation is usually empty on the day's first Home load. **Deduped against tickers already shown as a deterioration/Act card in the same Brief run**, via `decision_bucket._ticker()`'s canonical resolver rather than a raw `ticker` key — erosion score's tier component comes from the same `exit_signals` detector that fires those cards, so re-flagging an already WATCH/TRIM/EXIT ticker would restate, not add, information. **Opus plan review: FIX-FIRST (2 blocking) → fixed → SHIP.** Round 1 caught that a literal calendar-yesterday baseline would silently re-fire every Monday for every stable ≥50 name (Sunday never has a cache row, so the absolute-floor fallback fired weekly regardless of real change — a stable-state-noise regression against the app's calm-advisor posture) and the deterioration-card double-surface (erosion score's tier component is circular with those cards, the same concern Phase 2's Surface 2 already worked around by hiding its own score chip). Round 2 (re-review of the fix) caught that the fix's own dedup set read the raw `ticker` key — missing macro `PROTECTIVE_TRIM`/`WATCH` items, which carry `ticker=None` with the real subject in `action["trim_ticker"]` — and a hardcoded `range(10)` lookback that should be a named policy constant per Hard Rule #1; both fixed (canonical `decision_bucket._ticker()` resolver; new `THESIS_EROSION_BASELINE_LOOKBACK_DAYS` constant, documented in `docs/architecture.md`, `check_constants_documented.py` passing). Final Opus review: SHIP, 0 blocking. Full pytest suite (1160 tests) passing throughout. **Lesson for any future day-over-day baseline in this codebase: never assume "yesterday" has a row just because it was a trading day — a cache populated by user-triggered visits (not a cron) needs an explicit "most recent day that actually has data" walk-back, or a weekly/sporadic-visit cadence will manufacture false "new" events out of stale-but-unchanged state.**
- **Entry Timing tab — Phase 1 (F-220; 2026-07-28; commits `34f4782`, `eea519e`, `29540d1`; `stock_analyzer/predictive_analytics.py`, `app.py`, `tests/test_predictive_analytics_entry_timing.py` (new); plan `docs/plans/entry-timing-tab.md`; memory `project_entry_timing_tab`).** New "⏱️ Entry Timing" 6th tab on 📊 Predictive Analytics — diagnostic-only, never feeds back into the composite score or the 5-gate pipeline. Answers: does momentum running far ahead of the composite consensus at the moment a `new_pick` fires predict a rough first few days, even when the pick is right over its real horizon (motivated by AMD firing 5x in 2 weeks in 2026-07, composite 69-71/momentum 94-100, losing ~9-11% every time)? Five new pure functions in `predictive_analytics.py`: `dedupe_repeated_tickers()`, `divergence_at_entry()`, `forward_alpha_at_horizon()` (live per-ticker forward-close fetch via an injectable fetcher, defaulting to `providers.orchestrator.get_historical_close`), `by_divergence_band()` (Aligned/Diverging/Extreme bands via new provisional constants `ENTRY_TIMING_DEDUP_WINDOW_DAYS`=5, `ENTRY_TIMING_DIVERGENCE_ALIGNED_MAX`=15, `ENTRY_TIMING_DIVERGENCE_DIVERGING_MAX`=25), `find_illustrating_case()` (surfaces whichever ticker is CURRENTLY the most-repeated same-ticker Extreme-divergence re-firer in the live data — never a fixed historical citation, per explicit user decision; returns `None`, never fabricated, when no ticker qualifies). `synthesize_directives()` gains an `entry_timing_bands` kwarg emitting a single `caution` (never `action`) directive. Button-gated (`st.session_state["_entry_timing_cache"]`) since the per-ticker forward-price fetches are heavier than the page's shared load. **Opus pre-ship review: FIX-FIRST (2 blocking) → fixed → SHIP** — Round 1 caught a `NameError` crash on every "Analyze" click (`_pac_spy_by_date` was only built on the page's cache-miss branch, but the new tab referenced it unconditionally; fixed by hoisting the SPY-dict build to run on every page load) and a stale "trading days" comment on `ENTRY_TIMING_DEDUP_WINDOW_DAYS` that contradicted the actual calendar-day implementation. **Two live-screenshot-driven follow-up fixes the same day, both caught by the user comparing the deployed result against real expectations rather than a verbal description** (direct application of `feedback_mockup_first_ux`'s principle): (1) the initial build's chart/stat-card presentation had drifted from a mockup approved in an earlier, now-`/clear`ed session — the mockup was never saved to the repo, so this session had no way to retrieve it; reworked to color bars by alpha sign (not horizon), move stat cards to a bordered right-hand column with a rule-based `band_narrative()` sentence per band, and add the dynamic illustrating-case callout; (2) `band_narrative()` had a real logic bug where a Day+1 alpha that rounded to `-0.0` (a rounding artifact) fell through every negative-Day+1 branch and produced a wrong "not enough Day+20 history" message even though a severe Day+20 loss (-14pp) was present and computed — fixed with a dedicated "looks calm early, fades hard by Day+20" branch, checked first, requiring BOTH Day+1 and Day+5 to be genuinely calm (not just Day+1) so a monotonically-worsening pick isn't mislabeled as a "surprise late loss." 30 tests in the new test file, full suite passing throughout. **Takeaway for future sessions: any approved UI mockup must be saved into the repo (e.g. `docs/plans/` or a scratch image committed alongside the plan), not left to live only in a conversation that a later `/clear` can erase.**
- **Entry Timing tab — Phase 2 (F-220; 2026-07-28; commit `de12323`; `stock_analyzer/daily_briefing.py`, `stock_analyzer/predictive_analytics.py`, `app.py`, `tests/test_predictive_analytics_entry_timing.py`; plan `docs/plans/entry-timing-tab.md`; memory `project_entry_timing_tab`).** Built the same day as Phase 1, once the user confirmed the plan's own explicit gate ("validate the divergence pattern over a real sample, not a date") was cleared: the Extreme band showed n=20 matured Day+20 outcomes averaging -14.2% alpha vs SPY, not the 6-row anecdote that motivated the plan. A live caution annotation directly on the Grow Today "New Positions to Initiate" pick card — the only mechanism with a plausible causal path to actually preventing a loss, since Phase 1's retrospective tab requires the user to remember to check it. `daily_briefing.py::_grow_today()` attaches `pick["divergence"]` (reusing Phase 1's `divergence_at_entry()` as-is — single source of truth, no duplicated subtraction logic) right after the composite/fundamentals/staleness/macro/sector gates all pass, covering both curated and mover new_picks (never `add_winner`, matching Phase 1's scope). `app.py`'s Grow Today render shows a caption only when `divergence > ENTRY_TIMING_DIVERGENCE_DIVERGING_MAX` (the existing, already-reviewed Extreme-band threshold — zero new constants). Deliberately does not quote a live historical alpha % in the caption — that would require re-running Phase 1's per-ticker forward-close fetch on every Daily Brief build, defeating the reason that tab is button-gated — instead points to ⏱️ Entry Timing for current numbers. **Stays a caution, never a block/gate** — the pick has already cleared every gate by the point this attaches. **Opus pre-code design review: FIX-FIRST (1 blocking) → fixed → SHIP** — traced the integration point (safe: `pick` is never persisted by key-spread, `db.save_recommendations` builds an explicit payload), confirmed no circular import (`predictive_analytics.py` stays streamlit-free), confirmed Extreme-only scoping is correct (Diverging band's effect was ~10x weaker), and confirmed no coordination gap with the existing signal-reconciliation badge. The one blocking finding: the first caption draft claimed high-divergence picks "trade rougher in the first few days" — backwards from the validated shape (calm early, damage by Day+20); reworded to match. **The same review caught a live bug in Phase 1's own already-shipped `synthesize_directives()` caution text**, which carried the identical inversion ("open red on Day+1... though the effect fades by maturity") — fixed in this same commit, regated from `day1_n`/`day1_pct_red` to `day20_n`/`day20_alpha` (the horizon the directive actually describes), since that was the field the wrong claim was riding on. 4 new regression tests pin the corrected directive logic (including a case asserting the OLD gate would have wrongly fired on a thin Day+20 sample). **Closes F-220 — both phases of the Entry Timing tab now shipped.**
- **Thesis Red Team Agent — Phase 2: Haiku counter-evidence + Pre-Mortem loop (F-196; 2026-07-27; commit `78a6adc`; `stock_analyzer/thesis_red_team.py`, `app.py`, `tests/test_thesis_red_team.py`; plan `docs/plans/thesis-red-team-phase2.md`; memory `project_agentic_intelligence_roadmap`).** Adds a Haiku-generated counter-evidence narrative (`generate_counter_evidence()`, 0-3 validated `{claim, severity, signal_basis}` items) on top of Phase 1's quantitative erosion score (shipped 2026-07-23), triggered only when `erosion_score >= THESIS_EROSION_HAIKU_MIN` (30, already in `constants.py`) and a `user_thesis` is on record. Closes the loop with the Pre-Mortem Protocol (F-187): `trades.premortem_commitment`, read from the same most-recent-BUY-with-a-thesis row as `user_thesis`, is passed as additional Haiku context and the model is instructed to quote it back explicitly when current evidence supports it. Second surface: a read-only "⚠️ Red Team — since you bought" expander on 🏠 Home's Act Today deterioration TRIM/EXIT cards (`_render_act_card`), sitting alongside the existing "⚔️ Challenge This Exit" debate button — a pure `thesis_erosion_cache` read, no compute triggered from that site. **Built ~3 days ahead of the original Phase 1 plan's own `~2026-07-30` production-observation gate, per explicit user decision** — the same forward-only precedent as Multi-Agent Debate Phase 3. The observation window's actual purpose (confirming the erosion score's distribution stayed directionally sensible) was already independently satisfied by Phase 1's own same-day production validation (FSLR 44/Softening, TEAM 38/Softening, both already above the 30 trigger threshold) — nothing in Phase 2's own correctness depended on the calendar buffer elapsing. **Deliberately excludes the erosion score/label and the still-hardcoded PT-revision placeholder from the Haiku prompt** — `compute_erosion_score()` always bakes the PT placeholder (`pt_pts=7.0` until `analyst_target_snapshots` accumulates) into the aggregate, so handing the model the score/label would launder synthetic weight back in as if it were real evidence; the counter-evidence is built only from primitive, individually-real signals (deterioration tier, RS vs SPY, composite delta, price/entry price/position age). **Every one of those four numeric values is coerced through a `math.isfinite()` check at the call site before reaching the prompt** — their real producers (an un-dropna'd trailing-NaN Close bar, a malformed BUY row's price cell, a NaN enriched Score, a zero-denominator RS division) can each fail via a non-`None` `NaN`/`inf` rather than a clean exception or `None`, and would otherwise reach the prompt as literal `"$nan"`/`"+infpp"` text if only `is None` were checked. The trade-date parse (`date.fromisoformat()`, for position age) is wrapped in `try/except (ValueError, TypeError)` for the identical reason — a null/`NaT` `traded_at` stringifies to a truthy `"NaT"`, which passes a naive truthiness guard and then raises unguarded. **An empty counter-evidence result (`[]`) is a valid, cacheable "nothing grounded today" outcome, explicitly distinct from `None`** (the call was never attempted, or failed) — every call site and both render sites test `is not None`, never truthiness, since `[]` is falsy but valid. **6 Opus design-review rounds** (`docs/plans/thesis-red-team-phase2.md`) — 5 of them found a real, blocking issue, 4 of which were variants of the exact same bug class (a value assumed clean that could actually be `None`, `NaN`, `inf`, or a truthy-but-garbage string) surfacing on a different signal each round: Round 1/2 caught the PT/erosion-score laundering above; Round 3 caught an unguarded `None` for `price`; Round 4 caught the unguarded `NaT`-string date-parse crash plus unguarded `NaN` for price/entry-price/composite-delta; Round 5 caught the identical `NaN`/`inf` gap on relative strength, after Round 4 had incorrectly asserted it was already clean. Round 6: SHIP, 0 blocking. New `tests/test_thesis_red_team.py` (30 tests) backfills `compute_erosion_score()`'s previously-zero Phase 1 coverage and pins the Phase 2 contract, including a regression test asserting the word "erosion" never appears in the built prompt (guards the score-laundering class directly). Opus pre-ship code review: SHIP, 0 blocking. **Closes the Thesis Red Team Agent feature (Phase 3, a Daily Brief annotation, remains a distinct future item, not gated on anything from this roadmap) and — since this was the last of the three originally-tracked Agentic Intelligence Roadmap gated phases — closes the entire Agentic Intelligence Roadmap (v1 + v2) with nothing left open.**
- **Multi-Agent Debate — Phase 3: Debate Log (F-197; 2026-07-27; commit `3efeedd`; `stock_analyzer/db.py`, `app.py`; plan `docs/plans/multi-agent-debate-phase3.md`; memory `project_agentic_intelligence_roadmap`).** Closes the entire Multi-Agent Debate feature and the last of the three originally-tracked Agentic Intelligence Roadmap gated phases (Phase 1 entry debates shipped 2026-07-23, Phase 2 exit debates shipped 2026-07-24). New "⚔️ Debate Log" 6th tab on 🧠 AI Insights, read-only and diagnostic, showing every stored `debate_cache` row (both `entry` and `exit` types) chronologically via new `db.load_all_debates()` (most-recent-first, `debate_date` then `created_at` as a same-day tiebreak — added specifically because same-day collisions across tickers/types have no defined Supabase tie order otherwise; excludes `corpus_snapshot`, not needed for display). **Built forward-only per an explicit user decision:** `debate_cache` held only ~4 rows (all `entry`-type, zero `exit`-type) at build time — Phase 3's own gate ("Phase 2 stable AND enough debate history to be worth showing") is a data-sufficiency judgment call, not a date, and the user confirmed shipping now with an honest "no debates yet" empty state rather than waiting, the same precedent as Behavioral Fingerprint (F-193). **2-round Opus plan review caught two real bugs in the render-extraction sketch before any code was written**, both fixed in the plan and verified faithful by line-by-line comparison against the original code once built (`app.py` has zero pytest coverage by design, so this extraction's correctness rests on that manual parity check): Round 1 — the sketch silently dropped the exit card's calm-advisor disclaimer ("this debate challenges the exit signal — it does not change it"), which would have been a real §2B posture regression; fixed by restoring it inside the new `_render_debate_result()`, gated on `debate_type == "exit"`. Also caught: the sketch tried to both render the debate body AND return the verdict icon for the caller's expander title, but Streamlit requires the title string before the expander body renders — a function that returns its value only after rendering is unusable at either existing call site; fixed by splitting into a pure `_debate_verdict_icon(verdict, debate_type)` helper (used by all three render sites for the title) and a body-only `_render_debate_result(row, debate_type)`. Both existing sites (entry "⚔️ Debate" on Grow Today, exit "⚔️ Challenge This Exit" on deterioration cards) were refactored to call the shared helpers instead of keeping a third independent copy — deliberately chosen over triplication, since a third un-linked copy would be exactly the "duplicated logic drifts" class of risk this house has already been bitten by (D1's `.iloc[-1]` bug, the verdict-divergence header/card mismatch), with no test to ever catch the drift. No filter/search UI or aggregate debate-history chart in this phase — the original 2026-07-23 plan's own "What NOT to build" section already deferred a historical chart to "Phase 3+," and a filter UI has nothing to filter at today's volume; both are natural follow-ups once real usage accumulates, not built pre-emptively. No new `constants.py` entries, no new cache table, no new LLM call — the cheapest class of addition on this whole roadmap. 204/204 tests unaffected (no test changes needed; this is a pure `app.py` UI addition/refactor plus one new `db.py` reader). Opus pre-ship code review: SHIP, 0 blocking (independently verified both refactored sites via line-by-line comparison against the original inline blocks, confirmed no leftover dead code or variable collisions, confirmed def-order compliance and `db.py` convention adherence).
- **Structural Vulnerability Scanner — Phase 2 (F-198/F-218; 2026-07-27; commit `ce9282b`; `stock_analyzer/structural_scanner.py`, `stock_analyzer/db.py`, `app.py`, `tests/test_structural_scanner.py` (new); plan `docs/plans/structural-scanner-phase2.md`; memory `project_agentic_intelligence_roadmap`).** Closes the last open phase of P3 (Agentic Intelligence Roadmap v1) — Phase 1 shipped 2026-07-24 gated on "≥3 days production observation" AND "≥2 days of `structural_scan_cache` history"; both confirmed cleared same day the user asked to proceed (rows verified directly in Supabase on 2026-07-24 and 2026-07-27, via a user-run SQL query, not assumed). New 🏠 Home "🧬 Structural alert" banner flags a genuinely new correlation cluster among held positions since the last 🧬 Structural Scan. `stock_analyzer/structural_scanner.py::detect_new_clusters()` diffs today's live `correlation_clusters()` output against `db.load_structural_scan_baseline()`'s most recent scan **on or before** today (new function; deliberately `.lte`, not `.lt` — the Round-1-caught bug: strict-before-today would never resolve to today's own scan once it exists, so the banner would keep flagging already-reviewed clusters and misdate its own copy for the rest of the day). A cluster is flagged only when it contains a ticker pair that (a) wasn't already co-clustered in the baseline snapshot and (b) is a **verified direct positive edge today** (`corr ≥ CORR_HIGH_PAIRS_THRESHOLD`, signed — matching `correlation_clusters()`'s own edge condition exactly) — pair-level, not whole-cluster-set, comparison, so a cluster merely losing a member (decorrelation) is never flagged. A baseline of `None` (no scan has ever run) suppresses the feature entirely; an empty list `[]` (a real scan found zero clusters) does **not** suppress — every one of today's real clusters then flags as new, the cleanest possible zero-to-some formation signal. Zero new data fetches, zero new LLM calls, zero new `constants.py` entries, zero new table (reuses the existing `structural_scan_cache`). New coordination-pattern cache key `_structural_alert_cache` (`None`=offline / `[]`=checked-empty / list=flagged, added to CLAUDE.md's cache-key inventory). Placed in `app.py` after the Home page's `_home_synth_cache` hit/miss synthesis converges (`_load_slot.empty()`), not next to the earlier Day Shock banner — `corr_df`/`_corr_df_cache` aren't freshly published for the current render until that synthesis block completes, so placing it earlier would read the previous render's stale value. **3-round Opus plan review caught two real fabrication-risk bugs before any code was written**, both now pinned as regression tests in new `tests/test_structural_scanner.py` (which also backfills `blast_radius()`, Phase 1's own function, previously zero coverage since 2026-07-24): Round 1 — citing a transitively-linked pair (e.g. A-C, connected only via a shared B) as a "new correlation pairing" would be a fabricated direct-co-movement claim never actually checked; fixed by only citing pairs verified as a direct edge in TODAY's `corr_df`. Round 2 — the first fix used `abs(corr) >= threshold`, which let an **anti-correlated** pair through as a "new pairing" — same fabrication class, different mechanism (`correlation_clusters()` itself only forms edges on signed positive correlation); fixed to the signed check. 19 new tests, 204 total, all passing. Opus pre-ship code review: SHIP, 0 blocking (independently verified the scope/convergence-point claims in `app.py`, the signed-edge fix, the `.lte` self-clear, and that the tests assert what they claim). **Diagnostic only — never gates or modifies the composite score.** Closes the Structural Vulnerability Scanner feature (both phases now shipped) and removes it from CLAUDE.md's "What's queued" — only Thesis Red Team Phase 2 and Multi-Agent Debate Phase 3 remain open from the whole Agentic Intelligence Roadmap.
- **Weekly debrief email — NaN-return fix + capital-flow contamination flag (2026-07-27; commits `374a7d5`, `fc3b164`; `stock_analyzer/debrief_advisor.py`, `stock_analyzer/notify.py`, `cron_runner.py`, `docs/requirements.md` F-152).** Two related production fixes to the weekly debrief email (distinct from the Summary *page*'s Alpha vs SPY tile, which got its own equivalent fixes the same week — `48ef8b1`/`374a7d5` are the email-side counterparts). **`374a7d5`:** the email showed literal "S&P 500 +nan%" / "Alpha +nan%" instead of N/A — traced to yfinance's SPY fetch returning a ≥2-row frame with a NaN `Close` (holiday gap / upstream glitch) that survives as `float('nan')`, not `None`, skipping every "is None" guard downstream and printing as the literal string "nan" in both the LLM prompt formatter and the email tile renderer. `build_debrief_package()` now sanitizes `spy_week_pct` to `None` at the one place both callers (cron + on-demand) funnel through; `_pct()`/`notify.py`'s `_pct_cell()` treat NaN as `None` too, as defense-in-depth. **`fc3b164`:** the email's KPI tiles showed a plain equity-value delta with the buys/sells caveat buried in a footnote — not enough to stop the number being read at face value, the same finding already fixed on the Summary page's Alpha vs SPY tile (`48ef8b1`). `render_debrief_email()` now takes a `week_had_trades` flag (the same value `debrief_advisor.py` already computes for the LLM narrative's own caveat) and renders an always-visible amber caption under the tiles when true; `cron_runner.py` threads `package['week_had_trades']` through at the one call site. Both display-only; no constants/gates/scoring touched, no Opus review required per CLAUDE.md hard rule #4.
- **Railway Cloud pilot — deploy config + password-gate hardening (2026-07-24; commits `1c879be`, `3310a61`, `78318a1`; `app.py`, `railway.toml`, `railway_start.sh`; plan `docs/plans/railway-migration.md` Phase 1; memory `project_railway_migration`).** Three commits standing up the Railway Hobby pilot alongside Streamlit Cloud. **`1c879be`:** adds `railway.toml` (start command, health check, `replicas=1` guard) plus a real security behavior change — a failed-attempt counter and 5-minute lockout added to `_check_password()`, since Railway has no Streamlit OAuth layer on top of the password gate the way Streamlit Cloud does. **`3310a61`:** Railway's Variables tab exposes flat env vars, not a mounted `secrets.toml` file, so `st.secrets["app"]["password"]` reads empty there and the gate would fail open — added an `APP_PASSWORD`/`APP_READONLY_PASSWORD` env-var fallback, mirroring the existing dual-source pattern in `providers/_util.get_secret()`. **`78318a1`:** root-caused a real Home-page crash on Railway — ~50 call sites across `app.py`/`stock_analyzer/` call `st.secrets.get(...)` directly, and with no `secrets.toml` file present anywhere, Streamlit's lazy loader raises `StreamlitSecretNotFoundError` on the FIRST access (not a graceful missing-key miss), crashing whichever page touches a secret first (the FRED macro-calendar build on Home, in practice). Fixed via `railway_start.sh`, which materializes `.streamlit/secrets.toml` from the already-configured Railway env vars before launching Streamlit — fixes every `st.secrets` call site at once without touching app code, keeping the Variables tab as the single source of truth.
- **Act Today — deterioration WATCH/TRIM/EXIT classification + cache-invalidation chain (2026-07-23; commits `305e160`, `b2207f7`, `936dff9`, `f72ccba`; `stock_analyzer/decision_bucket.py`, `stock_analyzer/daily_briefing.py`, `app.py`).** A same-day, 4-commit bug-fix chain closing a real contradiction: a deterioration exit/trim card could carry "→ ACT: Reduce aggressively" language while being classified as Monitoring/Awareness ("FYI — no action needed"), producing a three-way clash — "Act Today — you're set" banner + "Nothing to act on" + a card inside saying "exit most/all shares." **`305e160`:** added `deterioration_exit`/`deterioration_trim` to `_ACT_KINDS` so they route to the Act bucket where their language already places them; `reduce_call_tickers`/`reduce_call_items` and all suppression gates unaffected (they scan both buckets regardless). **`b2207f7`:** a second, independent bug in the same area — `deterioration_blocked_adds` was only populated inside `if tone == "bull"`, so in Protect/bear mode the list stayed empty and the `app.py` dedup (`_grow_shown`) had nothing to block WATCH names with; FSLR (on WATCH, -11% P&L) leaked through as "ADD — Winning Position" in Protect mode. Fixed by pre-populating `deterioration_blocked_adds` for all held WATCH names before the tone gate, with a duplicate guard so the bull-mode loop doesn't double-add. **`936dff9`:** the `b2207f7` fix's dedup still wasn't catching FSLR live — root cause was `_buy_candidates` itself: it annotated WATCH tickers with a conflict note but still appended the "ADD — Winning Position" item, so FSLR entered `buy_candidates` regardless of WATCH status. Fixed by suppressing WATCH add_winner items at the source (same continue-based gate `_grow_today` already used). **`f72ccba`:** Streamlit Cloud's soft-redeploy hot-swaps code but keeps session state, so the stale `_home_synth_cache` (built from the pre-fix `_daily_brief` that still had FSLR as an add_winner) kept matching the synthesis signature and being served after the `936dff9` deploy. Bumped `_SYNTH_SCHEMA_VER` (4→5) to force a cache-miss on next render. No scoring/gate/constant changes across any of the four.
- **Summary page — Risk Posture card: real jump button + title icon (2026-07-27; `app.py`, `docs/requirements.md`; memory `project_summary_page`).** Live-review catch: the shipped Risk Posture card (Tier 3) rendered its "→ see 🔗 Risk Analysis" line as a plain `st.caption` — not clickable — while the two pointer cards shipped alongside it (🩺 Thesis Review, 🔔 Catalyst Watch) both use a real `st.button` + `_pending_page` navigation. The original code's own comment reasoned "no fake `<a href>` link... Streamlit has no in-page anchor navigation" — true for anchor tags, but conflates that with `_pending_page` cross-page navigation, which does work and was already proven on the same page by the other two cards. Fixed: swapped the caption for `st.button("→ Risk Analysis", key="sm_ptr_riskposture")` → `_pending_page` → `st.rerun()`, the identical pattern. Also added a `🔗` title-icon prefix ("🔗 Risk Posture") — matching Risk Analysis's actual nav icon (verified: `"🔗 Risk Analysis"` in the nav list) — since the card title previously had no icon at all, unlike the other two cards' titled headers. The severity emoji inside the card body (`_sm_posture['emoji']`, e.g. 🛡️ for "Steady") is untouched — that's real state data from `market_risk_posture()`, not a page identifier, so it stays. Display-only; no constants/gates/scoring touched, no Opus review required per CLAUDE.md hard rule #4.
- **Summary page pointer-cards plan — CLOSED 2026-07-27 (3 of 4 cards shipped, card #4 deliberately dropped, not built; plan `docs/plans/summary-page-pointer-cards.md`; memory `project_summary_page`).** Final status: Risk Posture (card #1, via a parallel session), 🩺 Thesis Review (card #2), and 🔔 Catalyst Watch (card #3) all shipped 2026-07-27 — see their own entries below. **Card #4 (Watchlist actionable count) investigated then dropped, not built:** `build_watchlist_recommendation()` needs a per-ticker `data` dict that only exists after the Watchlist page's own `_parallel_load_all(list(watchlist), period="6mo")` full-bundle fetch runs — confirmed neither that raw data nor the computed recommendations are published to `st.session_state` anywhere, so there was no cheap reuse path, unlike the other three cards. Building it would have meant Summary silently re-triggering that same expensive fetch — exactly the "second independent computation" this plan's own design principles exist to rule out, and a real cost/latency regression on a page whose whole point is to be the cheap, lean glance. A bare ticker-count substitute (no actionability signal) was considered and rejected as too weak to be worth building. **User decision: drop the card, close the plan at 3.** Removed from CLAUDE.md's "What's queued" (fully resolved, nothing left pending).
- **Summary page — 🔔 Catalyst Watch pointer card (2026-07-27; `app.py`, `docs/requirements.md`; plan `docs/plans/summary-page-pointer-cards.md` card #3; memory `project_summary_page`).** Second card in the "🧭 Elsewhere in DRISHTA" section, alongside 🩺 Thesis Review. Shows a count of held names reporting earnings within `CATALYST_WATCH_WINDOW_DAYS` (7) days. Reuses the SAME 24h process-wide `@st.cache_data`-cached `_cached_held_earnings_dates()` call (identical `tickers_tuple` + 90-day range) that Catalyst Watch's own "Your Holdings — Earnings" tier (`_render_holdings_earnings()`) uses — whichever page runs first this cache window pays the fetch once, the other gets a free hit, never a second independent computation. Prefers `held_data`'s own bundle-loaded earnings date first (zero cost, matching `_render_holdings_earnings()`'s exact preference order) before falling back to the cached backfill. **Verified before building, not assumed:** confirmed `_cached_held_earnings_dates` is genuinely the same shared cache (not a per-page instance) by reading its `@st.cache_data(ttl=86400)` decoration and matching its exact call signature. Always shows a state ("N reporting" or "None soon") rather than hiding — earnings timing is informative even when the answer is none, same posture as Act Today's "you're set." A "→ Catalyst Watch" button (`_pending_page` navigation) sends the user there. Display-only — no constants/gates/scoring touched, no Opus review required per CLAUDE.md hard rule #4.
- **Summary page — 🩺 Thesis Review pointer card (2026-07-27; `app.py`, `docs/requirements.md`; plan `docs/plans/summary-page-pointer-cards.md` card #2; memory `project_summary_page`).** New "🧭 Elsewhere in DRISHTA" section on 🧾 Summary, between Act Today and Holdings — the first card in this section (card #1, Risk Posture, already lives in the Tier-3 KPI row shipped separately). Shows a count of held tickers whose most recent `thesis_reviews` row is WEAKENING or BROKEN, reusing the identical "most recent review per ticker" logic (sort by `reviewed_at` desc, `drop_duplicates(subset="ticker")`) already used by the AI Insights page's own "🩺 Positions" header count and the Act Today "Thesis broken" button's `_broken_thesis_tickers` set — never a second independent verdict. **Corrects a naming mistake from this plan's own origin session:** `thesis_reviews` is F-1's per-holding INTACT/WEAKENING/BROKEN review (renders on AI Insights' 🩺 Positions tab), NOT the same thing as the ⚠️ Red Team tab's separate quantitative erosion score (`thesis_erosion_cache`, a different table entirely) — the plan doc and CLAUDE.md's queue both originally mislabeled this card "Thesis Red Team" and pointed it at the wrong tab; both corrected same session. Shows "All Intact" (calm, not hidden) when none need attention — never disappears just because nothing's wrong, matching Act Today's own "you're set" pattern; withheld entirely only when no held ticker has any saved review yet. A "→ AI Insights" button (`_pending_page` navigation, same pattern used elsewhere for ticker→Analysis jumps) lands on AI Insights' default first tab, which happens to be 🩺 Positions — the correct destination by coincidence of tab order, verified not assumed. Display-only — no constants/gates/scoring touched, no Opus review required per CLAUDE.md hard rule #4.
- **Summary page — Portfolio Value sparkline + Alpha vs SPY tile + Risk Posture pointer (Tier 2+3 KPI-chip follow-up; 2026-07-27; commits `2eafa64`→`8793ce7`; `app.py`, `docs/requirements.md`; memory `project_summary_page`).** Closes the gap between the shipped 8-metric KPI row and an earlier approved "Option 1" mockup that paired each hero metric with a delta chip + trend line. **Tier 2 (`2eafa64`):** `_render_sparkline()` (bare Plotly line, no axes/legend, 36px) added to the Portfolio Value tile, reading the existing optional `daily_snapshots` table (same one Home's Tier-B day-P&L reads, no new DDL); degrades silently below 2 days of history. **Tier 3 (`8793ce7`):** a new second row under the 8-tile row with (a) an **Alpha vs SPY (Nd)** tile — portfolio equity return vs `_cached_spy("3mo")` over a trailing window up to 30 trading days, **explicitly NOT money-weighted** (doesn't adjust for mid-window buys/sells — a user design call; tooltip points to 💰 Account → Capital Trend for the precise Modified-Dietz number), window label always shows the real day count used rather than hardcoding "30d," and withholds the tile entirely below 5 days of overlapping history; and (b) a **Risk Posture** read-only pointer badge (emoji + label + one-line summary + "→ see 🔗 Risk Analysis" caption, no fake in-page `<a href>`) that reuses the exact `exit_advisor.market_risk_posture()` call + published `_fragility_cache` the Risk Analysis dial itself uses — never a second independent verdict. Also switched the Tier-2 sparkline's query from row-count-limited (`load_recent_snapshots`) to date-filtered (`load_daily_snapshots`) so its window matches what's actually labeled. **Live-verified same-day follow-up fix (`48ef8b1`):** the Alpha vs SPY tile showed +15.9% right after shipping while Unrealized P&L read +3.8% — traced to a capital addition inside the trailing window (the exact equity-vs-money-weighted gap the tooltip already caveated, but a hover-only note wasn't enough); added an always-visible caption when `trades_df` has a BUY/SELL dated inside the joined window, reusing the same `trades_df` read Home's Tier-B day-P&L already does. **Collision reconciled same day:** a parallel session's `docs/plans/summary-page-pointer-cards.md` (a separate 4-card pointer-section plan, unrelated origin) had independently scoped the same Risk Posture idea as its card #1 — that plan is annotated to point here instead of rebuilding it; its build order now starts at card #2 (Thesis Review). Display-only throughout — no constants/gates/scoring touched, no Opus review required per CLAUDE.md hard rule #4.
- **Recommendations History — fix blank missed-alpha for Conflicted/Hold-zone/Sell-zone (2026-07-26; commit `51b2441`; `stock_analyzer/daily_briefing.py`, `app.py`; no memory file — see MEMORY.md `project_rec_engine_evaluation`).** Found during a manual health-check sweep of the scorecard (re-running the 2026-06-18 engine-quality review against current data): the by-verdict and by-composite-band rollups showed blank missed-alpha for the Conflicted verdict bucket (421 of ~600 historical recs) and the Hold-zone/Sell-zone composite bands, even though those buckets had hundreds of missed rows well past `REC_SCORE_MIN_DAYS` maturity — meaning the app couldn't verify its own core claim that skipping conflicted/low-composite names saves money. **Root cause:** `buy_candidate` items built in `_buy_candidates()` (`daily_briefing.py`, two `items.append` sites, scanner-pick and add-winner-style) never captured a `"price"` key from their source row, even though both source DataFrames (`scanner_results`, `port_df`) carry one. `app.py`'s rec-save loop resolves `price_at_surface` via `_price_for(ticker, pick_price=None)`; `new_pick` already passes its own price through and resolves fine, but `buy_candidate`'s write (`app.py` ~line 4311) passed no `pick_price` at all, so it fell through to the held-position-only fallback — which structurally can't resolve a not-yet-held candidate ticker. `price_at_surface` ended up `None` for most `buy_candidate` rows **permanently** (waiting longer never fixes a snapshot that was never taken). **Fix:** both `_buy_candidates()` append points now set `"price": _f(row.get("Price"), None)`; `app.py`'s buy_candidate write now passes `_price_for(_tk, _p.get("price"))`, mirroring the existing `new_pick` pattern exactly. **Fixes going forward only — NOT retroactive:** the ~419 already-blank Conflicted rows (and the Hold-zone/Sell-zone ones) stay blank forever; reconstructing a historical price-at-past-date would need a separate OHLC backfill job, out of scope here. Pure analytics blast radius — `price_at_surface` is read only by `recommendations_history.py` (missed-outcome math) and `missed_opportunity.py` (price-band bucketing); no live gate reads it. Opus reviewer: SHIP, 0 blocking (one non-blocking note: `app.py`'s `add_winner`/grow_today write still omits `pick_price`, correct today because those tickers are always held so the fallback resolves, but would regress if a future refactor ever surfaced a not-yet-held ticker through that path — worth a guarding comment, not urgent).
- **Catalyst-Specific Stress (F-207 / D4; 2026-07-26; commit `0dcecf1`; `stock_analyzer/catalyst_stress.py` (new), `stock_analyzer/macro_calendar.py`, `stock_analyzer/constants.py`, `stock_analyzer/db.py`, `app.py`; plan `docs/plans/catalyst-specific-stress.md`; memory `project_agentic_intelligence_roadmap`).** Last item of Agentic Intelligence Roadmap **v2** Phase 4 — closes the entire v2 roadmap. New "📅 Catalyst-Specific Stress (Beta)" expander, sibling to P5's "🎯 Regime-Aware Adversarial Scenario" on 🔗 Risk Analysis → 🔥 Stress Testing — the event-driven twin of that feature: where P5 asks "what ongoing macro CONDITION threatens this book," D4 asks "what single upcoming DATED EVENT (a HIGH-impact, sector-specific macro print, or a held position's own earnings report, within `CATALYST_STRESS_WINDOW_DAYS`=14 days) threatens the SAME structural weak points P3/P5 already find." HEAD audit found more reuse available than the roadmap's one-liner suggested: `macro_calendar.build_macro_calendar()` already returns dated events with impact severity + affected tickers; `app.py`'s `_cached_catalyst_calendar()` already returns held-ticker earnings dates; `blast_radius()`/`correlation_clusters()` are the same P3/P5 substrate — the only genuinely new piece is a pure-Python ranking function. **Opus design review caught a real structural bias pre-build**: summing a many-ticker macro event's weight-overlap against a one-ticker earnings event's own weight would always favor macro events, and two categories (`Fed Policy`/FOMC, `Growth`/GDP) use an `__ALL__` sector-impact sentinel that returns EVERY held ticker — guaranteeing those events score the maximum possible overlap by construction, precisely because they say nothing about *which* weak point is exposed. Left as designed, the feature would predictably always surface "the next CPI/FOMC," an uninformative, non-decision-relevant repeat prompt — a direct §2B calm-advisor violation. **Fixed: macro and earnings candidates rank in two SEPARATE lists, never cross-scored, and `__ALL__`-category events are excluded from the macro list entirely.** Also fixed: an undefined "blast-radius contribution %" field pinned to the real `portfolio_impact_pct`; an ambiguous "weak-point ticker set" pinned exactly to the union of `blast_radius()`'s `shocked_ticker` + cascade `contributing_tickers`, plus every `correlation_clusters()` member. One Haiku call synthesizes a narrative covering whichever candidate(s) exist — and is never called at all when neither list has a candidate, so "nothing catalyst-specific stands out" is decided by the ranking step, not asserted by an LLM call with no evidence. Recomputes `blast_radius()`/`correlation_clusters()` independently rather than depending on P5's local variables (never published to session_state) — a deliberate render-order decision so this feature never silently depends on whether the user opened P5's section first this session. New constant `CATALYST_STRESS_WINDOW_DAYS=14` (user-set) — deliberately distinct from `EARNINGS_URGENCY_SOON_DAYS` (a different feature's display tier) to avoid a silent cross-feature policy coupling. New `catalyst_stress_cache` table (RLS, mirrors `regime_scenario_cache`'s exact shape). Opus design review: FIX-FIRST → SHIP (3 blocking, all above). Opus pre-ship code review: SHIP, 0 blocking, 2 cosmetic non-blocking fixed (a None-guard on impact-percent formatting; a code comment on the deliberate `abs()` in earnings scoring). Strictly diagnostic: never gates, scores, or modifies `blast_radius()`, `correlation_clusters()`, `build_macro_calendar()`, or the earnings calendar itself. **This ships the last item of Agentic Intelligence Roadmap v2** — all 4 phases (D2; D1+O1; D3+O4; O5+O6+D4, with O2 shelved and O3 parked) are now complete. **Same-day production fix (`9080f0e`):** a live run surfaced a real fabrication — given AMD's earnings and the NFP release as two independent candidates, the narrative invented an unsupported causal link between them ("...if AMD's earnings have already signaled demand weakness"); each candidate's own facts were real but the connecting mechanism wasn't in the supplied evidence. The original "describe them as two separate risks" instruction wasn't strong enough. Reworded per Opus review to an explicit "two separate paragraphs that never reference each other" framing — no shared theme, no ordering language, no if-X-then-Y, no cross-candidate signal/confirm/amplify claim, no combined figure spanning both. Verified the evidence assembly has no field that could ground a real cross-candidate relationship, so the blanket prohibition costs nothing. Opus-reviewed SHIP (prompt-only, but decision-adjacent on a live production surface).
- **Sizing Alpha + Premature-Exit Cost (F-205/F-206 / O5+O6; 2026-07-26; commit `0b6c50d`; `stock_analyzer/investor_mirror.py`, `stock_analyzer/constants.py`, `app.py`; plans `docs/plans/sizing-alpha.md` / `docs/plans/premature-exit-cost.md`; memory `project_agentic_intelligence_roadmap`).** Priority 6 of Agentic Intelligence Roadmap **v2**, Phase 4 ("the sizing/exit pair"), the first Phase 4 items shipped. **O5 Sizing Alpha** corrects the roadmap's "flat sizing across conviction tiers" framing — verified no field anywhere in the schema reliably records "conviction at the time of a specific buy" (`recommendations.conviction` only covers same-day engine surfacings, the same coverage-gap class O1 already hit; `decision_context` captures portfolio-level context, never the traded ticker's own score; `conviction_alignment()` is current-state only). Reframed around the one thing reliably recorded for every trade — dollar size actually committed — grouping `build_closed_lots()`'s FIFO fragments back to their **originating BUY LOT** (`groupby(ticker, buy_date, buy_price)`, NOT by sell fragment; fragment-level bucketing would shred a large, later-scaled-out winning bet into several small-dollar fragments, biasing the whole comparison toward a false "flat sizing" conclusion — the round-1 design-review blocking finding). Splits buy lots into dollar-size terciles via a **tie-robust rank split** (`pd.qcut` on rank, not raw value — guards against round-dollar buys collapsing a plain `pd.qcut` into a degenerate split; explicit `None` guard for <3 distinct dollar values — the second round-1 blocking finding), shows each tercile's **dollar-weighted** (not share-weighted, for cross-ticker comparability) average realized gain%, with an explicit bolded caveat that sizing is absolute-dollar, not portfolio-%-adjusted (no point-in-time portfolio-value history exists to normalize by). Reuses `INVESTOR_MIRROR_MIN_CLOSED_LOTS` as the per-tercile floor (now over buy lots, post-grouping) — no new constant needed. **O2 Conviction Under-Confidence — SHELVED same day (`d0c486f`, no code), same shelve-not-rehash precedent as P6:** re-evaluated against its own "only if O5/O6 leave a gap" condition once the sizing/exit pair shipped — O2's premise needs composite score at a past buy/hold date joined to realized outcome, and no table anywhere stores historical per-ticker composite scores (the same wall O5 hit and worked around via dollar size, which IS reliably recorded). O5 already ships the closest honest proxy this schema supports; building O2 anyway would rehash it under a different label or need a new forward-only day-cache sitting empty for weeks. User confirmed: shelve. **O6 Premature-Exit Cost** corrects the "Cost" framing away from any forecasted/counterfactual price estimate (fabrication risk) toward a real, already-realized comparison: among winning closed lots, the share-weighted average gain% of "quick exit" winners (held < `PREMATURE_EXIT_RATIO`=0.5 × the user's own average winner-hold) vs. "patient" winners. Filters explicitly on `is_gain == True` AND non-null `pnl_abs`/`pnl_pct` — not `is_gain` alone, since `build_closed_lots()`'s `is_gain = (pnl_abs or 0.0) >= 0` evaluates True when `pnl_abs` is `None`, which would otherwise admit phantom "winners." Computes its own average-winner-hold independently of `disposition_effect()`'s return, since that function requires BOTH a populated winner AND loser group and would silently withhold `winner_avg_days` whenever losers are thin. **New feature-specific constant `PREMATURE_EXIT_MIN_LOTS=5`** (user-set) — deliberately NOT `INVESTOR_MIRROR_MIN_CLOSED_LOTS` (10), since O6's quick/patient buckets draw from a winners-only, twice-split population roughly half the size that floor was sized for; reusing it — the round-1 blocking finding — would likely have left the card permanently dark. Both new render surfaces show BOTH directions of their comparison (never only the "confirms a concern" case) and reuse the exact same `_mi_lots` (`build_closed_lots()` result) already computed for the existing Behavioral Biases cards — zero new LLM calls, zero new cache tables, the second and third items on this roadmap (after D3/O4) needing neither. Opus design review: O5 FIX-FIRST → SHIP (2 blocking, both above); O6 FIX-FIRST → SHIP (1 blocking, the floor). Opus pre-ship code review: SHIP, 0 blocking, 2 cosmetic non-blocking fixed (mean-vs-median honesty note on O6's help text; a Medium-tercile summary clause on O5's caption). Strictly diagnostic: never gates, scores, or modifies `build_closed_lots()`, `disposition_effect()`, `win_loss_closure_ratio()`, `breakeven_anchoring()`, or `conviction_alignment()`. **Same-day production fix (`2b4fb0c`):** Streamlit renders text between two unescaped `$` as LaTeX/KaTeX math, so the "a \$5,000 bet... than \$5,000 today" caption and the tercile dollar-range tooltip both garbled in production (confirmed via screenshot) — the whole span between the two literal dollar signs rendered as a broken math block instead of plain text. Escaped as `\$` in all three spots.
- **Option-A visual refresh + new 🧾 Summary page (2026-07-26; commits `ad7c28b`→`5d28539`; `app.py`, `docs/requirements.md` (F-01, F-204/§3.1h), `docs/architecture.md`; no memory file — see MEMORY.md `project_summary_page` / `feedback_mockup_first_ux`).** User-driven UX pass, built via static HTML mockups reviewed and approved BEFORE any `app.py` change at every step (3 competing style mockups → pick Option A; a 4th mockup for the Summary page using the real sidebar structure, not an illustrative one). **Phase 1 — nav + type foundation:** renamed the `ALERTS` nav group to `SIGNALS` and moved `📡 Signals & Advice` into it from the overloaded `PORTFOLIO` group (9→8 items); loaded Inter + JetBrains Mono and applied tabular-nums to every `st.metric` app-wide; hid Streamlit's `#MainMenu`/footer chrome. **Phase 2 — Home Holdings table (F-01), later relocated:** added a full-width Holdings table + unified severity-color constants (`_HOME_URGENT`/`_HOME_ELEVATED`/`_HOME_CALM`/`_HOME_GAIN`/`_HOME_LOSS`, fixing one real drift — `TRIM_AND_TIGHTEN`'s action label was rendering green like a gain) — then, once the Summary page shipped, **removed** the table from Home to avoid duplication (F-01 now points to §3.1h). **New "🧾 Summary" page (F-204, MAIN nav group, right after Home):** a first attempt to split Home into "Home" + "Daily Brief" (moving content out) was researched and abandoned once it surfaced real entanglement (Act Today's render functions are wired into content that would move; Home's preamble is the sole place that populates the session-state caches every other page depends on). Pivoted to strictly additive instead, per explicit request to avoid touching Home's cache/data-load mechanism: Summary reads what Home's preamble already publishes (`_port_df_enriched`, `_live_prices`, `_portfolio_value`, `_home_synth_cache`'s bundle) rather than duplicating or moving computation, showing an 8-metric KPI row, Act Today (same `_daily_brief` + the identical `split_defensive()` call Home uses, via a new deliberately leaner `_render_simple_action_card()` — no Analyze button/debate/journal/tax notes), and the (now sole) Holdings table. Enabled by hoisting `_fmt_action()` and the holdings-table builder from Home-local closures to true module-level functions — necessary because Streamlit only executes the matching `if`/`elif page ==` branch per rerun, so a `def` textually inside Home's block never exists on another page's run. **Two live-review fixes same day:** dropped a duplicated RAG banner ("Action Required" vs. a simultaneous "Act Today — you're set" read as contradictory, even though both were individually correct — different signals, danger-alerts vs. same-day decisions); corrected the Act Today card markup + layout (pill badge + ticker-on-its-own-line + 3-column grid) after it had drifted from the actually-approved mockup during the initial build. Display/nav only throughout — no gate, threshold, or recommendation logic touched at any step; no Opus review required per CLAUDE.md hard rule #4.
- **Watchlist Resurrection (F-203 / O4; 2026-07-26; commit `631e6ba`; `stock_analyzer/constants.py`, `stock_analyzer/db.py`, `app.py`; plan `docs/plans/watchlist-resurrection.md`; memory `project_agentic_intelligence_roadmap`).** Priority 5 of Agentic Intelligence Roadmap **v2**, Phase 3 (cheapest item on the board, parallel slot alongside D3). On the existing 👁️ Watchlist page, highlights watchlist tickers that are old (added ≥ `WATCHLIST_STALE_DAYS`=30 days ago, user-set) **and** currently actionable (`ENTER_NOW`/`NEAR_ENTRY`) **and not already held**. **Corrects the roadmap's original "back at the setup you originally wanted" framing** — the `watchlist` table only ever stored `ticker`/`added_at`, no original-setup snapshot to compare against ever existed, so this reframes to "old + independently actionable today" rather than inventing a comparison. **One resurrection predicate drives both the KPI count and every per-card caption** (the blocking fix from design review — the original draft would have counted a held+stale `ENTER_NOW` ticker in the summary while its card silently vanished under the pre-existing "Already in Portfolio" override, the same double-surface mismatch class this app has hit before); the predicate excludes held tickers for BOTH actions, since that override only covers `ENTER_NOW`. No new LLM call, no new page, no new cache table — a highlight over already-computed page data plus one new read, `db.load_watchlist_added_dates()`. Re-adding a removed ticker resets its clock by design (fresh `INSERT`, not an update) — the correct clock for "days since you last re-engaged," not a bug to fix later. New constant `WATCHLIST_STALE_DAYS = 30` (not real precedent from `ANALYST_COVERAGE_FRESH_DAYS=30` elsewhere, which means the opposite thing for a different, passive surface — stands on the user's own judgment call). Opus design review: FIX-FIRST → SHIP (1 blocking: the single-predicate fix). Opus pre-ship code review: FIX-FIRST → SHIP (1 blocking: the new constant was initially missing from the `docs/architecture.md` constants table, tripping the mechanically-enforced `scripts/check_constants_documented.py` CI gate; 2 non-blocking fixed — caption wording overstated `NEAR_ENTRY` as "qualifies to enter" rather than "actionable again," and `load_watchlist_added_dates()` initially truncated the raw UTC `added_at` string instead of converting to America/New_York first, which could undercount `days_since_added` by 1 near the midnight ET boundary). Strictly diagnostic: never gates, scores, or changes `build_watchlist_recommendation()`'s classification, sort order, or the existing KPI metrics.
- **Signal Coherence Auditor (F-202 / D3; 2026-07-26; commit `df36c6d`; `stock_analyzer/signal_reconciliation.py`, `stock_analyzer/db.py`, `app.py`; plan `docs/plans/signal-coherence-auditor.md`; memory `project_agentic_intelligence_roadmap`).** Priority 4 of Agentic Intelligence Roadmap **v2**, Phase 3. New 5th tab, "🧭 Signal Coherence," on 🧩 Intelligence. For every held ticker, mechanically joins three EXISTING, independent advisory surfaces — the composite score's own directional class (new public `signal_reconciliation.classify_composite_direction()`, a 2-line wrapper over the existing private `_composite_class()`), the weekly Thesis Red Team's erosion status (`thesis_reviews.status`), and the most recent Bull/Bear debate verdict across BOTH debate types (`debate_cache.verdict`, new `db.load_debate_verdicts()` read) — and surfaces only the tickers where the present signals genuinely disagree (≥1 bullish-aligned + ≥1 bearish-aligned). A ticker needs ≥2 of the three signals present to be evaluated; full agreement among evaluated tickers renders no card, per the roadmap's own noise warning for this exact item. **Zero new LLM calls and zero new cache table** — the only item on this roadmap that needed neither, since all three inputs already exist as stored/computed data; the feature is a pure-Python presence-check + direction-map, computed live on every render. **Corrects the roadmap's original one-liner**, which named "regime-fit" as a 4th input: verified `regime_targets.regime_position_gap()` returns one portfolio-level beta/cash gap, not a per-ticker directional signal — forcing it into a per-name join would be a category error, so it was dropped rather than shoehorned in during the HEAD-audit step. **Verified invariant, does not flip:** in BOTH entry-type and exit-type debates, `bull_wins` is bullish-aligned and `bear_wins` is bearish-aligned (an exit debate's Bull argues to continue holding, Bear argues to exit — matching entry-debate polarity exactly); a same-date tie between an entry and an exit debate prefers the exit debate (the current hold decision), and a selected entry debate predating the ticker's most recent BUY is tagged "buy-time" rather than shown as a live re-litigation. No synthesized prose — each card shows the raw signal chips (type + date on each) as the finding itself, deliberately never a templated "these disagree because…" sentence that would invent a causal story neither signal actually asserts. No `constants.py` entries. 1-round Opus design review (SHIP, 0 blocking, 5 non-blocking refinements folded in: the polarity-invariant documentation, the same-date tiebreak rule, unknown-as-neutral gating semantics, buy-time tagging, always-show-all-present-chips). Opus pre-ship code review: SHIP, 0 blocking, 2 cosmetic non-blocking fixed (a NaN `Signal` value rendering as literal "nan"; a null erosion `status` rendering as literal "None"). Strictly diagnostic: never gates, scores, or modifies any of the three source surfaces (`reconcile_signals()`'s existing scanner-vs-composite use, `thesis_advisor`, or `debate_agent` are all untouched).
- **Missed-Opportunity Pattern (F-201 / O1; 2026-07-24; commit `b11e34a`; `stock_analyzer/missed_opportunity.py` (new), `stock_analyzer/db.py`, `app.py`; plan `docs/plans/missed-opportunity-pattern.md`; memory `project_agentic_intelligence_roadmap`).** Priority 2 of Agentic Intelligence Roadmap **v2** (paired with D1 as "one flagship per side"), deliberately the "P6 done right" idea — P6 (Autonomous Pattern Discovery) was shelved the same day because its only untapped dimension (`decision_context`) had 17 rows; O1 points the same unsupervised-discovery instinct at real, already-accumulating data instead: `recommendations_history.distinct_missed()`, which grades every engine "new_pick" buy recommendation the user never acted on against its real outcome (alpha vs SPY). New "🔍 Missed-Opportunity Pattern" section on 📊 Recommendations History → Summary, a sibling to (not nested inside) F-161's existing flat missed-opportunities list — **built from the UNFILTERED `_rh_enriched_all` snapshot, deliberately not the page's status-filtered `_rh_enriched`**, so `distinct_missed()`'s "acted via ANY surfacing" safeguard can't silently break under the Acted-only/Missed-only dropdown (a real data-source bug caught at plan-review stage, not shipped). **A naive statistical rebuild was ruled out during planning** — only 44 distinct tickers have ever surfaced as `new_pick` (account query, 2026-07-24), so a sector/price-band bucketing with a `min_n` gate (mirroring `predictive_analytics.py`'s `by_sector_alpha`/`by_conviction`) would leave most buckets at 0-1 members, the exact wall that sank the original P6. Instead, one Haiku call/day finds a **descriptive** pattern — reframed from a causal "why you skipped them" to "what these skipped names have in common" specifically to avoid inviting an unverifiable psychology claim that reads as forward-looking advice — across the missed-ticker corpus enriched with 3 pre-computed **closed-category** fields: `sector` (empty→"Other"), `price_band` (fixed bands), `composite_band` (reuses `recommendations_history.by_composite_band()`'s exact `COMPOSITE_STRONG_BUY`/`COMPOSITE_BUY`/`COMPOSITE_HOLD` thresholds — no new breakpoints invented), alongside the already-present `verdict`/`outcome_label`. **A two-layer fabrication guard** (pure Python, mirrors D1's structure): every cited ticker must exist in the real corpus, AND the claimed `shared_dimension`/`shared_value` must mechanically match that specific ticker's real value for that specific dimension — a non-conforming ticker is dropped from its pattern, not the whole pattern discarded. Because every groupable field here is a closed, checkable category, this guard is **stricter** than D1's own free-text quote-matching, not a weaker analog of it (the original plan draft argued the opposite and was FIX-FIRST'd for it). Includes ALL outcomes, not winners-only — a deliberate posture safeguard against becoming a FOMO amplifier — and each pattern renders its real win/dodge/flat mix so it can never read as "these were all winners, buy the next one like it." **Explicitly never cross-references a found pattern against current Grow Today candidates, the watchlist, or held positions** — the one design choice that would turn a retrospective pattern into a disguised forward-looking buy signal; any future join needs its own Opus review. Day-cached to new `missed_opportunity_cache` table (RLS included). No `constants.py` entries — sample-size floors (`_MIN_MISSED_TICKERS=3` matching `predictive_analytics.py`'s `min_n=3` convention; `_MIN_PATTERN_TICKERS=2`, safe only because the predicate guard exists) are module-level tuning, not policy thresholds. 1-round Opus plan review (FIX-FIRST → resolved: added the predicate-verification guard and the unfiltered-snapshot fix, both above). Opus pre-ship code review: SHIP, 0 blocking, 2 of 4 non-blocking fixed (a constant-drift risk between `app.py` and the module; a cosmetic composite-band label mismatch), 2 accepted as harmless low-risk edges. Strictly diagnostic: never gates, scores, or modifies `distinct_missed()`/`missed_split()` themselves.
- **Hidden Same-Bet Detector (F-199 / D1; 2026-07-24; commit `5e8479f`; `stock_analyzer/thesis_cluster.py` (new), `stock_analyzer/db.py`, `app.py`; plan `docs/plans/hidden-same-bet-detector.md`; memory `project_agentic_intelligence_roadmap`).** Priority 2 of Agentic Intelligence Roadmap **v2** (paired with O1 as "one flagship per side"). New "🧠 Hidden Same-Bet Detector" section on the 🧬 Structural Scan tab (below F-198's content), detecting positions that look diversified by sector and price correlation but secretly bet on the same underlying assumption (e.g. three names all implicitly long "AI capex keeps ripping") — concentration risk F-198's price-correlation scan structurally cannot see, since it only sees price behavior, not the reasoning behind a position. **Gated only on saved-thesis coverage, deliberately independent of the price-correlation gate above it** — an earlier design draft nested this inside that gate and would have silently hidden the whole section (including the "not enough saved theses" message) whenever price history was thin, caught in Opus design review. One Haiku call/day semantically clusters currently-held positions' saved buy theses via a **two-layer fabrication guard**: every returned ticker must normalized-match the supplied corpus, AND every returned "quote" must verifiably appear in that *specific* ticker's own stored thesis text — protecting against a spurious grouping of two genuinely-held tickers, not just a fake ticker identity. This is a materially harder grounding problem than P3/P5's structured-data citation, since semantic clustering over free prose is inherently fuzzier — Opus design review flagged that ticker validation alone wasn't sufficient and required the evidence-quote layer. "No shared assumption found" is a valid, expected, cacheable result — never forced. A pure-Python classification step (zero LLM, cannot hallucinate) labels each validated cluster against F-189's `correlation_clusters()` output as **unverified** (no price-correlation data this session — never coerced into a false positive/negative, closing a second Opus-caught bug where missing data defaulted to a false "hidden" label), **possible** (checked, not already price-correlated — the headline finding), or **confirmed** (checked, already price-correlated — de-emphasized corroboration), plus any already-price-correlated sub-pairs within a `possible` group. Classification is recomputed fresh on every render from the *current* session's correlation data rather than frozen at generation time — the cache stores only the validated clusters, so a cluster found `unverified` correctly reclassifies once price data becomes available later the same day. Day-cached to new `thesis_cluster_cache` table (RLS included). No `constants.py` entries — truncation length (~1500 chars/thesis) is prompt-sizing tuning, not a policy threshold. 1-round Opus plan review (FIX-FIRST → resolved: added the evidence-quote guard, the `unverified` state, and the render-independence fix, all three above). Opus pre-ship code review: SHIP, 0 blocking, 3 non-blocking fixed — a `.iloc[-1]` oldest-vs-newest-BUY bug (also present in the just-shipped F-197 Phase 2 exit-debate code; flagged there for a follow-up fix, not yet applied), a ticker-dedup guard in cluster validation, and truncation-note persistence across `st.rerun()`. Strictly diagnostic: never gates, scores, or modifies `correlation_clusters()`/`factor_tilt()` themselves.
- **Exit Red-Team — "Challenge This Exit" (F-197 Phase 2 / D2; 2026-07-24; commit `fb3676e`; `stock_analyzer/debate_agent.py`, `app.py`; plan `docs/plans/exit-red-team.md`; memory `project_agentic_intelligence_roadmap`).** Priority 1 of Agentic Intelligence Roadmap **v2** (the 360° completion roadmap) and the long-parked Phase 2 of the Multi-Agent Debate feature (F-197). Adds a "⚔️ Challenge This Exit" button to single-name deterioration TRIM/EXIT cards (Today's Brief 🔴 Act Today lane) that runs the *existing* 5-Haiku Bull-vs-Bear debate in `debate_type="exit"` mode: a Bull defends continuing to hold (anchored on the saved `user_thesis`, explicitly barred from sunk-cost "hold because we're underwater" reasoning) vs a Bear arguing the fired deterioration signal, citing the real `exit_advisor.assess_holding` numbers (drawdown-from-peak, sessions below trend, relative strength). Closes the app's **own-vs-sell asymmetry** — we red-team reasons to *own* continuously but had never challenged reasons to *sell* (where panic/premature selling does the most damage). **Reuses the shipped machinery, near-zero net-new surface:** `run_debate` is now `debate_type`-aware (the 4 round user-prompts branch entry/exit; system prompts + Judge unchanged); `_format_corpus` renders exit-only keys; `build_exit_corpus` (was a Phase-2 stub) assembles the corpus from the deterioration payload. Button gated on `_db_item["kind"]` (authoritative — never the action string, which would false-match "EXIT"), rendered full-width in `_render_act_card` with on-click data assembly from session caches (recomputes `deterioration_signals()` for the one ticker — pure pandas, no API). Shares the `debate_cache` table (`debate_type="exit"`, **zero DDL**) and the `DEBATE_SESSION_CEILING=3` per-session cap with entry debates; failed runs never cached and never consume a slot. **Strictly a second opinion — never suppresses the card, changes the deterioration tier, or touches the composite score.** Opus design review (FIX-FIRST resolved: re-targeted from the wrong renderer `_render_review_card`—which only ever sees WATCH tier—to `_render_act_card`; added the `deterioration_payload` param so the Bear cites real signals instead of fabricating). Sonnet implementer stalled on an infra timeout (zero edits); built directly instead. Opus pre-ship code review: SHIP, 0 blocking, 2 non-blocking both fixed (momentum-NaN guard via `math.isnan`; debate_exit button key given an action suffix). No `constants.py` change. Entry debate byte-for-byte unchanged. Phase 3 (AI Insights debate log) still follows.
- **Agentic Intelligence Roadmap — CLOSED at P1-P5, P6 evaluated and shelved (2026-07-24; plan `docs/plans/autonomous-pattern-discovery.md`; memory `project_agentic_intelligence_roadmap`).** No code shipped for P6 — this entry records a design decision, not a feature. P6 (Autonomous Pattern Discovery)'s ≥30-completed-trade gate cleared (66 SELL/75 BUY), but two findings closed off every viable design: (1) the only genuinely untapped dimension, `decision_context`, has just 17 populated rows (started capturing 2026-07-17) — too thin to test safely, confirming the roadmap's own "highest statistical validity risk" framing was correct, not just cautious; (2) a rescoped "cross-surface ranking" design (merging the pre-existing `🪞 Trade Review` page's 9 diagnostics — itself discovered mid-session, undocumented in memory, predating both Behavioral Fingerprint F-193 and this roadmap — with Behavioral Fingerprint's 6 patterns into one "Blind Spots" list) got an Opus **FIX-FIRST**: Behavioral Fingerprint has no severity tier at all, and promoting its explicitly neutral momentum/conviction cards into a "blind spot" label would contradict that tab's own on-screen non-accusatory promise (`app.py:27602-27603`) — a posture regression, not a fixable bug. The safe fallback (Trade-Review-only ranking) turned out to duplicate an already-existing, already severity-sorted section (`app.py`'s "🎯 Course-Correction Recommendations", `app.py:19819-19824`). User decision: shelve rather than ship a diminished or redundant feature. **The roadmap is complete at P1-P5 shipped** — see the plan doc's Resolution section for the "if revisited" trigger (wait for `decision_context` to accumulate materially more rows).
- **Regime-Aware Adversarial Stress Testing — Phase 1 (F-200; 2026-07-24; commit `a3a9a12`; `stock_analyzer/regime_stress.py` (new), `stock_analyzer/db.py`, `app.py`; plan `docs/plans/regime-aware-stress-testing.md`; memory `project_agentic_intelligence_roadmap`).** P5 of the Agentic Intelligence Roadmap. New "🎯 Regime-Aware Adversarial Scenario (Beta)" expander at the bottom of the existing 🔥 Stress Testing tab (🔗 Risk Analysis) — the 9 named scenarios + Custom Scenario slider (F-14/F-14a) are completely untouched. **Zero new quantitative modeling** — synthesizes three already-shipped systems into one Haiku narrative: `structural_scanner.blast_radius()` (F-198's cascade estimate, reused verbatim as "the" damage figure), `portfolio_intelligence.correlation_clusters()` (F-189), the FRED macro regime detector (`macro_calendar.detect_macro_regime`, aliased `detect_macro_regime_fred` — **not** the unrelated same-named function in `macro.py`), and `cross_asset.py`'s USD/dollar stress signal (F-09c). **Scope deliberately narrowed from the roadmap's original "scenario plausibility in the next 90 days" framing** — the only historical regime persistence (`daily_regime`, since 2026-07-21) has ~3 days of data, nowhere near enough for a genuine base rate; replaced with the regime detector's real, already-computed confidence score, honestly relabeled as "how confident the detector is in its CURRENT read," never a forecast. Reuses the existing session-state regime cache byte-for-byte (same 3-source `fred_key` fallback chain as the existing Regime Fit feature, F-188) so the same page can never show two different regime confidence numbers. Button-gated, day-cached to new `regime_scenario_cache` table; a failed/empty narrative is never cached. Indicator watchlist entries are normalized-matched against the regime detector's real signal labels, always displaying the canonical stored label — never the LLM's echo — so an indicator can neither be fabricated nor silently dropped over casing drift. No `constants.py` additions. 3-round Opus plan review (FIX-FIRST×2 then SHIP — Round 1 caught a session-state cache-key derivation that would've mismatched the existing Regime Fit section for users who added their FRED key via the in-app UI; Round 2 caught the same "fixed in one place, stale elsewhere" pattern from an earlier plan this session). Opus pre-ship code review (SHIP, 0 blocking — including a specific ruling that this cache is correctly left `_READONLY`-ungated, matching `structural_scan_cache`'s precedent as a recomputable narrative cache, not user-authored data). Strictly diagnostic: nothing here gates, reorders a scenario, or modifies the composite score. Roadmap's Idea #6 (Autonomous Pattern Discovery, P6) is the only unplanned item remaining.
- **Information Asymmetry Detector — Phase 1: price cross-check history (F-126; 2026-07-24; commit `6e3ebd2`; `stock_analyzer/data.py`, `stock_analyzer/db.py`, `app.py`; plan `docs/plans/information-asymmetry-detector.md`; memory `project_agentic_intelligence_roadmap`).** P4 of the Agentic Intelligence Roadmap. **Scope deliberately narrowed from the roadmap's original 3-source fundamentals-divergence idea** ("three sources disagree on forward PE") — research found Finnhub supplies live price only in this codebase (no fundamentals capability at all), and FMP's own forward PE is sometimes a synthetic derivation from its trailing PE rather than an independent read, making a true 3-way comparison structurally impossible and a 2-way one methodologically ambiguous. Narrowed to the one field with a genuine, already-shipped multi-source comparison: **price**. Persists the already-shipped F-123 price cross-check result (`orchestrator.crosscheck_price`/`crosscheck_batch`) to new `price_xcheck_history` table (one row per ticker per ET trading day) — the check previously had zero history (a 5-minute cache only). Annotates the existing red "sources disagree" banner per failing leg (prior-close vs live-price) with "widened from X% to Y% since `<date>`" when the most recent prior row (21-day lookback) shows a smaller gap. New `data.divergence_widened()` — pure diff+threshold helper, zero LLM dependency (cheapest of the four Agentic Intelligence features shipped so far). **Written from the interactive Home page path only, day-deduped via session state, NEVER from cron** — the premarket cron path doesn't call the cross-provider validator today, so logging from cron would add a new per-ticker second-provider fetch every run; the interactive path already computes this for free every 5 minutes. No new tab, no new page, no `constants.py` additions (`min_widen_pp=1.0`/`days_back=21` are function defaults). 2-round Opus plan review (FIX-FIRST then SHIP — Round 1 caught 3 blocking findings: a dotted `data.divergence_widened()` call that would NameError since app.py has no bare `data` alias; widening compared only `live_gap_pct` regardless of which leg actually failed; the save function spec omitted the mandatory `_READONLY` guard). Opus pre-ship code review (SHIP, 0 blocking). Strictly additive: the annotation never changes whether the banner fires, never gates a stop/P&L decision. Also this session: a related `fix(ai)` commit (`e15829d`) relabeled the driving-pillar "momentum" to "technical" in `debate_agent.py`/`premortem_advisor.py` to avoid colliding with the market scanner's separate momentum score — validated in a prior session, unrelated to this feature.
- **Structural Vulnerability Scanner — Phase 1 (F-198; 2026-07-24; commit `9b47117`; `stock_analyzer/structural_scanner.py` (new), `stock_analyzer/db.py`, `app.py`; plan `docs/plans/structural-vulnerability-scanner.md`; memory `project_agentic_intelligence_roadmap`).** P3 of the Agentic Intelligence Roadmap. Adds a "🧬 Structural Scan" 4th tab on 🧩 Intelligence, composing the three already-shipped panels (F-189 Correlation Clusters, F-190 Risk Budget, F-191 Factor Tilt) with one new calculation — a **Blast Radius Map**: single-factor beta approximation (`beta = corr(T,H) × vol_H/vol_T`) estimating cascade portfolio impact from shocking each of the top-3 risk-budget contributors by -20%, gated on `abs(corr) ≥ CORR_HIGH_PAIRS_THRESHOLD` (reused, no new cutoff), signed correlation (a strong negative correlation is a real hedge/offset). Derives entirely from already-computed data (`_corr_df_cache` + `risk_budget()`'s own `vol_annualized_pct`) — zero new API calls; computes unconditionally every render. Plus a **daily Haiku narrative** (1 call/portfolio/day) synthesizing cluster/risk-budget/blast-radius evidence into a 2-4 sentence plain-English "exploit chain" explanation — **button-gated, not auto-computed**, because Streamlit executes every tab body on every page rerun regardless of which tab is selected, so an auto-compute design would fire the LLM call far more than once/day. Day-cached to new `structural_scan_cache` table (one row per `scan_date`, portfolio-wide — not per-ticker). Failed/empty narrative never cached (mirrors Debate's day-cache-poisoning fix). No new `constants.py` entries — the -20% shock and top-3 selection are module-level what-if inputs (`stress_test.py`'s `_SECTOR_SHOCKS` precedent). 3-round Opus plan review (FIX-FIRST×2 then SHIP — Round 1: a 100x weight-scaling bug in the portfolio-impact formula + the auto-compute Streamlit-execution-model flaw; Round 2: stale auto-compute language surviving in three sections after the authoritative fix — both rounds fully resolved). Opus pre-ship code review (SHIP, 0 blocking; 2 cosmetic non-blocking notes). Strictly diagnostic: nothing on this tab gates, reorders, or modifies the composite score. **Phase 2 (Home "Structural alert" banner for a new-cluster-since-yesterday) deferred pending ≥3 days production observation.**
- **Multi-Agent Debate Agent — Phase 1: entry candidate debates (F-197; 2026-07-23; commit `e745e48`; `stock_analyzer/debate_agent.py` (new), `stock_analyzer/db.py`, `app.py`; plan `docs/plans/multi-agent-debate.md`; memory `project_agentic_intelligence_roadmap`).** P2 of the Agentic Intelligence Roadmap. Adds a "⚔️ Debate" button per Grow Today entry candidate that runs a structured 4-round Bull vs Bear debate (5 Haiku calls: Bull opens → Bear responds → Bull rebuts → Bear closes → Judge issues verdict). Judge outputs `{verdict: bull_wins|bear_wins|contested, key_dispute, bull_case_score, bear_case_score, grounded}`. Day-cached to new `debate_cache` Supabase table. Per-session ceiling of 3 new debates; cache hits free. Button guarded: only shown when ticker is in `_grow_composites` and composite_score is not None. Failed/empty runs not cached (prevents day-cache poisoning); session slot only consumed on content-bearing runs. `DEBATE_WIN_MARGIN=20` f-string injected into Judge system prompt (live constant, not dead literal). 3-round Opus plan review (FIX-FIRST×2 then SHIP; 6 blocking findings across both rounds, all resolved). Opus pre-ship code review (FIX-FIRST, 1 blocking: failure-render/cache-poisoning — fixed). Strictly additive: debate result never gates or modifies composite score. DDL applies manually (see db.py docstring, `debate_cache` block). **Phase 2 (Exit Advisor TRIM/EXIT card debates) gated on ≥3 days production observation.** Phase 3 (AI Insights debate log) follows Phase 2.
- **Thesis Red Team Agent — Phase 1: erosion score (F-196; 2026-07-23; commits `1fa4fa6`→`bae1ba0`; `stock_analyzer/thesis_red_team.py` (new), `stock_analyzer/exit_advisor.py`, `stock_analyzer/constants.py`, `stock_analyzer/db.py`, `app.py`; plan `docs/plans/thesis-red-team-agent.md`; memory `project_agentic_intelligence_roadmap`).** First feature in the Agentic Intelligence Roadmap (6-idea adversarial-AI layer). Adds a new "⚠️ Red Team" 5th tab on 🧠 AI Insights that scores every held position 0–100 daily on four adversarial signals: deterioration tier, 20-session RS vs SPY, 5-session composite delta, and analyst PT revision direction. Each signal renders with a 🔴/🟢 direction indicator and a plain-English interpretation ("X of 4 signals show thesis pressure" summary per card). New `thesis_erosion_cache` Supabase table (DDL applied 2026-07-23); new `compute_relative_strength()` pure helper in `exit_advisor.py`; new `thesis_red_team.py` module; 3 new constants (`THESIS_EROSION_HAIKU_MIN=30`, `THESIS_EROSION_BRIEF_MIN=50`, `THESIS_EROSION_BRIEF_JUMP=15`). Production validated same day: FSLR 44/Softening (WATCH+bad RS), TEAM 38/Softening (EXIT+near-flat RS), intact positions 7/Intact — directionally correct. 3 post-ship fixes in same session (is_trading_day arg, composite label clarity, plain-English UX). 2-round Opus plan review (FIX-FIRST both rounds; 9 blocking findings total, all resolved). **Phase 2 (Haiku counter-evidence narrative + pre-mortem loop) gated on ~1-week production observation (~2026-07-30), then Opus plan review.** Phase 3 (Daily Brief annotation) follows Phase 2. Strictly display-only; erosion score never gates or feeds a recommendation.
- **2026-07-22 full-app UX audit (`docs/reviews/2026-07-22-UX-review.md`) + same-day fixes.** Structured audit (4 Critical, 10 Improvements, Quick Wins, Consistency Audit) across `app.py` + 68 `stock_analyzer/` modules. Same-day fix commits: `48119ef` (Tier 1+2 — confirm-gate consistency, hardcoded-threshold-literal fixes incl. `SINGLE_NAME_CEILING`, jargon scrub), `843d9fd` (Tier 3 — label stability, remaining jargon, quick wins), `e57de0e` (I2 — collapsed dense "Institutional Lens" prose into "Why this matters" expanders across 5 modules). **Nav rename + restructure (finding-adjacent, same day):** `291b0a9` renamed the PORTFOLIO nav item `⚠️ Alerts & Actions` → `📡 Signals & Advice` (avoids confusion with the separate ALERTS nav group) and split the flat page into two tabs — **📡 Active Signals** (F-169/F-170/F-13) and **🧩 Diversification** (F-13a); `be26c64`/`a89e0c6`/`6b88949` polished Active Signals same-day (category-card grid replacing full-width banners, de-duplicated severity icons, rebalance-status chip moved to top). **I6 (native-looking copy):** `b8939eb` relabeled the Predictive Analytics "Data Readiness Audit" expander (F-192) to "Signal History Coverage" and stripped internal-QA-tooling framing — copy-only, zero logic. **Unrelated same-day refactors, no doc impact:** `6cfa5c8` hoisted duplicated email-render helpers (`_md_inline`/`_section`) to module level in `notify.py` (pure code move, zero behavior change); `6e150a4` relocated the AI Insights Behavioral KPI row (F-166) from a page-level strip into the 🩺 Positions tab; a concurrent session's `b3309ea` grouped the User Guide's 26 flat expanders into 4 navigable tabs (confirmed content-preserving via diff — only tab-wrapping/indentation + one `expanded=True→False` changed). Docs synced same session: `docs/requirements.md` §3.1c/F-192/F-166, `docs/architecture.md` §10 (2 new rows + 1 corrected), `CLAUDE.md` cache-key list, memory `project_portfolio_intelligence`. No `constants.py` changes in any of these commits (mechanically verified via `scripts/check_constants_documented.py` — all-clear).
- **Research Scorecard — analyst call accuracy tracking (F-154c; 2026-07-22; commits `dacf357`→`5ba1d21`; `stock_analyzer/analyst_intel.py`, `db.py`, `scripts/backfill_analyst_prices.py`, `stock_analyzer/providers/*`, `app.py`; plan `docs/plans/analyst-research-accountability.md`).** Grew out of the user questioning whether their CNBC Pro subscription (fed into the existing Ideas Inbox, F-154) was actually paying off — pasted calls fed the Valuation pillar with zero feedback loop on whether they were ever right. New own tab "📊 Scorecard" on 🧠 AI Insights (4th sibling tab, split out from "🏦 Research" after live UX feedback showed it required too much scrolling past the Ideas Inbox cards) shows: directional accuracy + PT hit-rate KPIs, a color-coded per-call table, a firm leaderboard (min `ANALYST_ACCURACY_LEADERBOARD_MIN_CALLS`=2 calls), and best/worst-call highlights (gated ≥`ANALYST_ACCURACY_HIGHLIGHTS_MIN_EVALUABLE`=5 evaluable). Directional accuracy uses the window-end close; PT-hit uses the window's **intra-period HIGH** (≥`ANALYST_ACCURACY_PT_HIT_PCT`=0.75 of avg_pt) over `ANALYST_ACCURACY_DIRECTION_DAYS`=30 days — a deliberate compromise after Claude pushed back on the user's initial ask (a lenient 75%-of-target threshold on a plain endpoint close would have inflated the hit rate; checking the window's high instead keeps 75% honest). Two new `analyst_coverage` columns: `price_at_article_date` (auto-filled at save time for held tickers only, via `_ac_resolve_price_at_save`; backfilled for everything else) and `composite_score_at_save` (engine score at save, NULL for non-held — feeds the still-deferred Phase 3 above). **Live production bug found and fixed same day:** rows with no consensus rating at all (legacy data) were silently scored as an implicit Sell call instead of being excluded — added a `no_consensus` status. **Also added, after the user asked whether a secondary price source existed:** Finnhub's free tier has no historical candles at all (confirmed empirically, not just from docs) so it can't be the fallback; FMP can — new `historical_close()` capability added to the `DataProvider` base contract + both yfinance/FMP providers + a new `orchestrator.get_historical_close()` failover function (reuses the existing soft-cap/circuit-breaker quota guard), backing both the batch backfill script and a new live per-row "🔄 Fetch" button on no-anchor rows. Awareness-only throughout — verified zero references from `valuation.py`/`bundle_loader.py`; Opus-reviewed at each constants/provider-touching step (SHIP, one blocking NaN-vs-None bug fixed pre-commit). Phase 3 (engine-vs-analyst calibration) deliberately deferred — see above.
- **Analyst target-consensus daily snapshot — log-only Phase 1 (2026-07-21; commit `a93dd26`; `stock_analyzer/headless_alert_engine.py`, `cron_runner.py`, `stock_analyzer/db.py`, `docs/architecture.md` §6.23; memory `project_analyst_target_snapshots`).** Grew out of the same-day portfolio retrospective (chip rally up, book down -0.21%, TEAM -6.05% on 4-firm PT cuts) — investigation found the existing `"revisions"` alert (`portfolio.py:472-492`, fed by yfinance's `upgrades_downgrades`) only tracks rating-ACTION changes (`up`/`down`/`init`/`main`/`reit`); a firm that keeps its rating and just cuts the price target logs as `main`/`reit`, never `down`, so it's structurally blind to PT-only cuts regardless of size — exactly TEAM's scenario. New `analyst_target_snapshots` table (one row/ticker/day: `target_mean`, `num_analysts`, `info_source`) persists `financials["analyst_target"]`/`["num_analyst_opinions"]` — already fetched every load, zero extra API cost — via an additive `compute_protective_alerts()` return key, written by `cron_runner._run_premarket()` (cron-only, reuses bundles already loaded for the protective-alert checks; mirrors the `exit_signals`/`daily_regime` precedent below). Skips stale-cache-served bundles entirely (no stale flag column — closes the INTC-class contamination risk at the source) and records `info_source` so a future comparison can discount a delta that coincides with a yfinance↔FMP provider switch (the two compute consensus differently). **Deliberately log-only: no alert, no `constants.py` threshold, no gate** — the % drop worth flagging stays uncalibrated until real snapshots accumulate (2-3 weeks); wiring an actual alert into the existing `"revisions"` category is a deferred Phase 2 that will need its own policy discussion + Opus review (touches `constants.py` + alert logic, unlike this pure-plumbing phase). No requirements.md F-row / User Guide change — zero user-visible surface, same precedent as `daily_regime` below.
- **exit_signals cron capture + daily_regime persistence (2026-07-21; `stock_analyzer/headless_alert_engine.py`, `cron_runner.py`, `stock_analyzer/db.py`, `docs/architecture.md` §6.21/§6.22).** Two gaps closed while designing "what to extend next." (1) **Cron capture for exit_signals** — closes the deferred "Cron/headless capture" item from `docs/plans/exit-signal-capture.md`'s own "What is NOT in scope" list; `headless_alert_engine.compute_protective_alerts()` now additionally returns `all_deterioration_signals`/`risk_off_signals` (full WATCH/TRIM/EXIT/RISK_OFF lists, composite_score-enriched from `port_df["Score"]`) alongside its existing EXIT-only `alerts` list (unchanged, zero risk to the protective email); `cron_runner._run_premarket()` writes these to `exit_signals` via the existing `db.save_exit_signals_batch()`. Signal history for hysteresis/behavioral analysis is no longer gapped on days the app isn't opened. (2) **New `daily_regime` table** — one row/day, portfolio-independent, persists `macro_calendar.detect_macro_regime()`'s output via a new `cron_runner._run_eod()` step (`db.save_daily_regime`/`load_daily_regime`, new). Pure persistence prerequisite for Concept D's still-not-built "regime changed since yesterday" Home annotation — **no consuming UI in this pass**, same capture-first pattern as `exit_signals` Phase 1. Also fixed doc drift found along the way: `exit_signals` (shipped 2026-07-18, see below) was completely undocumented in `architecture.md` and missing from this list; the hysteresis parked note above was stale. No `constants.py` touch, no gate/scoring change — no Opus review required.
- **Exit Signal Forward Capture, Phase 1 (Concept A exit-side prerequisite; 2026-07-18; commit `f86147d`; plan `docs/plans/exit-signal-capture.md`).** *(Retroactively added to this list 2026-07-21 — was shipped but never logged here.)* New `exit_signals` table persists every WATCH/TRIM/EXIT/RISK_OFF signal per (ticker, day) from the app's Home MISS-path build (`app.py:4137-4199`), the prerequisite for a future exit-side Behavioral Fingerprint tab (Phase 2, pending ≥30 days of accumulated data — not yet built). Silent background write, no UI. `db.save_exit_signals_batch`/`load_exit_signals`. No constants/gates touched; no Opus review required.
- **Lessons Learned Library (F-195, 2026-07-20; commit `7d7fd74`; reqs F-195).** Structured `lesson_category` text column on `trades`; 10-option fixed taxonomy dropdown in the Trade Journal SELL form's Decision Context expander (alongside the existing free-text `lesson` field, relabelled "Details (optional)"). `decision_journal.LESSON_CATEGORIES` is the single taxonomy source shared by UI and analytics. `compute_patterns()` returns `lesson_category_counts` + `lesson_category_by_outcome` cross-tab. Trade Journal Decision Journal section upgraded to "Pattern Library": frequency count + W/L/avg P&L per category + full entry log with category chips. No constants.py touch; no Opus review required. DDL applied 2026-07-20.
- **Investor Mirror (F-194, 2026-07-20; commits `3302d4c`→`92143af`; reqs F-194; memory `project_investor_mirror`).** New 5th tab "🪞 Investor Mirror" on 🎯 My Edge. Two lenses: (1) **Conviction Alignment** — Spearman rank correlation (rank+Pearson, no scipy) between composite score and portfolio weight; three misalignment patterns: Orphan Conviction (score ≥ `COMPOSITE_STRONG_BUY`, weight < median, blue `st.info`), Accidental Overexposure (score < `CONVICTION_WEAK_SCORE`=50, weight > median, amber `st.warning`), Legacy Overhang (top-`CONVICTION_LEGACY_TOP_N`=3 by weight, score < `CONVICTION_FADED_SCORE`=60, red `st.error`). (2) **Behavioral Biases** — FIFO lot matching from trade journal: Disposition Effect (share-weighted avg days held winners vs losers), Win/Loss Closure Ratio, Breakeven Anchoring (dwell histogram, flag if -2–0% bucket ≥ `BREAKEVEN_ANCHOR_DWELL_RATIO`=1.3× adjacent loss brackets). New module `stock_analyzer/investor_mirror.py` (pure). 10 new constants: `INVESTOR_MIRROR_MIN_CLOSED_LOTS`=10, `INVESTOR_MIRROR_MIN_POSITIONS`=5, `CONVICTION_ALIGNMENT_LOW`=0.30, `CONVICTION_ALIGNMENT_HIGH`=0.60, `DISPOSITION_CONCERN_RATIO`=1.5, `WINLOSS_CONCERN_RATIO`=2.0, `CONVICTION_WEAK_SCORE`=50, `CONVICTION_FADED_SCORE`=60, `CONVICTION_LEGACY_TOP_N`=3, `BREAKEVEN_ANCHOR_DWELL_RATIO`=1.3. **Decision integration:** Phase A — each callout shows current P&L% + role-specific next-step prompt + "📈 Analyse [TICKER]" navigation button per flagged ticker (via `_pending_page` + `_analysis_ticker`). Phase B — conviction_alignment runs eagerly on My Edge page load, publishes `_mirror_orphans`/`_mirror_overexp`/`_mirror_overhangs` to session_state; Analysis Trade Plan tab shows a colour-coded banner above gate checks when the viewed ticker is flagged. Awareness-only, zero gates.
- **Sidebar nav visual redesign + three page renames (2026-07-18; commits `acf5d62`→`2dc6473`; plan `docs/plans/sidebar-nav-redesign.md`).** Five nav groups now have distinct accent colors on headers and active-state left-border (MAIN=`#3b82f6` / RESEARCH=`#22d3ee` / PORTFOLIO=`#22c55e` / ALERTS=`#f59e0b` / AI=`#a78bfa`). Group emoji icons (🏠📈🧩🔔🧠) prepend each header. Active item border and tint match the group's accent — dynamic `<style>` block injected per-render, not a hardcoded blue. PORTFOLIO's 9 items split into Analysis / Activity clusters by a thin divider sentinel (`("__divider__", None, None)` in `_NAV_GROUPS`). Alert badges on Catalyst Watch and Alerts & Actions upgraded to Streamlit markdown chip syntax (`:red-background[● n]` / `:orange-background[● n]`). Three page renames to drop redundant "Portfolio" prefix (group header already implies context): **Portfolio Allocation → Overview** (`🥧 Portfolio Overview`), **Portfolio Health → Health** (`🏆 Health`, page title "Health Score"), **Portfolio Intelligence → Intelligence** (`🧩 Intelligence`). Icon vertical-alignment fix: `align-items:center` on button inner flex container — centers Material icon against wrapped two-line labels. Pure UI/CSS + render-loop only — no constants, gates, scoring, or session-state keys touched; no Opus review required.
- **Next-evolution roadmap — Concept A Behavioral Fingerprint v1, Buy-side only (2026-07-17; commit `da59b00`; reqs F-193; memory `project_behavioral_fingerprint`).** New 4th tab "🧬 Behavioral Fingerprint" on 🎯 My Edge, next to Decision Quality Timeline (same `recommendations`/`trades` substrate, per the plan's coordination note — not a parallel logger). Built AFTER the Week-1 audit above found only 12% historical Buy-side completeness (< 80% threshold) — **user's explicit choice to build anyway, forward-only, so cards fill in as trades accumulate** rather than waiting. Buy-side only: exit-side (TRIM/EXIT) patterns stay out of scope until a new forward-capture mechanism exists (the audit's own finding). New `stock_analyzer/behavioral_fingerprint.py` — three pure, sample-gated functions: `momentum_recency_pattern` (median-split action rate by momentum_score — recency/momentum-chasing proxy, zero new price fetches), `conviction_tier_pattern` (action rate for Strong Buy vs plain Buy, reuses existing `COMPOSITE_STRONG_BUY`), `opening_window_pattern` (SPY-adjusted alpha for opening-window vs later entries, opportunistically reuses Predictive Analytics' `_pac_enriched` cache rather than triggering its own live-price fetch). Every card suppresses to "insufficient data" below `BEHAVIORAL_MIN_SAMPLE_N` (8, new) in either compared bucket — the plan's own critical constraint against premature pattern labeling; given the 12% completeness, **most/all cards are expected to read "insufficient data" today and fill in over time, as intended.** Regime-conditioning requirement satisfied via framing ("observed correlation, not a verdict") rather than actual regime splits, which would fragment the already-thin sample permanently. Scoped to actionable `new_pick`/`add_winner` recs only — same scoping lesson as the F-192 audit (`buy_candidate` excluded). 2 more new constants: `BEHAVIORAL_OPENING_WINDOW_MIN`=30 (direct citation of the plan's own "first 30 minutes" example) and 2 display-copy-only constants (`BEHAVIORAL_MEANINGFUL_ACTION_RATE_DELTA_PP`=5.0 / `BEHAVIORAL_MEANINGFUL_ALPHA_DELTA_PP`=1.0) that only pick which descriptive sentence renders, never gate a card. **Opus review: FIX-FIRST (1 blocking) → SHIP after fix.** Blocking finding: the opening-window pattern's caller-side loop over `_pac_enriched` wasn't scoped to actionable rec_types, letting `buy_candidate` contaminate the opening-window sample — **the identical mistake already caught once earlier the same day** in the F-192 audit expander; fixed with the same rec_type filter. Non-blocking finding also applied: the ±5pp/±1pp "meaningful difference" bands were bare literals duplicated between the pure module and `app.py`; lifted to the two new `*_DELTA_PP` constants and threaded through as a parameter. **Unrelated incident during this build, resolved cleanly:** a concurrent session committed its own `daily_briefing.py` deterioration-WATCH fix (`f15b49f`) to `main` mid-review, which transiently reset the git staging area and caused the Opus reviewer's first pass to (correctly, at that instant) flag an inverted/wrong-file staging state as blocking — verified post-hoc that `f15b49f` is clean (zero Behavioral Fingerprint leakage) and re-staged the actual feature correctly via `git apply --cached` hunk-splicing before committing; no data was lost, nothing needed re-reviewing on this point. **Lesson: when working in a repo with a concurrently-active session/editor, a git-state finding from a review agent can be a snapshot of an in-flight race rather than a defect in the reviewed diff — re-verify the live git state directly before treating it as a commit blocker.** **Confirmed working in production same day (user screenshot):** with 94 actionable Buy-side signals live, the momentum-chasing check correctly fired (48 high-momentum / 46 low-momentum, both ≥ the 8-per-bucket floor) and found a genuine **contrarian-entry lean** — 8% action rate on already-hot names vs 15% on names that scored well without already running hot, i.e. the user acts LESS on momentum-confirmed signals, not more. The other two cards correctly suppressed to "insufficient data" with accurate live counts (conviction-tier: one composite tier too thin; opening-window: only 9 of ~11 acted signals had both a graded outcome and a resolvable trade timestamp). All three behaviors — one card firing, two staying honestly silent — matched the design intent exactly on first live view, no follow-up fix needed.
- **Next-evolution roadmap — Concept A Week-1 data-readiness audit (2026-07-17; commits `253f6ac`→`a57eddd`; reqs F-192; memory `project_behavioral_fingerprint_audit`).** Collapsed expander on 📊 Predictive Analytics answering the plan's own Week-1 question for Concept A (Behavioral Fingerprint): does enough historical signal-to-action data exist to build from history, or must it capture forward-only? **Result: build forward-only — do not attempt a historical-data build.** Buy-side completeness (scoped to actionable `new_pick`/`add_winner`, excluding the awareness-only `buy_candidate` feed) is **12%** all-time and last-90-days (94 signals, 11 acted) — far below the plan's own 80% go/no-go threshold (line 684). Trade-side: only 39% of BUY trades (27/70) were preceded by a logged `RECOMMENDATION` trigger; 61% (43) carry no app-signal trigger — but this is **not** a researched-vs-impulsive read (a trade driven by the user's own analyst research, entered manually, is indistinguishable at this field from a broker-imported trade or a genuinely cold entry; cross-reference 🎯 My Edge → 🔬 Workflow ROI's prep-tier classification for that separate question). **Exit-side signals (TRIM/EXIT/risk_advisor) have ZERO historical record at all, structurally** — computed live each session, never persisted — so disposition-effect/loss-aversion-timing bias patterns can't be built from history regardless of Buy-side completeness; Concept E's `decision_context` capture (Phase 1, shipped same day 2026-07-17) only records a COUNT of active Act-Today items, not per-ticker signal identity, so it does not yet close this gap either. **Reinforces the plan's own "calendar-gated by trade volume" framing** (70 total BUY trades logged) over "historical data already exists, just start now." **Two post-ship bugs, both fixed same day:** (1) the first-pass headline blended `buy_candidate` into the completeness rate, deflating it to 3% — the exact same analytics-integrity mistake the app already fixed once before on Recommendations History's "Missed" count (`feedback_analytics_integrity` memory) — caught by re-checking the screenshot against the plan's own scoping precedent before writing up the finding, not by an Opus review (this audit never touched constants.py/a gate/a scoring formula, so none was required under hard rule #4). (2) a `NameError` on the session cache-HIT path — `_pac_trades_df` is only ever bound inside the cache-MISS branch earlier on the Predictive Analytics page; the new expander referenced it unconditionally and crashed for any session that had already visited the page once. **Lesson: a variable's scope inside an `if/else` cache-branch pattern isn't obvious from a quick read — re-derive independently (`st.session_state.get(...)` + fallback) rather than assuming a sibling block's local var is always bound.**
- **Next-evolution roadmap Phase 2 remainder — risk-adjusted Benchmark Mirror alpha (2026-07-17; commit `7148890`; reqs F-183 update; plan `docs/plans/next-evolution-strategy.md` line 546).** Closes the last unbuilt Phase 2 item. New pure helper `stock_analyzer/benchmark_mirror.py::beta_adjusted_alpha(actual_return_pct, benchmark_return_pct, portfolio_beta)` = actual − (beta × benchmark), None-safe (never fabricates a beta of 1.0 when unavailable). Deliberately **beta-adjusted, not Sharpe** — Sharpe would need a daily portfolio volatility series that isn't readily assembled from existing data, while beta is already computed and cached (`_port_risk_cache`, published by Home's synthesis). 5th KPI card "Beta-Adj. Alpha" on 🎯 My Edge → 📐 Benchmark Mirror, alongside the existing raw "Your Alpha" card; shows "—" with an explanatory tooltip when `_port_risk_cache` hasn't been populated yet (Home not visited this session) rather than assuming a default beta. Answers the plan's own example concern (§1.2): a portfolio running 1.4 beta in a bull rally can show strong raw alpha that is actually just leverage, not stock-selection skill. Pure arithmetic, no new constants, no gate/scoring touch — no Opus review required under hard rule #4's trigger conditions.
- **Next-evolution roadmap Phase 2, Sub-wave 3 (final panel) — Concept B Factor Tilt Heatmap (2026-07-17; commit `a4c95cb`; reqs F-191; memory `project_portfolio_intelligence`). Closes Concept B / Phase 2 — all 3 Intelligence panels now shipped.** Fills in the 📐 Factor Tilt tab: directional exposure to 5 style factors (Momentum=MTUM/Value=VLUE/Quality=QUAL/Low Volatility=USMV/Growth=VUG) via Pearson correlation of held-position returns against factor ETFs over a trailing 6-month window (`FACTOR_ETF_TICKERS`/`FACTOR_TILT_WINDOW_DAYS`=126, new `constants.py` entries set with the user — 6mo chosen over 3mo for statistical stability). `stock_analyzer/portfolio_intelligence.py::factor_tilt()` — correlation-only per the plan's explicit instruction (NOT a regression/beta model, NOT FMP `.info` style tags); ≥20-overlapping-day floor per (ticker,factor) cell mirrors the existing `risk.py` empirical-corr minimum; `portfolio_tilt` renormalizes weights among only the tickers with a valid correlation per factor; all-None tickers excluded entirely, not just uncounted. Button-gated fetch (`_fetch_factor_etf_returns()`, mirrors `_fetch_sector_returns()`'s batch-download pattern) — the only PI panel costing a new API call. **Opus review: FIX-FIRST (2 blocking) on first pass → SHIP (0 blocking) on re-review** — (1) the 2 new constants were undocumented, tripping the mechanically-enforced docs-check gate; (2) `FACTOR_TILT_WINDOW_DAYS` was a genuinely DEAD constant — the fetch hardcoded `period="6mo"` directly, so changing the constant would have done NOTHING; fixed by wiring `_fetch_factor_etf_returns()` to `.tail(FACTOR_TILT_WINDOW_DAYS)` the fetched series, making the constant the real single source of truth (also widened the download buffer to `period="9mo"` for genuine headroom, per a non-blocking re-review note). **Lesson: a constant existing in `constants.py` with a plausible-looking value doesn't mean the code actually reads it — verify consumption, not just presence, especially when a value is "set with the user" and its whole point is to be the thing that changes behavior if adjusted.** Diagnostic only, directional/noisy-at-small-size stated prominently in-app. **Post-ship bug found + fixed same day (commit `f0b6ad7`):** the tab returned empty for every position in production — held-position price history (app's provider pipeline) and the raw `yf.download()`-fetched ETF series disagreed on tz-awareness, so the correlation join found zero overlapping dates for every pair. This exact bug class was already solved twice elsewhere in the codebase (`portfolio._to_tz_naive`, `perf_advisor.py`'s SPY alignment) but wasn't checked before building this — the first PI panel to combine two different data sources. Fixed by applying `_to_tz_naive` to both sides before the join; reproduced the failure with a synthetic tz-mismatched test first to confirm the diagnosis before fixing.
- **Next-evolution roadmap Phase 2, Sub-wave 2 — Concept B Risk Budget Gauge (2026-07-17; commit `698fcd5`; reqs F-190; memory `project_portfolio_intelligence`).** Fills in the ⚖️ Risk Budget tab on 🧩 Intelligence (was a placeholder after Sub-wave 1). Capital weight tells you how much money is in a position, not how much of the portfolio's REALIZED volatility it's responsible for. New `stock_analyzer/portfolio_intelligence.py::risk_budget()` — standard Euler/marginal-contribution-to-risk decomposition: rebuilds its OWN jointly-aligned returns frame from held price history (deliberately does NOT accept the cached `_corr_df_cache` — vol and correlation must come from the exact same date alignment or the covariance model is internally inconsistent), `Sigma = vol⊗vol × corr`, portfolio variance = `w·Σ·w`, each position's risk share = `w_i·(Σ·w)_i/portfolio_vol` (sums to ~100% by construction). Pure numpy/pandas, no new dependency, no new constant (deliberately no "outsize ratio" flagging threshold — just the plain weight%/risk%/ratio numbers, to avoid an unnecessary Opus-review trigger). **Verified pre-ship against 3 hand-checkable cases** (not just unit-tested against arbitrary numbers): equal-vol/zero-corr/50-50 weight → equal risk share; equal-vol/corr≈1/70-30 weight → risk tracks capital exactly (no diversification benefit at corr=1); a 10%-weight high-vol correlated name contributing 27% of portfolio risk (2.70×) against two 45%-weight low-vol names at 0.81× each — the actual "small position, outsize risk" insight this panel exists to surface. Small fix applied during review: the implementer's new `st.dataframe` call used the deprecated `use_container_width=True` — every other `st.dataframe` call in the file was already migrated to `width='stretch'` in the pyarrow-segfault-era cleanup; fixed to match before commit. Diagnostic only. Not a constants/gate/scoring change — no Opus review citation required under hard rule #4's trigger conditions. **📐 Factor Tilt is the one remaining Concept B panel — not started.**
- **Next-evolution roadmap Phase 2, Sub-wave 1 — Concept B Correlation Clusters (2026-07-17; commit `5c980b3`; reqs F-189; memory `project_portfolio_intelligence`).** New standalone "🧩 Intelligence" page (PORTFOLIO nav group, between Risk Analysis and Alerts & Actions) — answers what the portfolio's ownership MEANS in aggregate, not position-by-position. 3-tab skeleton; this sub-wave ships tab 1 only (⚖️ Risk Budget / 📐 Factor Tilt are placeholder "planned" tabs — separate follow-up sub-waves, per the user's chosen cheapest-first build order). **🕸️ Correlation Clusters:** the app already flags correlated PAIRS above `CORR_HIGH_PAIRS_THRESHOLD`(0.65)/`CORR_DANGER_PAIRS_THRESHOLD`(0.80) (`portfolio.diversification_score`) but never grouped them TRANSITIVELY — A-B and B-C flagged as two separate pairs, never surfaced as "these 3 names move together." New pure module `stock_analyzer/portfolio_intelligence.py::correlation_clusters()` — plain-Python connected-components over the existing pairwise `_corr_df_cache` (no scipy/sklearn/networkx dependency, no new constants — reuses the two existing thresholds exactly, zero new API/compute cost). Each cluster carries tickers, size, avg INTERNAL correlation (mean of every pair inside the cluster, not just the edges that formed it), combined portfolio weight, and a danger/warning tier; singletons excluded. Diagnostic only — closing caption explicitly defers to composite score for which name to act on, never re-ranks. Not a constants/gate/scoring change — no Opus review citation required under hard rule #4's trigger conditions. **Concept D's regime-fit item (the plan's Concept B panel 4) is already fully satisfied by Wave 3 — will NOT be rebuilt here.**
- **Next-evolution roadmap Wave 3 — Concept D Regime-Conditional Position Targets (2026-07-17; commit `0c6df7a`; reqs F-188; memory `project_regime_conditional_targets`; see [[project_regime_conditional_targets]] and Wave 1/2 below).** New "🧭 Regime Fit" diagnostic on 🔗 Risk Analysis → Action Plan tab: compares current portfolio beta and cash % against a regime-conditional target, naming the top beta contributors on a breach. **Policy-setting conversation held with the user before any code** (per the plan's own governance requirement) — two new investment-policy dicts in `constants.py`, keyed by the 5 REAL regime ids from `macro_calendar.detect_macro_regime` (`rate_cut`/`inflation_fight`/`recession_fear`/`stagflation_risk`/`neutral`), NOT the plan doc's illustrative Risk-On/Risk-Off labels: `REGIME_BETA_CEILING` (rate_cut=1.25 / neutral=1.10 / inflation_fight=1.00 / recession_fear=0.90 / stagflation_risk=0.85) and `REGIME_CASH_FLOOR_PCT` (5.0 / 5.0 / 10.0 / 15.0 / 20.0) — anchored to (tighter/looser than) the existing regime-agnostic `PORTFOLIO_BETA_TARGET`(1.0)/`_ELEVATED`(1.3)/`_CEILING`(1.4) baseline. Plus `REGIME_CONFIDENCE_MIN_DISPLAY`=40 (display-only — flags a low-confidence regime read rather than presenting it as certain). New pure module `stock_analyzer/regime_targets.py::regime_position_gap()` — zero Streamlit/DB imports, fully None-safe (guards both dict levels of `held_data[t]["risk_metrics"]["beta"]`, mirroring the established `risk_advisor.py` `tr_map` pattern). **Deliberately scoped narrow** (agreed live with the user, not unilaterally): no Growth/Quality Tilt columns from the plan's illustrative table (need Concept B's factor-tilt work — not started), no Home "regime changed" annotation (would need day-over-day regime persistence that doesn't exist — no new DDL added this wave). Risk Analysis independently resolves the FRED key and detects/caches the regime (same `f"_macro_regime_{date}_{bool(fred_key)}"` convention as the Economic Calendar page, reusing a same-day detection there for free) rather than depending on that page having been visited — the plan's "macro regime (already detected)" data-availability claim was not quite accurate (detection there is gated behind "past HIGH-impact events exist," irrelevant here). Opus-reviewed SHIP, 0 blocking (1 non-blocking applied pre-commit: lifted a bare `confidence < 40` literal to the new constant). Zero changes to `risk_advisor.py`/`portfolio.py`/any existing gate. **Post-ship bug found + fixed same day (commit `09be8bd`):** a real-margin user saw "Cash % vs Regime Floor: -178.5%" — the cash-floor formula was copied verbatim from the 💰 Account page's own cash% metric (`cash_balance / (equity+cash_balance)`), whose net-capital denominator shrinks fast under leverage — the SAME instability class the 2026-07-09 concentration-gate EQUITY-basis reversal already had to solve, not re-checked before reuse here. Fixed: skip the percentage and show a plain "Carrying a margin debit of \$X" message when `cash_balance < 0`; added the `ACCOUNT_CASH_STALE_DAYS` staleness guard this section had omitted (the adjacent `_leverage_cache` block already has it). Not a constants/gate change — no new review citation required. **Lesson: matching an existing formula for consistency doesn't make it sound — check whether a DIFFERENT existing precedent in the same codebase already had to fix that formula's edge case.**
- **Next-evolution roadmap Wave 1 + Wave 2 — Concept E capture, Concept F tax lens, Concept C pre-mortem (2026-07-17; commits `8c9c97a`→`f6a9d44`; plan `docs/plans/next-evolution-strategy.md`; reqs F-186/F-187; memories `project_decision_context_capture`/`project_tax_aware_lens`/`project_premortem_protocol`/`feedback_git_partial_staging_line_endings`).** Build order chosen with the user was **E+F → C → D** — differs from the plan's own literal C→F→D priority ordering, after live scoping showed F (Tax) was already ~80% built on the existing `tax_advisor.py`. **Wave 1a — Decision Reconstruction capture (Concept E Phase 1, `8c9c97a`):** new `trades.decision_context` jsonb column, frozen at every interactive Buy/Sell write (composite verdict seen, macro regime, portfolio beta/concentration, active-rec load) via `stock_analyzer/decision_context.py::build_snapshot()` — passive, None-safe, no UI; inert until the one-time DDL (`ALTER TABLE trades ADD COLUMN IF NOT EXISTS decision_context jsonb;`) is applied (now applied and live). Scoped OUT of broker/screenshot/split imports and the `recalculate_from_trades` replay (no live decision context to capture there). **Wave 1b — Tax-Aware Exit/Harvest Lens (Concept F / F-186, `e97658b`):** the exit-direction complement to the existing Buy-side HARVEST gate (F-16/G-08) — amber holding-period note on EXIT/TRIM cards within `TAX_LONGTERM_WINDOW_DAYS` (30, new) of long-term eligibility; wash-sale note on loss SELLs within `TAX_WASH_SALE_DAYS` (30, new); net-of-cost caption on the opportunity-cost expander. Reuses `tax_advisor.py`'s existing FIFO lot replay; 7 new display-only tax constants (5 of them LIFT prior bare literals, behavior-preserving; only `TAX_LONGTERM_WINDOW_DAYS`/`TAX_WASH_SALE_DAYS` are genuinely new). Opus-reviewed SHIP, 0 blocking. **Wave 2 — Pre-Mortem Protocol (Concept C / F-187, `c467c92`):** before a prospective LIVE Buy, an optional Haiku-generated case AGAINST the trade (3 counterarguments — composite-pillar concern, portfolio-concentration impact, macro/earnings context, each required to cite specific evidence) plus a REQUIRED "what would make me wrong" text box, enforced as one more `elif` in the existing ticker/shares/price validation chain — **deliberately NOT a write-interception** (avoided duplicating the ~120-line holdings-sync/concentration-nudge block that SELL's own confirm-card already duplicates as dead code). Scoped to the interactive Trade Journal BUY form only; SELL and every retroactive/batch import path untouched. Opus-reviewed SHIP, 0 blocking. **Post-ship bug found + fixed same day (`165a672`→`f6a9d44`):** the case-against silently failed in every live attempt — `driving_pillar_from_bundle()` assumed `t_signals`/`bq_signals`/`val_signals` were lists (from an earlier research agent's paraphrase), but `technical_score`/`business_quality_score`/`valuation_score` (`technicals.py`/`fundamentals.py`/`valuation.py`) are actually typed `tuple[float, dict]` — slicing a dict with `[:5]` raises `KeyError: slice(None, 5, None)`, not `TypeError`, which a hand-rolled list-shaped unit-test mock never caught. Diagnosed live via a temporary `_debug` hook (added then removed same day) after first differential-testing against the working F-5 "Draft thesis" button to rule out an API/key cause. Fixed with `_signals_dict_to_strings()` — ends up a better prompt input than the original design (real per-factor evidence). **Lesson: verify a dependency's ACTUAL return type from source, not a prior research agent's paraphrase of it — the doc zero-hallucination rule (below) extends to code, not just docs.**
- **Terminology consistency sweep — SHIPPED 2026-07-17 (commit `6f30dab`; 19 edits, `py_compile` clean).** Normalised user-facing copy across 5 dimensions: (1) "holding" → "position" for ownership noun (6 sites; Catalyst Watch tab "📋 Holdings" → "📋 Positions"; time-period uses left untouched); (2) "pick" → "recommendation" for app-surfaced buy directives ("Find Defensive Picks" expander + User Guide); (3) amber severity unified to "🟡 Watch" ("🟡 Warning" + "🟡 Monitor" on Risk Advisor tiles, Sector Rank column, User Guide); (4) red severity unified to "Act Today" ("🔴 Action Required", "⚠ RISK ALERT", "⚠ ACT TODAY" movers badges, "🔴 CRITICAL" news tag, "Fix Now" Trade Review critical tier); (5) "AM Brief" → "Today's Brief not yet loaded — visit Home first" in Evening Debrief fallback. Internal identifiers (`n_warning`, `_last_held_data`, rec_type DB values, etc.) untouched throughout. No constants.py touch; no Opus review required.
- **🎯 My Edge page — SHIPPED 2026-07-16 (commits `aac34b2` → `9303689`; reqs F-183/F-184/F-185; modules `stock_analyzer/benchmark_mirror.py` + `stock_analyzer/decision_quality.py`).** Three retrospective-analytics tabs; awareness-only, zero gates, zero investment recommendations. **Benchmark Mirror (F-183):** shadow portfolio computes what the same `account_flows` cash flows would be worth today if invested in SPY/QQQ at each deposit date. Modified Dietz MWR on both portfolios; three chart views: Cumulative Growth / Cash Flow Attribution / Benchmark Drawdown (portfolio drawdown excluded — `daily_snapshots` equity-only, selling distorts the figure). **Workflow ROI (F-184):** classifies each BUY trade into prep tier (Full/Thorough/Basic/Cold Entry) using thesis + analyst + earnings signals saved before the trade date (`WORKFLOW_ANALYST_LOOKBACK_DAYS` = 90d, `WORKFLOW_EARNINGS_WINDOW_DAYS` = 30d, `WORKFLOW_MIN_THESIS_LENGTH` = 10 chars, all before-only). Three chart views: Tier Comparison bars (zero-trade tiers show greyed "No data yet"), Trade Scatter with per-tier definition key, Coverage Funnel. **Decision Quality Timeline (F-185):** monthly/quarterly A–F grade (composite 0–100) from win rate + profit factor + alpha sub-scores (when benchmark loaded), overtrading penalty per period. Grade bands use `DECISION_QUALITY_GRADE_A/B/C/D` = 80/65/50/35. Three chart views: Timeline bars with grade-band zones, Heatmap (metrics × periods, definition-key strip below), Cohort Compare. Minimum `DECISION_QUALITY_MIN_TRADES` = 2 per period. Opus-reviewed (3 blocking fixes applied before ship: cost-basis cross-product, BUY-lot ascending sort, earnings prep window before-only). 9 new constants in `constants.py`, all measurement-policy only. Docs synced: `docs/architecture.md` constants table + `docs/requirements.md` F-183/F-184/F-185 + User Guide expander added.
- **Earnings Playbook Phase 3 (Catalyst Scanner) — SHIPPED 2026-07-13 (commit `0cac9ee`; was mislabeled PARKED in this queue until 2026-07-16):** `earnings_advisor.build_earnings_catalyst_candidates()` surfaces NON-held **watchlist** names near earnings (within 30d) passing beat_rate ≥ `EARNINGS_MIN_BEAT_RATE_ENTRY` (70.0, `constants.py:464`) + composite ≥ `COMPOSITE_BUY` (65) + reaction ≠ bearish, ranked beat-rate × composite × reaction. Renders as the awareness-only **🎯 Entry Candidates** tab on 🔔 Catalyst Watch (its own tab since the I11 restructure) — needs CNBC earnings context pasted via Ideas Inbox → Pre-Earnings to populate; **never a Buy rec**. reqs F-37b; architecture constants-table + §12.2 ledger; memory `project_earnings_playbook`. Don't re-propose as parked.
- **News sentiment → composite (Phase 2b) — SHIPPED 2026-07-14 (commits `10edd8f`→`9b371fd`; memory `project_sentiment_llm_phase2b`):** Shipped **differently from the deferred proposal** — there is NO additive `NEWS_SENTIMENT_WEIGHT`. Instead, the existing **10% sentiment pillar** (`COMPOSITE_WEIGHTS["sentiment"] = 0.10`, feeds `s_score`) had its VADER scoring replaced by a **bidirectional, ticker-aware Haiku rescore** (`rescore_headlines_llm()` in `news_intelligence.py`) that understands financial context — VADER was penalising AAPL for an IBM miss and scoring CRWD "Climbs 11%" at 0.00. Per-headline swing bounded by `SENTIMENT_LLM_MAX_SWING = 0.5` (`constants.py:415`). Day-cached in the `sentiment_llm_cache` Supabase table (PK `(ticker, score_date)`; first caller writes, rest read — app + cron share the same composite). Fail-open: `rescore_headlines_llm` returns `None` on failure → VADER preserved. This **did** change what the engine recommends (the sentiment-pillar input quality), shipped under the prior session. **Don't re-propose the 5% additive-weight design** — the pillar-rescore approach superseded it.
- **Critical-news noise gate + suppress-only LLM rescorer (2026-07-13; commit `6c055ec`; reqs F-72):** Two fixes for the "MONITOR — Critical News" Act Today card that was firing on a single borderline VADER-negative headline (BIIB −0.27, a Tier-2 source). **(1) Headline count gate:** `NEWS_CRITICAL_MIN_HEADLINES = 2` (new constant, `constants.py:408`) — card now requires ≥ 2 qualifying headlines (compound ≤ `NEWS_SENTIMENT_CRITICAL` (−0.25), tier ≤ 2) per held ticker before firing. **(2) Suppress-only LLM rescorer:** `rescore_news_items_llm()` in `news_intelligence.py` (Haiku, temperature=0, 8s timeout, VADER fallback) re-scores VADER values with financial-domain context — **suppress-only: LLM can only RAISE a score (remove false-positive negatives), never lower one**, so it cannot create new Act Today cards or flip buy-candidate verdicts. Cache key = day+sorted-ticker-set (independent per call site; two sites: Home + Analysis). Opus-reviewed ×2 (design + final). No new gates beyond the constant.
- **Grow Today staleness gate + mover label accuracy + section header UX (2026-07-14; commits `cfc8dc3`→`29e8c1f`; reqs F-180 / NF-44; architecture §4.0.1 + §10):** Four coordinated fixes. **(1) Data freshness gate (root cause: INTC incident):** INTC appeared as "New Position to Initiate" when `load_all()` fell back to the Supabase `bundle_cache` (data up to 5 days old, `stale_as_of ≠ None`), producing composite ≥ 65. After the 30-min TTL expired, fresh Analysis showed composite 32.1 (Sell) — the app had recommended based on stale data. Two checks added to `_grow_today()` in `daily_briefing.py`, inserted before the existing `fundamentals_available` gate: (a) `stale_as_of is not None` → `composite_unavailable` ("Pending Verification" banner + Refresh button); (b) `fund_cache_age_days > GROW_TODAY_MAX_FUND_AGE_DAYS` → same path. New constant `GROW_TODAY_MAX_FUND_AGE_DAYS = 2` added to `constants.py` after `BUNDLE_CACHE_MAX_AGE_DAYS`. `_SYNTH_SCHEMA_VER` bumped 1 → 2 to flush warm session caches. **(2) Mover label accuracy:** mover header chips were showing "Momentum 30/100" — a score that (a) did not gate them (movers enter via day-change breakout, bypassing the momentum bar), (b) isn't a composite pillar, and (c) doesn't appear in Analysis Deep Dive. `is_mover: bool = False` added to `reconcile_signals()` in `signal_reconciliation.py` and to `_cross_reference()` in `daily_briefing.py`; `is_mover=is_mover` passed at the `_grow_today()` call site. When `is_mover=True`: `mom_str = "Breakout today"` instead of `f"Momentum {momentum_score:.0f}"`. In `app.py`, mover header chips skip the "Momentum X/100" prefix; score line starts directly with composite. **(3) UX section header boxes:** "New Positions to Initiate" rendered as a styled box (`background:#15803d`, `border-left:4px solid #4ade80`) with subtitle: "Composite score, sector diversity, concentration limits, macro event check, and act-today conflicts all cleared. The app considers these ready to enter within your sizing rules." "More Buy Candidates" subtitle moved inside the existing box with an explicit disclaimer that concentration limits, sector caps, and macro gates have NOT been applied — treat as research leads, not direct entry signals. **(4) Macro gate same-day lift (commit `a691c57`):** the macro gate was treating same-day HIGH events as "still upcoming" all day — CPI at 08:30 ET suppressed new picks until midnight. When `_d == 0`, `_grow_today` now checks if the release resolved: ET clock ≥ `ev["time_et"]` (primary) OR FRED `ev["released"]` (secondary); gate lifts if either fires; fail-safe keeps it on if either check errors. FOMC ("Fed Policy") carved out — suppress-all-day preserved for Fed decisions.
- **Today's Brief auto-refresh timer + Pre-Earnings watchlist auto-add (2026-07-13; commits `6aff705`, `218296c`; reqs F-177 / F-174 update; docs/architecture.md §10 gained both rows).** User noticed the "Built at" freshness chip promised "auto-refreshes in N min" but nothing ever triggered a rebuild (Streamlit only reruns on interaction; the `_home_synth_cache` signature had no time component) — fixed with a `@st.fragment(run_every=60)` watchdog (mirrors the existing live-price-strip fragment) that bumps a new `_brief_auto_refresh_nonce` and forces a full rerun once `BRIEF_AUTO_REFRESH_MINUTES` (30, new constant) elapses AND the market's open AND Setup isn't Locked — **deliberately never calls `st.cache_data.clear()` or `scan_sectors()`**, so it can't multiply FMP/Finnhub/yfinance quota usage beyond the pre-existing 30-min `load_all()` TTL cadence. Separately, investigating why a CNBC-mentioned pre-earnings stock (SPOT) never surfaced in Grow Today despite a 74 composite/Buy signal surfaced a real gap: saving a Pre-Earnings preview (Ideas Inbox) enriched `earnings_context` but never touched the watchlist, and `_grow_today`'s candidate pool is sourced ENTIRELY from `scanner_results` (curated `SECTOR_UNIVERSE` + watchlist `extra_tickers`) — a name absent from both can never be scanned, let alone recommended, regardless of score. Fixed by auto-adding saved Pre-Earnings tickers to the watchlist on save (mirrors F-154's Stock Research mode). Also clarified (no code change) that a bear-tape "🛡️ Protect Mode" day hard-blocks ALL new Grow Today entries (`daily_briefing._grow_today`, `tone == "bear"` → empty `new_picks`) independent of any ticker's own score — the Watchlist page's per-ticker "Ready to Enter" readiness card is a separate, tone-blind module (`watchlist_advisor.py`) that answers a different question. In-app User Guide updated (🏠 Reading Today's Brief + 🔭 How the app finds candidates expanders).
- **Earnings Playbook — Phase 1 (CNBC extractor + playbook enrichment) + Phase 2 (post-earnings thesis checkpoint), SHIPPED 2026-07-13 (commits `7d09857`→`cf6cc19`; plan `docs/plans/earnings-playbook.md`; memory `project_earnings_playbook`).** **Phase 1:** new `stock_analyzer/earnings_intel.py` — `extract_playbook()` LLM-extracts `beat_rate_pct`/`recent_reaction_direction`/`consensus_growth_pct`/`what_to_watch_cnbc` per ticker from pasted CNBC Pro preview articles (same zero-hallucination/graceful-degradation idiom as `analyst_intel.py`). 3 new constants: `EARNINGS_BEAT_RATE_REDUCE_THRESHOLD`=60.0, `EARNINGS_BEAT_RATE_STRONG_THRESHOLD`=75.0, `EARNINGS_BEARISH_REACTION_COMPOSITE_GATE`=75. `earnings_advisor.build_earnings_playbook()` gains an optional `earnings_context` param; `_recommend()` adds two CNBC-derived REDUCE conditions (poor beat history + weak composite; bearish reaction + composite below gate) and a HOLD_OR_ADD annotation when beat rate + reaction are both strong — **CNBC data cannot override an already-determined EXIT.** Catalyst Watch playbook cards gain a CNBC badge/expander + beat-rate/consensus-growth/reaction-history lines. `db.save_earnings_context/load_earnings_context/load_earnings_context_batch()` — DDL applied 2026-07-13, active. **Phase 2 (thesis checkpoint):** `thesis_advisor.generate_earnings_thesis_update()` — suggestion-only (INTACT/WEAKENING/BROKEN), never auto-saves; a CTA on 🧠 AI Insights → Positions surfaces when an `earnings_results` row exists within 14 days; Confirm writes a `thesis_reviews` row with a `source="earnings_checkpoint"` annotation, Dismiss clears the pending state. **Post-Earnings capture rebuilt same-day (`cf6cc19`):** CNBC doesn't publish consolidated post-earnings roundups (paste-per-ticker was impractical UX), so the original LLM-paste capture was replaced with a Finnhub-native fetch — `earnings_intel.fetch_recent_results()` (pure; Finnhub `/stock/earnings?limit=1` per ticker, filters to `lookback_days` (default 90), returns actual/estimated EPS + beat + surprise% + quarter; `[]` on failure, no LLM dependency) behind a held-ticker multi-select + "Fetch from Finnhub" button, with a manual-entry fallback for tickers Finnhub's free tier doesn't cover; the LLM paste path (`extract_results()`) stays in the module but is no longer wired to the UI. Ideas Inbox on AI Insights gained a 3-mode toggle [📰 Stock Research · 📅 Pre-Earnings · 📥 Post-Earnings Results]; Catalyst Watch nav badge now includes earnings EXIT+REDUCE count (`_earnings_posture_alerts_cache`). DDL applied 2026-07-13 for both `earnings_context` (Phase 1) and `earnings_results` (Phase 2) — both active. **Docs synced 2026-07-13:** `docs/requirements.md` gained F-174 (Phase 1 CNBC enrichment)/F-175 (Phase 2a Finnhub auto-fetch)/F-176 (Phase 2b F-1 checkpoint); `docs/architecture.md` gained the 3 constants-table rows, §6.18/§6.19 (`earnings_context`/`earnings_results`), a §10 Known-Behaviours row, and 2 rows in the §12.2 AI-feature table (F-175's Finnhub fetch has no LLM call, so it's requirements-only, not in §12.2).
- **Nav follow-up — 3-phase cleanup, Home fully de-tabbed (2026-07-12/13; commits 2a61398, bb508c7, + Phase C):** User caught issues reviewing the live Phase-3 tab consolidation, resolved over 3 phases. **Phase A:** Home's "📊 Portfolio" tab collided with the pre-existing PORTFOLIO sidebar nav group (Trade Journal/Trade Review/Recommendations History) — extracted it + "📈 Analytics" into a new standalone page `elif page == "🥧 Portfolio Overview":` (PORTFOLIO group, first), 2 internal tabs, `_render_portfolio_not_loaded()`+`st.stop()` gate. New cache `_risk_advisor_recs_cache`; `_last_held_tickers` newly published so the rebind uses Home's full holdings list, not a subset. Opus-reviewed SHIP. **Phase B:** remaining "🤖 AI Brief" tab renamed to "🤖 AI Snapshot" — zero functional overlap with 🧠 AI Insights found (on-demand/multi-provider vs persisted/Anthropic-only/cadence-driven), but real discoverability gap (zero User Guide coverage); renamed not moved (moving would break AI Insights' single-provider/persisted design). Mutual cross-reference captions added both surfaces. **Phase C:** split "⚠️ Risk & Alerts" into two standalone PORTFOLIO-group pages — "🔗 Risk Analysis" (reuses pre-Phase-3 name, reduces doc drift) and "⚠️ Alerts & Actions" — plus **Home's remaining 2 tabs (Evening Debrief, AI Snapshot) converted to plain sequential full-width sections, dropping `st.tabs()` from Home entirely** (matches Today's Brief's own promoted-section treatment; user's explicit choice — 2 tabs left didn't justify the widget). 8 new cache keys (`_alert_list_cache`, `_actions_cache`, `_div_recs_cache`, `_corr_df_cache`, `_div_score_cache`, `_avg_corr_cache`, `_risk_pairs_cache`, `_div_label_cache`) + `_n_danger_cache`/`_n_warning_cache` (restores the sidebar danger/warning badge the old tab label carried, now on the "Alerts & Actions" nav entry, mirroring the existing Catalyst Watch badge pattern). Rate Sensitivity's "no holdings" empty-state branch deleted as confirmed-dead code (verified `rate_sensitivity_per_ticker()` can only return empty when `port_df` is empty, which the new page's own load-gate already precludes). Opus-reviewed SHIP (0 blocking) on both Phase A and Phase C (the two structural moves); Phase B was label/doc-only, no separate review. `CLAUDE.md`'s own cache-key list (this section) was refreshed 2026-07-13 — it had drifted stale even before this work.
- **UX review remediation — 5 phases (2026-07-12; commits 1b33c8c→dfa1b77; docs/reviews/2026-07-12-UX-review.md):** Phase 1 stripped dev-string/identifier leaks from user-facing copy (file paths, SQL locations, a fixed DDL date, raw `rec_type`/`pt_change` tokens — the latter two now display-labeled via `format_func`/`.map()` while the underlying DB values + the `analyst_intel.py` AI-extraction contract stay untouched). Phase 2 single-sourced 11 hardcoded display values to `constants.py` (composite weights/tiers, VIX regime, correlation-pair tiers, earnings/calendar windows) — 3 new constants (`RISK_ON_VIX_LEVEL`=15.0, `CORR_HIGH_PAIRS_THRESHOLD`=0.65/`CORR_DANGER_PAIRS_THRESHOLD`=0.80, `ECONOMIC_CALENDAR_WINDOW_DAYS`=45), no values changed, Opus-reviewed SHIP. **Phase 3 — the Home page's 11 tabs consolidated to Today's Brief promoted to an always-visible full-width section (no longer a tab; its `_db_icon` urgent-action badge moved to `st.subheader`) + 5 tabs: Evening Debrief (unchanged), Portfolio (= Overview+Performance+P&L), Risk & Alerts (= Alerts & Actions+Risk Analysis), Analytics (= Relative Strength+Sector Rotation+Rankings), AI Brief (unchanged).** Purely structural — verified via an independent content-preservation diff (zero unexpected differences across ~17,785 real-code lines) and a second Opus review pass with its own tokenizer-level re-verification; SHIP, 0 blocking. Phase 4 consolidated 4 differently-worded "portfolio not loaded" messages into one parameterized helper (`_render_portfolio_not_loaded`); Rate Sensitivity deliberately excluded (different meaning — zero holdings vs haven't-visited-Home). Phase 5 standardized WATCH/MONITOR→"Watch", fixed a genuine raw-code leak on the Watchlist page (`_action.replace('_',' ')` was rendering shouty codes like "ENTER NOW"), compacted a verbose verdict badge, and confirmed Trade Plan/Exit Plan's dynamic label is correct as-is. Deliberately **not done in this pass**: the "Composite Score vs Conviction" terminology item (needs per-site manual judgment across ~190 occurrences, not a mechanical swap) and the full terminology-consistency sweep (signal/alert/recommendation/pick, Position/holding, Brief-name variants, severity-system unification — each 200-1000+ occurrences, mixing user-facing copy with code identifiers; needs its own scoped pass). `docs/requirements.md` F-32/NF-43 updated to drop stale "tab" framing for Today's Brief.
- **fix(deps): pyarrow pin — second production segfault (2026-07-12; commits 9e5c913→d6dfad9):** App SIGSEGV'd on every Streamlit Cloud deploy. First fix attempt (migrating deprecated `use_container_width=`→`width=` on all `st.dataframe`/`st.table` calls) was necessary cleanup but didn't stop the crash — turned out coincidental (the deprecation warning and the crash were both triggered by reaching the same call, not causally linked). Root cause: `requirements.txt` never pinned `pyarrow`, and streamlit's own `pyproject.toml` deliberately leaves it unbounded (`>=7.0`, no ceiling — "pyarrow is not semantically versioned... we don't put an upper bound on it"); pyarrow 25.0.0 published 2026-07-10, 2 days before the incident — Streamlit Cloud's `uv` resolver almost certainly pulled that unbaked native wheel on both crashing deploys. Pinned `pyarrow==24.0.0` (~3mo field-tested at time of pinning) + migrated 4 `st.data_editor` calls the first pass missed (same Arrow-render path as `st.dataframe`). See memory `project_pyarrow_unbounded_dep` — this is a **recurring risk**, not a closed one-time fix: any future pyarrow major release could reintroduce the same crash class since streamlit will always accept it.
- **Broker history text import — replace Vision with regex parser (2026-07-10; commit 4e4f46c; reqs F-87b):** `broker_screenshot.parse_robinhood_screenshots()` (Vision/Opus 4.8) replaced by `parse_robinhood_text()` (pure-Python regex, no Vision API). Root cause: Robinhood History screenshots don't include full dates, making year inference unreliable; the actual user workflow is copy-paste from the History page, which is four structured lines per order. Parser anchors on `"Individual · [Month Day]"` lines, captures company+order-type+buy|sell from the preceding line, skips "Canceled" orders, reads shares+price from the following line. Ticker resolution: local lookup table (~60 company names) → Claude text API fallback for unknowns (optional; no API call required for common names). `_infer_year()` logic unchanged. `last_screenshot_sync_date()` accepts both `"RH screenshot"` and `"RH text import"` notes tags for backward compat. All downstream logic (three-bucket reconciliation, editable preview, `find_app_only_in_range`, `recalculate_from_trades` write path) is unchanged. Tab label changed from "📸 Screenshots" to "📋 Paste history". No new policy constants or gates.
- **deps: numpy ABI pin + pytz bound extension (2026-07-10; commit 751cd4b):** `numpy>=1.24.0,<2.0` added to `requirements.txt` — Streamlit Cloud's `uv` resolver was picking numpy 2.x, whose changed C API caused a SIGSEGV during the first `st.dataframe` render (the ABI break hits any binary wheel compiled against numpy 1.x). All `pandas>=2.0.0,<3.0` versions support numpy 1.24+, so no package conflicts. Also extended `pytz<2026.0` → `<2027.0` (the old bound was expiring). No logic change.
- **Stress test — historical scenario replay / model-vs-actual comparison (2026-07-10; commit 0e8dc9f; reqs F-168):** For the three scenarios that map to a real market event, an "📅 How did your holdings actually perform?" expander appears below the stress-test results on 🏠 Home. `stress_test.HISTORICAL_WINDOWS` maps three scenario IDs to `(start, end)` tuples: `covid_crash` (2020-02-19 → 2020-03-23), `rate_shock_2022` (2022-01-03 → 2022-10-13), `gfc_2008` (2007-10-09 → 2009-03-09). `fetch_historical_drawdowns(scenario_id, tickers)` fetches actual OHLCV via yfinance (lazy import; `multi_level_index=False`) and computes peak-to-trough as `(min_close − first_close) / first_close × 100`; tickers with < 5 trading days return `None` (shown as N/A). Loading is button-gated and cached in `st.session_state[f"_hist_stress_{scenario_id}"]`; a Refresh button clears the cache. Table shows **Model Est. (%) / Actual (%) / Δ (Actual−Model)**; rows green (Δ > 5) / red (Δ < −5). Custom Scenario and non-historical scenarios show no expander. Awareness only; never gates.
- **Portfolio intelligence uplift — stress test expansion + behavioral analytics + rate sensitivity + engine trust + opportunity cost + broker import (2026-07-10; commits 3cbd6a6→97590fb; reqs F-14a / F-87a / F-87b / F-164–F-167):** Six surfaces shipped across two sprints. (1) **Stress test GFC 2008 + Stagflation** — `stress_test.SCENARIOS` now has 9 named scenarios; GFC (`spy_move=−57%`, Financials −78%, EV & Auto −75%) and Stagflation (`spy_move=−18%`, Energy +30%, AI & Data −30%) both use per-sector overrides via `_SECTOR_SHOCKS` (not a pure beta-scaled move). (2) **Overtrading detection** — `trade_analytics._build_overtrading_stats()` compares current calendar-month BUY+SELL count vs rolling 12-month avg; HIGH ≥ 2.0× / MEDIUM ≥ 1.5×. **Also fixed:** `build_full_analytics()` now returns `win_rate` (was computed but silently dropped). (3) **Behavioral KPI strip on AI Insights** — second HTML row on the "At a glance" status strip: Win Rate / Profit Factor / Trades This Month / Behavioral Alert count; HIGH insights surface as `st.warning()`. (4) **Rate sensitivity table** — `risk.rate_sensitivity_per_ticker()` (structural macro sector score + empirical TLT Pearson corr, ≥20 days), `data.fetch_tlt("3mo")`, `_cached_tlt()` (30-min TTL), rendered between correlation heatmap and Risk Action Plan on 🔗 Risk Analysis. (5) **Engine Trust by Band** — `recommendations_history.engine_trust_by_band()` (3 bands: <65 / 65–74 / ≥75; action rate + acted/passed alpha + edge comment), collapsed `st.expander` in Trade Journal. (6) **Opportunity Cost expander** — 90-day `new_pick` window, `distinct_missed()` + `missed_split()` + `compute_outcomes(min_days=5)`, 4-KPI strip + top-5 tickers, lazy in `st.expander`. (7) **Broker history text import (F-87b v1)** — `broker_screenshot.parse_robinhood_text()` (regex, pure Python, no Vision); see the 4e4f46c entry below for the definitive description. All awareness/display-only; no new policy constants or gates. **Also resolved:** the ImportError on Streamlit Cloud was caused by `f0b946c`'s `app.py` importing `broker_screenshot` before `broker_screenshot.py` was in origin — fixed by pushing `97590fb`. **Also fixed (04280d5):** `_live_prices` stores `{ticker: {"price": float, ...}}` dicts, not plain floats — `float(p)` in the opportunity-cost and engine-trust price comprehensions raised `TypeError`; fixed to `p["price"]` with `isinstance(p, dict)` guard.
- **Rate-limit resilience Phase 3 + fine-grained error taxonomy + observability hardening (2026-07-10; commits 2b8a778→587fe3d; reqs NF-22/NF-28; memory project_second_data_source):** Four changes shipped together. (1) `classify_error()` in `providers/_util.py` routes exceptions to `auth`/`quota`/`rate_limit`/`parse`/`error`; all three keyed providers (Finnhub, FMP, yfinance) now call it — replacing the old `is_rate_limit()` bool. `api_health` gains `auth_errors`/`parse_errors`/`quotas` counters; RED trips on auth ≥ 1; YELLOW on quota ≥ 1 or parse ≥ 3 or error-rate > 20%; cooldown still at auth/rate ≥ 3 or consec ≥ 5. (2) FMP daily quota counter: `db.increment_daily_quota("fmp")` fires after each successful FMP call; `api_health.get_fmp_daily_quota()` caches the Supabase read (5-min TTL); when ≥ `FMP_DAILY_SOFT_CAP` (220), orchestrator drops FMP from capable list — Phase 3 of rate-limit resilience. DDL applied in Supabase (2026-07-10) — feature is active. (3) Finnhub endpoint tagging: every error is prefixed with endpoint/ticker (`"quote/AAPL: ..."` vs `"news-sentiment/AAPL: ..."`); empty-body guard raises `ValueError` before `resp.json()` so silent HTTP 200+empty correctly routes to `rate_limit`. (4) Data Health chip: shows `quota`/`auth`/`parse` badge counts, "today: N/250" FMP quota line, and an explanatory note under the bundle cache row. Bundle-cache `maybe_single()` bug fixed (`b4da95d`; use `.limit(1)` everywhere per `feedback_supabase_single_row`). **Also:** Recommendations History default filter now loads with `new_pick + add_winner` only — `buy_candidate` (awareness feed, never a gated rec) was inflating "Missed" counts; still selectable for traceability (`587fe3d`). **Do NOT re-chase any of these sub-items; all done.**
- **Hard-Cap-Breach rebalance plan + concentration EQUITY-basis reversal (2026-07-09; reqs F-22c / G-19 / F-09d; memories project_concentration_discipline, feedback_recommendation_transparency, project_home_synth_memoization):** the sector "Hard Cap Breach" Act Today card is no longer a dead-end — an opt-in, button-gated **🔎 Rebalance plan** (read-only; engine composite is the SOLE ranker/gate; correlation + analyst only annotate) names **what to trim** (holdings ranked by composite ASC with the pillar/momentum **basis** + a greedy **quantitative plan** — `portfolio.trim_allocation` distributes the headline `excess_dollar`: full-exit weakest→partial last, `$ · pp · ≈shares · full/partial · 🧾 gain/loss`, reconciles to 25%) and **where to redeploy** (engine-vetted candidates in under-rep sectors via `diversifying_candidate_pool`+`annotate_add_candidates`, each with **correlation-to-your-actual-book** `portfolio.correlation_to_portfolio` + `portfolio_return_series`, `REDEPLOY_CORR_DIVERSIFIER_MAX` 0.40 / `_CORRELATED_MIN` 0.70 = display labels not gates; + awareness-only analyst note — the NARROW re-open of the deferred F-154a Brief chip, this surface only). Pure helpers also incl. `trailing_return`. Commits `4b89680`→`ff5b1ac`, 2× Opus-reviewed. **Then the gate-basis REVERSAL (`a3fe9c1`, Opus-reviewed):** concentration DECISIONS (15%/35% gates + risk_advisor trim/nudge recs + peripherals) moved from the 2026-06-26 net-capital "tighter-of-both" basis back to **plain EQUITY weight** — a transient margin balance was lurching a hard gate (a sector read 166.9%, demanded ~85% liquidation). One wiring point (`app.py` ~2710: `Gate Weight (%)`=equity `Weight (%)`, `gate_denom`=equity→`_acct_f`=1); `gating_denominator` retired from the gate path. **Leverage/margin risk preserved as awareness-only** (`_leverage_cache`→🔗 Risk Analysis read + 💰 Account note; F-09d) — NEVER gates. **Do NOT re-base gates to net-capital without an explicit policy discussion.** Also `9cf82d9`: `_SYNTH_SCHEMA_VER` added to the Home-synthesis signature so a deploy that changes the synthesis OUTPUT SHAPE invalidates warm session caches (bump it on any shape change). All docs synced (G-19/F-09d/F-22c, architecture known-behaviours + constants, account-baseline 2026-07-09 reversal §, User Guide).
- **Stop transparency — held-stop explainer + what-if simulator (📈 Analysis Trade Plan; read-only, NEVER gates/sizes/scores; reqs F-47/F-47a; memory project_stop_ladder_and_display):** an opt-in "🛡️ How your stop is set — and what happens next" expander on a HELD position's **Hold** tab AND its **Buy/Strong-Buy (add)** tab. Shows a **stop-ladder chart** (active stop = ✓-star, losing candidate dimmed, "tightest wins" arrow, red/green stop-out-vs-holding zones, colour-coded role legend), a **profit-lock staircase** (how the ratchet floor climbs tier-by-tier, from `stop_ladder`'s new `ratchet_rungs`), a **3-layer plain-English walk-through** (ATR volatility floor → profit ratchet → manual override), and a **what-if price simulator**. Faithful by construction — one pure helper `portfolio.stop_ladder` reuses `protective_stop` + the new single-source `ATR_STOP_MULT` (2.0). **Also fixed a latent bug:** the Analysis "Stop Loss" metric/narrative/scenarios for a held position showed the raw ATR entry stop `r["stop"]`, UNDER-reporting the ratcheted protective stop the Brief acts on (`_sa_holding["Stop"]`) once a ratchet tier engages — now shows the held stop (Analysis = Brief). On the Buy-add tab the note distinguishes the **sizing stop** (`r["stop"]`, ATR fresh-entry or manual — what add shares/R:R size off) from the **protective stop** (ratcheted). **Policy decided (don't re-open):** add-sizing STAYS on the ATR fresh-entry stop — sizing off the ratchet would over-buy into strength (pro-cyclical). All SHIPPED 2026-07-06 (`5a553e2`→`859c708`), 3× Opus-reviewed. **Manual-gate unified 2026-07-07:** the Analysis manual-override now gates on the same ratcheted `protective_stop` (= max(ATR, floor); avg_cost from the frozen `_port_df_enriched` frame) as `build_portfolio_df` — via the shared `portfolio.manual_stop_wins` predicate — so a manual stop in the `[ATR, floor)` gap is rejected on both surfaces and Analysis agrees with the Brief (was a raw-ATR outlier). **Breach-gate also unified 2026-07-07:** the Analysis `_stop_breached` gate now reads the ratcheted/manual `_sa_holding["Stop"]` (Brief's Gap basis) and uses the Brief's exact `round(gap,1) ≤ 0` test, so the red "⛔ stop breached" banner fires exactly when the Brief flags a breach (was under-firing in ratchet-wins territory; `ps`/R:R still on `r["stop"]` = ATR fresh-entry — only the gate moved). **Price-basis also unified 2026-07-07:** the Analysis page now merges the same `_live_prices` snapshot into `results` current_price before the stop gates (mirrors the Home merge into `held_data` before `build_portfolio_df`; reuses the strip snapshot, zero extra API cost), so the breach/manual gates + Price/P&L/sizing/R:R run on the live price the Brief used — the mirror is now **price-exact**. **All three stop divergences (manual-gate + breach-gate + price-basis) closed; Analysis fully mirrors the Brief on stops.** Only inherent floor (not a defect): `_live_prices` refreshes every 60s while the synthesis snapshot is frozen, so Analysis can read a slightly newer price than `build_portfolio_df` used (same skew as Home's live strip vs its frozen `port_df`; self-heals). **Review-cluster hardening 2026-07-07 (P1 `fdcd908` / P2 `8402390` / P3 `3016226`; `docs/reviews/2026-07-07-review.md`; 8 findings, 7 fixed + 1 accepted):** (F1/F6) the manual-gate avg-cost map now reads `_port_df_enriched["Avg Cost"]` with **per-row** coercion — a bare `except` wrapping the whole loop could wipe the map on one bad cell → silently drop the gate to raw ATR for the whole book; (F3) the breach gate now mirrors the Brief's Stop-Unavailable skip via `pd.isna("Gap to Stop (%)")` (None **and** float-NaN) so no ⛔ fires where the Brief is silent; (F8) `GAP_TO_STOP_ROUND_DECIMALS` single-sources the breach-boundary rounding; (F4) breach banner labels the stop naturally per type; (F7) `portfolio.manual_stop_wins` single-sources the override policy across all 3 sites (build/Analysis/`stop_ladder`); (F5, accepted) the sub-0.05% rounding false-positive is intentional (mirrors the Brief). P1 & P3 Opus-reviewed (SHIP).
- **Sector-classification resilience (own fix; reqs F-39f; memory project_sector_classification):** the held-position sector chain is now curated `TICKER_SECTORS` → provider `.info` → persisted `sector_cache` (Supabase; one-time DDL applied) → "Other". TRAP that motivated it: the 35% sector gate **excludes "Other"**, so when Yahoo's sparse-`.info` days dropped a holding into "Other" it silently **disabled sector gating** for that name (≈half the book on 2026-07-06). `bundle_loader` now write-throughs a good `.info` sector and serves last-known when live is empty; curated map expanded (COF/HOOD→Financials, BIIB/BSX→Healthcare, LRCX→Semiconductors, EOG→Energy, BKNG→Consumer Tech, SPCX→Communications). **Scoring stays on RAW `.info`** (not the cache) so the resilience fix can't move composites. SHIPPED 2026-07-06 (`4089fac`→`ae25e97`).
- **Analyst Coverage — "Ideas Inbox" (own surface on 🧠 AI Insights; awareness-only, NEVER gates/scores/verdicts; plan docs/plans/analyst-coverage.md; reqs F-154/F-154a/F-154b; memory project_analyst_coverage):** paste professional analyst research (CNBC Pro / broker notes) → LLM (`analyst_intel.extract_report` → `list[dict]`) extracts **atomic per-firm facts as ONE record per covered stock** (a multi-stock "top picks" roundup never merges — each analyst attaches only to their stock; `derive_consensus` computes avg/high/low PT + label in pure Python so no arithmetic is hallucinated), editable **N-card preview** → `analyst_coverage` table (one-time DDL, applied). **Phase 1** = Ideas Inbox capture. **Phase 2** = 🏦 Analyst Coverage tab on 📈 Analysis (reconciles the provider `targetMeanPrice` consensus vs your saved research) + F-1 thesis reviewer ingests consensus as **CONTEXT ONLY** (citable, never upgrades a verdict). **Phase 3** = Grow Today "New Positions to Initiate" card annotation (display-only). All SHIPPED 2026-07-04/05 (`19620b3`→`cacc0ab`). **Deferred by choice** (don't re-propose): the Brief awareness chip (calm-advisor / anti-noise persona, §2B), the analyst-conviction "soft tie-breaker" (would let analyst data influence recommendation ORDER — violates awareness-only), and PDF/file upload (input is paste-only).
- **🧠 AI Insights page layout** (2026-07-05, `b8bc336`; memory feedback_ui_polish): flat vertical scroll → persistent **"At a glance" status strip** (thesis-attention / weekly / monthly / research freshness) + **cadence tabs** [🩺 Positions · 📅 Debriefs · 🏦 Research]. Structural re-housing only (no logic change). The status-strip + cadence-tabs pattern is the **template for the eventual broader UX pass** on other multi-section pages.
- **CI Node 24** (2026-07-04, `3295598`): `actions/checkout@v5` + `actions/setup-python@v6` (Node.js 20 runner deprecation). No workflow-behaviour change.
- **Pullback-awareness Phase 3 — Market-Risk Posture dial** (2026-06-28, `6826388`; reqs F-09b; memory project_pullback_awareness): read-only 0–3 gauge on the 🔗 Risk Analysis tab = `fragility severity_rank + (1 if risk_off else 0)`. Composes existing reads (Phase-1 fragility + `risk_off_regime`), **no new threshold/data feed**; `armed` points to the F-25e Brief de-risk cards (single-surface), never a forecast/directive. `exit_advisor.market_risk_posture`. **All 3 pullback phases now done.** The richer VIX-term-structure/credit-spread version was intentionally NOT built.
- **AI Intelligence Layer (own 🧠 AI Insights page; LLM narrates, never gates; strictly additive / zero runtime dependency; plan docs/plans/ai-intelligence-layer.md; reqs §3.12 F-150–F-153):** **F-1 Thesis Tracking** (INTACT/WEAKENING/BROKEN per held position w/ `user_thesis`; `thesis_advisor.py`/`thesis_reviews`), **F-3 Weekly Debrief** (4-section Sunday email + on-demand; `debrief_advisor.py`/`weekly_debriefs`; light-mode email), **F-4 Monthly Intelligence Report** (Q0 entry-quality + Q1 signal-discipline + a pattern; `intelligence_report.py`/`monthly_reports`; first-Sunday cron + on-demand) — all SHIPPED 2026-06-27. **F-2 earnings-call intelligence DEFERRED** (transcript-API budget). **AI surfaces patterns but NEVER tunes a gate**; prompts interpolate thresholds from `constants.py` at call time (never hand-transcribed).
- **F-5 Thesis Authoring Phase 1 + F-1 evidence fix (2026-06-28, `a7f22da`→`955b071`; reqs F-150a; plan docs/plans/thesis-authoring-analyst-desk.md; memory project_thesis_authoring):** "✨ Draft thesis" on the Trade Journal BUY form LLM-drafts an **editable, falsifiable** candidate thesis (durable claim + evidence + "Breaks if…") from the engine's entry-time evidence; the draft only **seeds an editable field — never auto-saved** (author-of-record), saves to `trades.user_thesis` + new `trades.thesis_source` ('manual'/'ai_draft'/'ai_edited'), and feeds F-1. Advisory-only / zero runtime dependency (offline → plain text box). **Also fixed a latent F-1 bug:** the reviewer (on-demand + weekly cron) read bundle keys that don't exist (`indicators`/`revenue_growth`/`news`) → graded on the thesis text alone; the new shared `thesis_advisor.bundle_evidence` is the single extractor for BOTH review and authoring (reads `df`/`financials`/`headlines`, ×100 fundamentals) so they can't drift again. **Phases 2 (entry pre-mortem) / 3 (exit narration) deferred.**
- **F-4 count-integrity + freeze hardening (2026-06-28, 5a23206→f2f5d21):** headline acted/missed are **distinct tickers** via `signal_flow` (not per-surfacing); narration **count-discipline** (no fabricated "graded-outcomes" tally, no count > distinct total); report **frozen as an immutable artifact** (`recommendations_history.report_viz_snapshot` → `monthly_reports.viz_json` jsonb; needed a one-time additive DDL, applied; old rows live-recompute); **period picker** (latest-per-calendar-month); `build_report_package` computes on the **full** enriched first (cross-rec_type acted safeguard) then scopes means to new_pick; per-band prose alpha = all-graded `avg_alpha` (matches the band chart). reqs §3.13 retrospective scorecard helpers (`distinct_missed`/`missed_split`/`signal_flow`/`report_viz_snapshot`) also drive the 📊 Recommendations History page + Overview/Grow Today Sankeys.
- Headless GitHub Actions alert cron (first non-Streamlit runtime; `cron_runner.py` / `headless_alert_engine.py` / `.github/workflows/alerts.yml`) delivers ALL three out-of-app jobs — exit Phase 3 protective alerts (premarket), pullback Phase 2 reactive drawdown email + Today's-P&L EOD snapshot (eod). LIVE 2026-06-24 (9add28f→cb37862; plan docs/plans/email-alerts-cron.md). The Sunday thesis lane now also chains F-3 debrief + F-4 monthly (`monthly` dispatch mode).
- Brief tone-staleness annotation + Action Log Phase B "log this trim" (307cac6).
- SDLC docs backfill + zero-hallucination doc-integrity rule (b3d444f, a4d9db4).
- Cross-check validator-health gate (220ab44) — no false "sources disagree" banner during a Yahoo outage; also closes the 401/crumb item (absorbed, no fragile retry). Bundle-load deferred queue now empty.
- Catalyst Watch holdings-earnings fix (19ba98a + f49ace0 + ca12cce) — decoupled FMP earnings backfill from `.info`-sparse (re-arms earnings gates), gave the holdings tier the SAME FMP-calendar→per-name-yfinance fallback as Radar (coverage parity), and ET-corrected the FMP earnings date. Confirmed working. Earnings-window literals → named constants (5b8306b). Future lever: Finnhub as a 3rd earnings source if yfinance per-name also fails from the datacenter IP.
- **Account-baseline foundation (own 💰 Account page; `stock_analyzer/account.py` + `account_cash`/`account_flows` tables; plan docs/plans/account-baseline.md):** v1 total value + cash% + true concentration (467b431/069b199), v2 flows ledger + growth-vs-contributed-capital (15b87b1), v3 money-weighted (Modified Dietz) return + annualized (0488cdc), v4 signed net cash = margin/liability awareness (f0abdf7). Reconciles to the user's Robinhood. Reuses the same tables the Robinhood MCP sync would auto-fill. **Gate-basis policy = EQUITY (current, decided 2026-07-09):** ALL concentration gates (Grow Today 15%/35% suppression) AND recs (risk_advisor sector/single-name trims, entry nudge, watchlist/quick-research/comparison) gate on **plain equity weight** = MV ÷ invested equity. This **reverses** the 2026-06-26 net-capital "tighter-of-both" basis — a transient margin balance was lurching hard gates (a sector read 166.9% and demanded ~85% liquidation), which fought §2B calm/anti-churn and conflated position-sizing with leverage. **Leverage/margin risk is now an awareness-only signal** (🔗 Risk Analysis leverage read + 💰 Account note; `_leverage_cache`) — monitored, NEVER gates. The gate wiring lives at one point (`app.py` ~2710, `Gate Weight (%)` = equity `Weight (%)`, `gate_denom` = equity → risk_advisor `_acct_f`=1); `concentration.gating_denominator` is retired from the gate path. Do NOT re-base gates to net-capital without an explicit policy discussion. See `docs/plans/account-baseline.md` (2026-07-09 reversal §), reqs G-19, `project_concentration_discipline`.
- **Buy Candidates verdict-bucket fix (2026-07-27, Opus-reviewed: SHIP, 0 blocking).** `daily_briefing._cross_reference()` ships TWO independently-computed verdicts on every candidate — a legacy `verdict` field (its own if/elif chain, considers an analyst-revisions layer) and `verdict_reconciled` (from `signal_reconciliation.reconcile_signals()`, which has no concept of revisions). Found while building regression-test coverage: the "More Buy Candidates" section header (`app.py` ~7472) tallied its "✅ N confirmed / 🔍 N need verification / ⚠️ N conflicted" counts from the raw legacy field while each candidate's own card two lines below already preferred `verdict_reconciled` — a real, on-screen scenario could show a card counted as "conflicted" in the header while its own text read "Go — All Signals Agree." New `signal_reconciliation.effective_verdict_bucket()` resolves the bucket the SAME way the cards render (reconciled-preferred, legacy fallback); the header now calls it instead of reading `verdict` directly. Scope deliberately narrow — `_buy_candidates`'s own pick-sort order and the historical Predictive Analytics "Sentiment Alignment" tab (reads a persisted verdict from the `recommendations` table) were left untouched, confirmed correct by the Opus reviewer. See `project_verdict_divergence` (memory) for the full writeup and `docs/plans/test-automation.md` Batch 6 for how it was found.
