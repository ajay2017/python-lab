---
audit_date: 2026-09-21
mode: incremental
base_commit: b16fcc379777c2ba8ca6248294600bb79d5711ba
audited_commit: 8350630db97bb0c14b47e0c918471bd886ac61ed
scope: 252 files changed, +39269/-1711 lines since b16fcc37 (20 days) + blast radius — treated as effectively full-blast-radius per the audit skill's own rule (56 non-test source modules touched, including app.py/cron_runner.py, is not a small incremental surface). Reviewed via 5 parallel subagents by area (2 Opus `reviewer`-tier for the decision-logic core and the data-integrity/cron layer, 3 Sonnet-tier for the new analytical modules, app.py orchestration/coordination, and test-suite/antipattern-gate/docs-sync quality), plus the lead's own direct re-verification of every prior-audit High and each area's most consequential new claim against HEAD.
---

## 1. Executive summary

This 20-day window (252 files, ~39K changed lines) shipped F-273b (recommendation-outcomes production verification), F-274 beta-repair + Utilities sector, F-275 Portfolio Investigator (all 7 chunks, plus two toolbox-growth commits), F-276/F-276b Reports (Tax Report + Performance Review tabs), F-278 behavioral-fingerprint SELL mirror, cross-asset stress signals (WTI/10Y), company-name-to-ticker resolution, and the Investor Maturity Roadmap's A1/A2/A4 real-data closures.

**Overall health: very good — no Critical or High findings, one real Medium, everything else Low/cosmetic.** All five area reviews independently reported the decision-logic core, data-integrity/cron layer, and new analytical modules as unusually clean: consistent None/NaN guarding, correct `today_et()`/`now_et()` usage, idempotent DB writes, and — notably — every constants.py/gate/scoring commit spot-checked carried a properly-formatted Hard-Rule-#4 Opus review citation. The one real finding is a dead-diagnostic gap: two new cron-written tables (`account_daily_snapshots`, `score_history`) are absent from `system_health.py`'s store inventory, so a silently-failed write or an un-applied DDL on either is currently invisible — the same bug class this project has already built (and closed) a System Trust check to prevent, now recurring on two tables the check didn't know to watch.

**Recommended next action:** add `account_daily_snapshots` and `score_history` to `system_health.py`'s inventory (mirrors the existing `portfolio_risk_snapshots`/`rec_events` rows — a small, mechanical fix). Everything else is schedulable; nothing here blocks shipping.

## 2. Prior-findings status

Re-verified directly against current HEAD (not just trusted), the one High and all Mediums from the last audit (`docs/reviews/2026-09-01-review.md`):

- **§4 High #1 (App Settings mid-edit-orphan bug)** — **fixed and holding.** `app.py:32822` now reads `disabled=bool(_as_edit_table)` — all three Edit buttons disable whenever any table is mid-edit, exactly the recommended fix, with a comment citing "2026-09-01 audit, High #1."
- **§5 Medium (`reference_data.validate_payload` TICKER_SECTORS-coverage gap)** — **fixed and holding**: `stock_analyzer/reference_data.py:174-296` now runs the coverage check for `sector_universe`/`discovery_universe`, not just `sector_candidates`.
- **§5 Medium (`home_risk_synthesis.py` hardcoded 42/30 diversification bands)** — **fixed and holding**: now `DIVERSIFY_WELL_PCT`/`DIVERSIFY_MODERATE_PCT` in `constants.py:188-189` (values unchanged, promoted to named constants).
- **§5 Medium (CLAUDE.md stale "deliberately not built" text)** — **fixed and holding**: current CLAUDE.md no longer contains that sentence.
- **§5 Medium (`portfolio_health.py:512` naive `date.today()`)** — **fixed and holding**: now `from stock_analyzer.market_time import today_et` / `today = today_et()`.

No regressions found. No fix-class resurfaced on a sibling path.

## 3. Critical issues

None found this pass.

## 4. High-priority issues

None found this pass.

## 5. Medium-priority issues

- **`stock_analyzer/system_health.py` (~lines 116-145) — the store inventory is missing two cron-written tables introduced this window: `account_daily_snapshots` (F-266, written daily by the EOD lane) and `score_history` (Investor Maturity Roadmap B1/F-269, written by the premarket lane via `headless_alert_engine.py`).** Verified by direct grep — neither string appears anywhere in `system_health.py`. This is the exact dead-diagnostic class System Trust's write-outcome check exists to close: a silently-failed write, or a DDL that was never applied in production, produces no signal at all on either table today. `score_history` is the higher-risk of the two — it's accumulating the history a future (Dec'26–Feb'27) decay readout depends on, so a multi-month silent gap would only surface once that readout ships and finds thin data, long after the cause was fixable. `account_daily_snapshots` feeds a live user-facing chart, so a failure there is at least self-revealing sooner. **Fix:** add both as `_Store(...)` rows — `account_daily_snapshots` as an unconditional daily-expected store (mirror the `portfolio_risk_snapshots` row), `score_history` as an existence-only conditional store (mirror the `rec_events`/`gate_suppressions` rows).

## 6. Low-priority / nits

- **`stock_analyzer/ticker_resolver.py:52`** — `yf.Search(query.strip(), max_results=max_results)` passes no explicit timeout, and the surrounding bare `try/except` only catches exceptions, not a hang. Unverified whether yfinance's `Search` has an internal default timeout — worth a quick check; if not, mirror `ai_provider.call_llm`'s explicit `timeout=` pattern.
- **`stock_analyzer/ai_provider.py:177-179`** — `LAST_CALL_ERROR = f"{type(e).__name__}: {e}"[:300]` truncates but doesn't redact; no evidence found that any of the SDKs in use leak the API key into an exception message (key is a constructor arg, not passed to the failing call), but this wasn't exhaustively verified across all four provider SDKs.
- **`stock_analyzer/broker_sync.py:~665`** (`parse_robinhood_csv_income`) — the CSV dedup key (`csv:{date}:{code}:{ticker}:{cents}`) would collapse two genuinely-distinct same-day/same-code/same-ticker/same-amount income rows into one, silently dropping the second via the upsert's ON-CONFLICT-DO-NOTHING. Rare, under-counts (never double-counts), and is a known/accepted tradeoff of the signed-cents design — worth a one-line comment acknowledging the collision since this file is in `_GATE_FILES` and income totals feed a live display.
- **`app.py:25892`** — `_pb_cc_gate = st.session_state.get("_acct_gate_cache") or {}` collapses "not yet computed" into "computed, empty" on a CLAUDE.md-documented coordination cache — the `feedback_sentinel_is_present` class. Currently inert: `gate_basis()` has returned `"equity"` unconditionally since 2026-07-09 so this branch is dead-code-adjacent and no wrong gate decision flows from it today, but it's worth the same explicit-`None`-check treatment the rest of the codebase uses, for consistency and in case that branch ever becomes live again.
- **`stock_analyzer/cross_asset.py`** — `CROSS_ASSET_STRESS_BRIEF_SCORE` stayed at 2 even though the module grew from fewer signals to 7 this window, so the Brief one-liner now triggers on a comparatively smaller share of total possible stress. Surfaced to and accepted by the owner during that commit's review — not a defect, noted for awareness only.
- **`stock_analyzer/valuation.py`** — after the consensus-rating leg was compressed (Strong Buy 30→15 etc.), the PT-Upside analyst leg (25 pts, unchanged) is now the single largest award in the pillar, larger than the objective FCF leg (20). This is the owner-approved shape of that change (only the consensus tier had the "describes 90% of everything" problem) — informational, not a fix.
- **`scripts/check_antipatterns.py`** docstring counts are self-described as already drifting ("this note will drift again") — cosmetic, no functional impact; the antipattern baseline itself genuinely *shrank* this window (3 naive-date entries removed, none added), so the ratchet is working as intended.

## 7. Patterns worth keeping

- **Hard Rule #4 compliance held up under spot-check**: every constants.py/gate/scoring commit sampled across this 20-day window (`722bed8`, `4061176`, `2262bae`, `0aa861a`, `8d6ef2d`) carried a correctly-formatted `Review = Opus reviewer (...)` citation with resolved model, verdict, and blocking count.
- **`beta_repair.py`'s denominator-sign guards and explicit 4-state `basis`-keyed dicts** avoid the recurring `net_capital is None` sentinel ambiguity entirely — never coerce an unresolvable lever to a favorable 0 or an unlevered default.
- **`risk_advisor.py`'s beta card now imports `beta_repair` rather than forking its own arithmetic** — consolidation of a previously-duplicated calculation, closing a class of drift risk before it could recur.
- **`cross_asset.py` is a genuinely new signal, not a duplicate** of `stress_test.py`/`macro.py`'s existing shock logic, and its total-outage path returns an explicit "unavailable" score rather than fabricating calm.
- **The 16 new/changed analytical modules are uniformly pure** (no `st.session_state` reads, data injected via params), every public function fails closed to `None`, and `today_et()`/`now_et()` is used correctly with zero naive-datetime instances found across all of them.
- **Test genuineness remains high**: pinned exact-float assertions with explanatory comments (not `approx` used to dodge a real discrepancy), tests named after real production bugs (`test_window_income_events_fixes_the_actual_bug_january_mint_excluded`), and `ai_provider`'s tests exercise real success paths via fake SDK modules rather than asserting only against a mock's own return value.
- **`investigator.py`'s toolbox deliberately excludes `analyst_coverage`**, meaning the one genuinely externally-authored free text this app ingests (forwarded analyst emails) is structurally unreachable by the only LLM-synthesis prompt in the codebase — a real mitigation, not an absence to take for granted if that toolbox ever grows toward it.
- **DB-write idempotency discipline is consistent across every new write path this window** — upserts keyed on stable identity columns, existence checks before insert, and external-call failures routing to `None`/skip rather than a fabricated value, across `rec_events`, `score_history`, `account_daily_snapshots`, and the earnings-prediction ledger's reschedule-vs-maturity ordering (the 2026-09-07 ledger-emptying bug class stays fixed).

## 8. Systemic observations

- **The `system_health.py` inventory gap (§5) is a recurrence of a class this project has already named and partially fixed once** — the 2026-09-01 write-outcomes check closed the dead-diagnostic gap for *interactive* saves, but nothing structurally forces a new *cron-written* table to be added to the inventory in the same commit that creates it. Two audits in a row have now found a real store missing from a health-check registry after the fact. Worth a lightweight structural guard (e.g., a test that greps `cron_runner.py`'s DB-write call sites and asserts each target table has a corresponding `_Store` row) rather than relying on remembering to update `system_health.py` by hand every time a new table ships.
- **The None-vs-empty-container sentinel discipline is markedly stronger in `stock_analyzer/`'s pure modules than in `app.py`'s orchestration code** — every area reviewer confirmed zero sentinel-collapse instances in the pure-logic layer, while the one instance found this window (`app.py:25892`) is in render/orchestration code. This tracks CLAUDE.md's own stated risk model (app.py is the least-verifiable layer) and reinforces the value of the "extract the decision" convention already in place.
- **No instance of the "gate-dodging refactor" pattern** (a fix that changes an antipattern's syntactic shape without changing its semantics) was found in any area this window — the antipattern baseline shrank, and every new gate-relevant file (`beta_repair.py`, `broker_sync.py`) was correctly added to `_GATE_FILES` in the same commit that made it gate-relevant.

## 9. Open questions

- Is the `system_health.py` inventory gap (§5) a pure oversight, or were `account_daily_snapshots`/`score_history` deliberately left off pending confirmation that their DDLs are applied in production? Worth a quick check before assuming it's simply unfinished bookkeeping.
- Does yfinance's `Search` (`ticker_resolver.py:52`) have an internal default timeout? Not resolvable from this codebase alone — needs a look at the installed yfinance version's implementation, or a deliberate test against a slow/unreachable endpoint.
