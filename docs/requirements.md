# Requirements Document
## DRISHTA — Beyond Noise
*Personal Portfolio Intelligence App*

**Version:** 2.0  
**Date:** June 2026  
**Status:** Active Development  
**Operating Posture:** Decides, not informs (see §2A)

---

## 1. Purpose and Scope

This application is a personal investment management and intelligence platform built for a retail investor managing a concentrated US equity portfolio. It replaces manual spreadsheet tracking and fragmented news/analysis tools with a single, always-current dashboard that surfaces actionable signals, manages risk, and builds a track record of decisions over time.

The app is configured to **make decisions, not merely inform**. Recommendations are presented as the call; suppressions are hard rather than soft. The app does not execute trades — the user retains all execution authority — but its recommendations are designed to be trusted and acted upon directly. See §2A for the operating-posture commitments that shape every requirement below.

The app is not a brokerage or order-execution system.

---

## 2. User Profile

- **Primary user:** Individual retail investor
- **Portfolio size:** Variable; moderate risk tolerance
- **Experience level:** Active investor, growing familiarity with technical and fundamental analysis
- **Usage pattern:** Daily briefing check before market open; ad-hoc analysis during trading hours; end-of-day journal entries
- **Access:** Web browser via Streamlit Community Cloud (primary) or the Railway Hobby pilot (since 2026-07-24, same Supabase DB); no mobile-specific UI required

---

## 2A. Operating Posture and Decision Policy

This is an explicit operating-mode commitment that drives functional design. It is the most important policy section in this document because every gate, threshold, and suppression below derives from it.

### 2A.1 Operating principles

| ID | Principle |
|---|---|
| OP-01 | The app makes decisions. Recommendations are issued only when conditions warrant action. The app would rather recommend nothing than recommend wrongly. |
| OP-02 | Gates are hard suppressions, not soft warnings. When a gate fires, the contradicting recommendation is removed from the UI with an explicit explanation, not appended as a caution badge. |
| OP-03 | Data integrity failures fail loud. When a required input is missing (stop price, composite score, daily briefing, portfolio risk metrics) the dependent feature surfaces an explicit "offline" or "unavailable" state. Fabricated fallbacks are never used. |
| OP-04 | Cross-feature coordination is mandatory. Two features that make overlapping decisions never silently contradict each other. The pattern is: one publishes state; downstream features read and gate. |
| OP-05 | Secondary objectives subordinate to investment view. Tax outcomes do not override the investment thesis (HARVEST suppressed on Buy/Strong Buy). Drift-rebalancing does not override active risk reduction (Rebalancer ADD suppressed when Risk Advisor TRIMs the same ticker). |
| OP-06 | All decision thresholds live in `stock_analyzer/constants.py`. Changes to any value are investment-policy decisions, not code tuning. |

### 2A.2 Decision thresholds (policy)

Single source of truth: `stock_analyzer/constants.py`.

| Threshold | Value | Type |
|---|---|---|
| Portfolio beta — target | 1.0 | Baseline |
| Portfolio beta — elevated (soft warn) | 1.3 | Soft |
| Portfolio beta — ceiling (hard breach) | 1.4 | **Hard** |
| Ticker beta — high (combines with elevated portfolio) | 1.5 | Soft |
| Ticker beta — critical (combines with breached portfolio) | 1.8 | **Hard** |
| Sector — ceiling | 35% | **Hard** |
| Sector — elevated | 25% | Soft |
| Single-name — ceiling | 15% | **Hard** |
| Composite — Buy boundary (entry AND add-to-winner) | 65 | Gate |
| Composite — Strong Buy boundary | 75 | Tier label |
| Composite — Hold floor (below = Sell zone) | 44 | Tier label |
| Fundamentals gate — min core metrics to trust the verdict | 1 | **Hard** (withhold verdict below it) |
| Entry reward:risk minimum (`RR_ENTRY_MIN`) | 2.0 | **Hard** on Watchlist ENTER_NOW (G-13); caveat on Analysis |
| Risk per trade | 1.5% of portfolio | Sizing |
| Earnings imminence window (`EARNINGS_IMMINENT_DAYS`) | 7 days | Caution (binary-event conflict) |
| Earnings "manageable window" — Brief verdict agreed signal (`EARNINGS_MANAGEABLE_DAYS`) | 8–21 days | Tier label |
| Earnings urgency "SOON" tier — Catalyst Watch playbook (`EARNINGS_URGENCY_SOON_DAYS`) | 8–14 days | Display tier |
| Macro imminence window (HIGH-impact event in pick's sector) | 3 days | **Hard** |
| Catalyst Watch forward window (`CATALYST_WATCH_WINDOW_DAYS`) | 7 days | Awareness |
| Review profit-lock — P&L trigger / trim size | 25% / 25% of position | Action target |
| Review stop-tighten ATR multiple (vs 2.0× initial) | 1.5× | Action target |
| Review earnings-overweight — trim trigger / target weight | 12% / 10% | Action target |
| Review weak-large — trim-to target weight | 8% | Action target |
| Review macro-affected — sector trigger / reduction | 30% / 5pp | Action target |
| Exit deterioration — WATCH / TRIM / EXIT drawdown-from-peak floors (`DETERIORATION_*_DD_PCT`) | 6% / 8% / 12% | Action target (exit ladder) |
| Exit deterioration — ATR-scaled TRIM / EXIT floor (capped) | 2.5× / 3.5× ATR; capped 14% / 20% | Action target |
| Exit deterioration — EXIT escalation on unrealized $ loss | $250 | Action target |
| Exit deterioration — trend reference / confirmation | close < SMA50; 2 of last 3 sessions (EXIT skips confirm) | Confirmation |
| Risk-off de-risk — SPY trend trigger (`RISK_OFF_TREND_MA`) | below 200-DMA | Regime (Faber) |
| Risk-off de-risk — VIX trigger (`RISK_OFF_VIX_LEVEL`) | ≥ 25 | Regime |
| Risk-off de-risk — min name β / top-N / trim size | β ≥ 1.2 / top 3 by β×weight / 25% | Action target |
| Concentration — high-beta cluster share (soft warn, `CONCENTRATION_HIGHBETA_SHARE_WARN`) | 60% | Soft |
| Pullback fragility — routine-correction yardstick (`FRAGILITY_PULLBACK_PCT`) | −10% | Awareness |
| Protective alert email — premarket / EOD ET hour (`ALERT_EMAIL_HOUR_ET` / `ALERT_EOD_HOUR_ET`) | 08:00 / ≥ 16:00 ET | Operational |
| Reactive pullback alert — index drawdown trigger (`PULLBACK_ALERT_INDEX_PCT`) | −3% | Awareness |
| Mover — min 1-day gain to qualify | 5% | Discovery gate |
| Mover — shortlist size composite-gated / max picks surfaced | 12 / 3 | Discovery sizing |
| Price cross-check — prev_close tolerance (settled) | 0.5% | **Integrity** (strict) |
| Price cross-check — live-price tolerance (latency) | 3.0% | Soft (loose) |
| Data live-price provider order | Finnhub → yfinance → FMP | Sourcing policy |
| Data general provider order (history/bundle) | yfinance → Finnhub → FMP | Sourcing policy |

The "Review action target" rows translate a *trigger* (when an item lands in Review Before Close) into a *concrete directive* (trim N shares, raise stop to $Y). See §3.2 F-25 and architecture §4.0.1.

### 2A.3 Hard gates currently enforced

| ID | Gate | Behaviour |
|---|---|---|
| G-01 | Risk Advisor TRIM → Grow Today add-to-winner | Suppress; banner explains |
| G-02 | Risk Advisor TRIM → Rebalancer ADD | Suppress; banner explains |
| G-03 | News Intelligence alert → Rebalancer ADD | Attach `news_warning`; critical drops urgency to bottom |
| G-04 | Single-name ≥ 15% → Grow Today add-to-winner | Suppress; concentration banner |
| G-05 | Sector ≥ 35% → Watchlist ENTER_NOW | Downgrade to NEAR_ENTRY with portfolio-fit card |
| G-06 | Portfolio β > 1.4 AND ticker β > 1.8 → Watchlist ENTER_NOW | Downgrade to NEAR_ENTRY |
| G-07 | Imminent HIGH macro (3d) → Grow Today new picks in affected sector | Suppress; macro banner |
| G-08 | Composite Buy → Tax HARVEST | Suppress; `HOLD_FOR_SIGNAL` action with banner |
| G-09 | Rebalancer drift-overweight → Grow Today add-to-winner | Suppress; banner |
| G-10 | Earnings within 7 days → Buy Candidates verdict | Escalate to "Caution" or "Conflicted" depending on other signals |
| G-11 | Stop data missing → Act Today SELL trigger | Skip mechanical SELL; show "stop unavailable" review item instead |
| G-12 | Composite Signal empty on held position → Confirmed verdict | Route to "🔍 Verify — Composite Signal Missing" |
| G-13 | ENTER_NOW without validated R:R (`rr is None`) | Downgrade to NEAR_ENTRY; R:R must be `≥ 2.0` |
| G-14 | `build_daily_briefing` failure → coordination caches | Set to `None`; dependent features show explicit offline banner |
| G-15 | Fundamentals absent from live sources (< `FUNDAMENTALS_GATE_MIN_METRICS`) AND no fresh cache → Analysis verdict AND Brief new-pick | **First, fall back to last-known-good:** `load_all` serves the persistent `fundamentals_cache` copy when the live leg is sparse and the cache is within `FUNDAMENTALS_CACHE_MAX_AGE_DAYS` — the verdict then renders normally with an amber "📦 Fundamentals as of N days ago" note (`fund_source = "cache"`). **Only when there's no fresh cache** is the verdict withheld: on Analysis, BOTH the Summary Scorecard row (Score → "—", Signal → "🚫 Withheld") and the Detailed Analysis block (red "verdict withheld" note) suppress the fabricated-neutral-50 verdict; price-derived columns (Stop, Target, R:R) remain. `daily_briefing` holds the ticker out of `new_picks` (→ `composite_unavailable`). |
| G-16 | Sector at/above `SECTOR_CEILING` (35%) → Grow Today new picks AND add-to-winner in that sector | Suppress; render "Suppressed — Sector Hard Cap" banner (`sector_blocked_picks` / `sector_blocked_adds`). A Strong Buy in an over-cap sector is a KEEP, not an add — deploy-capital defers to protect-capital |
| G-17 | CPI YoY > `REGIME_CPI_CONTROLLED_MAX` (2.5%) → macro "Rate-Cut Optimism" regime | Block: the regime cannot be selected even if risk-on signals (low VIX, strong SPY) win the score, because its rationale claims "controlled inflation." Reassigns to the next-best regime. Prevents a label that contradicts itself (e.g. 3.95% CPI in a "controlled inflation" regime). |
| G-18 | Held position with breached stop (price ≤ stop, i.e. Act Today's Gap-to-Stop ≤ 0) → Analysis "add to position" sizing | Suppress the add: the Analysis Trade Plan replaces the "Sizing below is for adding" note with a red "⛔ Stop breached — exit signal, not an add" banner (mirrors Act Today's SELL) and hides the position-sizing block. The Buy composite still shows (it rates the STOCK); the stop protects the POSITION — protect-capital overrides deploy-capital. Same detector condition as the Brief, so the two surfaces agree instead of contradicting (ADBE: Brief SELL vs Analysis "add"). |
| G-20 | Active early-deterioration **WATCH** on a held name → Grow Today add-to-winner | Suppress the add (moved to `deterioration_blocked_adds`, rendered as a "🟡 Add Suppressed — Early Deterioration Watch" banner). A name below its trend MA / drawing down from its peak must not be nudged as an add even on a Strong Buy composite. WATCH is **entry-price-agnostic** — this is the principled add gate, deliberately chosen over an entry-price-dependent profit/P&L gate (the whole composite engine ignores the user's entry price). To preserve single-surface, the ticker also joins the "More Buy Candidates" dedup set (F-26a) so it can't reappear there as "ADD — Winning Position" (`_buy_candidates` annotates WATCH rather than suppressing). **Changed 2026-07-21 from annotate-only** — the prior annotate-on-the-add-card behaviour read as "add more to a name I'm telling you is weakening" (the FSLR case). |
| G-21 | Ticker with a same-day BUY (per `trades_df`, America/New_York calendar day) → Risk Advisor `sector_concentration` trim plan | Exclude the ticker from today's `trim_candidates`; the excluded name(s) surface separately in `trim_excluded_recent` and the Rebalance Plan renders a "🕐 Held back from today's plan" caption naming them — never a silent shortfall. Prevents the same Brief recommending a buy (Grow Today / More Buy Candidates) and, hours later on the same session, flagging that exact fresh position as the #1 full-exit trim candidate (the SHOP case, 2026-07-28). The ticker returns to the normal trim ranking the next day. (Implements F-221.) |
| G-19 | **Concentration-gate basis — EQUITY (invested equity). [Current policy, 2026-07-09 — reverses the 2026-06-26 net-capital basis.]** The single-name (15%) and sector (35%) hard gates (G-16, F-39b), the `risk_advisor` `single_name_concentration` / `sector_concentration` trim recs, the Trade-Journal entry nudge, and the watchlist / quick-research / comparison entry-fit checks ALL gate on **plain equity weight** = MV ÷ invested equity (`Gate Weight (%)` == equity `Weight (%)`). **Why the reversal:** the 2026-06-26 net-capital "tighter-of-both" basis (`MV ÷ min(equity, net capital)`) let a **transient margin balance lurch a hard gate** — a sector read 166.9% and the app demanded ~85% liquidation off a temporary financing state, which fights §2B calm/anti-churn and conflates position-sizing (structural) with leverage (a different risk). Concentration is now judged on equity, which is **stable across transient margin**; the ceilings still fire on a genuinely over-concentrated book. **Leverage/margin risk is preserved as a separate awareness-only signal** (F-09d) — never a gate. One wiring point sets it (`app.py` ~2710: `Gate Weight (%)` = equity `Weight (%)`; `gate_denom` = equity → `risk_advisor` `_acct_f` = 1); `concentration.gating_denominator` is retired from the gate path. Do NOT re-base gates to net-capital without an explicit policy discussion (hard rule #1). Beta/Sharpe recs + display weights were already equity-basis. |

### 2A.4 Soft warnings (kept in addition to gates)

| ID | Warning | When |
|---|---|---|
| W-01 | Sector ≥ 25% concentration on a new entry | Soft caution in card |
| W-02 | Portfolio β > 1.3 AND ticker β > 1.5 | Soft caution; sizing recommendation |
| W-03 | Active HIGH risk alerts in portfolio | Resolve-first prompt in Watchlist |
| W-04 | Grow Today and Watchlist same-sector overlap | Inform the user one sector trade is enough today |
| W-05 | Trade Journal entry thesis missing on a weak-large-position review | Prompt user to log thesis on future entries |

---

## 2B. Intended Investor Persona and Time Horizon

This app is built for the **quality-first, medium-term investor with long-term tax discipline** — and a defensive bias. It is explicitly **not** a day-trading tool. This framing is a design constraint: features and gates are evaluated against it, and a request that only serves intraday trading is out of scope (see §5) unless the persona itself is revisited.

### 2B.1 Persona fit by horizon

| Persona | Horizon | Fit | Rationale |
|---|---|---|---|
| Day trader | seconds–hours | **Not supported** | The engine actively works against it (see 2B.2). |
| Swing trader | days–weeks | **Partial** | Technical signals, entry zones, ATR stops, R:R, Catalyst Watch and Movers serve this — but quality gates and the no-trade-into-earnings posture temper pure momentum. |
| Medium-term investor | weeks–months | **Primary (sweet spot)** | Composite scoring, sector rotation, macro-regime, rebalancing, diversification advisor all operate on this horizon. |
| Long-term investor | months–years | **Strong** | 40% fundamental weight, valuation/FCF, tax-loss harvesting, 1-yr cap-gains & wash-sale awareness, behavioural discipline. Fit strengthens with horizon. |

### 2B.2 Why the design is investor-oriented, not trader-oriented

| ID | Design choice | Implication |
|---|---|---|
| PH-01 | Composite = Technical 25% + **Business Quality 35%** + **Valuation 30%** + Sentiment 10%; verdict is WITHHELD when BQ fundamentals are unavailable. Analyst consensus (avg_pt + rating label from `analyst_coverage`) feeds the Valuation pillar; individual Ideas Inbox records remain awareness-only. | A combined 65% weight on business quality + valuation is central to an investor and irrelevant to an intraday trader. |
| PH-02 | Composite signals are computed on page load and held; only prices refresh (~60s). Signals do **not** recompute every tick. | Deliberate anti-overtrading choice (also a rate-limit guard). Intraday signal churn is intentionally absent. |
| PH-03 | Proximity gates suppress initiating into earnings; Catalyst Watch is awareness, **not** a buy rec. | The app declines binary-event volatility that a trader often seeks. |
| PH-04 | Stops are ATR-based, R:R is multi-day, sizing is risk-%-of-portfolio. | Swing-to-position constructs (holding across days/weeks), not intraday levels. |
| PH-05 | Macro-regime lens reads CPI / Fed / rates (months horizon); concentration caps, beta/Sharpe/Sortino/VaR, rebalancing, tax/wash-sale all operate at portfolio/horizon scale. | Portfolio-construction and protection, not per-trade edge capture. |
| PH-06 | Even Movers (1-day breakouts) are funnelled through the **same composite quality gate**, never traded on momentum alone. | The most opportunistic surface is still quality-first — the philosophy in miniature. |

### 2B.3 What day-trading support would require (fork, not extension)

Supporting day traders would mean intraday/real-time data feeds, Level 2 / order-flow, tick charts, removal of the fundamental weighting, and removal of the earnings/macro gates that make this app safe. That is a different product with an opposite risk philosophy — a fork, not an increment. Documented here so the boundary stays explicit.

---

## 3. Functional Requirements

### 3.1 My Portfolio (Home)

_Note (2026-07-13): this section's F-IDs describe functionality that originated on Home. Some still render there (F-01–F-05, F-08's core signal). The rest has since moved to standalone nav pages via a 3-phase nav cleanup (see `docs/architecture.md` §10 "Nav follow-up" rows) — **F-06/F-06a/F-07/F-10 → §3.1a Portfolio Overview** (PORTFOLIO nav group), **F-09/F-09a–d/F-14/F-14a/F-87a/F-168 → §3.1b Risk Analysis** (PORTFOLIO nav group), **F-13/F-13a → §3.1c Signals & Advice** (renamed from "Alerts & Actions" 2026-07-22, commit `291b0a9`; moved from the PORTFOLIO nav group into the renamed SIGNALS nav group 2026-07-26). The F-IDs below are unchanged (still the authoritative spec for that functionality) — only their page location changed; §§3.1a–3.1c point back here rather than duplicating the detail, and add F-IDs for the handful of features that never had one before the split (Active Alerts, Custom Price Alerts, Analytics tab)._

| ID | Requirement |
|----|-------------|
| F-01 | **Holdings table — moved to 🧾 Summary (§3.1h, F-204), 2026-07-26.** Added to Home 2026-07-26, then relocated the same day once the standalone Summary page shipped, so the table isn't duplicated across two pages — Home itself still has no persistent holdings table (only the live per-ticker price-strip cards: ticker/price/day-change, no shares/value/weight). See §3.1h for the current spec: Ticker, Shares, live Price, Market Value, Day Δ%, Total Δ% (= `P&L (%)`), Weight (%), rendered via the shared `_render_holdings_table()` (module-level, `app.py` near `_m()`). Average Cost and raw P&L ($) are intentionally not repeated (they're on the fuller detail table on 🥧 Portfolio Overview, `app.py:11037`, alongside Stop/Signal/Score) — this remains a quick at-a-glance read, not a duplicate of that one. |
| F-02 | Auto-refresh live prices every 60 seconds during market hours via a Streamlit fragment. Prices come from the multi-source layer (§3.10) — real-time from Finnhub during market hours, with gap-fill to yfinance/FMP. The price-strip caption shows the actual source(s); a fail-loud banner appears if held-position prices fail the cross-check. |
| F-03 | Show real-time daily P&L (total portfolio $ and %) based on prior-close vs current price |
| F-03a | **Today's P&L — Tier B (true positions day-P&L).** Beyond the held-only prior-close mark (F-03), compute the day's P&L as an equity delta against a baseline persisted in the Supabase `daily_snapshots` table (per-ticker shares + close written by the EOD cron, F-141). This captures positions opened/closed intraday that a held-only mark misses. The label fails loud about its scope; Tier B is inert until the one-time `daily_snapshots` DDL is applied. **Still out of scope:** cash/flows reconciliation and full broker reconciliation. **Cross-page sync (2026-07-27):** Home publishes the computed Tier-B result to `st.session_state["_dpnl_cache"]` (dated, so a stale cross-day value is never reused); 🧾 Summary's "Today's P&L" tile consumes it so the two pages show the identical number instead of Summary independently recomputing the cheaper held-only mark. If Home hasn't run yet this session (e.g. landing directly on Summary), Summary falls back to its own held-mark calc, honestly relabeled "Today's P&L (held)" so the narrower scope is visible on the tile itself, not just in a tooltip. |
| F-03b | **Account-level view — account-baseline v1 (cash + total account value).** The app otherwise reasons only about invested equity; setting a user-entered **net cash** balance (single-row Supabase `account_cash`, manually entered now — the same table the Robinhood MCP sync would later auto-fill; **v4: the value is signed — NEGATIVE = a margin debit**, so Total/Growth/Return/account-concentration all net out the loan since they derive from total = equity + cash) unlocks a dedicated **💰 Account** nav page (between Home and Market Scanner) showing **Total Account Value** (equity + cash), **Invested Equity**, **Cash %**, and **true concentration** (each holding as % of the *whole account* alongside its equity weight). **Display-only:** the 15%/35% concentration GATES still fire on equity weight — moving them to account-basis is a deferred investment-policy decision. Inert until the one-time `account_cash` DDL is applied (load returns None → equity-only behaviour + a nudge to set cash). Cash entry is data-sanity validated (non-negative; soft-warns implausible) and read-only-viewer-aware. Out of v1: growth/return (needs a flows ledger, v2) and TWR/IRR (v3). Plan: `docs/plans/account-baseline.md`. |
| F-03c | **Growth vs contributions — account-baseline v2 (flows ledger).** On the 💰 Account page, a cash-flow ledger (Supabase `account_flows`: a `baseline` anchor + `deposit`/`withdrawal` rows; manual now, broker-fillable later) separates money **deposited** from money the **market made you**. **Net Contributed Capital** = baseline + deposits − withdrawals; **Growth $** = total account value − NCC; **Growth %** = growth ÷ NCC (rendered "—" when NCC ≤ 0 or the portfolio/cash isn't loaded — never a bogus number). The baseline defaults to the current total account value (growth-from-today) or accepts lifetime net deposits (all-time gain). A deposit raises total value and NCC equally, so it can never masquerade as growth. **v3 adds the money-weighted (Modified Dietz) return** — `R = (EMV − BMV − F) / (BMV + Σ Fᵢ·wᵢ)` — which corrects for deposit/withdrawal **timing** (the distortion the naive gain/NCC ignores), plus an **annualized** figure shown only once the tracking period ≥ 30 days. Modified Dietz (not daily TWR) because we have daily equity (`daily_snapshots`) but no daily cash history; it needs only endpoints + dated flows. Pure calc in `stock_analyzer/account.py`; **display-only — feeds no gate.** Inert until the `account_flows` DDL is applied (v3 reuses the same table — no extra DDL). The **MWR label now shows the baseline date** ("Return since YYYY-MM-DD") rather than a generic label, so the metric's time-scope is unambiguous next to the weekly email figure. Plan: `docs/plans/account-baseline.md`. **Annualization caveat (2026-07-28):** when the annualized figure renders, `account.annualization_caveat(days, is_levered)` may add an explanatory caption below it — triggered when the tracking window is under `_ANNUALIZE_CAVEAT_MAX_DAYS` (90, a display-only floor kept local to `account.py`, not `constants.py`, since it never affects a gate/recommendation) and/or the account carries a margin debit (`_levered = _cash < 0`), both of which amplify a real but modest dollar move into a much larger-looking annualized rate. Pure display context; the underlying number is unchanged. |
| F-03d | **Capital Trend chart — account-baseline v4 (equity timeline with NCC overlay, SHIPPED 2026-07-19).** On the 💰 Account page, a `📈 Capital Trend` section (below Growth & Contributions, no extra tab) renders a Plotly area chart built from `stock_analyzer/account.build_equity_timeseries()` — daily equity totals aggregated from `daily_snapshots` (shares × close price per date) paired with a forward-filled NCC step function (from `account_flows`). Three visual layers: (1) **green filled-area equity line** (held-position mark-to-market); (2) **dotted NCC reference line** (contributed-capital floor — gap above = market-made growth, gap below = underwater); (3) **amber/green diamond marker** at the last chart date showing `_total_value` (equity + signed cash) — when carrying a margin debit the diamond falls below the equity line, and its distance below NCC is the exact source of the negative "Return since…" figure in Growth & Contributions above. A weekly / monthly / All granularity toggle filters the chart window. Auto-narration below the chart states the equity move in $ and % vs NCC, flags a material peak if one exists, and — when cash is negative — explains net account value vs contributed capital explicitly so the −X.X% return is traceable without ambiguity. **Bridges the 'positions look up but account is down' confusion** inherent in a leveraged account: the chart shows all three levels simultaneously so the story from equity growth through contributed capital to net account value is readable in one view. `build_equity_timeseries()` is a pure function in `stock_analyzer/account.py`; data source is `db.load_daily_snapshots()` accumulated by the EOD cron. Display-only; feeds no gate. |
| F-217 | **Day Shock awareness banner (Home).** Below the live price strip, a conditional banner flags any held ticker moving ≥`DAY_SHOCK_PCT` (5.0%, up or down) same-day, using the `change_pct`/`prev_close` already returned by `fetch_live_prices()` — no extra fetch. Renders only when at least one ticker crosses the threshold (nothing shown otherwise); each flagged ticker gets a red (down) or green (up) pill chip with the exact move and the prev-close→current price. **Awareness only — never alters `exit_advisor.classify_deterioration_tier`'s WATCH/TRIM/EXIT tier or any recommendation.** Exists because a single-day shock that stays above the 50-day MA doesn't trip the deterioration tier's trend-break condition and would otherwise be visible only as one equal-weight badge among several in the price strip, with nothing calling attention to its magnitude. Publishes `st.session_state["_day_shock_cache"]` (`{ticker: {price, prev_close, chg_pct}}`), read-only, no other consumer yet. |
| F-218 | **Structural alert banner (Home) — SHIPPED 2026-07-27.** Below the Day Shock banner (F-217), a conditional banner flags a newly-formed correlation cluster among held positions since the last 🧬 Structural Scan (F-198's tab on 🧩 Intelligence). Full detection mechanism (`detect_new_clusters()`, the `.lte` baseline lookup, the signed direct-edge citation guard) is specified under F-198, which this entry cross-references rather than duplicates. Renders only when at least one new cluster is detected; each flagged cluster shows its member tickers, tier, combined weight, and the specific verified pair(s) driving the flag. Placed after the Home page's `_home_synth_cache` hit/miss synthesis converges (later in the page than F-217) because `_corr_df_cache` isn't freshly published for the current render until that synthesis block completes — inserting it alongside F-217 would read the previous render's stale correlation data. **Awareness only — never alters the composite score or any gate.** Publishes `st.session_state["_structural_alert_cache"]` (`None` = correlation data offline this session, `[]` = checked and nothing new, else the flagged-cluster list), no other consumer yet. See F-198 for the full mechanism, Opus review history, and plan doc. |
| F-04 | Detect and prompt for stock splits; adjust shares and average cost accordingly |
| F-05 | Allow user to add, edit, and remove holdings; persist to Supabase `holdings` table |
| F-06 | Display sector allocation pie chart and P&L bar chart |
| F-06a | **Composition Sankey (Portfolio → Sector → Position).** On the 🥧 Portfolio Overview page, a Plotly Sankey shows where capital sits and how concentrated it is in one view; band width = share of portfolio value. **Concentration reads at a glance:** a sector node over `SECTOR_CEILING` (35%) or a single-name node over `SINGLE_NAME_CEILING` (15%) is flagged red. Flags fire on the **equity (gate) basis** (G-19); band width = share of portfolio value. Hover shows each node's % of portfolio. Display-only (the gates fire elsewhere). _(The net-capital overlay branch is retired with the equity-basis policy — conditioned on `_acct_gate_cache.basis ∈ {account, over-levered}`, now always `equity`, so it never fires.)_ |
| F-07 | Show sector exposure breakdown as a percentage of portfolio (invested equity). The concentration reference line is drawn at `SECTOR_CEILING` (35%, the actual hard cap — never a hardcoded literal). Bars are the **equity basis** — the same basis the gates fire on (G-19). _(The second net-capital bar per sector is retired with the equity-basis policy — its branch is conditioned on `_acct_gate_cache.basis ∈ {account, over-levered}`, now always `equity`, so it never draws.)_ Display-only. |
| F-222 | **Sector Gaps pointer + widened diversifier-sector coverage — SHIPPED 2026-07-30.** F-07's Sector Exposure chart only ever showed sectors currently held, giving no visibility into sectors at 0% — a real diversification blind spot the user flagged while reviewing 🥧 Portfolio Overview. Two coordinated changes, both in `stock_analyzer/portfolio.py` / `app.py`: (1) **"🧭 Sector Gaps" pointer section**, rendered on 🥧 Portfolio Overview directly below the Sector Exposure chart, reads the already-published `st.session_state["_div_recs_cache"]` (the same cache F-13a's Diversification Advisor consumes — no recompute, no new session-state key) and lists each `ADD`-type rec as a one-line awareness note with a "See candidates in Diversification Advisor →" button (`_pending_page` nav indirection). Deliberately a pointer, not a duplicate: per the single-surface-priority convention, the actionable card (candidates, $ sizing) stays on 📡 Signals & Advice only. (2) **Widened `_DIVERSIFYING_SECTORS` coverage** (`portfolio.py:1004-1024`): of the app's 13 curated thematic sector labels (`TICKER_SECTORS`), only 5 (Healthcare, Energy, Defense, Financials, Clean Energy) had ever been scored as genuine portfolio diversifiers; the other 8 were either scored-but-excluded (Consumer Tech, AI & Cloud, AI & Data — too correlated to a tech-heavy book) or simply never evaluated. Decided with the user: **added** Communications (corr 0.20 — classic telecom is rate-sensitive/defensive, closer to utilities than growth tech), EV & Auto (corr 0.40 — rate/commodity-cycle driven, distinct from the software/AI cycle), and Enterprise Tech (corr 0.45 — legacy IT/capex cycle, more value-oriented than AI/Cloud growth names); **scored but kept excluded** Semiconductors and Cybersecurity (corr 0.65 each — same AI/compute/SaaS cycle as the already-excluded AI & Cloud / AI & Data cluster). New candidate rosters (`_SECTOR_CANDIDATES`) and discovery-universe bucket mappings (`_DIVERSIFY_TO_DISCOVERY`) added for the 3 newly-included sectors so `diversifying_candidate_pool()` can actually return names for them. **Also fixed in the same pass**: F-13a's ADD card caption ("Avg correlation to your existing book") was misleading — `corr_to_tech` was always a static per-sector table value, never computed from the user's actual holdings; reworded to "Typical correlation to a growth/tech-heavy portfolio" (copy-only, no logic change). No new `constants.py` entries — these tables are plain Python dicts/lists co-located with the pre-existing pattern, same as the original 5-sector version. Opus pre-ship code review cited in commit. |
| F-08 | Calculate and display composite signal (Buy/Hold/Sell) for each held position |
| F-09 | Show portfolio-level risk metrics: Sharpe ratio, Sortino ratio, max drawdown, beta, correlation matrix |
| F-09a | **Pullback fragility gauge (pullback-awareness Phase 1).** Surface a portfolio "fragility" read — how hard a routine market pullback (`FRAGILITY_PULLBACK_PCT`, −10%) would hit the book given its beta/concentration — framed as: timing isn't predictable, but exposure and reaction are. Awareness only; it does not issue a buy/sell. Consumed downstream by the risk-off de-risk overlay (F-25e) and the reactive pullback email (F-142). |
| F-09b | **Market-Risk Posture dial (pullback-awareness Phase 3).** On the 🔗 Risk Analysis page, a read-only gauge that COMPOSES two already-computed reads — book fragility (F-09a `severity`) and the current market regime (`risk_off_regime`: SPY below its `RISK_OFF_TREND_MA`-day average OR VIX ≥ `RISK_OFF_VIX_LEVEL`) — into a single 0–3 exposure posture: `score = severity_rank(calm 0 / caution 1 / fragile 2) + (1 if risk-off else 0)` → 🛡️ Steady / 🌥️ Watchful / ⚠️ Defensive / 🔴 Risk-off & fragile. Pure logic in `exit_advisor.market_risk_posture` (no new threshold — reuses the F-09a bands + the F-25e regime legs). **Not a forecast** (reads current exposure, never predicts a pullback's timing) and **not a directive**: when both legs are elevated (`armed` = the SAME AND-gate as the F-25e de-risk overlay), it POINTS to the Brief's de-risk cards rather than duplicating the action (single-surface). **WITHHOLDs** (offline note, not a falsely-calm dial) when fragility is unavailable, mirroring the gauge. Read-only consumer of `_fragility_cache` + `_cached_spy("1y")` + `_cached_vix()`; publishes nothing. |
| F-09d | **Leverage / margin awareness (awareness-only — NEVER gates).** Since the concentration gates are equity-basis (G-19), leverage risk is surfaced separately as a monitoring signal, so a margined book can't look calm just because equity-basis concentration is fine. When the manually-seeded cash (💰 Account) shows a **fresh, real margin debit** (signed net cash < 0, not stale > `ACCOUNT_CASH_STALE_DAYS`), a text read renders on the 🔗 Risk Analysis page: the margin debit $, net capital (equity − loan), the leverage multiple (equity ÷ net capital), and an illustration ("a −10% move on your largest sector ≈ −$X, −Y% of your equity"). The existing ⚖️ note on the 💰 Account page is retained. Published as `st.session_state["_leverage_cache"]` at the same wiring point that sets the gate basis; computed from `db.load_account_cash()` + `total_val`. **It never gates, suppresses, sizes, scores, or forces a trim** — purely awareness (§2B monitoring lane semantics). Fail-soft: no signal when cash is unknown, stale, or not a debit. |
| F-09c | **Cross-Asset Pulse (macro stress signals).** On the 🔗 Risk Analysis page, a read-only panel scoring five cross-asset stress signals from 45 days of daily history (`stock_analyzer/cross_asset.py`): credit spreads (HYG `CROSS_ASSET_HYG_TREND_DAYS`-day trend, declining → stress), VIX term structure (VIX/VIX3M > `CROSS_ASSET_VIX_TERM_RATIO` = 1.0 → inverted), dollar strength (DXY `CROSS_ASSET_DXY_TREND_DAYS`-day trend rising AND `CROSS_ASSET_DXY_ROC_DAYS`-day ROC > `CROSS_ASSET_DXY_ROC_THRESHOLD` = 1.5%), copper as a growth proxy (HG=F `CROSS_ASSET_COPPER_TREND_DAYS`-day trend, declining → concern), and the 3m10y yield curve (^TNX − ^IRX < `CROSS_ASSET_CURVE_STRESS_BP` = −50 bp → deeply inverted). The aggregate score = count of stressed signals **among those with data** → Calm (0–1) / Caution (2) / Stress (3–4) / Alarm (5); the displayed denominator is the available-signal count (never a hardcoded 5), and a total data outage returns label "—" (never a fabricated "Calm"). When score ≥ `CROSS_ASSET_STRESS_BRIEF_SCORE` (2), Today's Brief shows a one-liner pointing to this tab; on a total outage the Brief shows a quiet offline caption rather than going silent. The bare yfinance batch fetch is timeout-wrapped (20 s) so a stalled download can't freeze the render. **Awareness only — never moves a gate or changes a recommendation.** |
| F-10 | Performance attribution: breakdown of return by position and sector |
| F-11 | Earnings calendar for held positions (KPIs, timeline, detail table with Fwd EPS/weight/P&L/signal, Pre-Earnings Playbook) lives on the 🔔 Catalyst Watch page ("Your Holdings — Earnings" tier), NOT a Home tab — consolidated there with the watchlist/universe awareness tier so all earnings detail is in one place. |
| F-12 | Risk advisor: flag positions breaching stop-loss, concentration risk, correlation clusters. Beta and Sharpe recommendations name specific trim targets, which downstream features (Grow Today add-to-winner, Rebalancer ADD) cross-check and suppress to avoid contradicting the active reduce-exposure recommendation. |
| F-12a | **Concentration / sizing discipline (entry-time enforcement).** Closes the asymmetry where manual journal buys bypassed the position-sizing ceilings the recommendation paths already respected (a single name had reached ~23%). Three coordinated surfaces, all from the pure `concentration.py`: (a) an **entry nudge** at trade entry when an add would push a single name past `SINGLE_NAME_CEILING` (15%); (b) a `single_name_concentration` MEDIUM recommendation on standing positions over the ceiling; (c) a **high-beta cluster** line when the high-β share of the book exceeds `CONCENTRATION_HIGHBETA_SHARE_WARN` (60%). Concentration breaches are structural, so they stay in **Act Today** (not the slow Tune-up lane). |
| F-12b | **Concentration-gate basis = EQUITY (2026-07-09 — reverses the earlier margin-aware basis).** The hard single-name/sector gates, the entry nudge, and the `risk_advisor` trim recs measure concentration against **invested equity** (MV ÷ equity), NOT net capital — so a *transient* margin balance can't lurch a hard gate. Leverage/margin risk is a **separate awareness-only** signal (F-09d), never a gate. Implements G-19. **Supersedes** the 2026-06-26 net-capital "tighter-of-both" basis (`min(equity, total_account_value)`) — see G-19 and `docs/plans/account-baseline.md` (2026-07-09 reversal §). |
| F-221 | **Same-day-buy trim exclusion (SHIPPED 2026-07-28 — SHOP whiplash fix).** `build_risk_advisor_recommendations()` takes an optional `trades_df` parameter; a ticker with a BUY trade dated today (America/New_York calendar day, converted from the stored UTC `traded_at` timestamp) is excluded from the `sector_concentration` rec's `trim_candidates` — held back from the greedy trim-allocation plan even when its composite is the sector's weakest. Excluded names surface separately in `trim_excluded_recent`; the Rebalance Plan renders a "🕐 Held back from today's plan" caption naming them and noting they're reconsidered in tomorrow's brief, so a resulting shortfall against the trim target is explained rather than silent (reuses the existing `trim_allocation()` shortfall mechanism unchanged). One-day grace only — the ticker returns to normal composite-ascending trim ranking the next day. Pairs with F-39h on the buy side. Implements G-21. |
| F-13 | Rebalancer: suggest target weights and trim/add trades. ADD actions cross-check News Intelligence alerts (suppressed for critical news; warned for negative) and Risk Advisor TRIM recommendations (ADD suppressed on tickers being trimmed). Suppressions render as explicit banners. |
| F-13a | Diversification Advisor ADD card: candidates are sourced from the broad discovery universe (≈20 names per sector, curated roster unioned first so known names are never dropped; capped by `DIVERSIFY_SCAN_CAP`), not a fixed roster — so the best diversifier surfaces, not a frozen list. Each candidate is cross-validated against the quality engine (composite/signal/R:R, the SAME numbers as Analysis, via the `_grow_composites` cache + load_all fallback), gated at `COMPOSITE_BUY`, ranked best-first, and shows the top `DIVERSIFY_DISPLAY_TOP` with a 🎯 best-passer / 🚦 none-clears banner. Failing names stay visible-but-demoted (never silently filtered). Each carries an "▶ Analyze {ticker}" button to the full Analysis scorecard (the bridge from candidate to decision/trade). |
| F-14 | Stress test: model portfolio impact under defined macro scenarios (rate shock, recession, etc.) |
| F-14a | **Stress test — historical scenario expansion.** Two new named scenarios added to `stress_test.SCENARIOS` (9 total), each with per-sector overrides in `stress_test._SECTOR_SHOCKS`: (1) **2008 GFC** (`id="gfc_2008"`, `spy_move=−57.0%`) — peak-to-trough Global Financial Crisis (Oct 2007–Mar 2009); Financials −78%, EV & Auto −75%, Energy −50%, Healthcare −22%. (2) **Stagflation** (`id="stagflation"`, `spy_move=−18.0%`) — high inflation + stagnant growth (1970s analogy); Energy +30%, Defense +12%, AI & Data −30%, Clean Energy −12%. Both scenarios apply the sector-override path (non-None `sector_key`) so each holding's projected loss uses the sector-specific drawdown rather than the blunt beta-adjusted SPY move — preserving the "sector matters, not just beta" design of the existing 2022 Rate Shock and COVID Crash scenarios. Awareness only; never issues a rec or gates an entry. `stock_analyzer/stress_test.py`. (Commit 3cbd6a6.) |
| F-200 | **Regime-Aware Adversarial Stress Testing — Phase 1 (P5 of the Agentic Intelligence Roadmap) — SHIPPED 2026-07-24, commit `a3a9a12`, display-only.** New "🎯 Regime-Aware Adversarial Scenario (Beta)" expander at the bottom of the 🔥 Stress Testing tab, additive to F-14/F-14a's 9 named scenarios + Custom Scenario slider (all untouched). Synthesizes three already-shipped systems into a single Haiku-narrated compound scenario: `structural_scanner.blast_radius()` (F-198's cascade estimate — reused verbatim, no new shock formula), `portfolio_intelligence.correlation_clusters()` (F-189), the FRED macro regime detector (`macro_calendar.detect_macro_regime`, aliased `detect_macro_regime_fred` — **not** the unrelated same-named function in `macro.py`), and `cross_asset.py`'s USD/dollar stress signal (F-09c). Names the compound macro condition (2-3 concurrent factors, e.g. rate direction + dollar strength + credit stress) most likely to hurt the SPECIFIC structural weak points identified, citing only supplied evidence. **The roadmap's original "scenario plausibility in the next 90 days" framing was dropped** — the only historical regime persistence (`daily_regime`, since 2026-07-21) has ~3 days of data, far short of a genuine base rate; replaced with the regime detector's real, already-computed confidence score (0-100), explicitly relabeled as "how confident the detector is in its CURRENT read," never a forecast. Reuses the existing session-state regime cache (same `fred_key` three-source fallback as the existing Regime Fit feature, F-188) rather than re-fetching — the two sections on the same page can never disagree. Button-gated (never auto-computed on expander render), day-cached to new `regime_scenario_cache` table; a failed/empty narrative is never cached. The "early indicator watchlist" is selected by the LLM from the regime detector's real signal list only — validated via normalized matching, always displaying the canonical stored label, never the LLM's echoed text, so an indicator can neither be fabricated nor silently dropped over casing drift. No new `constants.py` entries — zero new quantitative modeling. 3-round Opus plan review (FIX-FIRST×2 then SHIP) + Opus pre-ship code review (SHIP, 0 blocking). **Strictly diagnostic — nothing here gates, reorders a scenario, or modifies the composite score.** Plan: [docs/plans/regime-aware-stress-testing.md](plans/regime-aware-stress-testing.md). |
| F-15 | Display market-closed context note in sidebar showing last close date when market is shut |
| F-16 | Tax Advisor: per-position tax-lot analysis with HARVEST recommendations on harvestable losses. HARVEST is **suppressed** on positions rated Buy or Strong Buy (action becomes `HOLD_FOR_SIGNAL`) so the tax tail does not wag the investment dog. The UI explicitly surfaces every suppressed harvest with the position's current signal so the user can revisit if conviction degrades. |
| F-186 | **Tax-Aware Exit/Harvest Lens (Concept F) — awareness-only, NEVER gates.** The exit-direction complement to F-16 (which is the Buy-side HARVEST gate, G-08): holding-period context layered onto exit signals for this taxable account. (1) **Exit-signal tax note:** when an EXIT (Act Today) or TRIM (Review Before Close) card fires on a position within `TAX_LONGTERM_WINDOW_DAYS` (30) of long-term-gains eligibility, the card gains an amber note ("becomes long-term eligible in N days; waiting can cut the tax rate ≈Xpp if selling at a gain"). The investment signal is **unchanged** — never suppressed or delayed. (2) **Wash-sale awareness:** a SELL **at a loss** within `TAX_WASH_SALE_DAYS` (30) of a same-ticker BUY shows a wash-sale note on the SELL confirmation card; the sale is not blocked. (3) **Net-of-cost note:** the opportunity-cost expander clarifies returns are gross, Robinhood equity commissions ≈ $0, and tax is not netted there (see Tax Advisor). Pure arithmetic via `tax_advisor.holding_period_status()` / `wash_sale_risk()` (reuse the F-16 lot replay); no new APIs. Tax constants are **display-only policy** in `constants.py` (never gate). |
| F-17 | Stop data integrity: when a position's stop value is missing or zero, the portfolio table displays "—" for Stop and Gap, marks Stop Type as "Stop Unavailable", and the mechanical SELL trigger in Act Today is skipped (the user is prompted to set a manual stop). No fabricated fallback is substituted. |
| F-187 | **Pre-Mortem Protocol (Concept C) — entry friction only, never a gate.** Before a prospective LIVE Buy is recorded (Trade Journal, including a Grow Today / Analysis "act on this" prefill — never broker/screenshot/split imports or the `recalculate_from_trades` replay, which have no live decision to stress-test), the investor sees a "🔍 Run Pre-Mortem" section: an optional Haiku-generated case AGAINST the buy (3 counterarguments — a composite-pillar concern with its specific driving signals, the portfolio-concentration impact of adding this position, and macro/earnings context — each required by the prompt to cite specific evidence, never a generic "stocks can go down" line), computed once per explicit click and discarded if the ticker changes before submit. The LLM prompt also includes the investor's own past exit lessons (from `lesson_category` on SELL trades in the Trade Journal — ticker-specific exits first, cross-ticker patterns as fallback) so counterarguments can reference the investor's own documented behavioural patterns. Regardless of whether the LLM ran, a chip row of past exit lessons appears above the commitment field — always visible when lessons exist — closing the loop between F-195 (Lessons Learned Library) and the pre-mortem commitment. The **hard requirement** is a required text box ("What would make me wrong about this?") enforced as a validation gate alongside the existing ticker/shares/price checks — **independent of whether the LLM call succeeded**, so an API outage or missing key can never block a legitimate Buy. Stored on `trades.premortem_case_against` (jsonb, the 3 counterarguments or null) and `trades.premortem_commitment` (text, the investor's own answer). Never re-scores, re-gates, reorders, or vetoes the recommendation — SELL and every retroactive/batch write path are untouched. `stock_analyzer/premortem_advisor.py`. |
| F-228 | **Pre-Commitment Enforcement — SHIPPED 2026-08-03.** The one genuinely unbuilt idea from a 2026-08-03 brainstorm ranked list (the other two turned out already covered — self-calibrating gates by the Monthly Intelligence Report F-153/F-153a, weekly-alpha attribution by the Weekly Debrief's per-position contributor/detractor breakdown): F-187's `premortem_commitment` is captured at BUY but nothing ever checked it against live data — Thesis Red Team and Portfolio Q&A only ever quote it back as passive context. `stock_analyzer/premortem_monitor.py::extract_trigger()` is a ONE-SHOT Haiku call at BUY-submission time (never on every rerun, never retried — the trades grid is delete-only) that parses the free-text commitment into a structured, checkable fact: `checkable=False` (stored as `premortem_trigger_direction='not_checkable'`) for any commitment with no explicit stated price — conservative bias is the whole safety net, the LLM never infers or guesses a number. `detect_premortem_triggers()` is pure Python, ZERO LLM cost, called once per Daily Brief build (same call site as `deterioration_signals()`): for each ticker's **currently open lot** (reusing `tax_advisor._build_open_lots()`, extended to track each lot's cumulative split ratio) it compares the stored trigger — divided by that split ratio — against price history since the lot's `buy_date`. **Lot-scoped, not ticker-scoped:** a sell-then-rebuy can never resurface an unrelated, already-closed lot's commitment against a brand-new position. **Self-resolving:** a trigger only fires while the MOST RECENT daily close is still beyond the level (close-price basis, matching the deterioration ladder's own convention) — "crossed at any point since BUY" would nag forever after a recovered dip, so the gate is always "still beyond the level today," not a historical fact. Fires as a new, first-class Act Today card kind (`premortem_triggered`, `decision_bucket._ACT_KINDS`) — deliberately shown **alongside** any existing same-ticker WATCH/TRIM/EXIT card rather than merged (`_consolidate_act_today()` exempts this kind from its per-ticker consolidation entirely — the investor's own stated condition firing is a genuinely different reason from the algorithm's deterioration read, not a restatement of it). A pre-build research pass found the original brainstorm's literal "Judge-style veto authority" framing would have been wrong here too — this is deliberately an audit/confront pattern (a loud Act Today card), never a new suppression of any recommendation. Two new nullable `trades` columns (`premortem_trigger_price`, `premortem_trigger_direction`) via the same additive-DDL pattern as `premortem_case_against`; ships inert until DDL is applied. No new `constants.py` entries. Opus design review: FIX-FIRST, 3 blocking (stock-split staleness, sell-then-rebuy lot mis-attribution, "ever crossed" stale-nag temporal semantic) + 2 fix-first (underspecified `_act_today()` plumbing, a persistent-daily default that initially conflicted with the calm-advisor principle but was resolved by the self-resolving temporal fix) — all resolved by reusing `tax_advisor.py`'s existing FIFO lot-replay rather than inventing new logic. **Opus code review of the finished code: SHIP, 0 blocking**, 1 cosmetic wording fix applied ("broke $50 above" → "broke above $50.00"). 44 new tests (`tests/test_premortem_monitor.py`, `tests/test_premortem_act_today.py`, extended `tests/test_tax_advisor.py`). Plan: [docs/plans/premortem-enforcement.md](plans/premortem-enforcement.md). |
| F-18 | **Action Log — manual stop override (closes the recommend→act→log loop):** when Review Before Close advises raising a stop, the user can record the new stop level. It persists to the Supabase `manual_stops` table and overrides the computed ATR/ratchet stop in `build_portfolio_df`. Override-wins is **one-directional**: a manual stop is honoured only when it is ≥ the current computed stop (you can tighten, not loosen below the mechanical floor). Overridden positions show a 📌 badge and Stop Type="Manual" wherever the stop is rendered (portfolio table, Trade Plan, Summary Scorecard). |
| F-19 | A manual stop is auto-cleared (orphan cleanup) when the ticker's share count drops to 0 — `save_holdings` symmetric-sweeps `manual_stops` so a stop override never outlives the position it protects. |

### 3.1a Portfolio Overview

Standalone page (PORTFOLIO nav group, positioned first) extracted from Home in the 2026-07-12/13 nav cleanup. Five internal tabs (restructured 2026-07-15 per the I2 UX remediation): **📊 Overview**, **⚖️ Rebalancing**, **💰 Tax**, **📈 Performance** — distributing the functionality formerly in the single "Portfolio Overview" tab (F-06, F-06a, F-07, F-10, and the F-22c rebalance plan) across four focused tabs — and **📈 Analytics** (F-171–F-173 below, unchanged from the original split). Same "haven't visited Home this session" empty-state gate as every other extracted page (`_render_portfolio_not_loaded`).

| ID | Requirement |
|----|-------------|
| F-171 | **Relative Strength (Analytics tab).** Each holding's 6-month return vs. its sector ETF benchmark (`relative_strength_table`). **Outperforming** = genuine stock-specific alpha, not just riding the sector tide; **Underperforming** = the sector rallied but this position lagged — an institutional rotation flag. Requires ≥1 holding with price history. Awareness only. |
| F-172 | **Sector Rotation Heatmap (Analytics tab).** Button-gated, lazy-loaded (`_fetch_sector_returns`): each sector ETF's 1W/1M/3M/6M return, cross-referenced against the user's own current sector exposure % (`sector_exposure`) — an "is my exposure aligned with what's actually working" read. Awareness only. |
| F-173 | **Rankings (Analytics tab).** Button-gated universe scan (`scan_sectors` + `rank_holdings_in_universe`) ranking each holding's momentum score against the broader scanner universe — Universe Rank, Percentile, and a top-performer callout for names in the top decile. Awareness only. |
| F-223 | **Portfolio vs. S&P 500, Real Sector (Analytics tab) — Phase 3 of the sector-diversification work (F-222 was Phases 1-2), SHIPPED 2026-07-30.** F-07/F-222's Sector Exposure and Sector Gaps use the app's 13 curated thematic sector labels (`TICKER_SECTORS`), which don't map 1:1 onto real GICS sectors (e.g. "Consumer Tech" spans AAPL=Technology, AMZN=Consumer Discretionary, NFLX=Communication Services). This feature answers a different question — "are we over/underweight the actual S&P 500 by real sector" — using a **deliberately separate, parallel classification**: `stock_analyzer/portfolio.py::real_sector_exposure()` reads each held ticker's REAL provider-reported sector (already fetched for every position via `bundle_loader.py` as `loaded_data[ticker]["sector"]`, but normally discarded downstream in favor of the curated thematic label by `resolve_sector()`) — zero new API calls. `_normalize_provider_sector()` maps common provider naming variants (e.g. Yahoo/Morningstar-style "Financial Services"/"Consumer Cyclical"/"Healthcare"/"Basic Materials") onto the 11 real GICS sector names; anything unmapped falls to `"Other"` (matches the existing `UNCLASSIFIED_SECTOR` catch-all — never silently miscounted). `sector_benchmark_tilt()` outer-joins the result against `SP500_SECTOR_WEIGHTS` (a static reference table — no live benchmark-weight data source exists in the provider layer — sourced via the English Wikipedia "S&P 500" article's GICS sector-weighting table, as of 2026-07-01; needs periodic manual refresh, see CLAUDE.md's "What's queued") so a benchmark sector held at 0% still shows correctly as a negative tilt. Renders as a green/red tilt bar chart on 📈 Analytics, alongside F-171/F-172 (both also "vs. a benchmark" diagnostics). **The UI caption explicitly disambiguates** that this is a second, coexisting sector taxonomy from the thematic one used elsewhere on the same page — a deliberate design choice, not a bug, since the two won't sum the same way. Reference data, not a decision threshold — stays in `portfolio.py` alongside `SECTOR_ETF`/`_SECTOR_PROFILES` rather than `constants.py`, same reasoning already established for `_SECTOR_PROFILES`. **Diagnostic only — never gates, scores, or feeds any recommendation.** 6 new tests in `tests/test_portfolio.py`. |

### 3.1b Risk Analysis

Standalone page (PORTFOLIO nav group) extracted from Home in the 2026-07-12/13 nav cleanup — before that, briefly a merged "Risk & Alerts" Home tab; before that, a separate "Risk Analysis" Home tab (the current name/icon is a deliberate reuse of that original naming). Aggregates: portfolio risk metrics (F-09), fragility gauge (F-09a), Market-Risk Posture dial (F-09b), leverage/margin awareness (F-09d), Cross-Asset Pulse (F-09c), rate sensitivity (F-87a), stress testing (F-14/F-14a), the historical scenario replay (F-168), Regime Fit (F-188), the Regime-Aware Adversarial Scenario expander (F-200, added 2026-07-24 to the 🔥 Stress Testing tab), and the Outcome Range historical-bootstrap Monte Carlo simulator (F-224, added 2026-08-01 as its own 4th tab). Same empty-state gate as Portfolio Overview. (2026-07-15: restructured into three internal tabs — **📊 Dashboard**, **📋 Action Plan**, **🔥 Stress Testing** — for navigability; no content added or removed. 2026-08-01: a 4th tab, **🎲 Outcome Range**, added for F-224.)

| ID | Requirement |
|----|-------------|
| F-188 | **Regime Fit (Concept D, Wave 3 of the next-evolution roadmap) — Action Plan tab, SHIPPED 2026-07-17, commit `0c6df7a`.** Read-only diagnostic answering "given the current macro regime, is my portfolio's beta and cash cushion where it should be?" — the app already detects a regime (`macro_calendar.detect_macro_regime`, one of `rate_cut`/`inflation_fight`/`recession_fear`/`stagflation_risk`/`neutral`) but never connected it to a target portfolio profile. The section independently resolves the FRED key and detects/caches the regime (same `f"_macro_regime_{date}_{bool(fred_key)}"` cache-key convention as the Economic Calendar page, so a same-day detection there is reused for free; deliberately omits that page's "past HIGH-impact events" gate, which is irrelevant here) and compares current portfolio beta (`_port_risk_cache`) and cash % (`db.load_account_cash()`, same formula as 💰 Account) against `REGIME_BETA_CEILING` / `REGIME_CASH_FLOOR_PCT` (`constants.py`, new investment-policy dicts keyed by the 5 real regime ids, set in a dedicated conversation with the user — anchored to the existing regime-agnostic `PORTFOLIO_BETA_TARGET`/`_ELEVATED`/`_CEILING` baseline: tighter beta ceilings and higher cash floors in defensive regimes, a looser ceiling in rate-cut optimism). On a beta breach, names the top-3 beta contributors (`stock_analyzer/regime_targets.py::regime_position_gap`, pure/None-safe, ranked by β × weight). Regime-detection confidence below `REGIME_CONFIDENCE_MIN_DISPLAY` (40) or a non-`fred+market` source is flagged as low-confidence/estimated rather than shown as certain. **Diagnostic only — never gates, resizes, suppresses, or reorders anything**; the investor decides whether/how fast to close the gap. **Deliberately scoped narrow for this wave** (per the plan's own governance requirement + a live scoping conversation): no Growth/Quality Tilt columns (need Concept B's factor-tilt work, not yet started), no Home "regime changed" annotation (would need day-over-day persistence that doesn't exist yet — no new DDL added this wave). Opus-reviewed SHIP, 0 blocking. |

### 3.1c Signals & Advice

Standalone page (SIGNALS nav group, moved from PORTFOLIO 2026-07-26 — see below) extracted from Home in the 2026-07-12/13 nav cleanup — same lineage as §3.1b (briefly merged with Risk Analysis as "Risk & Alerts", now split back out). **Renamed "⚠️ Alerts & Actions" → "📡 Signals & Advice" (2026-07-22, commit `291b0a9`)**, and split into two tabs: **📡 Active Signals** (Active Alerts, Custom Price Alerts, Rebalancer cards — F-169/F-170/F-13) and **🧩 Diversification** (Diversification Advisor — F-13a). **Nav group move (2026-07-26):** moved out of the PORTFOLIO nav group (which had grown to 9 items) into the ALERTS nav group, which was renamed **SIGNALS** for the occasion — the old "ALERTS" label undersold this page's mix of live signals + advice, and the rename gives the group room to describe all three of its pages (Signals & Advice, 🔔 Catalyst Watch, 📅 Economic Calendar) as one "things that need your attention" family. Same-day follow-up polish, all `app.py`-only with no constants/gate/logic change: `be26c64` replaced full-width alert banners on Active Signals with a 3-column category-card grid; `a89e0c6` removed duplicate severity icons and fixed bold-ticker rendering; `6b88949` moved the "well-balanced, no rebalancing needed" success chip to the top of the tab. **UI label disambiguated 2026-07-26:** the Rebalancer (F-13) section's on-screen heading renamed "💡 Rebalancing Recommendations" → "💡 Signal-Driven Actions" (and the empty-state chip's wording updated to match) — it was colliding in name, but not in mechanism, with Portfolio Overview's "⚖️ Rebalancing Advisor" (weight-vs-target drift; §3.1). The two features are unrelated: F-13 here triggers off a ticker's score/signal deterioration, the Portfolio Overview one off allocation-% drift from a target weight. F-13's internal/gate-table name stays "Rebalancer" (used in G-02/G-03/G-09, F-12, OP-05) — only the user-facing heading changed. Aggregates the Rebalancer (F-13) and Diversification Advisor (F-13a), plus two features that never had a dedicated F-ID before the split:

| ID | Requirement |
|----|-------------|
| F-169 | **Active Alerts.** Portfolio-condition-triggered alerts (not calendar/event-triggered — contrast with 🔔 Catalyst Watch, F-11) grouped by category: stop-proximity, signal, concentration, earnings, analyst revisions, and signal-change (a holding's signal degrading/improving/unchanged since the last check). Shown as danger/warning/info counts; "no active alerts" renders a plain success state. **Analyst revisions (Phase 2, 2026-07-31):** fires on either a rating-action spike (3+ downgrades net ≤-2 = danger, 2+ net <0 = warning) OR — only when no rating-action alert already fired for that ticker — a consensus price-target cut of ≥7% (warning) / ≥15% (danger) over a 5-trading-day window with no rating change at all, closing a gap where a firm quietly guts its target without a formal downgrade. Withheld (no alert either way) for any ticker without 6+ days of accumulated target-snapshot history yet. |
| F-170 | **Custom Price Alerts.** Per-ticker user-set take-profit and floor price triggers; checked against the live price strip each run. Distinct from the mechanical stop-loss system (F-17/F-18) — these are user-defined watch levels, not the engine's protective stop. Config form (input fields + save button) rendered in a collapsed `st.expander`; fired alerts (triggers that have tripped) render **above** the expander so they remain visible without opening the config form. |

### 3.1d Intelligence

New standalone page (PORTFOLIO nav group, between Risk Analysis and Signals & Advice) — Concept B ("Portfolio-as-One Positioning Intelligence") from `docs/plans/next-evolution-strategy.md`, Phase 2 of the next-evolution roadmap. Answers what the portfolio's ownership MEANS in aggregate, not position-by-position. Now five tabs; built incrementally (cheapest/most decision-relevant first, per the user's chosen build order). The 4th tab (F-198, added 2026-07-24) is the first Agentic Intelligence Roadmap feature to land here; the 5th tab (F-202, added 2026-07-26) is the second — see §3.12 for the roadmap's other entries (F-196, F-197, F-199, F-201).

| ID | Requirement |
|----|-------------|
| F-189 | **Correlation Clusters (Concept B panel 1) — SHIPPED 2026-07-17, commit `5c980b3`.** 🕸️ Correlation Clusters tab. The app already flags correlated PAIRS above `CORR_HIGH_PAIRS_THRESHOLD` (0.65) / `CORR_DANGER_PAIRS_THRESHOLD` (0.80) (`portfolio.diversification_score`), but never grouped them transitively — A-B and B-C flagged as two separate pairs, never surfaced as "these 3 names move together." `stock_analyzer/portfolio_intelligence.py::correlation_clusters()` closes that gap with plain-Python connected-components over the existing pairwise correlation matrix (`_corr_df_cache`, already computed at Home — no new API/compute cost, no new constants, no new library dependency). Each returned cluster carries its tickers, size, average INTERNAL correlation (mean of every pair inside the cluster, not just the edges that formed it), combined portfolio weight, and a danger/warning tier (danger if any internal pair ≥ `CORR_DANGER_PAIRS_THRESHOLD`). Singletons (no correlated pair) are excluded. Sorted by combined weight (biggest real exposure first). **Diagnostic only** — the closing caption explicitly defers to composite score (🔗 Risk Analysis / 📈 Analysis) for which name to act on; correlation never re-ranks or gates. |
| F-190 | **Risk Budget Gauge (Concept B panel 2) — SHIPPED 2026-07-17, commit `698fcd5`.** ⚖️ Risk Budget tab. Capital weight tells you how much money is in a position; it doesn't tell you how much of the portfolio's actual realized VOLATILITY that position is responsible for. `stock_analyzer/portfolio_intelligence.py::risk_budget()` computes a standard Euler / marginal-contribution-to-risk decomposition: rebuilds its own jointly-aligned returns frame from held price history (so per-ticker volatility and the correlation matrix can never drift out of sync), builds a covariance matrix (`vol ⊗ vol × corr`), and attributes total portfolio variance across positions (`w·Σ·w` for portfolio variance; `(Σ·w)/portfolio_vol` for each position's marginal contribution) so `Σ risk_pct ≈ 100%` by construction. Pure numpy/pandas — no new dependency, no new constant (deliberately no "outsize" flagging threshold; the raw weight%/risk%/ratio numbers are shown as-is). Renders a portfolio-annualized-volatility metric, a grouped bar chart (Capital Weight % vs Risk Contribution % per position, risk-descending), and a detail table including the risk÷weight ratio (>1 = a position eating more risk than its capital share). Verified pre-ship against 3 hand-checkable cases: equal-vol/zero-correlation/equal-weight → equal risk share; equal-vol/correlation≈1/70-30 weight → risk share tracks capital weight exactly (no diversification benefit); and a 10%-weight high-vol correlated position contributing 27% of portfolio risk (2.70× its capital share) — the "small position, outsize risk" case this panel exists to catch. Realized/historical only — not a forecast. **Diagnostic only** — identifies where risk concentrates, defers to composite score for any action. |
| F-191 | **Factor Tilt Heatmap (Concept B panel 3, final panel) — SHIPPED 2026-07-17, commit `a4c95cb`.** 📐 Factor Tilt tab. Directional exposure to 5 major style factors (Momentum=MTUM, Value=VLUE, Quality=QUAL, Low Volatility=USMV, Growth=VUG — `FACTOR_ETF_TICKERS` in `constants.py`) via Pearson correlation of each held position's daily returns against each factor ETF's returns over a trailing `FACTOR_TILT_WINDOW_DAYS` (126, ~6 months) window — set with the user over a 3-month alternative for statistical stability. `stock_analyzer/portfolio_intelligence.py::factor_tilt()` — returns-based style analysis, deliberately correlation-only (NOT a regression/beta model, and explicitly NOT FMP `.info` style tags, which the plan warns produces garbage). A (ticker, factor) cell requires ≥20 overlapping trading days (mirrors the existing empirical-correlation floor in `risk.py`'s rate-sensitivity feature) or is `None` — never a fabricated correlation. `portfolio_tilt` (the portfolio-level weighted read per factor) renormalizes weights among only the tickers with a valid correlation for that specific factor. Tickers with zero usable correlations across every factor are excluded entirely (not just from the "N included" count). Button-gated fetch (`app.py::_fetch_factor_etf_returns()`, mirrors the existing `_fetch_sector_returns()` batch-download pattern) — the only Intelligence panel that costs a new API call, unlike Correlation Clusters/Risk Budget which reuse already-cached data. Renders a ticker×factor heatmap (RdBu diverging scale) plus a weighted portfolio-tilt bar and a one-sentence dominant-tilt callout. **Explicitly directional, not precise** — factor estimates are noisy for a 10-20 position book over any single window; stated prominently in the tab's own caption. **Diagnostic only** — defers to composite score for any action. Opus review: FIX-FIRST (2 blocking) → SHIP (0 blocking) on re-review, after fixing an undocumented-constants gate trip and a dead constant (`FACTOR_TILT_WINDOW_DAYS` wasn't actually wired into the fetch on the first pass). **Closes Concept B (Phase 2) — all 3 panels now shipped.** |
| F-198 | **Structural Vulnerability Scanner — Phase 1 (P3 of the Agentic Intelligence Roadmap) — SHIPPED 2026-07-24, commit `9b47117`.** New "🧬 Structural Scan" 4th tab on 🧩 Intelligence, composing the three panels above (F-189/F-190/F-191) with one genuinely new calculation and a daily narrative. **Blast Radius Map**: `stock_analyzer/structural_scanner.py::blast_radius()` estimates cascade portfolio impact from shocking each of the top-3 risk-budget contributors (F-190's `risk_pct` ranking) by a fixed -20% — a single-factor beta approximation (`beta = corr(T,H) × vol_H/vol_T`; `comove = shock% × beta`) using SIGNED correlation (a strong negative correlation is a real hedge/offset) and gated on `abs(corr) ≥ CORR_HIGH_PAIRS_THRESHOLD` (reused from F-189, no new cutoff invented). Derives entirely from already-computed data (`_corr_df_cache` + `risk_budget()`'s own `vol_annualized_pct`) — zero new API calls. Computes unconditionally every render (pure Python). **Structural Narrative**: a single Haiku call per portfolio per Eastern trading day synthesizes the cluster report, weakest-link ranking, blast-radius results, and (if already loaded this session) Factor Tilt data into a 2-4 sentence plain-English explanation of the single most dangerous structural pattern — never inventing a real-world catalyst not present in the evidence. Day-cached to new `structural_scan_cache` table (one row per `scan_date`, portfolio-wide, not per-ticker). **Button-gated, not auto-computed** — Streamlit executes every tab body on every page rerun regardless of which tab is selected, so an auto-compute design would fire the LLM call far more than once/day; a failed/empty narrative is never cached, so a transient failure can be retried immediately. No new `constants.py` entries — the -20% shock and top-3 selection are module-level what-if inputs (same class as `stress_test.py`'s `_SECTOR_SHOCKS`), not investment-policy thresholds. **Diagnostic only — never gates or modifies the composite score.** 3-round Opus plan review (FIX-FIRST×2 then SHIP — Round 1 caught a 100x weight-scaling bug in the portfolio-impact formula and the auto-compute design flaw; Round 2 caught stale auto-compute language surviving in three sections after the authoritative fix) + Opus pre-ship code review (SHIP, 0 blocking). Plan: [docs/plans/structural-vulnerability-scanner.md](plans/structural-vulnerability-scanner.md). **Phase 2 — SHIPPED 2026-07-27** (both gates cleared same day: Phase 1 stable ≥3 days in production, and ≥2 days of real `structural_scan_cache` history verified directly in Supabase — rows on 2026-07-24 and 2026-07-27, with an expected gap on 07-25/07-26 since the narrative is button-gated, not automatic). New 🏠 Home "Structural alert" banner: `stock_analyzer/structural_scanner.py::detect_new_clusters()` diffs today's live `correlation_clusters()` output against `db.load_structural_scan_baseline()`'s **most recent scan on or before today** (deliberately not strictly "prior" — using `.lte`, not `.lt`, so once today's own narrative has been generated, live clusters compare against themselves and the banner self-clears rather than nagging about already-reviewed clusters for the rest of the day). A cluster is flagged only when it contains a ticker pair that (a) wasn't already co-clustered in the baseline snapshot and (b) is a **verified direct positive edge today** (`corr ≥ CORR_HIGH_PAIRS_THRESHOLD`, signed — matching `correlation_clusters()`'s own edge condition exactly) — pair-level, not whole-cluster-set, comparison, so a cluster merely losing a member (decorrelation) is never flagged. A prior snapshot of `None` (no scan has ever run) suppresses the feature entirely; an empty list `[]` (a real scan found zero clusters) does not suppress — every one of today's real clusters then flags as new, the cleanest possible zero-to-some formation signal. Zero new data fetches, zero new LLM calls, zero new `constants.py` entries, zero new table (new read-only `db.load_structural_scan_baseline()` against the existing `structural_scan_cache`). New coordination-pattern cache key `_structural_alert_cache` (see CLAUDE.md's coordination-pattern list). **Diagnostic only — never gates or modifies the composite score.** 3-round Opus plan review (FIX-FIRST×2 then SHIP — Round 1 caught a transitive-pair fabrication risk in the "new pairing" citation and the `.lt`-never-clears-same-day bug; Round 2 caught a signed-vs-`abs()` correlation regression in Round 1's own fix, which would have let an anti-correlated pair be cited as a "new pairing") + Opus pre-ship code review (SHIP, 0 blocking). New regression tests: `tests/test_structural_scanner.py` (also backfills `blast_radius()`, which had zero coverage since Phase 1 shipped). Plan: [docs/plans/structural-scanner-phase2.md](plans/structural-scanner-phase2.md). **Closes the Structural Vulnerability Scanner feature — both phases now shipped.** |
| F-199 | **Hidden Same-Bet Detector (D1 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-24, commit `5e8479f`.** New "🧠 Hidden Same-Bet Detector" section on the 🧬 Structural Scan tab (below F-198's content), gated **only** on saved-thesis coverage — deliberately independent of the price-correlation gate above it, so thin price history never hides this thesis-only signal. Detects positions that look diversified by sector and price correlation but secretly bet on the same underlying assumption (e.g. three names all implicitly long "AI capex keeps ripping") — concentration risk F-198's price-correlation scan structurally cannot see. One Haiku call/day (`stock_analyzer/thesis_cluster.py::generate_thesis_clusters()`) semantically clusters currently-held positions' saved buy theses (`user_thesis`, most recent BUY-with-a-thesis per ticker), grounded by a **two-layer fabrication guard**: every returned ticker must normalized-match the supplied corpus, and every returned "quote" must verifiably appear in that specific ticker's own stored thesis text — protecting against a spurious grouping of two genuinely-held tickers, not just a fake ticker identity (semantic clustering over free prose is fuzzier than citing structured data, so ticker validation alone wasn't sufficient). "No shared assumption found" is a valid, expected, cacheable result — never forced. A pure-Python classification step (`classify_clusters()`, zero LLM, cannot hallucinate) labels each validated cluster against F-189's `correlation_clusters()` output as **⚪ unverified** (no price-correlation data this session — never coerced into a false positive/negative), **🟠 possible** (checked, not already price-correlated — the headline finding), or **🟡 confirmed** (checked, already price-correlated — de-emphasized corroboration), plus any already-price-correlated sub-pairs within a `possible` group. Classification is recomputed fresh every render from the *current* session's correlation data — the cache stores only the validated clusters, never a frozen classification, so a cluster found `unverified` correctly reclassifies once price data becomes available later the same day. Day-cached to new `thesis_cluster_cache` table (one row per `scan_date`, portfolio-wide). No `constants.py` entries — truncation length (~1500 chars/thesis) is prompt-sizing, not a policy threshold. **Diagnostic only — never gates, scores, or modifies `correlation_clusters()`/`factor_tilt()` themselves.** Opus design review (FIX-FIRST → resolved): added the evidence-span citation requirement; fixed a false "hidden" default when price data is simply unavailable (added the `unverified` state); fixed a render-nesting bug that would have hidden the whole section whenever price data was thin. Opus pre-ship code review: SHIP, 0 blocking, 3 non-blocking fixed (a `.iloc[-1]` oldest-vs-newest-BUY bug — also flagged in F-197 Phase 2's exit-debate code for a follow-up fix; a ticker-dedup guard; truncation-note persistence across rerun). Plan: [docs/plans/hidden-same-bet-detector.md](plans/hidden-same-bet-detector.md). |
| F-202 | **Signal Coherence Auditor (D3 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-26, commit `df36c6d`.** New 5th tab, "🧭 Signal Coherence," on 🧩 Intelligence. For every held ticker, mechanically joins three EXISTING, independent advisory surfaces — the composite score's own directional class (`signal_reconciliation.classify_composite_direction()`, a public wrapper over the existing `_composite_class()`), the weekly Thesis Red Team's erosion status (`thesis_reviews.status` ∈ INTACT/WEAKENING/BROKEN), and the most recent Bull/Bear debate verdict across BOTH debate types (`debate_cache.verdict`, new `db.load_debate_verdicts()` read) — and surfaces only the tickers where the present signals **genuinely disagree** (at least one bullish-aligned + at least one bearish-aligned). A ticker needs ≥2 of the three signals present to be evaluated at all; full agreement among evaluated tickers renders no card (the roadmap itself flagged this item as the most noise-prone — only disagreement is shown, never a wall of "everything's fine"). **Zero new LLM calls, zero new cache table** — unlike every other item on this roadmap, this is a pure-Python presence-check + direction-map computed live on page load, since all three inputs already exist as stored/computed data. **Verified invariant, does not flip:** in BOTH entry-type and exit-type debates, `bull_wins` is bullish-aligned and `bear_wins` is bearish-aligned (an exit debate's Bull argues to continue holding, Bear argues to exit — the same polarity as an entry debate). A same-date tie between an entry and an exit debate on one ticker prefers the exit debate (the current hold decision); a selected entry debate that predates the ticker's most recent BUY is tagged "buy-time" in its chip rather than shown as a live re-litigation. **Corrects the roadmap's original one-liner**, which also named "regime-fit" as a 4th input — verified `regime_targets.regime_position_gap()` returns one portfolio-level beta/cash gap, not a per-ticker directional signal, so forcing it into this per-name join would be a category error; dropped rather than shoehorned in. No synthesized prose — each card shows the raw signal chips (type + date on each) as the finding itself, never a templated "these disagree because…" sentence that would invent a causal story neither signal actually asserts. No `constants.py` entries. **Diagnostic only — never gates, scores, or modifies any of the three source surfaces.** Opus design review: SHIP, 0 blocking, 5 non-blocking refinements folded in (the polarity-invariant documentation, the tiebreak rule, unknown-as-neutral gating semantics, buy-time tagging, always-show-all-present-chips). Opus pre-ship code review: SHIP, 0 blocking, 2 cosmetic non-blocking fixes (NaN Signal rendering "nan", null erosion status rendering "None"). Plan: [docs/plans/signal-coherence-auditor.md](plans/signal-coherence-auditor.md). |
| F-203 | **Watchlist Resurrection (O4 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-26, commit `631e6ba`.** On the existing 👁️ Watchlist page, highlights watchlist tickers that are **old** (added ≥ `WATCHLIST_STALE_DAYS` = 30 days ago, user-set policy value) **and** currently **actionable** (`ENTER_NOW` or `NEAR_ENTRY` per the page's existing `build_watchlist_recommendation()` classification) **and not already held**. **Corrects the roadmap's original "back at the setup you originally wanted" framing** — verified the `watchlist` table (`ticker`, `added_at` only) never recorded any original setup/target/reason-for-add to compare against, so the honest reframe is "old + independently actionable today," not a reconstruction of a number that was never saved. **One resurrection predicate drives both the KPI summary count and every per-card caption** (fixed during design review — the original draft would have let a held+stale `ENTER_NOW` ticker get counted in the summary line while its card is silently dropped by the pre-existing "Already in Portfolio" override, the same double-surface mismatch class this app has hit before); the predicate explicitly excludes held tickers for **both** `ENTER_NOW` and `NEAR_ENTRY` (the existing override only covers the former). No new LLM call, no new page, no new cache table — a highlight over already-computed page data plus one new read, `db.load_watchlist_added_dates()` (UTC `added_at` converted to America/New_York before taking the calendar date, matching the app's existing NY-timezone convention). A ticker with no readable `added_at` is silently skipped, not treated as "very stale" or "never stale." Re-adding a removed ticker resets its clock by design (a fresh `INSERT`, not an update) — the correct clock for "days since you last actually re-engaged," not a bug. New constant `WATCHLIST_STALE_DAYS = 30` in `constants.py` (`ANALYST_COVERAGE_FRESH_DAYS = 30` elsewhere is not real precedent — that constant means the opposite, "still fresh," for a passively-accumulated surface; 30 stands on its own as the user's judgment call for an actively-curated watchlist). **Diagnostic only — never gates, scores, or changes `build_watchlist_recommendation()`'s classification, sort order, or the existing KPI metrics.** Opus design review: FIX-FIRST → SHIP (1 blocking: the single-predicate fix above). Opus pre-ship code review: FIX-FIRST → SHIP (1 blocking: `WATCHLIST_STALE_DAYS` initially missing from the `docs/architecture.md` constants table, the mechanically-enforced `scripts/check_constants_documented.py` CI gate; 2 non-blocking fixed — caption wording overstated `NEAR_ENTRY` as "qualifies to enter," reworded to "actionable again"; the UTC→ET conversion above, originally a raw string truncation that could undercount by 1 day near the midnight ET boundary). Plan: [docs/plans/watchlist-resurrection.md](plans/watchlist-resurrection.md). |
| F-205 | **Sizing Alpha (O5 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-26.** New full-width "📏 Sizing Alpha" section on 🪞 Investor Mirror (My Edge), below the existing Behavioral Biases grid. **Corrects the roadmap's "flat sizing across conviction tiers" framing** — verified no reliable "conviction at time of buy" field exists anywhere in the schema (`recommendations.conviction` only covers same-day engine surfacings, the same coverage-gap class O1 already hit; `decision_context` captures portfolio-level context, never the traded ticker's own score; `investor_mirror.conviction_alignment()` is current-state only). Reframed around the one thing reliably recorded for every trade: dollar size actually committed. Groups `investor_mirror.build_closed_lots()`'s FIFO fragments back to their **originating BUY LOT** (`groupby(ticker, buy_date, buy_price)`, not by sell fragment — bucketing at the fragment level would shred a large, later-scaled-out winning bet into several small-dollar fragments, biasing the comparison toward a false "flat sizing" conclusion), then splits buy lots into **dollar-size terciles** (Small/Medium/Large) via a **tie-robust rank split** (`pd.qcut` on rank, not raw value — guards against round-dollar buys collapsing a plain `pd.qcut` into a degenerate split; explicit `None` if fewer than 3 distinct dollar values exist). Shows each tercile's **dollar-weighted** average realized gain % (dollar-weighted, not share-weighted, since raw share counts aren't comparable across tickers at very different prices). **Explicit, bolded caveat**: shown in absolute dollars, not adjusted for portfolio growth over the account's history — no point-in-time total-portfolio-value history exists to normalize by. Reuses `INVESTOR_MIRROR_MIN_CLOSED_LOTS` as the per-tercile floor (now applied to the buy-lot count, post-grouping) — no new constant needed for O5. Descriptive, not prescriptive: captions cover both directions (large-beats-small and small-beats-large) plus whether Medium sat between them. No new LLM call, no cache table — pure Python, reuses the same `_mi_lots` already computed for Section 2. Opus design review: FIX-FIRST → SHIP (2 blocking, both above: the buy-lot-vs-fragment unit fix, the tie-robust split). Opus pre-ship code review: SHIP, 0 blocking. Plan: [docs/plans/sizing-alpha.md](plans/sizing-alpha.md). |
| F-206 | **Premature-Exit Cost (O6 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-26.** New 4th card ("⏱️ Premature-Exit Cost") in the existing 2×2 Behavioral Biases grid on 🪞 Investor Mirror, filling `_mi_bc2`'s one open slot alongside Breakeven Anchoring. **Corrects the "Cost" framing away from any forecasted/counterfactual price estimate** (a "you'd have made $X more if you'd held" figure requires forecasting a hypothetical later price — fabrication risk this app forbids) toward a real, already-realized comparison: among winning closed lots, the share-weighted average gain % of "quick exit" winners (held less than `PREMATURE_EXIT_RATIO`=0.5 × the user's own average winner-hold) vs. "patient" winners (the rest). Filters explicitly on `is_gain == True` **and** a non-null `pnl_abs`/`pnl_pct` — not `is_gain` alone, since `build_closed_lots()`'s `is_gain = (pnl_abs or 0.0) >= 0` evaluates **True when `pnl_abs` is `None`**, which would otherwise admit phantom "winners" with no real P&L. Computes its own average-winner-hold independently rather than depending on `disposition_effect()`'s return, since that function requires BOTH a populated winner AND loser group to return anything and would silently withhold `winner_avg_days` whenever losers are thin. **New feature-specific constant `PREMATURE_EXIT_MIN_LOTS = 5`** (user-set) — deliberately NOT `INVESTOR_MIRROR_MIN_CLOSED_LOTS` (10), since O6's quick/patient buckets draw from a winners-only, twice-split population roughly half the size that floor was sized for; reusing it would likely have left the card permanently dark. Captions cover both directions (patient-beats-quick and quick-ties/beats-patient) and note that some quick exits are deliberate risk management, not impulsiveness, and that one sell decision can contribute fragments to both buckets. No new LLM call, no cache table — reuses the same `_mi_lots`. Opus design review: FIX-FIRST → SHIP (1 blocking: the floor-sizing correction above). Opus pre-ship code review: SHIP, 0 blocking, 2 cosmetic non-blocking fixed (mean-vs-median honesty note; Medium-tercile summary clause — shared with F-205 since both shipped in the same pass). Plan: [docs/plans/premature-exit-cost.md](plans/premature-exit-cost.md). |
| F-220 | **Entry Timing — 6th tab on 📊 Predictive Analytics (Phase 1 SHIPPED 2026-07-28, commits `34f4782`, `eea519e`, `29540d1` — plan `docs/plans/entry-timing-tab.md`; full history in `docs/shipped-log.md`).** New "⏱️ Entry Timing" tab, sibling to F-178's existing 5. Answers a narrower question than the other tabs: does technical momentum running ahead of the composite consensus at the moment a `new_pick` fires predict a rough first few days, even when the pick is right over its real weeks-to-months horizon — distinguishing bad entry timing from ordinary short-term noise (motivated by a live case: AMD fired as `new_pick` 5x in 2 weeks in 2026-07 at composite 69–71 / momentum 94–100, losing ~9–11% / alpha −7pp to −10pp vs SPY every time). Five new pure functions in `stock_analyzer/predictive_analytics.py`: `dedupe_repeated_tickers()` (collapses same-ticker `new_pick` re-firings within `ENTRY_TIMING_DEDUP_WINDOW_DAYS` (5) calendar days of a prior kept firing into one opportunity, keeping the first firing of each cluster — the daily scanner re-firing an unbought name is one opportunity, not N data points); `divergence_at_entry()` (= `momentum_score − composite_score`; only positive divergence is in scope — negative divergence is a different question, filtered out downstream, not computed as zero); `forward_alpha_at_horizon()` (Day+1/Day+5 alpha via a live per-ticker forward-close fetch — `providers.orchestrator.get_historical_close`, injectable so `app.py` can wire in a cached wrapper — minus the SPY return over the identical window, reusing `recommendations_history._spy_return_pct`); `by_divergence_band()` (groups into Aligned/Diverging/Extreme bands using `ENTRY_TIMING_DIVERGENCE_ALIGNED_MAX` (15) / `ENTRY_TIMING_DIVERGENCE_DIVERGING_MAX` (25), reporting Day+1/Day+5 forward alpha plus the existing mature-outcome `alpha_pct` as the Day+20 leg — no new SPY fetch, no new DB columns); `find_illustrating_case()` (finds whichever ticker is CURRENTLY the most-repeated same-ticker Extreme-divergence re-firer in the live, un-deduped data — grounds the tab's illustrating-case callout in real, present data rather than a fixed historical citation, so it can never go stale; returns `None`, never fabricated, when no ticker has fired ≥2 times in the Extreme band yet); `band_narrative()` (rule-based, non-LLM one-line plain-English description of a band's Day+1→Day+5→Day+20 alpha trajectory shape — e.g. "recovers quickly," "deepest drawdown of the three," "no recovery pattern yet" — with an optional `illustrating_ticker` callback appended only to the band `find_illustrating_case()`'s ticker actually sits in). **Button-gated, not auto-computed:** the per-ticker forward-close fetches are heavier than the page's existing shared load, so the tab computes lazily into its own `st.session_state["_entry_timing_cache"]` key only after the user clicks "🔍 Analyze Entry Timing" — mirrors the existing Sector Rotation Heatmap load-button idiom, not the page's own always-on `_pac_enriched` load; the page's own "🔄 Refresh data" button also clears this cache. Renders a Plotly grouped bar (one x-axis group per band labelled with its numeric range, e.g. "< 15 (aligned)," one bar per horizon Day+1/Day+5/Day+20, **colored by alpha SIGN** — green = positive, red = negative, not by horizon) alongside a right-hand stack of bordered stat cards (one per band, border-colored by band identity) each showing a "chance of a red Day+1" headline (gated on `PREDICTIVE_MIN_BAND_N`) plus its `band_narrative()` sentence, a dynamic "📌 Illustrating case" callout (from `find_illustrating_case()`, omitted entirely when no qualifying repeat-offender exists yet), and a raw-values expander — same idioms as the page's other tabs, reworked once to match a previously-approved visual mockup that the initial build had drifted from (caught via live screenshot comparison, not verbal recollection — see `feedback_mockup_first_ux` memory). Scoped to `new_pick` only; `add_winner` deliberately not pooled in (different risk story — adding to existing exposure vs. fresh-capital timing — analyzed later as its own cut if ever revisited). `synthesize_directives()` gains an `entry_timing_bands` kwarg — emits a single `caution` directive (never `action`) when the Extreme band's Day+1 sample clears `PREDICTIVE_MIN_BAND_N`, phrased as awareness, not a sizing prescription (the app has no position-sizing mechanism to act on that framing yet). **Diagnostic only — never feeds back into the composite score or the 5-gate new-position pipeline;** if it ever produced evidence strong enough to justify a real gate, that would be a separate, explicit `constants.py` policy decision with the user's sign-off, not something this tab does on its own. **All 3 new constants are explicitly provisional** — fit to the single AMD anecdote, not yet validated against the real divergence-band distribution; re-check before treating as tuned. RSI-at-entry and earnings-proximity-at-entry are explicitly out of scope for v1 (neither is threaded into the `new_pick` dict today — wiring either touches multiple files across `daily_briefing.py`/`app.py`/`db.py`, more than this build's scope). **Phase 2 — SHIPPED 2026-07-28, commit `de12323`** (same day as Phase 1, once the user validated the divergence pattern held over a real sample: Extreme band n=20 matured Day+20 outcomes averaging -14.2% alpha vs SPY — not the 6-row anecdote that motivated the plan). A live caution annotation directly on the Grow Today "New Positions to Initiate" pick card, at the moment a fresh `new_pick` fires — the only mechanism with a plausible causal path to actually preventing a loss, since the retrospective tab alone requires the user to remember to check it. `daily_briefing.py::_grow_today()` attaches `pick["divergence"]` (via `divergence_at_entry()`, reused as-is from Phase 1) to every new_pick candidate — curated and mover picks alike, `add_winner` still excluded — right after the composite/fundamentals/staleness/macro/sector gates have all passed. `app.py`'s Grow Today card render shows a caption when `divergence > ENTRY_TIMING_DIVERGENCE_DIVERGING_MAX` (the existing, already-reviewed Extreme-band threshold — no new constant): momentum is running well ahead of the composite, and similar Extreme-divergence picks have historically looked calm early but underperformed by full maturity — pointing to the ⏱️ Entry Timing tab for current numbers rather than quoting a live percentage (which would require re-running Phase 1's per-ticker forward-close fetch on every brief build, defeating why that tab is button-gated). **Stays a caution, never a block or gate** — the pick has already cleared every gate by the point this attaches. **Opus pre-code design review: FIX-FIRST (1 blocking) → fixed → SHIP** — the first caption draft claimed picks "trade rougher in the first few days," which was backwards from the validated shape (calm early, damage late); reworded to match. **The same review also caught a live bug in Phase 1's own already-shipped `synthesize_directives()` caution text**, which had the identical inversion (claimed picks "open red on Day+1... though the effect fades by maturity") — fixed in the same commit, regated from `day1_n`/`day1_pct_red` to `day20_n`/`day20_alpha` (the horizon the directive actually describes). 4 new tests for the corrected directive logic. |
| F-207 | **Catalyst-Specific Stress (D4 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-26, commit `0dcecf1`. Last item of Phase 4 — closes the roadmap.** New "📅 Catalyst-Specific Stress (Beta)" expander, sibling to F-200's "🎯 Regime-Aware Adversarial Scenario" on 🔗 Risk Analysis → 🔥 Stress Testing. The event-driven twin of that feature: where F-200 asks "what ongoing macro CONDITION threatens this book," this asks "what single upcoming DATED EVENT (a HIGH-impact, sector-specific macro print, or a held position's own earnings report, within `CATALYST_STRESS_WINDOW_DAYS`=14 days) threatens the SAME structural weak points F-198/F-200 already identify." `stock_analyzer/catalyst_stress.py::rank_catalyst_threats()` ranks macro and earnings candidates in **two separate lists, never cross-scored** — summing a many-ticker macro event's overlap against a one-ticker earnings event's weight would always favor macro events, making the "top-ranked catalyst" predictably always the next CPI/FOMC rather than something genuinely informative. **Macro events whose category resolves to the `__ALL__` sector-impact sentinel are excluded entirely** (`macro_calendar.is_all_sector_category()`, new public wrapper over `_SECTOR_IMPACT`) — Fed Policy (FOMC) and Growth (GDP) events threaten every sector equally, so their overlap with any weak-point set is the maximum possible by construction, telling you nothing about which SPECIFIC holding is exposed (the opposite of this feature's purpose). The **weak-point ticker set** is pinned exactly: the union of every `shocked_ticker` and cascade `contributing_tickers` across `structural_scanner.blast_radius()` results, plus every ticker in any `portfolio_intelligence.correlation_clusters()` cluster. Earnings-candidate scoring uses only real, already-computed numbers — a ticker's own portfolio weight% plus (if it's a `shocked_ticker`) that entry's own `portfolio_impact_pct` — never an invented "contribution" figure. One Haiku call synthesizes a narrative covering whichever candidate(s) exist; if the ranking finds neither a macro nor an earnings candidate, the LLM is never called at all — "nothing catalyst-specific stands out" is decided by the pure-Python ranking step, not asserted by an API call with no evidence. **Recomputes `blast_radius()`/`correlation_clusters()` independently** rather than depending on F-200's local variables (never published to session_state), so this feature never silently depends on whether the user opened that section first this session — the same cheap, uncached recompute F-200 itself already pays. New constant `CATALYST_STRESS_WINDOW_DAYS = 14` (user-set) — deliberately distinct from `EARNINGS_URGENCY_SOON_DAYS` (a different feature's display tier) so tuning one never silently affects the other. New `catalyst_stress_cache` table (RLS, mirrors `regime_scenario_cache`'s exact shape). **Diagnostic only — never gates, scores, or modifies `blast_radius()`/`correlation_clusters()`/`build_macro_calendar()`/the earnings calendar themselves.** Opus design review: FIX-FIRST → SHIP (3 blocking: the cross-scoring bias, an undefined "contribution %" field pinned to the real `portfolio_impact_pct`, an ambiguous weak-point definition pinned exactly). Opus pre-ship code review: SHIP, 0 blocking, cosmetic non-blocking fixed (a None-guard on impact-percent formatting; a code comment explaining the deliberate `abs()` in earnings scoring). Plan: [docs/plans/catalyst-specific-stress.md](plans/catalyst-specific-stress.md). |
| F-224 | **Outcome Range — historical block-bootstrap Monte Carlo simulator. New 4th tab "🎲 Outcome Range" on 🔗 Risk Analysis, sibling to 📊 Dashboard / 📋 Action Plan / 🔥 Stress Testing.** Where F-14/F-14a/F-168/F-200/F-207 are all single-POINT-estimate scenarios, this is the app's first probability-weighted DISTRIBUTION of portfolio outcomes. `stock_analyzer/monte_carlo.py` resamples REAL historical daily returns per held ticker (`MC_HISTORY_PERIOD`="5y", via `data.fetch_price_history`) in contiguous `MC_BLOCK_DAYS`-length blocks (=20 trading days), sampling the SAME calendar dates across every ticker in a given trial — this is what preserves the real correlation between holdings (a historically correlated selloff day hits correlated names together in the resample) instead of destroying it via independent per-ticker resampling. **Deliberately does NOT attach a probability to any macro regime** — the same thin-data problem that made F-200 drop that framing (`daily_regime` has only ~3 days of history) applies here too, so this sidesteps it entirely by resampling raw historical returns instead of regime labels. Tickers with fewer than `MC_MIN_HISTORY_DAYS`=252 trading days of history (recent IPOs, fetch failures) are excluded and reported in the UI (never silently dropped); their weight is redistributed among the rest, and the $ outcome table is scaled to the included holdings' value only — not the full portfolio — so the % and $ ranges shown always describe the same thing. User selects a horizon (`MC_HORIZON_OPTIONS_DAYS`=[21,63,252] trading days, ~1mo/1qtr/1yr; default `MC_HORIZON_DEFAULT_DAYS`=63) and clicks an explicit "Run Simulation" button — never auto-run on page load, since the multi-year fetch across ~15-30 held tickers is new cost/latency the app doesn't pay anywhere else; result cached 24h (`_cached_monte_carlo`, keyed on the ticker/weight/market-value tuples + horizon). Renders a `plotly` fan chart (5th/25th/50th/75th/95th percentile bands over the horizon) plus a horizon-endpoint percentile table and a methodology/caveats expander (states the constant-weight, no-rebalancing simplification and the historical-bootstrap-not-forecast framing explicitly). **Diagnostic/awareness only — same class as Stress Testing and Regime Fit; never gates, scores, or modifies any recommendation, WATCH/TRIM/EXIT tier, or the composite score.** New constants: `MC_HISTORY_PERIOD`, `MC_MIN_HISTORY_DAYS`, `MC_TRIALS`(=2000), `MC_BLOCK_DAYS`, `MC_HORIZON_OPTIONS_DAYS`, `MC_HORIZON_DEFAULT_DAYS` (all simulation-method parameters, not investment-policy thresholds). 23 new tests in `tests/test_monte_carlo.py`. Opus-reviewed SHIP, 0 blocking (5 non-blocking findings — a dollar-basis/excluded-ticker inconsistency, imprecise "buy-and-hold" wording, a too-loose correlation-preservation test bound, cache-key drift, and a note on the hardcoded display-percentile tuple — all fixed same session except the cache-key note, accepted as a low-priority TTL-effectiveness observation, not a correctness issue). |

### 3.1e Behavioral Fingerprint — Signal History Coverage

| ID | Requirement |
|----|-------------|
| F-192 | **Behavioral Fingerprint — Signal History Coverage — SHIPPED 2026-07-17, commits `253f6ac`→`a57eddd`; relabeled 2026-07-22, commit `b8939eb`.** Collapsed expander on 📊 Predictive Analytics (titled "🧬 Behavioral Fingerprint — Signal History Coverage" as of the relabel — was "Data Readiness Audit"), below the existing Alpha Attribution placeholder. One-time, informational read answering the plan's own Week-1 Concept A question (`docs/plans/next-evolution-strategy.md` line 660): is there enough historical signal-to-action data to build the Behavioral Fingerprint from history, or must it capture forward-only? Reuses existing helpers (`recommendations_history.summary_stats`/`by_rec_type`, the already-loaded `_pac_enriched`) — zero new DB calls, zero new pure-logic module, zero new constant. Reports: (1) Buy-side completeness scoped to actionable `new_pick`/`add_winner` recs only (`buy_candidate` deliberately excluded — it's the awareness-only "More Buy Candidates" feed, never a gated rec, and blending it in would deflate the rate with signals never meant to be acted on, the same distortion class already fixed once on Recommendations History's default filter); (2) reverse trade-side provenance — % of BUY trades with a `trigger_type='RECOMMENDATION'` (app-signal-preceded) vs everything else; (3) a hardcoded finding that TRIM/EXIT/risk_advisor signals have zero persistent historical log at all (computed live each session, never written to a table), so exit-side bias patterns (disposition effect, loss-aversion timing) cannot be built from history regardless of Buy-side completeness — Concept E's `decision_context` capture only records a count of active Act-Today items, not per-ticker signal identity, so even that doesn't close the gap; (4) a go/no-go verdict against the plan's own 80%-complete threshold (line 684). Awareness only — no gate, no recommendation. Two post-ship bugs fixed same day: the first-pass headline blended `buy_candidate` into the completeness rate (deflating it to 3% instead of the correctly-scoped 12%); a `NameError` on the session cache-HIT path (`_pac_trades_df` was only ever bound on the cache-MISS branch earlier on the page). See `project_behavioral_fingerprint_audit` memory for the actual audit result and Concept A go/no-go call. **The 2026-07-22 relabel (commit `b8939eb`) is copy/label only, zero logic change:** stripped internal-QA-tooling framing that had leaked into permanent user-facing copy (raw `rec_type` tokens, module/table/Concept-E references, the "one-time audit — not a live feature" caption) in favor of plain-English wording — same data, same computation, same location. |

### 3.1f Behavioral Fingerprint (Concept A v1)

| ID | Requirement |
|----|-------------|
| F-193 | **Behavioral Fingerprint — v1, Buy-side only — SHIPPED 2026-07-17, commit `da59b00`; Opus reviewed FIX-FIRST (1 blocking) → applied → SHIP.** New 4th tab "🧬 Behavioral Fingerprint" on 🎯 My Edge, next to Decision Quality Timeline (same `recommendations`/`trades` substrate — per the plan's coordination note, not a parallel logger). New module `stock_analyzer/behavioral_fingerprint.py`: three pure, sample-gated pattern functions (`momentum_recency_pattern`, `conviction_tier_pattern`, `opening_window_pattern` — see `docs/architecture.md`). Every card suppresses to "insufficient data — N more decisions needed" below `BEHAVIORAL_MIN_SAMPLE_N` (8) in either compared bucket — the plan's own critical constraint against premature pattern labeling. Scoped to actionable `new_pick`/`add_winner` recs only, matching the F-192 audit's established scoping (excludes the awareness-only `buy_candidate` feed). Regime-conditioning requirement satisfied via explicit framing ("an observed correlation in your own decisions, not a verdict on it") rather than actual regime splits, which would fragment the already-thin sample (12% historical Buy-side completeness per F-192) into permanent insufficient-data. Exit-side (TRIM/EXIT) bias patterns explicitly out of scope for v1 — no historical or forward capture exists yet (F-192 finding). Awareness only — never re-ranks, re-scores, or gates a recommendation; no LLM. **Opus review caught one blocking issue before ship:** the opening-window pattern's caller-side loop wasn't scoped to actionable rec_types, letting `buy_candidate` contaminate the sample — the exact mistake already caught once earlier the same day in F-192's own audit; fixed with the identical rec_type filter. |
| F-226 | **Personalized Discovery — runs Behavioral Fingerprint's analysis FORWARD.** Behavioral Fingerprint (F-193) only ever compares two buckets of past decisions against each other (e.g. high-momentum vs low-momentum action rate) — it has no notion of "what did a typical winner look like at entry." This feature builds that profile and uses it to flag which of TODAY's already-fully-gated 🏠 Home Grow Today picks resemble the user's own REALIZED winning trades. New module `stock_analyzer/personalized_discovery.py`: `build_winner_profile(closed_lots, matched_recs, min_n, pctl_low=25, pctl_high=75)` joins `investor_mirror.build_closed_lots()`'s realized round-trips (filtered to `is_gain=True` AND a non-null `pnl_pct` — `is_gain` alone is insufficient, matching the guard `investor_mirror.py`'s own winner-filtering functions already apply) back to `recommendations_history.match_recs_to_trades()`'s matched, acted-on, actionable (`new_pick`/`add_winner`) rows via `ticker` + `rec_date == buy_date` (sound because `match_recs_to_trades()` only ever matches a rec to a SAME-DAY trade). Multiple sell-fragments of the SAME scaled-out entry collapse to one sample via `drop_duplicates(subset=["ticker","buy_date"])` before counting — a position sold in 3 tranches is one entry decision, not three. Returns `None` below `min_n` matched winning entries (reuses the existing `BEHAVIORAL_MIN_SAMPLE_N`=8 floor, not a parallel constant) — withheld, never a profile fabricated off a handful of trades. `score_candidate_match(composite_score, momentum_score, sector, profile)` checks 3 traits independently (in-band composite, in-band momentum, sector-in-top-sectors) against a candidate, `None`-safe throughout. Zero new fetches — entirely a replay of already-loaded trades/recommendations; deliberately avoids `compute_outcomes()`'s mark-to-market path (would need a live current price for every historically-recommended ticker, including delisted/no-longer-held ones). **Two surfaces:** (1) `stock_analyzer/daily_briefing.py::_grow_today()` attaches `pick["personalized_match"]` to every `new_pick` (NOT `add_positions` — same "brand-new entry only" scoping F-220 Phase 2 already established) after every existing gate has passed; `app.py`'s Grow Today card renders a "🧬 Matches your historical winning profile (…)" caption only when `n_matched >= PERSONALIZED_DISCOVERY_MIN_MATCH_TRAITS` (2 of 3) — silence, never a warning, when it doesn't match, since a non-match isn't itself a negative signal on an already-vetted pick. (2) A new "🔭 Your Winning Entry Profile" card on 🎯 My Edge → 🧬 Behavioral Fingerprint, computed independently and locally there (own full-history `db.load_recommendations(start_date=None, end_date=None)` call, distinct from that tab's existing 30-day-default read) rather than via a shared session_state cache — same "cheap pure recompute beats cross-page order-dependency" precedent already used by `catalyst_stress.py`. New constants: `PERSONALIZED_DISCOVERY_MIN_MATCH_TRAITS`=2, `PERSONALIZED_DISCOVERY_PROFILE_PCTL_LOW`=25, `PERSONALIZED_DISCOVERY_PROFILE_PCTL_HIGH`=75. **Diagnostic/awareness only — never changes which tickers clear the 5-gate `_grow_today()` pipeline, never re-scores or re-ranks a pick, never suppresses a non-matching one.** `_home_synth_cache`'s `_SYNTH_SCHEMA_VER` bumped 5→6 (new `personalized_match` key on `new_picks`). 21 new tests (`tests/test_personalized_discovery.py` + extended `tests/test_daily_briefing.py`). Opus round 1: FIX-FIRST (2 blocking — `is_gain` alone missed a null-`pnl_pct` trap `investor_mirror.py` already guards against elsewhere; `n` counted winning SELL-fragments instead of distinct winning ENTRIES, letting one scaled-out position triple-count and defeat the sample-size floor) → both fixed, 2 regression tests added. Opus round 2: **SHIP, 0 blocking.** Plan: `C:\Users\ajay.x.ku\.claude\plans\moonlit-splashing-dove.md`. |

### 3.1g Investor Mirror (F-194)

| ID | Requirement |
|----|-------------|
| F-194 | **Investor Mirror — Conviction Alignment + Behavioral Biases — SHIPPED 2026-07-20.** New 5th tab "🪞 Investor Mirror" on 🎯 My Edge. New pure module `stock_analyzer/investor_mirror.py` (no Streamlit, no DB, no yfinance). **Awareness only — zero gates, zero recommendations, zero scoring impact.** Answers two questions that complement the existing My Edge tabs: (1) does the user's portfolio allocation actually reflect their composite conviction? (2) does their sell behaviour reflect rational action, or recognisable behavioural biases? **Section 1 — Conviction Alignment:** computes the Spearman rank correlation between each position's composite Score and its portfolio Weight (%) using `pd.Series.corr(method='spearman')` — no scipy dependency. A ρ near +1.0 means the largest bets are the strongest convictions; ρ near 0 means allocation is essentially random relative to beliefs. Three named misalignment patterns identified: **Orphan Conviction** (Score ≥ `COMPOSITE_STRONG_BUY` and weight < median — high conviction, undersized), **Accidental Overexposure** (Score < `CONVICTION_WEAK_SCORE` (50) and weight > median — significant allocation on weak conviction), **Legacy Overhang** (top-`CONVICTION_LEGACY_TOP_N` (3) by weight and Score < `CONVICTION_FADED_SCORE` (60) — a large position whose conviction has drifted out of Buy territory, often from price appreciation rather than a deliberate sizing decision). Rendered as a headline Spearman ρ metric + plain-English label (Disciplined ≥ `CONVICTION_ALIGNMENT_HIGH` 0.60 / Partial / Random < `CONVICTION_ALIGNMENT_LOW` 0.30), a Score-vs-Weight scatter chart with an ideal-alignment reference diagonal, and three callout containers listing each misaligned position. Uses today's composite scores (historical composite scores are not stored — stated clearly in the methodology note). Requires `_port_df_enriched` to be populated (Home visited); shows an info gate otherwise. **Section 2 — Behavioral Biases:** three cards derived from `investor_mirror.build_closed_lots(trades_df)` — a FIFO lot-matching function adapted from `tax_advisor._build_open_lots`, extended to track buy_price per lot so P&L can be computed per matched fragment. Partial trims are handled correctly via lot depletion. SPLIT rows scale both share count and buy_price per lot so cost basis stays accurate. Cached in `st.session_state["_mirror_closed_lots_{n}"]` keyed by trade count (auto-refreshes when trades change). **Card A — Disposition Effect:** share-weighted average holding period for gain-lots vs loss-lots; ratio = loser avg ÷ winner avg; ≥ `DISPOSITION_CONCERN_RATIO` (1.5×) renders an amber concern note (consistent with the published retail benchmark range of 1.5–2.0×). **Card B — Win/Loss Closure Ratio:** groups matched lots by (ticker, sell_date) to get per-transaction net P&L; ratio = gain-realising transactions ÷ loss-realising transactions; ≥ `WINLOSS_CONCERN_RATIO` (2.0×) renders a loss-aversion note. **Card C — Breakeven Anchoring:** buckets each closed lot by P&L% at exit into 10 fixed presentation brackets (< −20% through > +20%); computes share-weighted average hold-time per bracket; flags when the −2 to 0% bracket avg hold-time is ≥ `BREAKEVEN_ANCHOR_DWELL_RATIO` (1.3×) the mean of the adjacent loss brackets, indicating positions are held unusually long when close to breakeven. Rendered as a horizontal bar chart (loss brackets red, gain brackets green). Each card individually suppresses to "insufficient data" when below `INVESTOR_MIRROR_MIN_CLOSED_LOTS` (10) in its required comparison group. **Opus review: SHIP** after 4 blocking constants-externalization findings applied (bare literals `>= 75`, `< 50`, `< 60`, `1.3` lifted to `COMPOSITE_STRONG_BUY`, `CONVICTION_WEAK_SCORE`, `CONVICTION_FADED_SCORE`, `BREAKEVEN_ANCHOR_DWELL_RATIO`). 10 new display-only constants in `stock_analyzer/constants.py` F-194 block. **Cross-feature coordination fix (2026-07-30, the BKNG incident):** Orphan Conviction candidates now exclude any ticker carrying an active Act Today/Review reduce call (`decision_bucket.suppress_orphans_under_reduce_call`, reads the published `_reduce_calls` cache) — previously a "size up" Orphan Conviction card and a same-week "size down" reduce/trim call could fire on the same ticker with no cross-check, which is how the fix was found (the user sized up on the Orphan Conviction cue, which then itself tripped the earnings-overweight-trim threshold). Excluded tickers render a visible "⛔ excluded" note naming the active reduce call rather than being silently dropped; the filtered set is also published to `_mirror_orphans` so the Analysis page's 🪞 Investor Mirror banner (a second, independent consumer of the same data) can't surface the same contradiction. |

### 3.1h Summary (F-204)

New standalone page (MAIN nav group, positioned right after Home), added
2026-07-26 for a lean "portfolio state + today's actions" view without
reworking Home itself.

| ID | Requirement |
|----|-------------|
| F-204 | **Summary page — KPI tiles + Act Today + Holdings, additive to Home.** Home (§3.1) keeps its full Today's Brief/Grow Today/Monitoring/Evening Debrief content completely unchanged — this page adds nothing new there and reads nothing Home doesn't already publish. Gated the same way as every other Home-extracted page (`_render_portfolio_not_loaded()` + `st.stop()` on `_port_df_enriched`/`_last_held_data`). Renders: (1) an 8-metric KPI row (Portfolio Value, Unrealized P&L, Today's P&L, Alerts, Avg Score, Diversification, Best/Worst position) — Portfolio Value and the Alerts/Diversification figures read the same published `_portfolio_value`/`_home_synth_cache` caches Home populates; the rest (Unrealized P&L, Avg Score, Best/Worst) are cheap independent recomputations from `port_df`, so no session-state coupling is added. **Portfolio Value tile also shows a trailing sparkline** (shipped 2026-07-27) — daily equity (`Σ shares × close_price`) grouped from the optional `daily_snapshots` table via `db.load_daily_snapshots()` (date-filtered to the trailing ~45 calendar days so the window is exactly what's shown, not row-count-limited), rendered by the shared `_render_sparkline()` helper (bare Plotly line, no axes); silently omitted (not an error) when fewer than 2 days of snapshot history exist. Alerts/Avg Score deliberately carry no delta/trend — point-in-time counts with no natural "vs. what." **A second row (Tier 3, shipped 2026-07-27)** sits below the 8-tile row: **Alpha vs SPY (Nd)** — portfolio equity return vs SPY return over the same trailing window (N up to 30 trading days, shrinking honestly and relabeling when less snapshot history exists; withheld below 5 days), with its own sparkline of the running differential; tooltip is explicit that this is an **approximate, equity-level** read NOT adjusted for mid-window buys/sells (the precise money-weighted return lives on 💰 Account → Capital Trend). **Confirmed live 2026-07-27 that this contamination is a real, not just theoretical, risk** (a mid-window capital add produced a misleading +15.9% headline) — a same-day follow-up added an always-visible caption ("⚠️ Capital added/removed during this window — not a pure performance read"), checking `trades_df` for any BUY/SELL dated within the joined window, since a hover-only tooltip wasn't enough to stop the number being read at face value. **Risk Posture** — a read-only pointer badge (🔗 title + severity emoji/label from `market_risk_posture()` + one-line summary + a "→ Risk Analysis" **button**, `_pending_page` navigation), reusing the identical `exit_advisor.market_risk_posture()` call and the already-published `_fragility_cache` the Risk Analysis dial uses — never a second independent verdict; withheld (no card) when fragility isn't computed yet. **Fixed 2026-07-27 (live-review catch):** originally shipped with a plain non-clickable caption instead of a real jump button — inconsistent with the two pointer cards below, which both navigate. Corrected to the same `st.button` + `_pending_page` pattern; also added the 🔗 title-icon prefix (matching Risk Analysis's own nav icon) since the card title previously carried none, unlike 🩺 Thesis Review / 🔔 Catalyst Watch below. **A new "🧭 Elsewhere in DRISHTA" pointer section (shipped 2026-07-27, card #2 of `docs/plans/summary-page-pointer-cards.md`)** sits between Act Today and Holdings: **🩺 Thesis Review** shows a count of held tickers whose most recent `thesis_reviews` row (F-1's per-holding INTACT/WEAKENING/BROKEN review — NOT the ⚠️ Red Team tab's separate quantitative erosion score) is WEAKENING or BROKEN, reusing the identical "most recent review per ticker" logic the AI Insights page's own "🩺 Positions" header count and the Act Today "Thesis broken" button already use — never a second independent verdict. Shows "All Intact" (calm, not hidden) when none need attention; withheld entirely only when no held ticker has any saved review yet. A "→ AI Insights" button (`_pending_page` navigation) lands on that page's default first tab, 🩺 Positions, where this exact count already lives. **A second pointer card, 🔔 Catalyst Watch (shipped 2026-07-27, card #3), sits alongside it:** a count of held names reporting earnings within `CATALYST_WATCH_WINDOW_DAYS` (7) days, reusing the SAME 24h process-wide `@st.cache_data`-cached `_cached_held_earnings_dates()` call (identical tickers-tuple + 90-day range) Catalyst Watch's own "Your Holdings — Earnings" tier uses — whichever page runs first pays the cached fetch once, the other gets a free hit, never a second independent computation. Prefers `held_data`'s own bundle-loaded earnings date first (zero cost) before falling back to the cached backfill, matching `_render_holdings_earnings()`'s exact preference order. Always shows a state (including "None soon") rather than hiding, since earnings timing is informative even when the answer is none. A "→ Catalyst Watch" button navigates there. **No RAG banner** ("Action Required"/"Monitor"/"All Clear") — dropped 2026-07-26, same day as ship: it duplicated Home's identical Command Center bar, and its `n_danger`-driven label (Active Alerts, F-169) can legitimately disagree with an empty Act Today bucket (a different signal — same-day trade decisions), which read as a flat contradiction on this short page; the Alerts KPI tile already carries the same counts calmly. Today's P&L uses the simpler held-mark calc (F-03) rather than Home's fuller Tier-B upgrade (F-03a), to avoid a second `daily_snapshots` round-trip on a second page. (2) **Act Today**, reusing Home's exact `_daily_brief` data (read from `_home_synth_cache`) and the identical pure `split_defensive()` call Home uses for its own bucket — never an independent recommendation computation. Cards are a deliberately **leaner** rendering than Home's (`_render_simple_action_card()`: tier badge + ticker + one-line rationale, matching the approved Option-A mockup) — no Analyze button, Exit Red-Team debate, journal context, or tax notes; those stay Home-only. (3) The full-width **Holdings** table (`_render_holdings_table()`, module-level, shared/reusable) — this is now its ONLY home; it was briefly also on Home (same-day, F-01) but removed from there once this page shipped, so it isn't duplicated across two pages. Both the Holdings table and the shared severity-color constants (`_HOME_URGENT`/`_HOME_ELEVATED`/`_HOME_CALM`/`_HOME_GAIN`/`_HOME_LOSS`) and `_fmt_action()` were hoisted from Home-local closures to module-level functions as part of this change so Summary could reuse them — a pure hoist, verified to leave Home's own rendering unchanged. |

### 3.1i The Judge (F-227)

New standalone page (MAIN nav group, between Home and Summary), the North
Star portfolio-judgment layer — see `docs/plans/judgment-layer.md` for the
full design history and Opus review log. Built forward-only across all 5
phases, all shipped 2026-08-03.

| ID | Requirement |
|----|-------------|
| F-227 | **The Judge — a portfolio-level judgment layer reconciling the app's other features into one decomposable posture, never a black box.** Existing subsystems (composite score, momentum scanner, verdict reconciliation, exit_advisor deterioration tiers, the concentration gate, the fragility gauge) each already emit a decision-relevant call; the Judge collects them as tagged **opinions** (`stock_analyzer/judgment_opinion.py::build_opinion()` — `source`/`dimension`/`signal` normalized −1..+1/`confidence`/`as_of`+`is_live`/`evidence`/`ticker`/`advisory`) and reconciles same-ticker (and portfolio-wide) opinions into one read via `stock_analyzer/judgment_synthesis.py::synthesize()`. **Three routing outcomes, never a plain average:** (1) **PROTECTIVE VETO** — a protective-dimension opinion (`position_health`/`concentration`/`structural_risk`/`leverage`) at/below `JUDGMENT_VETO_PROTECTIVE_THRESHOLD` (−0.4) hard-suppresses EVERY same-ticker positive acquisitive-dimension opinion (`quality`/`momentum`) at once — the most severe (lowest-signal) protective opinion wins when more than one qualifies; suppressed opinions are shown as dissent, never blended in. (2) **CONTRADICTION AUDIT** — two different sources on the same dimension with opposite-sign signals, both at/above `JUDGMENT_CONTRADICTION_MIN_MAGNITUDE` (0.3), are flagged rather than silently averaged away. (3) **WEIGHTING** — everything else blends via a confidence-weighted average. Advisory opinions (`verdict_reconciliation` — a meta-witness that already synthesizes momentum+composite+news+earnings) are shown for context but excluded from veto/blend/contradiction routing, so a meta-witness never double-counts its own inputs. **Read-only, no override/gating authority at any phase built so far** — visiting the page changes nothing about any recommendation. **Phase 0 (instrumentation, log-only):** 5 core-set witnesses wired at their existing call sites (exit_advisor→`position_health`, composite_score+scanner_momentum+verdict_reconciliation→`quality`/`momentum`, fragility_gauge→`structural_risk`, concentration_gate→`concentration`, the last being a real single-name/sector ceiling-breach check against `SINGLE_NAME_CEILING`/`SECTOR_CEILING`), persisted to `judgment_opinions` (architecture.md §6.29). **Phase 1 (read-only decomposition):** the "🧑‍⚖️ The Judge" page itself — an overall posture line, per-ticker and portfolio-wide vote decomposition with veto/contradiction callouts, and a "reduced visibility" caption when a protective dimension had no live witness this run (an offline protective sensor must never silently read as "clear"). **Phase 2 (grading harness):** `stock_analyzer/judgment_grading.py` grades individual WITNESSES (never the aggregate posture — posture-correctness is a derived, secondary read) against realized forward alpha vs SPY over a per-dimension horizon (`JUDGMENT_HORIZON_MOMENTUM_DAYS`=5, `..._QUALITY_DAYS`=20, `..._POSITION_HEALTH_DAYS`=10, `..._CONCENTRATION_DAYS`=20, `..._STRUCTURAL_RISK_DAYS`=10), reusing `predictive_analytics.forward_alpha_at_horizon()` (the Entry Timing tab's own mechanism) for per-ticker grading and a net-new `portfolio_value_series_from_snapshots()` for portfolio-wide grading. **Protective dimensions are deliberately WITHHELD from grading (return `None`, distinct from "pending maturity"), not graded naively** — sign-matching a TRIM/caution call against forward alpha would mark the witness "wrong" for every risk that correctly never fired, the exact anti-caution posture inversion the design's Gap B exists to prevent; a real counterfactual grader is a future gap, not yet built. A manual "▶ Run grading" button persists new grades to `judgment_grades` (architecture.md §6.30); results roll up per (source, dimension) via `track_record_summary()`, gated on the shared `BEHAVIORAL_MIN_SAMPLE_N` (8) floor — below that, a witness's accuracy shows as "building history," not a number. **Phase 3 (evidence-based weighting, still no authority):** once a (source, dimension) pair clears the min-sample gate, its blend weight in `synthesize()` is scaled by a track-record multiplier (`_weight_multiplier()` in `judgment_synthesis.py`) — `accuracy / JUDGMENT_TRACK_RECORD_NEUTRAL_ACCURACY` (0.5, coin-flip-is-neutral), clamped to `[JUDGMENT_TRACK_RECORD_WEIGHT_FLOOR, JUDGMENT_TRACK_RECORD_WEIGHT_CEILING]` (0.25x–2.0x, user-confirmed "moderate" band) so no single witness can be fully silenced or dominate the blend alone. **The multiplier only ever touches the blend — the protective veto and the contradiction-audit magnitude floor are never weighted**, preserving them as hard gates rather than softening them into a vote (the same structural principle the original Q1 design review required). Each opinion's effective weight is shown next to its signal on the page (e.g. "1.8x weight") whenever it differs from neutral, so the adjustment is never hidden. Below the min-sample gate, or with no track record supplied, behavior is byte-identical to Phase 1/2's equal-weight blend. **A real production bug was found and fixed via a live screenshot during Phase 1** (not caught by design/code review): opinion-capture code had been written only inside the Home `_home_synth_cache` cache-MISS branch, so it silently never ran on the common cache-HIT case; fixed by consolidating capture to the hit/miss reconvergence point. **Phase 4 (authority — audit only, never a new gate):** a pre-build research pass found that granting the Judge's protective veto literal override/gating authority over all 4 dimensions, as the original one-line design sketch described, would either duplicate mature enforcement that already exists elsewhere (`position_health` via the `_reduce_calls` mechanism across 5+ surfaces — Grow Today, Rebalancer, Investor Mirror, Analysis add-sizing, deterioration-WATCH blocking; `concentration` via the entry-gate pipeline's own `SINGLE_NAME_CEILING`/`SECTOR_CEILING` checks, fully decoupled from the Judge) or silently reverse a standing, deliberately-documented house policy (`leverage` — CLAUDE.md's coordination pattern explicitly states `_leverage_cache` is "read-only, never gates"). Presented with this finding, the user chose an **audit-only scope**: `stock_analyzer/judgment_synthesis.py::audit_coherence()` cross-checks every ticker under an active protective veto against `_reduce_calls` (the app's one other already-published cross-feature risk surface, read directly from `session_state` with no new plumbing) and reports `"covered"` (a veto exists AND the ticker has an active reduce call — validating: the Judge agrees with what's already flagged) vs `"uncovered"` (a veto exists with NO active reduce call — a genuine coherence gap the Judge alone caught this run). This operationalizes the design's own "Job 3: audit for cross-feature contradiction systematically" as a standing, automatic check rather than something only caught reactively (the class of incident the BKNG orphan-conviction collision and SHOP buy/sell whiplash represent). **Never suppresses or modifies any recommendation — purely surfaces a finding loudly** (a red banner per uncovered gap, a calm confirmation when none exist, and an explicit "unavailable — visit Home first" state when `_reduce_calls` hasn't been published yet this session, never silently treated as zero reduce calls). No new `constants.py` entries — reuses the existing veto computation and threshold. Includes the portfolio-wide bucket under a `"_PORTFOLIO_WIDE"` sentinel key for completeness, though a portfolio-wide veto cannot currently fire (no portfolio-wide acquisitive opinion is emitted yet). **This closes the Judge's full 5-phase build (0-4).** Opus 4.8 reviewed Phase 1 (FIX-FIRST, 2 blocking, both fixed), Phase 2 (FIX-FIRST, 2 blocking, both fixed), Phase 3 (SHIP, 0 blocking, 2 non-blocking hardening tweaks applied anyway), and Phase 4 (SHIP, 0 blocking, 3 non-blocking findings fixed anyway — see the plan doc's status log). Plan: [docs/plans/judgment-layer.md](plans/judgment-layer.md). |

### 3.2 Today's Brief (Daily Briefing)

| ID | Requirement |
|----|-------------|
| F-20 | Show market tone header (bull / bear / flat) based on S&P 500 daily change (≥+0.5% bull, ≤-0.5% bear). Header date uses ET timezone; when market is closed, appends "data as of [last trading day]". |
| F-21 | Display date, S&P 500 %, Nasdaq %, and top 2 leading sectors by 1-week return |
| F-22 | Act Today (defense / right column): prioritised list of urgent actions — stop breaches, sell signals, critical news, macro events, risk-advisor trims. Each item carries a structured directive (what to do), why, and trigger — not a one-line label. (See F-22a/F-22b.) |
| F-22a | Act Today items are **consolidated per ticker**: a mechanical-exit signal (stop breach or sell signal) on a ticker suppresses any risk-trim card for the same ticker (you don't trim what you're exiting); multiple risk flags on one ticker merge into a single card. Consolidation logic lives in `daily_briefing._consolidate_act_today`. |
| F-22b | Each Act Today item exposes `kind` (stop_breach / sell_signal / critical_news / macro / risk), a `directive` (concrete action), `why`, and `trigger`; risk items additionally carry a `risk_flags` list. A back-compatible `reason` field is synthesised for legacy consumers. |
| F-22c | **Rebalance plan on the sector Hard-Cap-Breach card.** A `sector_concentration` Act Today card (Risk Advisor "Hard Cap Breach" / "Elevated Sector Concentration") completes the trim→redeploy loop its copy promises but previously left blank. An **opt-in, button-gated** "🔎 Rebalance plan — what to trim, where to redeploy" reveals: (a) **Trim first** — the breached sector's held names ranked by **engine composite ascending** (`risk_advisor` `trim_candidates`), so "trim your lowest-conviction names first" is concrete; names with **no computed composite are excluded** (None-preserving `score_raw`, never coerced to 0 → they can't masquerade as lowest-conviction). Each trim name shows its **basis** so a bare conviction number never proposes a trim blind: the composite's three **pillars** (`tech`/`fund`/`sentiment`, from the held bundle's `t_score`/`f_score`/`s_score`) + **recent momentum** (1wk/1mo `portfolio.trailing_return` off the bundle `df`) + a factual "conviction capped by {weakest pillar}" note. The list is also a **quantitative plan that reconciles to the headline directive**: `portfolio.trim_allocation` greedily distributes the rec's own `trim_target_dollar`/`trim_target_pp` (= `excess_dollar`/`excess_pp`, same `_gd` denominator so Σpp = the headline pp) across the conviction-ordered names — **fully exit the weakest first, partial-trim the last** — and each row shows **$ · pp · ≈shares · [full exit|partial]** plus a **directional tax flag** (🧾 realizes a gain/loss/breakeven, from the position's unrealized P&L — informs take-profit-vs-cut-laggard; not lot-level accounting). A running total (`✓ $X of ~$target · sector → SECTOR_ELEVATED`, 25%) reconciles to the target, with a rolled-up remainder past a display cap and an honest **shortfall** note when the scored names can't cover the target. **Advisory, not an order** — shares are `≈`, the $/pp are authoritative, nothing auto-executes. Framed honestly as **display-only** context: order stays composite-ascending (engine authority), small gaps are called out as within scoring noise, and the two legitimate schools when a sector MUST be cut — **trim the laggard** (momentum) vs **take profit on the runner** (rebalance) — are named so the user reconciles the engine's conviction read with their own and makes the final call. And (b) **Redeploy into** — engine-vetted buy candidates in the under-represented sectors (`redeploy_sectors`), scored and gated by the **SAME** pipeline as the Diversification ADD card (`portfolio.diversifying_candidate_pool` → `annotate_add_candidates`, gate = `COMPOSITE_BUY`, cached `_grow_composites`/`load_all`, bounded by `DIVERSIFY_SCAN_CAP`), globally re-ranked composite-first across sectors and showing the top `DIVERSIFY_DISPLAY_TOP` with a ▶ Analyze bridge. Each redeploy candidate is annotated with its **correlation to YOUR ACTUAL BOOK** (`portfolio.correlation_to_portfolio` vs `portfolio_return_series`, data-driven — supersedes the tech-heavy-assuming `_SECTOR_PROFILES` for this surface; labels `REDEPLOY_CORR_DIVERSIFIER_MAX` 0.40 / `REDEPLOY_CORR_CORRELATED_MIN` 0.70 are **display classifications, not gates**), and the lowest-correlation gate-passer is badged "🏆 cleanest diversifier." **Invariants:** the engine composite + `COMPOSITE_BUY` are the **sole ranker/gate**; correlation is a display **highlight** (never re-ranks past the composite); the analyst note (per F-154a) is **awareness-only**. **Calm-advisor (§2B):** button-gated (not an `st.expander`, whose body always executes) so no scoring/correlation compute runs until the user asks. The rebalance payload rides on the item's `risk_flag` (not the item top-level) so it survives `_consolidate_act_today`, which keeps only the primary item's keys but gathers all flags. `app.py _render_act_card`. |
| F-23 | Grow Today (offense / left column): market-tone-aware growth setups — new positions and add-to-winners on bull/flat days; deferral message on bear days |
| F-24 | Each Grow Today pick includes: ticker, sector, scanner score, thesis one-liner, suggested entry zone (lo–hi range), position size (shares, cost, stop) |
| F-24a | **Movers** (discovery picks from outside the curated universe — see F-60a) surface in the SAME "New Positions to Initiate" list as curated picks, not a separate developer-facing section; the user sees one action list regardless of how the ticker was sourced. Movers face the composite gate (≥65), macro gate, and act-today block like curated picks, but get their OWN allowance (`MOVER_MAX_PICKS`=3) separate from the curated cap, skip the curated momentum bar and the 1-per-sector diversity rule, and are **exempt from the flat-day high-conviction suppression** (a composite-Buy stock up ≥5% today is itself the clearer direction the flat-day caution waits for). They still respect bear-day risk-off (no Grow Today processing at all on bear days). |
| F-25 | Review Before Close (defense / right column, under Act Today): approaching stops, near-term earnings, weakening large positions. Each item issues a **quantitative directive** — not prose. Item types: WATCH, TIGHTEN_ONLY (raise stop to current − 1.5×ATR), TRIM_AND_TIGHTEN (profit-lock 25% of position + tighten stop when P&L ≥ 25%), TRIM_TO_TARGET (earnings-overweight → trim to 10%; weak-large → trim to 8%), PROTECTIVE_TRIM (macro-affected sector → reduce lowest-conviction holding by 5pp). Each carries `headline`, `action`, `why`, `trigger`. |
| F-25a | **Calm-advisor split (§2B persona enforcement).** The defensive column is split by URGENCY into **🔴 Act Today (decisions only)** — stop_breach / sell_signal / risk / critical_news + active trims (TRIM_AND_TIGHTEN / TRIM_TO_TARGET / PROTECTIVE_TRIM) — and **👁️ Monitoring / Awareness (FYI)** — macro / WATCH / TIGHTEN_ONLY + anything unrecognised (fail-to-calm). Borderlines are policy flags (`BUCKET_CRITICAL_NEWS_IS_ACT`=True, `BUCKET_TIGHTEN_ONLY_IS_ACT`=False). When the Act bucket is empty, a prominent **"✅ Nothing to act on — you're set for today · monitoring N items"** banner replaces it (derived from empty-Act; no new persistence). Logic: `decision_bucket.split_defensive`. **Risk-metric refinement:** slow-moving Risk-Advisor drags (Sharpe / beta / volatility / drawdown / tail risk — `_TUNEUP_RISK_TYPES`) are NOT same-day decisions, so they route to a separate **🔧 Portfolio Tune-up** lane (`portfolio_tuneup`) rendered below Act/Awareness, not into Act Today. Concentration breaches stay in Act (structural). |
| F-25b | **Position lifecycle + settling grace.** Each held position has a lifecycle state (`position_lifecycle.classify_position_state`: exit / at_risk / settling / winning / established) from age (oldest still-held lot via `tax_advisor._build_open_lots`), P&L, and gap-to-stop. A "settling" position (held < `POSITION_SETTLING_DAYS`) is exempted from the routine approaching-stop tighten nudge (it sits at its normal entry-to-stop distance by construction); exits and ≤3%-gap items are NEVER suppressed (danger beats age; no-journal age=None never settles). A 🌱/📈/⚠️ lifecycle badge renders on Review cards. |
| F-25c | **Signal hysteresis ("steady vs yesterday").** A Grow-Today pick whose composite moved ≤ `HYSTERESIS_COMPOSITE_DELTA` since its most-recent prior surface AND whose verdict didn't flip is marked with a calm grey "↔ Steady vs yesterday" chip (`signal_hysteresis.apply_hysteresis`, fed by a 4-day look-back over `db.load_recommendations`). Continuity reads as continuity, damping the urge to re-litigate persistent picks daily (§2B). **Annotate-only — never adds, removes, re-orders, or suppresses a pick** (cannot fight the buy gates); skipped under the AM lock and when offline. |
| F-25d | **Exit-discipline deterioration ladder (`exit_advisor.py`).** Idiosyncratic (name-specific) deterioration is graded into WATCH / TRIM / EXIT off **drawdown-from-peak** (peak re-anchored on material adds — Phase 1.1), gated by a broken trend (close < SMA50) and confirmation (2 of last 3 sessions; a deep EXIT skips confirmation). Floors are the larger of a fixed % (6 / 8 / 12) and an ATR-scaled level (capped 14 / 20), so volatility cannot quietly disable the stop; a TRIM escalates to EXIT on an unrealized loss past `DETERIORATION_EXIT_DOLLAR_LOSS` ($250). Renders as a Review item with the lifecycle badge. Motivated by a trade-log loss review. |
| F-25e | **Risk-off market-wide de-risk overlay (exit-discipline Phase 2).** When the **book is fragile** (F-09a) AND the regime is risk-off (SPY below its 200-DMA OR VIX ≥ 25 — Faber trend rule + high-vol cut), surface a single `risk_off_derisk` action that trims the **top-3 beta contributors** (β × weight, only genuinely high-β names with β ≥ 1.2) by a modest 25% (or tighten their stops instead) — it does not touch the whole book or defensives. Computed AFTER the act/review buckets and **single-surfaced**: tickers already flagged by an idiosyncratic exit (F-25d) or other reduce action are excluded so a name is never double-asked. Ranked highest-urgency in the defensive column. |
| F-25f | **Brief tone-staleness reconciliation (annotate-only).** Pre-market, when the tone banner is stale (computed from the last scanner run, e.g. Friday's close — `not is_open AND last_close != today`) AND live pre-market futures materially contradict it (tone bear + futures bull, or tone bull + futures bear; reuses the existing futures bull/bear direction, no new threshold), append an amber reconciliation note to the tone banner, e.g. "Reflects Fri Jun 20 close — live futures currently higher (ES +0.6%); refresh after the open." **Annotate only — pre-market futures NEVER flip the tone, gates, or recommendations.** Resolves a red "Protect Mode" banner silently sitting above green live futures. |
| F-25g | **Action Log — Phase B "Log this trim" (closes the recommend→act→log loop for trims).** Each Review trim card (`TRIM_TO_TARGET` / `TRIM_AND_TIGHTEN` / `PROTECTIVE_TRIM`) carries a one-click "📒 Log this trim" button that pre-fills the Trade Journal SELL form (trim ticker + suggested shares + decision context, `followed_intent="yes"`) so an executed trim is logged without leaving the Brief; once logged, holdings recompute and the recommendation stops re-firing. The macro `PROTECTIVE_TRIM` case correctly targets `action.trim_ticker` (the weakest sector holding; the card's own `ticker` is `None`) — the trim subject is resolved before the button gate. Button keys use a per-render card index so two macro cards trimming the same name can't collide. (Extends F-18's manual-stop loop from stops to trims.) |
| F-25h | **Act Today — "Log this sell" on mechanical, fully-quantified exit cards.** A `kind="stop_breach"` card (`Gap to Stop (%) ≤ 0`) or a `kind="deterioration_exit"` card (`exit_advisor` EXIT tier — a full-position drawdown/trend-break exit) carries a one-click "📒 Log this sell (N sh TICKER)" button alongside "▶ Analyze", pre-filling the Trade Journal SELL form (full held share count + decision context, `followed_intent="yes"`) so the user isn't forced through an Analysis detour to act on a signal the engine already fully quantified. Analyze stays available alongside it (not replaced) in case the user wants to reconsider before executing. Deliberately excludes `sell_signal` and the sibling `deterioration_trim` tier — `exit_advisor` computes a full-exit share count for EXIT but has no equivalent computed quantity for TRIM (its directive is qualitative, "trim into the weakness"), so a button there would need a fabricated share count. `daily_briefing._act_today` (adds `shares` to the item — `None` on TRIM items so the button can't render for them), `app.py _render_act_card`. |
| F-26 | Buy Candidates (offense / left column, under Grow Today; titled "More Buy Candidates"): scanner picks cross-referenced across 5 layers (Technical, Composite, News, Earnings, Revisions) with a confidence verdict (Confirmed / Mixed / Conflicted / Caution / Unverified). Earnings risk is checked against a unified `earnings_lookup` derived from held positions AND pre-fetched composites, so non-held new picks are also screened. |
| F-26a | Buy Candidates must be **de-duplicated against everything already shown in Grow Today** — both the shown lists (new picks, add-to-winners, composite-skipped, macro-blocked, composite-unavailable) AND the suppressed-add buckets (deterioration-blocked, cooldown, sector-blocked, concentration-blocked, risk-blocked) — so the same ticker never appears in both lists on the same screen. The suppressed-add buckets matter because `_buy_candidates` *annotates* (does not suppress) a WATCH name, so without deduping `deterioration_blocked_adds` a WATCH add would leak into Buy Candidates as "ADD — Winning Position" right below its suppression banner (the exact double-surface contradiction). The other four are defensive (they don't leak today only because `_buy_candidates` independently skips them). |
| F-26b | The Brief renders as a two-column **offense / defense** layout: left column = Grow Today + More Buy Candidates (where to deploy capital); right column = Act Today + Review Before Close (what to protect). Section chips must stay within their column and not bleed across the page when the opposite column is empty. |
| F-26c | **Add-to-winner Brief-wide coordination fix (2026-07-30).** Both `_grow_today`'s `add_positions` block and `_buy_candidates`'s own separate `add_winner` block (F-26 — the two are independently implemented, not shared code) previously cross-checked ONLY `act_today` before recommending an add to a held Strong Buy position, via a naive `_ai.get("ticker")` loop. A **review-origin** reduce call (`review_list`'s `TRIM_TO_TARGET` — earnings-overweight or weak-large-position trim) never suppressed either add-to-winner surface, so a same-day "add to X" could sit next to (or precede) a same-week "trim X" with no cross-check — the identical class of gap found in the Orphan Conviction fix (F-194) above, in two more places. Both blocks now use `decision_bucket.all_flagged_tickers(act_today, review_list)` — the canonical broad "any card, either lane" set (also used by the risk-off de-risk exclusion, 2026-07-29 audit H6) — which additionally resolves macro `ticker=None`/`action.trim_ticker` card shapes a naive ticker lookup would miss. |
| F-27 | Each Buy Candidate card shows: ticker, sector, score, verdict badge (amber for Verify, not blue), technical summary, conflicts and agreed signals |
| F-28 | Quick Research panel: user enters any ticker (e.g. from external news), app returns up to 5-bullet actionable summary — signal, momentum, entry timing, key context, and (when portfolio_ctx is supplied) portfolio-fit including sector-level Act Today awareness |
| F-29 | Entry timing verdicts (boundaries inclusive): High Risk — Avoid Chasing (RSI≥80 or 1D≥15% or 5D≥25%), Wait for Pullback (RSI≥68 or 1D≥5% or 5D≥12%), Oversold — Potential Entry (RSI≤35), Normal Entry Conditions otherwise |
| F-30 | All Analyze buttons navigate to Stock Analysis with a Back button returning to Today's Brief |
| F-31 | On flat market days, Grow Today must output confirmed-verdict picks before unverified-verdict picks so highest-conviction ideas lead |
| F-32 | Pre-Market Intel panel: visible 4:00–9:29 AM ET weekdays only; appears at top of Today's Brief |
| F-33 | Pre-Market Intel shows US futures (S&P 500, Nasdaq 100, Dow, Russell 2000) with price and % change vs prior close |
| F-34 | Pre-Market Intel shows overnight % change for major global indices (Nikkei, Hang Seng, DAX, FTSE, CAC 40) |
| F-35 | Pre-Market Intel shows pre-market movers (≥0.5% change) for all held positions and watchlist tickers; held positions are visually distinguished. Each mover is cross-checked against an independent price source (`data.crosscheck_price`); on disagreement beyond tolerance the mover is still shown (never silently filtered) but tagged with an "⚠ unverified" badge, a summary caption counts how many are flagged, and Pre-Market Stance's narrative is instructed not to assert an unverified mover's direction as settled fact. Added 2026-07-30 after an incident where the AI narrative asserted a mover's move in the wrong direction with nothing to catch it. |
| F-36 | Pre-Market Intel shows today's HIGH and MEDIUM impact economic events as "catalysts" |
| F-37 | Pre-market expected open tone (bull/bear/flat) is derived from ES=F futures % change (≥+0.4% bull, ≤-0.4% bear) |
| F-37a | **Catalyst Watch** — its OWN nav page (🔔 Catalyst Watch, before Economic Calendar; viewable any time, not gated to pre/post-market). TWO tiers: (1) **Your Holdings — Earnings** = the full per-position detail + Pre-Earnings Playbook (rendered from the canonical port_df/held_data the Home brief built, via `_render_holdings_earnings`). The held-name earnings DATE comes from the bundle (`held_data["earnings"]`); when that is missing (a yfinance hiccup — the date is a different yfinance endpoint than `.info`, so it can be blank even when fundamentals are present), it is backfilled for DISPLAY ONLY via `_cached_held_earnings_dates`, which uses the SAME two-source path as the Radar tier: the FMP market-wide calendar first, then a per-name yfinance fallback (`fetch_next_earnings`) for names the (sparse, on free tier) FMP calendar didn't cover — so held names get equal earnings coverage to watchlist/universe names. This is awareness-only and never feeds a gate. The underlying bundle date itself is also repaired at the data boundary: `orchestrator.get_bundle` backfills the earnings date from FMP's light per-ticker accessor independently of whether `.info` was sparse, so the earnings-proximity gates (which read the bundle date) stay armed; (2) **On Your Radar — Watchlist & Universe** = upcoming earnings within `CATALYST_WATCH_WINDOW_DAYS` (7) for NON-held tracked names, grouped Today/Tomorrow/Next-7d, each row ticker · sector (🔥 when leading) · date · before-open/after-close · watchlist/universe chip. Both tiers have a ticker→Analysis "Analyze" control. **Awareness only** — must NOT recommend initiating into earnings (proximity gates still suppress that); removes the blind spot of a tracked name reporting unannounced (the PANW case). Radar dates: FMP market-wide calendar (one call) then a per-name yfinance fallback for names FMP didn't cover, so universe coverage doesn't depend on FMP's tier. 24h-cached. **Presented as three tabs since the 2026-07-16 I11 restructure — 📋 Holdings (tier 1) / 📡 Radar (tier 2) / 🎯 Entry Candidates (F-37b); structural only, no logic change.** |
| F-37b | **Entry Candidates (Earnings Catalyst Scanner) — third Catalyst Watch tab.** `earnings_advisor.build_earnings_catalyst_candidates()` surfaces NON-held **watchlist** names reporting earnings within 30 days that clear three filters: historical **beat rate ≥ `EARNINGS_MIN_BEAT_RATE_ENTRY`** (70%), **composite ≥ `COMPOSITE_BUY`** (65), and a post-earnings **reaction that is not bearish** (bullish/mixed). Ranked by beat-rate × composite × reaction. Beat-rate/reaction are sourced from CNBC earnings context pasted via 🧠 AI Insights → Ideas Inbox → 📅 Pre-Earnings (`db.load_earnings_context_batch`); with no context pasted the tab prompts the user there. **Awareness only — never a Buy recommendation** (each candidate carries an Analyze bridge to the Analysis page). Shipped 2026-07-13 (commit 0cac9ee); Earnings Playbook Phase 3. |
| F-38 | When `build_daily_briefing()` fails, the page must render an explicit "offline" state on dependent features (notably the Watchlist) rather than silently disabling coordination gates. The user must see that gates are inactive. (Implements G-14.) |
| F-39 | Grow Today must surface a "🔍 Couldn't score N candidate(s) — data source returned an error" banner when the composite pre-fetch failed for any of the intended top picks, so the user knows the multi-factor gate did not run for those tickers. |
| F-39a | Grow Today must hard-suppress new picks in any sector with a HIGH-impact macro event scheduled within `MACRO_IMMINENT_DAYS` (3 days), and surface a "Picks Suppressed — Imminent HIGH-Impact Macro Event" banner with the affected sectors and event date. Same-day HIGH macro events (e.g. CPI, NFP, GDP — pre-market 08:30 ET releases) lift the gate once the release time has passed (primary: ET wall clock ≥ scheduled `time_et`; secondary: FRED `released` flag). FOMC ("Fed Policy") is never lifted same-day — post-announcement volatility makes it the wrong window to open new positions. (Implements G-07.) |
| F-39b | Grow Today add-to-winner blocks must suppress: (a) tickers Risk Advisor recommends trimming, (b) positions at or above the single-name ceiling (15%), (c) positions drift-overweight beyond equal-weight + tolerance, and (d) held names under an active early-deterioration WATCH. Each suppression class renders a distinct banner with the conflict reason. (Implements G-01, G-04, G-09, G-20.) |
| F-39c | Review Before Close items flagged as weak-large-positions must pull the entry thesis (most recent BUY notes) and up to two recent lessons from the Trade Journal for that ticker, rendered as an amber-bordered block below the mechanical assessment. When no journal entry exists, prompt the user to log thesis on future entries. |
| F-39d | Grow Today must hold a candidate OUT of new picks when its composite was computed without real fundamental data (`fundamentals_available == False`, i.e. < `FUNDAMENTALS_GATE_MIN_METRICS` core metrics present). Such a ticker is routed to the `composite_unavailable` bucket, never surfaced as a "new position to initiate". (Implements G-15.) |
| F-39e | Grow Today must suppress BOTH new picks and add-to-winners whose sector is at/above `SECTOR_CEILING` (35%), surfacing a "Suppressed — Sector Hard Cap" banner. This keeps the deploy-capital signal from contradicting the Risk Advisor's concurrent sector-trim recommendation (the ESTC case). (Implements G-16.) |
| F-39f | Portfolio sector classification resolves **curated `TICKER_SECTORS` → provider `.info` sector → persisted last-known sector (`sector_cache`) → `"Other"` catch-all**, so unmapped tickers carry a real sector and the "Other" bucket cannot accumulate a spurious hard-cap breach. The `sector_cache` layer exists because `.info` is the ONLY live sector source and comes back sparse on flaky provider days — without persistence, unmapped holdings collapsed to "Other" and, since the gate **excludes** "Other", **silently dropped out of sector-concentration gating** (~half the book, observed 2026-07-06). `bundle_loader` write-throughs a good `.info` sector and reads the cache when the live fetch is empty (sector is near-static, so a cached value never staleness-expires); scoring stays on raw `.info` (NOT the cache) so this resilience cannot move composite/fundamental scores. `risk_advisor` and the F-39e gate both read this same `port_df["Sector"]`, so the Act Today breach card and the Grow Today gate agree. |
| F-39g | **Reach-funnel Sankey (decision transparency).** Grow Today shows a Plotly Sankey drawing the **same real numbers as the 🔭 reach line** to scale: **screened universe** (tracked + watchlist + discovery) → **reached full composite scoring** (the finalists) → **New Positions to Initiate**, with the complements as drop nodes (*Not scored this pass* = universe − scored; the scored set splits into picks + *Composite below Buy bar* + *Other filters*). **Honest by construction:** every band is pure subtraction off the reach counts, so it always balances and never fabricates a universe — "Other filters" is the residual (scored − picks − below-bar) covering sector-cap / macro / data / flat-day-caution drops, which the suppression banners name specifically. Display-only; shown when ≥ 1 candidate reached composite scoring. |
| F-39h | **Sector elevated-band warning, not suppression (SHIPPED 2026-07-28 — SHOP whiplash fix).** Grow Today's new-pick and add-to-winner loops, AND the separate "More Buy Candidates" surface (`_buy_candidates`), now compute an `_elevated_sectors` set (weight in `[SECTOR_ELEVATED, SECTOR_CEILING)` = 25–35%) alongside the existing hard-cap `_breached_sectors` (F-39e/G-16). A pick in the elevated (not yet hard-capped) band is NOT suppressed — it carries a `sector_elevated_warning` string, rendered as an amber caption on the pick/add card — extending W-01's soft-caution policy to two surfaces that previously had no elevated-band awareness at all. **`_buy_candidates` also gained the G-16 hard-cap suppression itself in the same pass** — before this, that surface had zero sector-concentration awareness of any kind (not even the hard cap), a wider gap than Grow Today's. Root cause: the buy side only checked whether a sector was ALREADY over the hard cap; it had no signal for a sector Risk Advisor was already watching in the 25–35% band, so a buy recommended just under the cap could tip the sector over it and — same brief, hours later — that exact fresh position became the trim engine's #1 full-exit candidate (paired with G-21 on the trim side). |
| F-180 | **New-position data freshness gate.** Grow Today suppresses a pick from "New Positions to Initiate" when its underlying bundle data is stale, routing it to the `composite_unavailable` bucket → "Pending Verification" banner with a Refresh button. Two checks in `_grow_today()` in `stock_analyzer/daily_briefing.py`, inserted before the existing `fundamentals_available` gate (F-39d): (a) `_comp_data.get("stale_as_of") is not None` — the bundle was served from the Supabase `bundle_cache` fallback (potentially up to `BUNDLE_CACHE_MAX_AGE_DAYS` (5) days old rather than from live providers); (b) `fund_cache_age_days > GROW_TODAY_MAX_FUND_AGE_DAYS` (2 calendar days) — fundamentals older than the allowed floor for a new-position recommendation. New constant `GROW_TODAY_MAX_FUND_AGE_DAYS = 2` added to `stock_analyzer/constants.py` after `BUNDLE_CACHE_MAX_AGE_DAYS`. `GROW_TODAY_MAX_FUND_AGE_DAYS` imported in `daily_briefing.py`. **Rationale:** new-position recommendations carry higher trust expectations than held-position display — a stale bundle cache can flip a Sell-composite ticker to a Buy score, producing a false recommendation (INTC incident, 2026-07-14: bundle fallback produced composite ≥ 65 / Buy; fresh data showed composite 32.1 / Sell 30 min later). The 2-day floor is more conservative than `FUNDAMENTALS_CACHE_MAX_AGE_DAYS` (7), which governs held-position display only. `_SYNTH_SCHEMA_VER` bumped 1 → 2 to flush warm session caches. |
| F-181 | **Composite freshness auto-refresh on Home cache-HIT path.** When `_home_synth_cache` is a HIT (synthesis signature unchanged), the app now selectively refreshes stale Grow Today composite bundles before rebuilding the Brief. For each ticker in `_grow_composites`, if the bundle's `fetched_at` timestamp is older than 1 800 s (the `@st.cache_data` TTL of `load_all()`), `load_all()` is re-called — hitting the Streamlit data cache at no API cost while warm, re-fetching fresh provider data when stale. If any ticker was refreshed, `build_daily_briefing()` is re-run with the updated composites so the Grow Today card picks and Analysis page always show the same composite score. Falls back to the snapshot bundle if `load_all()` fails transiently (no pick disappears). Skips the rebuild when Setup is Locked (mirrors the auto-refresh fragment behaviour). `_grow_today_sectors_cache` is updated to reflect the rebuilt brief. The snapshot is updated in-place so the next HIT reads the refreshed `fetched_at` values. `_SYNTH_SCHEMA_VER` bumped 2 → 3 to flush warm caches that predate the new `composite_fetched_at` pick-dict field. **Root cause closed:** prior to this fix, the two-TTL mismatch between the synthesis cache (no sub-day TTL) and `load_all()` (30-min TTL) meant Grow Today could show a stale composite (e.g. 67/100 Buy) while Analysis showed fresh data (56.2/100 Hold) — a financial risk for a decision-making app. Implementation: `app.py`, injected at the end of the `_home_synth_cache` HIT block (~line 3 195). |
| F-183 | **🎯 My Edge — Benchmark Mirror (SHIPPED 2026-07-16; commit `aac34b2`).** New standalone page "🎯 My Edge" in the PORTFOLIO nav group. New module `stock_analyzer/benchmark_mirror.py`; **awareness only — zero gates, zero recommendations.** Answers: "Am I beating passive investing?" by computing a **shadow portfolio** — the same cash flows (baseline + deposits from `account_flows`) invested in SPY or QQQ at each flow date instead of individual stocks. **Modified Dietz money-weighted return** applied to both the real portfolio and the shadow portfolio so the comparison is fair regardless of when deposits were made (same formula as `account.money_weighted_return`). KPI strip shows annualised return for both (falls back to period return with "(total, <30d)" label when the period is < 30 days, the floor for reliable annualisation). Three chart views on a segmented control: (1) **Cumulative Growth** — both portfolio and shadow indexed to 100 at baseline, revealing compounding divergence over time; (2) **Cash Flow Attribution** — per-deposit gain/loss in the shadow portfolio (how well each investment tranche would have done in the benchmark); (3) **Benchmark Drawdown** — how far SPY/QQQ fell from its own peak at each date, for passive-investing context. Portfolio drawdown deliberately excluded: `daily_snapshots` tracks open equity only (shares × close price), so selling positions makes equity appear to drop even though cash was received — the figure would mislead. Requires 💰 Account baseline to be set; renders an info gate otherwise. Benchmark selector (SPY recommended / QQQ) at the top of the page, shared across all three tabs. **Risk-adjusted alpha added 2026-07-17 (commit `7148890`; next-evolution-strategy.md Phase 2):** 5th KPI card "Beta-Adj. Alpha" = `benchmark_mirror.beta_adjusted_alpha()` — Your Return minus (portfolio beta × benchmark return), isolating stock-selection skill from beta exposure (a portfolio running high beta in a rally can show strong raw alpha and near-zero beta-adjusted alpha). Portfolio beta read from the existing `_port_risk_cache` session-state cache (published by Home); shows "—" with an explanatory tooltip if that cache isn't populated yet (Home not visited this session). Pure arithmetic, no new constants, no gate/scoring touch. **Ending-value basis fix (2026-07-28):** "Your Return"'s ending value is **Total Account Value** (invested equity + net cash, matching the 💰 Account page's own `_total_value = _equity + _cash` and the baseline-setting form's "current total account value" default) — computed the same way (`db.load_account_cash()` + `_last_port_df["Market Value"].sum()`), not the bare `_portfolio_value` cache (equity only, ignores a margin debit). The prior equity-only basis compared a total-account-value baseline against an equity-only ending value, inflating the Modified Dietz gain by the full margin balance and exploding further once annualized (observed: +138,927% on an account carrying an $8,271 margin debit). Falls back to equity-only when cash isn't set this session, same as the Account page. A caption states which basis is in effect. **Annualization caveat (2026-07-28, same commit as the fix above):** shares `account.annualization_caveat()` with the Account page (F-03c) — when "Your Return" annualizes over a short window (< 90 days) and/or the account is levered, an explanatory caption clarifies that the figure is amplified, without changing the number itself. |
| F-184 | **🎯 My Edge — Workflow ROI (SHIPPED 2026-07-16; commit `aac34b2`).** Second tab on the My Edge page. New module `stock_analyzer/decision_quality.py`, function `classify_all_buys()` / `build_workflow_roi()`. Answers: "Does in-app prep lead to better trades?" Classifies every BUY trade into a **prep tier** based on signals saved before the trade date: **Full Prep** (thesis + analyst notes + earnings context all present within their look-back windows), **Thorough** (thesis + one of analyst/earnings), **Basic** (thesis only, `WORKFLOW_MIN_THESIS_LENGTH` ≥ 10 chars), **Cold Entry** (none). Look-back windows: analyst `WORKFLOW_ANALYST_LOOKBACK_DAYS` = 90 days before-only; earnings `WORKFLOW_EARNINGS_WINDOW_DAYS` = 30 days before-only (forward-looking data excluded to prevent leakage). Outcome matching uses FIFO lot replay from `tax_advisor._build_open_lots` to correctly attribute sell proceeds to the right lot. Coverage strip at top: total BUY trades / since prep tracking started / with thesis / with analyst / with earnings context. Warning banner if most trades predate prep tracking (they land in Cold Entry by definition, not because prep wasn't done). Three chart views: (1) **Tier Comparison** — bar chart of avg P&L % / alpha vs SPY / hold days by tier (zero-trade tiers show "No data yet" greyed card and are excluded from the bar chart); (2) **Trade Scatter** — one dot per closed trade, hold days × metric, color = prep tier, with a tier definition key below explaining each color; (3) **Coverage Funnel** — Plotly funnel showing how many trades had thesis / analyst research / earnings context / all three. No new gates. |
| F-185 | **🎯 My Edge — Decision Quality Timeline (SHIPPED 2026-07-16; commit `aac34b2`).** Third tab on the My Edge page. `decision_quality.build_monthly_grades()` / `build_quarterly_grades()`. Answers: "Am I improving month over month?" Each calendar period earns a composite score (0–100) and an **A–F grade**: win rate sub-score + profit factor sub-score + alpha vs SPY sub-score (when benchmark loaded, scaled by `DECISION_QUALITY_ALPHA_SCALE` = 5.0), with an overtrading penalty applied when trade pace ≥ 1.5× the 12-month average. Minimum `DECISION_QUALITY_MIN_TRADES` = 2 closed trades required to grade a period; fewer shows n/a. Grade bands: A ≥ `DECISION_QUALITY_GRADE_A` (80) / B ≥ 65 / C ≥ 50 / D ≥ 35 / F < 35 — same boundaries as Portfolio Health. KPI strip (custom HTML cards): Best Period / Latest Period grade + win rate + PF / Trend (3-period rolling avg direction). Granularity toggle (Monthly / Quarterly). Metric selector (Composite Grade / Win Rate / Profit Factor / Alpha vs SPY). Three chart views: (1) **Timeline** — bar chart with grade-band background zones and 3-period rolling avg line, lower panel shows winner/loser trade counts per period; (2) **Heatmap** — metrics × periods grid (Win Rate / Profit Factor / Alpha vs SPY / Overtrading ×) with RdYlGn scale and a definition-key strip below explaining each row; (3) **Cohort Compare** — pick any two periods, side-by-side horizontal bars with delta annotations. Awareness only; never gates or alters any recommendation. |
| F-182 | **Portfolio Construction Health Score page (SHIPPED 2026-07-15; Portfolio Dynamics enhanced 2026-07-16).** New standalone page, nav label **"🏆 Health"** (page title "Health Score"), in the PORTFOLIO nav group between 🥧 Portfolio Overview and 🎯 My Edge — position/label corrected 2026-07-27; originally documented as "🏆 Portfolio Health" between Portfolio Allocation and Risk Analysis, which had drifted from the actual nav (My Edge shipped later and now sits between Health and Risk Analysis). Pure-logic module `stock_analyzer/portfolio_health.py`; **awareness only — never gates or alters any recommendation.** Two public functions: `compute_health_score()` and `compute_portfolio_dynamics()`. **Five construction sub-scores (0–100 each), averaged to an overall A–F grade:** (1) Concentration — three-zone model: score is 100 below the elevated threshold (10% single-name / `SECTOR_ELEVATED` (25%) sector), decays linearly to 0 at the hard caps (`SINGLE_NAME_CEILING` (15%) / `SECTOR_CEILING` (35%)); binding = min(name, sector); (2) Sector Balance — Shannon entropy of sector weights, normalised 0–1, ×100, capped at 55 for n=2 sectors; (3) Diversification — `(1 − avg_corr) × 100` on the 0–1 equity correlation range (reads `_avg_corr_cache`); (4) Beta / Fragility — base score by fragility severity (calm 85 / caution 55 / fragile 20) minus high-beta-share penalty (≥60% → −25, ≥40% → −12); (5) Signal Integrity — % of book weight at composite ≥ `COMPOSITE_BUY` (65). Grade bands: A ≥ 80 / B ≥ 65 / C ≥ 50 / D ≥ 35 / F < 35. **Portfolio Dynamics section** (below the 5 dimension cards): interactive scatter (X = months held, Y = P&L% or annualized return, size = weight, color = verdict/sector/age cohort — user-toggled via two dropdowns); tenure cohort bar (Fresh <30d / Growing 1–6mo / Established >6mo, avg P&L% + count); engine alignment donut (BUY/HOLD/WATCH/EXIT split, Vitality % in centre); **Sleeping Capital / Working Hardest return efficiency panels** (see below). `compute_portfolio_dynamics()` reads only session state (`_port_df_enriched` + `trades_df`) — zero extra API calls. Verdict tiers: BUY ≥ `COMPOSITE_BUY` (65), HOLD ≥ `COMPOSITE_HOLD` (44), WATCH ≥ `COMPOSITE_SELL` (30), EXIT < 30. No new constants added. Session state consumed (read-only): `_port_df_enriched`, `_div_score_cache`, `_avg_corr_cache`, `_highbeta_share`, `_fragility_cache`, `_port_risk_cache`, `trades_df`. Commits: `042f4ba` → `1696471`. **2026-07-16 Portfolio Dynamics enhancements (commits `9e6ef20` → `7010d2a`):** (1) **FIFO-aware tenure** — `months_held` now uses `_build_open_lots()` from `tax_advisor.py` (replays BUY/SELL/SPLIT in FIFO order) instead of the earlier `dates.min()` across all historical BUYs; a fully-closed-and-re-entered position correctly resets the clock to the new buy date. `days_held` (exact open-lot days) added to each position dict. (2) **Period toggle** — `st.segmented_control` (key `ph_return_period`) with options Weekly / Monthly / Yearly (default Yearly) above the Sleeping Capital / Working Hardest panels. Each selection re-normalises P&L to that cadence (Weekly = `pnl × 7 / days_held`; Monthly = `pnl × 30.44 / days_held`; Yearly = `pnl × 12 / months_held`) and re-sorts both panels by the resulting rate. (3) **Loss-always-visible gate** — positions with negative P&L bypass the 15-day minimum-hold check and always surface in Sleeping Capital regardless of how recently they were entered; the 15-day gate applies only to profitable positions to avoid inflating short-hold gains by annualisation. (4) **Panel UX** — each panel header shows "N of M holdings" and the ranking criterion; negative period rates render in red (positive in green); positions held under 1 month display "< 1 mo" instead of "0 mo"; a footer caption explains the gate logic. Panel split: `ceil(n/2)` positions in Sleeping Capital, remaining (capped at 3) in Working Hardest — no ticker appears in both, no ticker is excluded to a gap. |
| F-178 | **Predictive Analytics — 5-tab personal edge map (SHIPPED 2026-07-13; Tab 5 added 2026-07-14; moved to RESEARCH nav group 2026-07-13).** New standalone page "📊 Predictive Analytics" in the RESEARCH nav group (after 🌐 Macro) — it is a backward-looking calibration tool, not a portfolio-action surface; moved from its original PORTFOLIO placement by the 2026-07-13 UX review. Pure-logic module `stock_analyzer/predictive_analytics.py`; **awareness only — never gates or alters any recommendation.** Two constants: `PREDICTIVE_MIN_BAND_N = 5`, `PREDICTIVE_SCORE_BAND_SIZE = 5`. **Shared data pipeline (outside tabs, cached in `_pac_enriched` session key):** loads full all-time `recommendations` (no date filter), matches to trades via `match_recs_to_trades`, computes SPY-adjusted alpha via `compute_outcomes` (min_days = `REC_SCORE_MIN_DAYS`; 1y SPY history). Page-level gate: < `PREDICTIVE_MIN_BAND_N × 2` (10) graded outcomes → info banner, `st.stop()`. **"📋 What This Means For You" synthesis panel** (above tabs): `synthesize_directives(bands, thresh, avm, conv, rtype, sec_alph, n_graded, sentiment_alignment=None)` emits 2–5 rule-based directives ranked action → caution → watch → context. Each directive links to its source tab. Rules cover: confirmed alpha threshold / all-negative-in-bull-tape / partial signal (Score Calibration); discretion adding/destroying/neutral alpha (Decision Quality); strongest/weakest sector / no-sector-positive watch (Sector Alpha); rec-type or conviction outperformance gap ≥ threshold (Signal Breakdown); Confirmed-vs-others edge ≥ 2pp → action directive (Sentiment Alignment); always-on confidence footnote with graded-outcome count and thin-band count. **Tab 1 — 🎯 Score Calibration:** `calibration_by_score_band` (5-point intervals; per band: n, P(alpha>0), avg_alpha, avg_alpha_acted/missed); `personal_alpha_threshold` (lowest floor where every band above has P(alpha>0) ≥ 0.5 AND n ≥ min_n); calibration bar chart (green/red/grey; threshold dotted line; n label); raw dataset expander. **Tab 2 — ⚖️ Decision Quality:** `acted_vs_missed_comparison` — side-by-side acted/missed metrics (n, avg_alpha, hit rate); verdict callout (action/caution/neutral/insufficient); grouped bar chart of acted vs missed alpha by score band. **Tab 3 — 🏷️ Signal Breakdown:** `by_conviction` and `by_rec_type_stats` — avg_alpha + hit rate by conviction tier (BUY/Strong BUY) and rec type (new_pick/add_winner/buy_candidate); bar charts + metric tiles; cells with n < 3 omitted. **Tab 4 — 🌐 Sector Alpha:** `by_sector_alpha` — ranked bar chart of avg_alpha by sector; best/worst sector callouts; sector × broad-band (< 65 / 65–74 / 75+) heatmap from `calibration_by_sector`; cells with n < 3 omitted. **Tab 5 — 🧭 Sentiment Alignment (F-179):** see F-179. **Alpha Attribution (collapsed expander below tabs):** placeholder; rendered as `st.expander("📊 Alpha Attribution — activates after 180 days of snapshots", expanded=False)` below the 5 live tabs so the feature is discoverable without occupying a tab slot that currently delivers nothing; `daily_snapshots` coverage progress bar toward 180-day activation threshold (factor attribution by sector / hold-duration / score-tier / market-regime). **UX update (2026-07-15; commits `421b0fe`, `97852d4`):** Three metric cards renamed to plain English ("Recommendations tracked" / "Outcomes available" / "Used in this analysis"). Data context strip added below metrics showing scored-outcome date range, count of recommendations excluded due to the 5-day results window, and a session-cache note. Raw dataset expander renamed "Full outcome history" with an inline caption explaining what each row represents and why recent entries are absent; table now sorted date-descending. Four user-facing directive strings in `synthesize_directives()` updated from "recs" shorthand to "signals" / "recommendations". **Panel presentation regrouped (2026-07-28, mockup-approved "Option 3 Variant" — render-layer only, `synthesize_directives()` itself unchanged):** Action-type directives render as side-by-side cards (1 full-width / 2 columns / 3-column grid wrapping past 3, count-based); Caution/Watch/non-footnote Context directives collapse into a single "📎 Also worth noting (N)" expander, closed by default; the always-on confidence footnote stays a plain caption below everything, unchanged. |
| F-179 | **Sentiment Alignment tab + Tier 2 data foundation (SHIPPED 2026-07-14).** Two-part change: **Tab 5 (Tier 1)** — new "🧭 Sentiment Alignment" tab on 📊 Predictive Analytics. Uses `calibration_by_verdict(enriched)` to group graded outcomes by cross-check verdict (`Confirmed`/`Conflicted`/`Caution`/`Unverified`/`Mixed`) and `sentiment_alignment_summary(by_verdict)` for a binary Confirmed-vs-others comparison. Renders: Confirmed-wins/no-edge/insufficient-data callout; side-by-side Confirmed vs non-Confirmed metric tiles (count / avg alpha / hit rate); horizontal bar chart of avg alpha by verdict (green/red/grey); per-verdict detail table in expander. `synthesize_directives` gains a `sentiment_alignment` kwarg — emits an `action` directive when Confirmed edge ≥ 2pp, a `watch` note when edge ≈ 0. **Awareness only; never gates.** The tab will show "insufficient data" until both Confirmed and non-Confirmed groups each have ≥ `PREDICTIVE_MIN_BAND_N` (5) graded recs. **Tier 2 (data collection foundation):** New `sentiment_history` table (`ticker`, `snap_date`, `vader_compound`, `vader_score`, `headline_count`, `bullish_pct`, `bearish_pct`, `buzz_score`, `company_score`, `vs_sector_pp`, `source`, `created_at`; PK `(ticker, snap_date)`) — **requires one-time Supabase DDL before writes activate**. New columns `s_score FLOAT` and `avg_sent FLOAT` on `recommendations` — **requires one-time `ALTER TABLE` DDL**. `db.save_sentiment_snapshot(snap_date, rows)` (mirrors `save_daily_snapshot` pattern; requires at least one of `vader_compound`/`bullish_pct` per row). `db.save_recommendations` updated to include `s_score`/`avg_sent` in each row payload (backward-compatible NULL for old rows). Cron `_run_eod()` writes a sentiment snapshot for all held tickers immediately after `save_daily_snapshot` — VADER from `held_data` bundle + Finnhub `fetch_news_sentiment` per ticker (Pipeline B, +1 API call per held ticker per day). `compute_eod()` return dict gains `held_data` so the cron runner can access per-ticker bundles without re-running `_build_context`. The `recommendations` upsert in `app.py` is enriched with `s_score`/`avg_sent` via helper functions `_s_score_for` / `_avg_sent_for` that probe `_grow_composites` and `_last_held_data` session caches in order. |
| F-177 | **Today's Brief auto-refresh timer (SHIPPED 2026-07-13).** The "📌 Built at HH:MM ET" freshness chip's "auto-refreshes in N min / stale — click Refresh Signals to update" countdown is backed by a real mechanism, not just a label: a 60-second `st.fragment` watchdog (`app.py`) checks whether the Brief is older than `BRIEF_AUTO_REFRESH_MINUTES` (30, `constants.py`); once stale, AND the market is in premarket or regular trading hours, AND "Today's Setup" isn't Locked, AND the shared heavy-refresh cooldown isn't currently armed, it bumps a dedicated `_brief_auto_refresh_nonce` (part of the Home-page synthesis memoization signature, alongside the existing manual Refresh/Lock/Unlock nonce) and forces a full-page rerun so the Brief rebuilds. **Deliberately narrow scope to protect metered API quotas (FMP free tier, Finnhub, yfinance):** the auto-tick never calls `st.cache_data.clear()` and never re-runs the market-wide scanner — it only re-reads whatever is already sitting in the existing 30-minute `@st.cache_data` provider cache, so the incremental provider-call cost is bounded to the pre-existing cache cadence, not multiplied by the timer. The scanned candidate universe (Grow Today's discovery pool) still only refreshes via a manual "Refresh Signals" click or the ~9:45 ET cron scan — unchanged. |

### 3.3 Stock Analysis

| ID | Requirement |
|----|-------------|
| F-40 | Analyse any ticker (not limited to held positions) |
| F-41 | Summary scorecard table: price, **Composite Score**, signal, position/entry zone, stop, base target, R:R, shares, P&L/cost, earnings. When a single ticker is analyzed, a compact verdict banner (label + composite score + rationale) renders **above** the scorecard for immediate readability; the full formula breakdown remains in the Detailed Analysis tabs below. |
| F-42 | Scorecard adapts to context: held tickers show actual shares and P&L%; non-held show entry zone and suggested entry cost |
| F-43 | Signal banner shows composite score breakdown: **Technical × 25% + Business Quality × 35% + Valuation × 30% + Sentiment × 10%**. Pillar names are spelled out in full in the formula display (not abbreviated) — Business Quality carries 35% weight and must not be misread. |
| F-43a | When fundamentals are unavailable from all sources (`fundamentals_available == False`), the Analysis page must WITHHOLD the Buy/Hold verdict — suppressing the signal banner and composite number — and render a red "Verdict withheld — fundamentals unavailable" note. The note must explain the data gap and the possible Brief↔Analysis mismatch (a ticker that appeared as a Brief new-pick when data was present), and advise re-checking / broker verification. The verdict must not be computed on the fabricated neutral-50 fundamental. (Implements G-15.) |
| F-43b | When the Analysis verdict is Buy/Strong Buy but the entry R:R is below `RR_ENTRY_MIN` (2.0), the Trade Plan must surface a caveat distinguishing stock quality (composite) from entry timing (R:R): per-share risk/reward, a suggested pullback level, and "KEEP, not an add here — user's judgment." The verdict must NOT be downgraded (Analysis is a research/judgement surface, not a hard gate; the hard R:R block is G-13 on Watchlist ENTER_NOW). |
| F-44 | Analyst upside note shown only when it reinforces signal direction (upside on Buy; suppressed on Sell and when downside on Buy) |
| F-45 | Trade Plan tab (Buy/Strong Buy): entry zone, stop loss, R:R, position sizing (shares, cost, max risk) |
| F-45a | **Held-position protect-overrides-deploy reconciliation (Trade Plan).** On the Buy/Strong-Buy Trade Plan, add-on Position Sizing is suppressed and a "not a place to add" banner shown when the held position is either (a) **stop-breached** or (b) under an active **Reduce/Exit call on the Daily Brief** (deterioration ladder / sell signal / risk-off trim / review trim). The stop-breach detector **mirrors the Brief exactly**: it compares the **live-merged price** (the same `_live_prices` snapshot the Home synthesis fed `build_portfolio_df`, merged into the Analysis `results` before the gates) against the **ratcheted/manual protective stop** `_sa_holding["Stop"]` (NOT the raw ATR `r["stop"]`), using the Brief's own `round(Gap%, 1) ≤ 0` test (precision single-sourced as `GAP_TO_STOP_ROUND_DECIMALS`) — so a ratchet-wins breach and a manual stop in the `[ATR, floor)` gap are treated identically on both surfaces. When the position has **no computable stop** (Stop Type "Stop Unavailable" → Gap-to-Stop `None`), the breach gate is **skipped**, exactly as the Brief skips it (`gap is None → continue`), so no breach banner fires on a name the Brief is deliberately silent about (tested via `pd.isna` so it holds whether the missing gap is `None` or the float-column `NaN` it coerces to). The manual-override acceptance itself is unified with `build_portfolio_df` through the shared `portfolio.manual_stop_wins` predicate (a manual wins only when ≥ the ratcheted `protective_stop` = `max(ATR, floor)`; avg-cost read from the same frozen `_port_df_enriched` frame the Brief used). Add sizing & R:R still compute off `r["stop"]` (ATR fresh-entry) by design. The composite Buy score is NOT changed — it rates the STOCK (and rewards the oversold setup); the exit discipline protects the POSITION. The Brief's reduce state is consumed via `decision_bucket.reduce_call_items()` published to `_reduce_calls` (same `_is_reduce`/`_ticker` canon as F-75), so the Analysis and Brief surfaces can't contradict (the "Buy 68.7 · add 18 shares" vs "REDUCE — Deterioration Exit" case). Stop-breach takes precedence; degrades to showing sizing if the Brief hasn't built this session. |
| F-46 | Exit Plan tab (Sell/Strong Sell): current price, shares held, position value, P&L if sold now; exit recommendation banner; ATR downside level |
| F-47 | Position Monitor tab (Hold): stop loss, shares held, P&L; guidance to maintain with stop; specific 7-day re-check date and two concrete action triggers (add if score ≥ `COMPOSITE_BUY` (65); exit if price closes below stop). For a **held** position the displayed stop (metric, narrative, scenarios line) is the **ratcheted / manual stop the engine actually acts on** (`_sa_holding["Stop"]` = `max(ATR stop, profit-ratchet floor)`, or a tighter manual override) — NOT the raw ATR entry stop `r["stop"]`, which under-reports the protective stop once a profit-ratchet tier engages. This keeps the Analysis metric, the Brief, and the F-47a explainer in agreement. Non-held Hold uses `r["stop"]` (a fresh ATR entry stop is correct). |
| F-47a | **Stop explainer + what-if simulator (Hold monitor).** An opt-in expander ("How your stop is set — and what happens next") on the held-position Hold tab. Read-only / educational — it never changes a gate, threshold, or recommendation. (a) A **stop-ladder chart** places entry, the profit-ratchet floor, the ATR stop, the active stop, an optional tighter level and current price on one price line. The stop actually **in force** is a large ✓ marker; the losing candidate stop is **dimmed** and a "tightest of these wins → your stop" arrow points to the winner (so the "tier label vs. binding number" case is legible at a glance); **red/green zones** mark stop-out vs holding territory; a colour-coded **role legend** + hover text name what each level is. (a2) A **profit-lock staircase** companion chart (from `stop_ladder`'s `ratchet_rungs`): a stepped floor-vs-price line showing how the locked floor climbs through each ratchet trigger, with reached/ahead/current-tier markers, a "you are here" line at price, and a dotted line at the actual active stop (which usually binds early because it's the higher of the floor and the ATR stop). Suppressed under a Brief Reduce/Exit call. (b) A **plain-English walk-through** of the three layers (volatility ATR floor → profit ratchet → manual override) with the position's live numbers, explicitly resolving the "tier label vs. binding number" case (e.g. AAPL labelled "Breakeven guard" while the ATR stop is the number in force). (c) A **what-if price slider**: downside tests the hypothetical price against the stop held **today** and reports realized P&L at that stop; upside recomputes the stop the app **would recommend raising to** at the higher price. Faithful by construction — all numbers come from `portfolio.stop_ladder`, which reuses `protective_stop` + the shared `ATR_STOP_MULT` formula (the current-state active stop is pinned to the engine's `r["stop"]` via `atr_stop_override`, so it is bit-exact with the metric/Brief). The ratchet is framed as a **per-run recommendation the user places** (not an automatic non-retreating guarantee, since `protective_stop` is stateless). When the position is under a Brief Reduce/Exit call (`_reduce_calls`), the "keep-climbing" nudges (tighten-option, next-ratchet) are suppressed so the explainer never softens an active exit directive. The same explainer also renders on the **Buy/Strong-Buy tab of a position you already hold** (`buy_add=True`, when not stop-breached and not under a reduce call): there it opens with a note distinguishing the **sizing stop** (`r["stop"]` = the ATR fresh-entry stop, or a manual override — what the add's shares & R:R are sized off, deliberately, so new capital is risked at today's volatility rather than off the tighter ratchet the held shares earned; sizing off the ratchet would over-buy into strength) from the **protective stop** (the ratcheted active level), plus a nudge that adding raises blended cost and can drop the ratchet tier. **Add-sizing and R:R are unchanged — this is display-only** (the policy question "should add-sizing use the ratcheted stop?" was decided NO: the ATR fresh-entry basis is the disciplined choice). |
| F-162 | **Analysis page — "📈 What would change this signal?" expander** (collapsed, below verdict banner). Shows upgrade path: composite pts gap to next verdict tier + top-2 easiest pillar levers with current→needed score. Shows downgrade path: pts buffer above current verdict floor + most-weight pillar named as highest-deterioration risk. Display-only; reads existing pillar scores and verdict thresholds. No new data dependency. |
| F-163 | **Analysis page — Gate checklist.** 5 Pass/Fail chips in Trade Plan tab (Buy/Strong Buy branch only): Data Quality (BQ metrics available), R/R ≥ 2.0×, Concentration < `SINGLE_NAME_CEILING` (15%), Sector < `SECTOR_CEILING` (35%), Earnings > `EARNINGS_IMMINENT_DAYS` (7d) clear. Shows "—" with page pointer when account/catalyst data not loaded. Makes implicit entry gates visible without changing gate logic. |
| F-48 | Price scenarios chart (Bull / Base / Bear) in all signal modes; sell mode shows position $ impact per scenario |
| F-49 | Chart tab: candlestick with Bollinger Bands, SMA 20/50, RSI panel, optional volume |
| F-50 | Risk tab: Sharpe, Sortino, max drawdown, beta, volatility vs SPY |
| F-51 | Deep Dive tab: fundamental signals, analyst revisions, earnings proximity, news sentiment |
| F-52 | Analysis Summary expander: formatted markdown summary for all analysed tickers |
| F-53 | Source links: Yahoo Finance, Finviz, SEC filings, Yahoo News |

### 3.3a Compare (⚖️ Compare page)

**Added to spec 2026-07-27 — a doc gap, not a new build; the page has existed since before shipped-log.md tracking began.** `app.py:17625-17833`, `elif page == "⚖️ Compare":`, backed by `stock_analyzer/comparison.py` (`build_comparison`, `_compute_verdict`, `_portfolio_fit`).

| ID | Requirement |
|----|-------------|
| F-208 | **Two-ticker side-by-side comparison.** Exactly two tickers via two text inputs, triggered by a Compare button; identical tickers rejected with a warning. Quick-pick buttons auto-fill from "Watchlist top 2" and/or "Portfolio top 2 by weight"; defaults pre-fill from the last comparison run or the first two watchlist tickers. Data via `load_all()` (30-min `@st.cache_data` freshness, not reused from another page's session state). No charts — custom HTML tables per section (Headline, Overview, Technicals, Business Quality, Valuation, Sentiment & Analyst, Risk, Setup), with the winning cell in each row shaded green. Result cached in `st.session_state["_cmp_result_cache"]` keyed by `(ticker_a, ticker_b)` so it survives reruns from the page's own "Deep dive" buttons (→ 📈 Analysis) without recomputing. |
| F-209 | **Verdict never fabricates a pick.** `_compute_verdict()` returns `"tie"` with `confidence: "low"` when the composite-score gap between the two tickers is under 3 points — a close call is shown as a close call, not forced to a winner. |
| F-210 | **Portfolio-fit awareness (not a gate here).** `_portfolio_fit()` checks each ticker's sector weight against `SECTOR_ELEVATED`/`SECTOR_CEILING` (same basis as the hard sector-cap gate, G-19) and flags "🚫 hard ceiling breached" or "⚠️ elevated" alongside the comparison — informational on this page; the actual gate still lives where a real BUY would be gated (Grow Today / Watchlist ENTER_NOW). |

### 3.4 Market Scanner

| ID | Requirement |
|----|-------------|
| F-60 | Scan a curated universe of ~73 tickers across 12 sectors for technical momentum, **extended at runtime with the user's Watchlist tickers** (tagged sector="Watchlist") so watched names are scored alongside the curated set |
| F-60a | **Movers discovery (close the discovery gap):** a separate `scan_movers()` pass scans the broad ~200-name `discovery_universe` for today's 1-day gainers ≥ `MOVER_MIN_DAY_GAIN_PCT` (5%). The top `MOVER_SHORTLIST_SIZE` (12) gainers are composite-gated; those clearing `COMPOSITE_BUY` (65) feed the unified New Positions list (see F-24a). The composite gate is the noise filter — a 1-day pop without fundamentals/sentiment behind it is rejected. Cached 30 min via `_cached_scan_movers`. **Two distinct sources:** (1) the candidate *list* is a **static, hardcoded curated set** (`DISCOVERY_UNIVERSE` in `discovery_universe.py`, ~200 liquid large/mid-caps grouped by sector, deduped against curated/held/watchlist at runtime) — NOT scraped from an index provider and NOT pulled from a screener API; it is treated as DATA and refreshed manually (~quarterly). (2) the price *data* used to compute each name's 1-day change comes from **yfinance** (a single batched `yf.download(..., period="3mo")`) — the same market-data source as the rest of the app; there is no paid/third-party movers feed. Consequently the only discovery limits are (a) a breakout must be one of the ~200 listed names, and (b) yfinance must return its data. Widening the net (full S&P 500 / live screener API) is a documented future-expansion path, not current behaviour. |
| F-61 | Score each ticker 0–100 using RSI, trend alignment (SMA 20/50), and price momentum |
| F-62 | Surface top picks ranked by score; display sector, signal, RSI, trend, momentum |
| F-63 | Scanner results persist in session state and feed the Daily Briefing buy candidates list |

### 3.5 News Intelligence

| ID | Requirement |
|----|-------------|
| F-70 | Aggregate news headlines for all held positions from Yahoo Finance |
| F-71 | Score each headline using VADER sentiment (Positive / Neutral / Negative) |
| F-72 | Flag tickers with predominantly negative news as "Requires Attention". A minimum of `NEWS_CRITICAL_MIN_HEADLINES` (2) qualifying headlines per held ticker is required before the card fires (qualifying = VADER compound ≤ `NEWS_SENTIMENT_CRITICAL`, tier ≤ 2), preventing a single borderline headline from triggering a stop-tighten directive. VADER compound values are pre-processed by a suppress-only LLM rescorer (`rescore_news_items_llm`, Claude Haiku, 8s timeout, VADER fallback) that can only raise a score — removing false-positive negatives — never lower one, so the LLM cannot create new Act Today cards or flip buy-candidate verdicts. (`stock_analyzer/news_intelligence.py`, `stock_analyzer/daily_briefing.py`, commit `6c055ec`, 2026-07-13) |
| F-73 | Provide Analyze button navigating to Stock Analysis for flagged tickers |
| F-75 | **Opportunity Signals ↔ Brief reduce-call reconciliation.** The Overview's "Opportunity Signals" (positive news on held names, composite Score ≥ `NEWS_OPPORTUNITY_SCORE_MIN` (55), VADER compound ≥ `NEWS_OPPORTUNITY_COMPOUND_MIN` (0.10)) never frames a position as "may support adding on a pullback" while the Daily Brief carries an active **Reduce/Exit call** on the same ticker — the protect-capital directive leads the composite (the deterioration ladder fires before the composite reacts), so an add prompt there would contradict the Brief (the PLTR deterioration-exit case). It reads the Brief-published `_reduce_calls` set (built once by `decision_bucket.reduce_call_items` — the SAME `_is_reduce`/`_ticker` canon the Act-bucket reconciler uses: act-origin stop / sell / risk / deterioration / risk-off reduces AND review-origin trim variants, across both the Act and Awareness buckets since deterioration cards bucket to Awareness), the same published cache the Analysis Trade Plan consumes (F-45a) — so all three surfaces read one definition and can't drift. Suppressed names are NOT silently dropped: an amber "⚠️ NOT SHOWN AS ADDS" note lists them with the reason, and the full headline still appears under "All News for Your Holdings." Display-only — no gate/score/verdict change. |
| F-74 | **News sentiment awareness (Finnhub).** Beyond VADER headline scoring (F-71), surface Finnhub's pre-computed `/stock/news-sentiment` read (`stock_analyzer/news_sentiment.py`) for held positions on two surfaces: (1) a **News Sentiment** awareness row in the Home Position Drill-Down showing a label — 🟢 Bullish (bullish_pct ≥ `NEWS_SENTIMENT_BULLISH_THRESHOLD` = 0.60) / 🔴 Bearish (bullish_pct < `NEWS_SENTIMENT_BEARISH_THRESHOLD` = 0.40) / 🟡 Neutral — plus bullish %, buzz (× average coverage), and pp-vs-sector-average; (2) a **News Sentiment Shift** card in Today's Brief for any held name where bullish_pct < `NEWS_SENTIMENT_SHIFT_ALERT_BULLISH` (0.40) **AND** buzz_score > `NEWS_SENTIMENT_SHIFT_BUZZ_MIN` (1.0) — both required, so thin/stale low-buzz bearishness is not alerted. **Strictly additive / zero runtime dependency:** when Finnhub is unconfigured or offline the row/card simply does not render and nothing else changes. **Awareness only — never a gate and never a buy/sell call.** The drill-down row reuses the Brief's batch sentiment cache (same sorted-CSV key) so the two surfaces can't show drifting data or duplicate the fetch. |

### 3.6 Trade Journal

Three internal tabs (restructured 2026-07-16 per the I12 UX remediation): **📝 Log Trade** (the log-a-trade form — F-80/F-81/F-81a-c/F-82), **📊 Performance** (Performance Dashboard, Behavioral Analytics F-83–F-86, Opportunity Cost, Decision Journal, Engine Trust by Composite Band), and **📋 History** (the Trade History table + broker-statement import F-87b). The shared preamble (title, drift-check banner, recommendation-prefill consumption, SELL confirmation card) renders above the tab strip; Log Trade is deliberately the first tab so a recommendation prefill (F-106) lands on it.

| ID | Requirement |
|----|-------------|
| F-80 | Log all buy and sell trades with ticker, action, shares, price, notes, and trigger type |
| F-81 | Persist trades to Supabase `trades` table |
| F-81a | **SELL integrity guard:** a SELL whose shares exceed what the logged BUYs can cover is hard-blocked at form submit. The guard validates against `db.recalculate_from_trades()` — the SAME trade-replay source the drift detector reads — so the guard and the drift detector can never disagree (validating against the `holdings_df` cache instead let an unmatched SELL slip through after a rebaseline; that was the May 2026 COIN double-SELL bug). An explicit override is required to record a SELL beyond accountable shares. |
| F-81b | **Double-submit dedupe:** an identical `(ticker, action, shares)` submission within 15 seconds is rejected with a "duplicate ignored" warning. The live-prefilled price is deliberately EXCLUDED from the dedupe signature — it ticks between reruns and otherwise disguises a double-click as two different trades (the mechanism behind the COIN double-SELL). |
| F-81c | **SELL confirmation card:** a SELL that passes all validation gates is not immediately written to the database. The verified record is stashed to `st.session_state["_tj_pending_sell"]` and the page reruns. A confirmation card renders in the shared preamble above the tab strip (so it stays visible regardless of active tab) showing the exact trade (ticker, shares, price, and realized P&L if computable) with [✅ Confirm SELL] and [✗ Cancel] buttons. Confirm executes the save + holdings sync; Cancel restores `_tj_prefill` so the form reopens pre-populated. BUY submissions skip the intercept — zero extra clicks for buys. (Commit 7793be2.) |
| F-82 | Decision Context capture on each trade: signal seen at time of trade (auto-filled from current portfolio signal; help text shows load-time timestamp so pre-fill is clearly dated), whether signal was followed (yes / no / discretionary), deviation reason, lesson learned |
| F-83 | My Patterns section: analyse historical trades to compute signal accuracy vs override accuracy |
| F-84 | Surface costly deviations (ignored signal, position lost money) and good overrides |
| F-85 | Behavioural insight: compare signal-follow win rate vs override win rate; flag if ≥2 costly deviations |
| F-86 | Lessons library: aggregated lessons from all logged trades |
| F-87a | **Rate sensitivity table (🔗 Risk Analysis page).** A per-holding rate sensitivity table, below the correlation heatmap and above the Risk Action Plan, combining two complementary reads: (1) the **structural sector score** from `macro.RATE_SENSITIVITY` (−1.0 to +1.0; e.g. Semiconductors −0.70, Financials +0.70 — sign convention: negative = hurt when rates rise) and (2) the **empirical Pearson correlation** of each holding's daily returns vs TLT (iShares 20-Year Treasury ETF), computed by `risk.pearson_corr_vs_benchmark()` over the 3-month window (`_cached_tlt("3mo")`, 30-min TTL). TLT falls when long rates rise; a negative TLT correlation means the holding tends to drop when rates rise (rate-sensitive); positive = rate beneficiary. The table sorts **most rate-sensitive first** (TLT Corr ascending; sector score as fallback when TLT data is unavailable for a holding — requires ≥ 20 overlapping trading days). A **weighted-portfolio TLT correlation summary line** caps the table (weighted avg of per-holding TLT corrs by `Weight (%)`). Columns: Ticker / Sector / Weight (%) / TLT Corr / Sector Score / Implication. **Awareness only — never gates, suppresses, or scores.** `risk.rate_sensitivity_per_ticker()`; `data.fetch_tlt()`. (Commit f0b946c.) |
| F-87b | **Broker history text import (paste-based).** A "📋 Paste history" tab on the 📒 Trade Journal broker-import expander (alongside the CSV tab). User pastes text copied from the Robinhood Account → History page; `broker_screenshot.parse_robinhood_text()` (pure-Python regex, no Vision API) extracts executed Buy/Sell trades, skips "Canceled" orders, and resolves company names to tickers. Each Robinhood History order is four lines: `[Company Name] [order_type] [buy|sell]` / `Individual · [Month Day]` / `$[total]` / `[N] shares at $[price]`. **Key design decisions:** (a) Ticker resolution uses a local lookup table (~60 company names); unknown names fall back to the Claude text API if the Anthropic key is available (no API call required for common tickers). (b) Ticker and Date columns are **editable** in the preview — user must verify before importing. (c) **Sync-since date picker** defaults to the day after the last import, derived from `broker_screenshot.last_screenshot_sync_date()` (reads `trades.notes` for "RH screenshot" or "RH text import" tags — backward-compatible). (d) **Three-bucket reconciliation**: import summary + editable new-row preview (new pre-checked, likely-duplicates unchecked) + "in app, not in paste" expander for date-range-scoped discrepancies via `broker_screenshot.find_app_only_in_range()` (content-match: same ticker/action/shares/price). (e) **No auto-deletes ever** — the discrepancy list is informational only. Write path is identical to the CSV import path (human-in-the-loop `st.data_editor` preview → `db.save_trade` + `recalculate_from_trades`). `stock_analyzer/broker_screenshot.py` (pure — no Streamlit or DB imports). (Commits 97590fb + 4e4f46c.) |
| F-87 | **Broker statement import — manual Robinhood sync (v1).** Upload a Robinhood account-activity CSV (📒 Trade Journal → "📥 Import from broker statement") to sync **Buy/Sell** trades into the `trades` table, keeping the app reconciled with the broker until Robinhood ships a mature integration (the automated MCP path stays deferred). `broker_import.parse_robinhood_csv` (pure) handles the export quirks (multi-line Description/CUSIP, `$`/comma/paren/space prices, `M/D/YYYY` dates, trailing junk + tax-disclaimer rows) and keeps only Buy/Sell; non-trade rows (dividends/ACH/interest/fees) are counted in a skipped summary (deferred to a v2 Account-ledger sync), and rows with shares/price ≤ 0 are surfaced in an `invalid` bucket, never silently dropped (DB `check (shares>0)`/`check (price>0)`). **Dedup is content-match, no schema change** (`classify_against_existing`): an **exact same-day** match on `(date, ticker, action, shares, price)` **plus a date-agnostic** secondary match — because trades logged via the interactive form store `traded_at` as the *logging timestamp*, not the trade date, a hand-logged trade is flagged **"possible duplicate — different date"** and left **unchecked** so a mixed manual+import workflow can't double-count; count-consistent so genuine repeat fills aren't over-flagged. **Human-in-the-loop:** an editable `st.data_editor` preview (new rows pre-checked, likely-duplicates unchecked, per-row Status) — the source-of-truth `trades` table is **never bulk-written without explicit confirmation**. On import, rows insert **chronologically** (date asc, BUY before SELL) via the existing `db.save_trade`, then `recalculate_from_trades` → `save_holdings` → `update_trade_realized_pnl` (the same tested reconciliation/rebuild path) produce a **reconciliation view** (resulting holdings + warnings). Read-only-guarded; parse call wrapped so a malformed file can't crash the page. **Honest v1 limit:** the CSV is a transaction log, not a positions snapshot — reconciliation is shown for the user to eyeball against Robinhood (a SELL whose BUY predates the app's history surfaces as a recalc warning). Plan: [docs/plans/broker-statement-import.md](plans/broker-statement-import.md). |

| F-164 | **Opportunity Cost expander (Trade Journal — Behavioral Analytics section).** A collapsed `st.expander` ("💸 Opportunity Cost — what you passed on") that computes lazily on open. Scope: the 90-day window of `new_pick` recommendations (`rec_types=("new_pick",)`) the user never acted on, using `recommendations_history.distinct_missed()` + `missed_split()` + `compute_outcomes(min_days=5)`. Renders a 4-KPI strip (recs passed / would've won / dodged losers / avg missed return) and a top-5 named ticker list with per-ticker alpha vs SPY. **Scope = new_pick only** (gate-cleared recommendations — never the `buy_candidate` awareness feed; passing on those is correct). No new DB table — reuses existing `db.load_recommendations()` + the `recommendations_history` helpers. Pointer to 📊 Recommendations History for the full chart. (Commit f0b946c.) |
| F-165 | **Engine Trust by Band (Trade Journal — Behavioral Analytics section).** A collapsed `st.expander` ("🔬 Engine Trust by Composite Band — did you act at the right conviction levels?") that computes lazily on open. Groups all enriched recommendation outcomes by composite band: sub-threshold (<65) / BUY (65–74) / Strong BUY (≥75). Excludes `outcome_maturing` rows. For each band: action rate (did you act when the engine said this?), avg alpha when you acted, avg alpha when you passed, and a plain-English **edge comment** comparing the two. Answers the discipline question "did you trust the engine more at higher conviction, and was that trust well-placed?" `recommendations_history.engine_trust_by_band()`. Wrapped in `try/except` for graceful degradation when history is sparse. (Commit f0b946c.) |
| F-166 | **Behavioral KPI strip on AI Insights, 🩺 Positions tab.** Four columns: **Win Rate** (green ≥ 55% / amber 45–54% / red < 45%), **Profit Factor** (green ≥ 1.5 / amber ≥ 1.0 / red < 1.0), **Trades This Month** (red prefix when overtrading is elevated), **Behavioral Alert** (count of HIGH-priority behavioral insights or "none"). HIGH-priority behavioral insights are also surfaced as `st.warning()` banners so they can't be missed. Only renders when ≥ 5 trades exist. Reads from `trade_analytics.build_full_analytics(trades_df)` (already computed globally). Caption points to Trade Journal. **Relocated 2026-07-22 (commit `6e150a4`)** from a page-level row above the cadence tabs into the 🩺 Positions tab (rendered before the Thesis Reviews section) — it had been reading as a page-level header rather than a Positions-tab concern; no logic changed, indentation only. (Original: commit 3cbd6a6; relocated: commit 6e150a4.) |
| F-167 | **Overtrading detection (behavioral analytics).** `trade_analytics._build_overtrading_stats()` computes the current calendar-month BUY+SELL count (excludes SPLIT rows) vs a rolling 12-month average. Returns `{current_month, current_month_count, rolling_avg, multiplier, is_elevated}`. `build_behavioral_insights()` surfaces two overtrading insight cards: **HIGH** at ≥ 2.0× rolling average (pace is markedly above normal, with institutional-lens framing on transaction costs and reactive trading risk); **MEDIUM** at ≥ 1.5× (elevated pace with context). `build_full_analytics()` now includes `win_rate` (was computed but not returned in prior versions — now in both the `empty` sentinel and the return dict) and `overtrading_stats` in the returned dict. (Commit 3cbd6a6.) |
| F-168 | **Stress test — historical scenario replay (model vs actual).** For scenarios that have a real event window (`stress_test.HISTORICAL_WINDOWS`), an optional "📅 How did your holdings actually perform?" expander appears below the stress-test results (2026-07-13: now on the 🔗 Risk Analysis page, moved from Home in the nav cleanup). Three scenarios have windows: COVID Crash (2020-02-19 → 2020-03-23), 2022 Rate Shock (2022-01-03 → 2022-10-13), GFC 2008 (2007-10-09 → 2009-03-09). Loading is button-gated and lazy (user clicks "📥 Load historical data"); results are cached in `st.session_state[f"_hist_stress_{scenario_id}"]`. `stress_test.fetch_historical_drawdowns(scenario_id, tickers)` fetches actual OHLCV via yfinance for each held ticker in the event window and computes peak-to-trough drawdown as `(min_close − first_close) / first_close × 100`. Tickers with fewer than 5 trading days in the window (IPO after event start, delisted) return `None` and display N/A. Results render as a **Model Est. (%) / Actual (%) / Δ (Actual−Model)** table; rows with Δ > 5 are green-highlighted (model over-estimated the loss — actual was better than feared), rows with Δ < −5 are red-highlighted (model under-estimated — actual was worse). A "🔄 Refresh data" button clears the session cache. Custom Scenario and scenarios without a `HISTORICAL_WINDOWS` entry show no expander. Awareness only; never issues a recommendation or gates an entry. (Commit 0e8dc9f.) |

### 3.6a Trade Review (🪞 Trade Review page)

**Added to spec 2026-07-27 — a doc gap flagged internally back on 2026-07-24 (see `docs/shipped-log.md`'s Agentic Roadmap P6 closing note: "🪞 Trade Review... itself discovered mid-session, undocumented in memory") and never closed until now.** `app.py:20449`, `elif page == "🪞 Trade Review":`. Pure-logic backing module `stock_analyzer/trade_review.py` (no Streamlit/API calls). Answers: "are app-followed trades outperforming ones made off external info?"

| ID | Requirement |
|----|-------------|
| F-214 | **Nine independently sample-gated behavioral diagnostics**, each rendered only above its own minimum-sample floor (never a fabricated read on thin data): Holding-Period Imbalance (winners cut short vs. losers held longer, ≥3 wins & ≥3 losses), Signal-Defying Bias (outcome when the user traded against the recorded signal, ≥3 defying trades), vs-SPY Drag (% of closed trades beating SPY + cumulative $ alpha, ≥3 closed), Re-Entered Tickers (net P&L on tickers bought ≥2 times, ≥2 trades/ticker), Trigger-Type Effectiveness (win rate/P&L by MANUAL/RECOMMENDATION/WATCHLIST_ENTRY/etc., ≥2 types × ≥3 trades each, total ≥6, spread ≥20pp), Lesson-Capture Rate (% of trades with a logged lesson, and whether lesson-writers outperform, ≥6 judged trades), Day-of-Week Timing (win rate/P&L by execution weekday, ≥10 total, ≥2 weekdays × ≥2 each, spread ≥30pp), Position-Size Discipline (vs. `SINGLE_NAME_CEILING` 15%), and Sector Mix (vs. `SECTOR_ELEVATED` 25%). All computation (FIFO buy/sell pairing, per-trade P&L, diagnostics) happens in `trade_review.py`; the page only calls `build_trade_review`/`build_recommendations`/`build_insights` and renders. |
| F-215 | **Position-size % uses current portfolio value, not historical** (an explicit, documented approximation — a trade's size-at-the-time isn't reconstructed from point-in-time portfolio value, which doesn't exist as a stored series). Findings are described in the page's own copy as "never speculative." |
| F-216 | **Deliberately NOT merged with Behavioral Fingerprint.** A plan to combine these 9 diagnostics with Behavioral Fingerprint's 6 patterns into one "Blind Spots" list was scoped and then shelved after Opus design review — Behavioral Fingerprint has no severity tiers, and folding its explicitly neutral cards into a "blind spot" label would have contradicted that tab's own non-accusatory posture. The considered fallback (a Trade-Review-only severity-ranked list) turned out to duplicate this page's own already-existing "🎯 Course-Correction Recommendations" section — so nothing was built; the two pages remain independent by design, not by oversight. |

### 3.7 Economic Calendar

| ID | Requirement |
|----|-------------|
| F-90 | Display upcoming macro events (Non-Farm Payrolls, CPI, FOMC, etc.) with dates |
| F-91 | Show relevance of each event to the current portfolio holdings |

### 3.7a Macro (🌐 Macro page)

**Added to spec 2026-07-27 — a doc gap, not a new build.** `app.py:14312-14484`, `elif page == "🌐 Macro":`, single linear flow (no tabs), backed by `stock_analyzer/macro.py`.

| ID | Requirement |
|----|-------------|
| F-211 | **Manual-load regime read (TLT/SPY/VIX).** A "Load macro signals" button fetches 3 price series and caches them in `st.session_state["_macro_raw"]` — not auto-loaded on page open, not auto-refreshed. `detect_macro_regime(tlt_ret, spy_ret, vix)` (`stock_analyzer/macro.py`) classifies: TLT 3-month return < -3% → `rising_rates`, > +3% → `falling_rates`, else neutral on rates; VIX ≥ `RISK_OFF_VIX_LEVEL` (25) → `risk_off`, ≤ `RISK_ON_VIX_LEVEL` (15) → `risk_on`, else neutral on volatility (SPY return only labels bull/bear/sideways, doesn't drive the combined regime). Rate signal wins if non-neutral, else the risk signal, else `neutral`. Output regime keys: `rising_rates` / `falling_rates` / `risk_off` / `risk_on` / `neutral`. |
| F-212 | **⚠️ Landmine — do not confuse with the FRED-based regime detector used elsewhere.** This page's `detect_macro_regime` (`macro.py`) is a SEPARATE, independent function from `detect_macro_regime_fred` (`stock_analyzer/macro_calendar.py`), which feeds real gates/targets elsewhere (Premortem stance narrative, Regime-Conditional Targets, the Risk Analysis regime cache). The two use different inputs (TLT/SPY/VIX price action here vs. FRED macro data there) and different label taxonomies. This page's regime read is diagnostic-only, lives in page-local session state, and is not consumed by any other page or gate. |
| F-213 | **Sector Rotation Playbook + portfolio macro alignment.** For the detected regime, shows an overweight/underweight sector list from a static `REGIME_FAVORED` table. `portfolio_macro_exposure()` labels each holding Tailwind↑/Headwind↓/Neutral↔ purely by whether its sector is in the current regime's overweight/underweight list (a categorical match, not a continuous alignment score), alongside a static per-sector `RATE_SENSITIVITY` score and a horizontal bar chart. No cross-asset signals (credit spreads, dollar, copper, yield curve) are used on this page — that's a distinct feature (Cross-Asset Pulse, F-09c). Awareness only; never gates or gets consumed by any recommendation. |

### 3.8 Watchlist

| ID | Requirement |
|----|-------------|
| F-100 | Allow user to add tickers to a watchlist; persist to Supabase `watchlist` table |
| F-101 | Show composite signal and key metrics for watched tickers |
| F-102 | Provide advisor recommendations for watchlist entries: REMOVE / HOLD_OFF_EARNINGS / ENTER_NOW / NEAR_ENTRY / WAIT_ENTRY / WAIT_CATALYST |
| F-103 | ENTER_NOW must require composite score ≥ `COMPOSITE_BUY` (65), price in/near entry zone, AND validated `R:R ≥ 2.0` (rr=None is NOT a green light). Tickers without R:R fall through to NEAR_ENTRY. (Implements G-13.) |
| F-104 | ENTER_NOW must apply the portfolio-risk gate using `_port_risk_cache`, `_risk_high_alerts_cache`, and `_grow_today_sectors_cache`. Hard breaches (sector ≥ 35% OR portfolio β > 1.4 + ticker β > 1.8) downgrade to a "Setup Ready, But Portfolio Fit Blocks Entry" NEAR_ENTRY card. Soft concerns keep ENTER_NOW with an amber caution banner. |
| F-105 | When `_daily_brief_offline` is True, the Watchlist must surface an explicit warning that coordination gates are disabled and instruct the user to revisit the Portfolio page. |
| F-106 | "Log Planned Trade" button on an ENTER_NOW card must prefill the Trade Journal form with ticker, price, stop, and trigger=`WATCHLIST_ENTRY`, and route via `_pending_page` (never assign `nav_page` directly — that key is widget-bound and raises `StreamlitAPIException`). |
| F-219 | **Actionable-first restructure (2026-07-27).** The page defaults to a **🎯 Actionable** filter (`ENTER_NOW` + `NEAR_ENTRY` only) on load instead of showing all watchlist tickers — additional `st.segmented_control` chips (Hold / Waiting / Remove / All) narrow the view, alongside a ticker search box and a sort dropdown (Priority default / Score / Readiness / Days watched). Rows render collapsed by default as custom `st.container(border=True)` blocks with a colored action-badge + left-border summary line (ticker, score, readiness bar, price, R:R, earnings countdown, and a 👁️ resurrected-days chip); clicking a row's toggle expands the same detail content the page has always shown (metrics strip, conditions, position sizing, quick actions) — no change to that content. Default display order is `stock_analyzer/watchlist_advisor.sort_key_for_action()`: `ENTER_NOW → NEAR_ENTRY → REMOVE → HOLD_OFF_EARNINGS → WAIT_ENTRY → WAIT_CATALYST` — a deliberate change from the prior order (`REMOVE`/`HOLD_OFF_EARNINGS` first), which buried actionable opportunities under earnings-hold cards on watchlists with many names on hold. Every row also gets a **"▶ Analyze"** button (the same `_pending_page`/`_analysis_ticker`/`_nav_origin` navigation pattern used elsewhere in the app) linking to the full 📈 Analysis page for that ticker. Purely a presentation-layer restructure — does not change `build_watchlist_recommendation()`'s classification, scoring, or the portfolio-risk gate (F-104), and does not conflict with F-203's invariant (that feature never reorders cards; this default ordering is separate, page-level logic). |

### 3.9 AI Snapshot

Renamed from "AI Brief" (2026-07-12) — the Home tab shares no functional overlap with the standalone 🧠 AI Insights page (thesis tracking / weekly debrief / monthly report / analyst coverage), but the two names were confusable (undocumented, same "AI"/"Brief" wording as Today's Brief). Both surfaces now carry a mutual cross-reference caption.

| ID | Requirement |
|----|-------------|
| F-110 | Generate a natural language portfolio snapshot using an LLM (Anthropic / OpenAI / Google) |
| F-111 | Snapshot should summarise portfolio state, key risks, and suggested actions |

### 3.10 Market Data Layer (multi-source: failover + cross-check)

The app must not depend on a single market-data source. A provider abstraction (`stock_analyzer/providers/`) sits behind `data.py`'s public functions so the rest of the app is source-agnostic. See architecture §4.0.4.

| ID | Requirement |
|----|-------------|
| F-120 | Market data must be served through a provider chain with automatic failover. A `DATA_MULTISOURCE_ENABLED` master switch toggles the layer; when off, `data.py` behaves exactly as the single-source yfinance code (instant rollback). |
| F-121 | **Live prices** use `DATA_LIVE_PRICE_ORDER` (Finnhub → yfinance → FMP). Finnhub (real-time US quotes) is primary; the chain gap-fills — later providers supply only tickers the primary couldn't. A Finnhub outage must degrade to yfinance (delayed), never fail outright. |
| F-122 | **History, the analysis bundle, indices, and risk-free rate** use `DATA_PROVIDER_ORDER` (yfinance → Finnhub → FMP). yfinance stays primary; when a yfinance call hard-fails (e.g. rate-limited), the request fails over to FMP so composite scoring does not go dark. The broad scanner/movers scans stay on yfinance only. |
| F-123 | **Price cross-check guardrail:** held-position prices are validated against an independent source (cached 5 min). `prev_close` is compared strictly (`DATA_XCHECK_PREVCLOSE_TOL_PCT` 0.5%) — a settled value whose mismatch signals a real integrity fault (missed split, wrong-symbol, poisoned feed). Live price is compared loosely (`DATA_XCHECK_LIVE_TOL_PCT` 3.0%) to tolerate delayed-vs-real-time latency. A breach renders a fail-loud red "Price unverified — sources disagree" banner naming the tickers and gaps. (Implements OP-03 at the data boundary.) |
| F-123a | **Validator-health gate (no false alarms during a validator outage).** The cross-check is **skipped** when its independent validator (the second live-price provider, yfinance / `"yahoo_finance"`) is RED in `api_health` (sustained degradation — `rate_limits ≥ 3` OR `consecutive_errors ≥ 5`, e.g. Yahoo 401 Invalid Crumb / 429 from a datacenter IP). A "disagreement" against a degraded validator is the validator's own fault, not a real integrity fault, so surfacing the red F-123 banner then would be a false alarm. In that state both surfaces (Home, Analysis) show a grey "Price cross-check paused — validator degraded" caption (transparent, never silent) and the check auto-resumes when the validator recovers. **Red-only by design:** a genuine integrity fault with a HEALTHY validator still fires the F-123 red banner — the gate must not mask a real fault. Single chokepoint in `orchestrator.crosscheck_batch`; the gate and the UI caption share one threshold (`api_health` red). |
| F-124 | **Source transparency:** every live-price record is tagged with its provider; the price-strip caption shows the actual source(s) ("Finnhub (real-time)" / "Yahoo Finance (15-min delayed)" / "FMP"), and the Data Health sidebar shows per-provider call / error / rate-limit counts. The user can always see where a price came from. |
| F-125 | **Keyed-provider configuration:** Finnhub/FMP API keys live in Streamlit secrets (`FINNHUB_API_KEY`, `FMP_API_KEY`). A missing key makes that provider report unconfigured and be skipped silently (no error). The secret reader tolerates a key mis-nested under a TOML `[section]` and falls back to an env var for offline self-test. |
| F-126 | **Information Asymmetry Detector — Phase 1: price cross-check history (P4 of the Agentic Intelligence Roadmap) — SHIPPED 2026-07-24, commit `6e3ebd2`, display-only.** Persists F-123's already-computed price cross-check result to a new `price_xcheck_history` table, one row per (ticker, ET trading day) — the check previously had zero history (a 5-minute `st.cache_data` TTL only). When a held ticker is currently failing cross-check, its bullet on the existing red banner is annotated per failing leg — "prior-close gap widened from X% to Y% since `<date>`" or "live-price gap widened from X% to Y% since `<date>`" — when the most recent prior recorded row (within a 21-day lookback) shows a smaller gap than today's. `stock_analyzer/data.py::divergence_widened()` is a pure diff+threshold helper (`min_widen_pp=1.0`, a display-annotation default, not a `constants.py` value). **Written from the interactive Home page path only, day-deduped via session state — never from the headless cron** — the premarket cron path does not call the cross-provider validator today, so logging from cron would add a new per-ticker second-provider fetch every run; the interactive path already computes this for free every 5 minutes via the existing `_cached_price_xcheck`. **Scope was deliberately narrowed from the roadmap's original 3-source fundamentals-divergence idea** (research found Finnhub supplies live price only — no fundamentals capability at all — and FMP's own forward PE is sometimes a synthetic derivation from its trailing PE rather than an independent read, making a naive "sources disagree on forward PE" comparison both structurally impossible for a true 3-way check and methodologically ambiguous for a 2-way one). No new LLM, no new tab. 2-round Opus plan review (FIX-FIRST then SHIP) + Opus pre-ship code review (SHIP, 0 blocking). **Strictly additive: the annotation never changes whether the banner fires, never gates a stop/P&L decision.** Plan: [docs/plans/information-asymmetry-detector.md](plans/information-asymmetry-detector.md). |

### 3.11 Headless Automation & Email Alerts (out-of-app cron)

The app must deliver its protective signals even when the user never opens it. A **headless runtime** (GitHub Actions, no Streamlit) runs the same decision logic and emails the user. This is the app's **second runtime**: `cron_runner.py` (dispatch) → `headless_alert_engine.py` (compute) → `notify.py` (Resend HTTP email), sharing the app's data path via the extracted `bundle_loader.py`. Credentials resolve **env-first** (GitHub repo secrets) then `st.secrets`, and the Supabase client is a module-global singleton (the cron has no `st.cache_resource`). See architecture §4.0.x and memory `project_email_alerts_cron`.

| ID | Requirement |
|----|-------------|
| F-140 | **Pre-market protective alert (exit-discipline Phase 3).** ~08:00 ET on trading days, recompute protective signals headlessly and email the user when (and only when) the actionable set is non-empty: breached/triggered stops, deterioration EXIT (F-25d), and the risk-off de-risk overlay (F-25e), single-surfaced via a shared `reduced` set so a ticker is never listed twice. |
| F-141 | **EOD snapshot + reactive pullback (post-close).** After `ALERT_EOD_HOUR_ET` (≥ 16:00 ET) on trading days, write the day's `daily_snapshots` baseline (per-ticker shares + close — the Tier B Today's-P&L baseline, F-03a) and evaluate the reactive pullback condition. |
| F-142 | **Reactive pullback email (pullback-awareness Phase 2).** When the index draws down past `PULLBACK_ALERT_INDEX_PCT` (−3%) AND the book is fragile (F-09a), email a reactive pullback notice — framed as exposure/reaction, never a market-timing call. |
| F-143 | **Once-per-ET-day idempotency + DST safety.** GitHub cron is UTC with no DST, so each job is scheduled on TWO UTC slots straddling EST/EDT; `cron_runner` guards on trading-day + ET-hour + a per-day Supabase `alert_state` flag (row 1 = premarket/protective lane, row 2 = EOD pullback lane) so exactly one run fires per ET day per lane — never a double-send. The weekday mode is derived from ET hour (< 12 ⇒ premarket, else eod); the mid-morning scan slots force `scan` and the **Sunday evening slots** (`0 22`/`0 23 * * 0`) force `thesis` — which **chains** the weekly debrief (F-152) in the same run. `ALERT_RUN_MODE` is honoured only for `scan` / `thesis` / `debrief`; `premarket` / `eod` override values are intentionally ignored so a YAML schedule change cannot accidentally fire the wrong weekday lane. The `workflow_dispatch` manual trigger exposes `mode` choices `auto`, `scan`, `thesis`, and `debrief` (auto = hour-derived). |
| F-144 | **Email delivery + dedup.** Email is sent via the Resend HTTP API (no SMTP); failures surface the HTTP status/reason in the run log (the API key is never logged). The premarket lane also dedups on a content fingerprint so an unchanged action set does not re-email the same day. A `workflow_dispatch` manual trigger exposes `mode` / `force` (bypass guards) / `test_email` (prove Resend→inbox without touching dedup state). |
| F-145 | **Fail-safe posture.** The cron always exits 0 (a failed run never red-flags the workflow into auto-disable) and is **inert without `RESEND_API_KEY`** — absent secrets mean it computes nothing and emails nothing rather than erroring. Secrets live in GitHub repo secrets (same service-role Supabase key class as Streamlit; RLS stays on). |
| F-146 | **Mid-morning scan persistence (cold-load buy candidates).** A third cron mode `scan` (~9:45 ET, post-open-gated; two DST UTC slots `45 13`/`45 14 * * 1-5` mapped to mode=scan via `github.event.schedule`) runs the full-universe sector scanner headlessly and persists the result to the single-row `scanner_cache` Supabase table (`save_scanner_cache`, whole DataFrame as JSON, overwrite — no dedup). On a **cold load** the app hydrates `scanner_results` from `scanner_cache` once per session (`load_scanner_cache`), so the Home "More Buy Candidates" + Grow Today new-pick lists populate **without** the user running the ~20s scanner; a "📡 From the {date} auto-scan" stamp shows freshness, and a manual full-universe Home scan re-persists. Inert until the `scanner_cache` table exists (load → None → today's empty-state, unchanged). The Market Scanner page's user-selected **subset** scan is intentionally NOT persisted (it would seed the cold-load list with a non-representative subset). |
| F-147 | **Morning buy-list email (the "offense" alert).** The same `scan` run, after persisting, emails the **high-conviction "New Positions to Initiate"** so the user can act from mobile. `headless_alert_engine.compute_morning_picks` reuses `_build_context` and assembles the SAME inputs the app feeds `build_daily_briefing` (market tone, per-pick composites, news from loaded bundles, **and the macro calendar** — so no gate is looser than Grow Today), then calls the SAME `build_daily_briefing`; the cron keeps only the green **"✅ Go — Composite Confirms"** set (`xref.verdict_reconciled.verdict == "go"` AND composite present — same field as the Home badge), excluding More-Buy-Candidates / unverified / conflicted / over-cap-sector / macro-suppressed names. Each pick is mobile-actionable (ticker · momentum + composite · entry zone · suggested shares/$ · stop) with an **entry-zone "only act if still in $X–$Y" guard** and a "scores as of the morning scan" stamp; *advisory — the user places the trade, nothing auto-traded*. **Silent when nothing clears the bar** (automatic on flat/bear tape — Grow Today defers new entries). Dedups on a per-day ticker-set fingerprint (`alert_state` row 3), saved only on a successful send (so a transient failure is retried by the later DST slot). Inert without `RESEND_API_KEY`. |

### 3.12 AI Insights (LLM intelligence layer)

All LLM-generated output is consolidated on a single dedicated **🧠 AI Insights** nav page. This layer is **strictly additive**: the core app has **zero runtime dependency on LLM connectivity** — if the Anthropic API is offline, rate-limited, or the key is absent, every other page and every gate operates exactly as it does today, and AI Insights shows an explicit offline/"not yet generated" state. **The LLM narrates and synthesizes; it never moves a gate, changes a composite score, or issues a buy/sell** — that authority stays with the rule-based engine. Full design + approval history: [docs/plans/ai-intelligence-layer.md](plans/ai-intelligence-layer.md). Cost-routing: Claude **Sonnet** for these batch jobs (Opus reserved for plan/review). All LLM features ship **inert** until `ANTHROPIC_API_KEY` is set.

| ID | Requirement |
|----|-------------|
| F-150 | **Thesis capture (F-1.1).** The Trade Journal BUY form carries an optional "Investment thesis" field (multi-line, nudged but not required, so quick trade logging is unblocked), stored in `trades.user_thesis` (nullable; legacy rows backfill NULL per the backward-compatible-column convention). Pure text storage — **no LLM call** at capture, so it always works. For existing positions with no captured thesis, the AI Insights page offers a "Set thesis" form that writes to the most recent BUY row for that ticker (`db.update_user_thesis`). |
| F-150a | **Thesis authoring at BUY (F-5, Phase 1).** The Trade Journal BUY form offers a "✨ Draft thesis" action (rendered **outside** the form so it can seed the field; disabled when no Anthropic key or in read-only) that assembles the engine's **entry-time** evidence for the ticker — composite + band, fundamentals (revenue growth, margin, earnings trend), next-earnings catalyst, recent headlines, technicals *labelled as entry-timing only*, market tone — reusing the `_grow_composites` cache (else one `load_all` fetch), and asks the LLM to draft a **candidate, falsifiable** thesis (durable claim + evidence-from-package + a "Breaks if …" clause; no price targets / buy-sell language / probabilities / gate values). The draft **seeds an editable field — it is never auto-saved**: the user edits and owns the final text (**author-of-record** invariant), which saves to `trades.user_thesis` exactly as today and feeds F-151 review. `trades.thesis_source` records provenance (`manual` / `ai_draft` / `ai_edited`) so analytics can tell authored from hand-written theses. Strictly additive / zero runtime dependency: offline → the field is a plain text box; a failed draft shows a visible warning, never a silent fake. `thesis_advisor.build_authoring_inputs` / `draft_thesis` (Sonnet). Ships **safe before** the additive `trades.thesis_source` DDL (`save_trade` retries without the column so trade logging never breaks). Plan: [docs/plans/thesis-authoring-analyst-desk.md](plans/thesis-authoring-analyst-desk.md). |
| F-151 | **Thesis review — INTACT / WEAKENING / BROKEN (F-1.2).** For each open position with a non-null `user_thesis`, an LLM reviews the original conviction against current evidence (last-30-day headlines, fundamental + technical trend, last earnings result — all from existing pipelines; the LLM cannot invent events) and returns a verdict + ~100-word summary. Runs **weekly (Sunday evening cron lane)** and **on-demand** ("Re-evaluate" per position). Conservative by default (defaults to WEAKENING when uncertain; BROKEN requires a clear, specific contradiction). **BROKEN does NOT issue an exit** — the rule-based deterioration ladder fires independently; this is awareness ("your reason for being in this has changed"). Stored in `thesis_reviews` (with `inputs_hash` for staleness). Surfaced on AI Insights only — no chip embedded on core pages. |
| F-152 | **Weekly Portfolio Debrief (F-3).** A weekly retrospective: the LLM narrates a structured 4-section debrief (What happened · Decisions you made · Patterns this week · One thing to watch) from a **Python-assembled data package** (portfolio vs. SPY weekly performance + alpha, top contributors/detractors, recs surfaced vs. acted-on) — the LLM narrates only the package, it cannot add facts. Delivered as a **Sunday email** (Resend; the `thesis` cron lane chains into the debrief) and **on-demand** ("Generate Now") on AI Insights. **Build-time guard:** requires ≥ 5 trading days of `daily_snapshots`; below that it shows a data-accumulation message rather than fabricating a trend. Stored in `weekly_debriefs` (unique on `week_ending` → upsert-safe). Email uses the **light-mode-first** template (`render_debrief_email`) with `**markdown**` → HTML rendering. The in-app view adds a **weekly alpha trajectory** bar (portfolio − SPY per week, green = beat) once ≥ 2 weeks of debriefs exist — the trend behind the single-week metric. **Email metric clarification (2026-07-19):** the three KPI cells now carry subtitle lines ("this week · equity positions" / "this week · SPY" / "vs benchmark") and the email closes with a footnote explaining that the weekly ± figure is a **simple equity-positions delta** (end − start equity / start equity, from `daily_snapshots`) — NOT the same metric as the Account page's money-weighted return (Modified Dietz, all-time, flow-adjusted). A leveraged account makes this difference stark: equity positions can show +17.6% for the week while the all-time MWR reads −7.4% (margin debit reduces net account value below contributed capital). The debrief system prompt was also tightened to say "equity positions gained/fell X%" rather than "the portfolio returned X%" to prevent the LLM from framing a positions-only delta as a full account return. **NaN guard (2026-07-27):** the SPY-return fetch (`yfinance`) can return a ≥2-row frame with a NaN Close (holiday gap / upstream glitch) rather than raising, which previously survived as `float('nan')` and rendered as the literal "+nan%" in the KPI tiles; `build_debrief_package` now sanitizes this to `None` at the shared entry point both the cron and on-demand callers use, so it displays "N/A" like any other unavailable SPY read. **Capital-flow caption (2026-07-27):** when a BUY/SELL landed inside the joined week (`package['week_had_trades']`), the email now adds an always-visible amber caption under the KPI tiles noting the figures reflect position-size changes as well as price moves — the same always-visible-caption treatment the Summary page's Alpha vs SPY tile got on 2026-07-26, since a footnote-only caveat wasn't preventing the numbers from being read at face value. |
| F-153 | **Monthly Portfolio Intelligence Report (F-4).** A monthly behavioural retrospective that answers the slower questions the weekly debrief cannot — led by **question 0: is the *entry engine* picking well?** Every position originates from a ticker that cleared all gates, scored **Composite ≥ `COMPOSITE_BUY` (65)**, and surfaced under "📈 High-Conviction Entries Only → 🆕 New Positions to Initiate"; v1 grades that engine quality (Q0) and the user's **signal discipline** (Q1: acted vs. ignored, and what each cost/saved). Headline **acted / not-acted counts are distinct tickers** (via `signal_flow`, not daily surfacings) so they match the decision-flow Sankey and never read as misleadingly large name counts; graded-outcome aggregates (alpha, bands) stay surfacing-based means. **Narration count-discipline:** the LLM prompt feeds *only* the distinct name counts (surfaced / acted / not-acted) and explicitly forbids stating any name/recommendation/outcome count greater than the distinct total or inventing a separate "graded-outcomes" tally — closing a prose-vs-chart contradiction where the per-surfacing maturity count (e.g. 26 surfacings) leaked into a distinct-name story (22 names) and read as fabricated. The per-band / per-verdict alpha the narrative quotes is `avg_alpha` — the engine's alpha across **all graded names** in the band (acted or not), the **same** figure the band chart plots — not the acted-only subset (which made the prose say a band was −3.0% while the chart showed it +0.8%). Reuses the existing rule-based scorecard in `recommendations_history.py` (`match_recs_to_trades` → `compute_outcomes` → `summary_stats`/`by_verdict`/`by_composite_band`); the LLM narrates the aggregates, it does not recompute them. Only **matured** recs (`days_since ≥ REC_SCORE_MIN_DAYS`, 5 days) feed graded outcomes. First Sunday of month (existing Sunday cron lane) + on-demand on AI Insights. Stored in `monthly_reports` (unique on `period_end` → upsert-safe). **Shipped 2026-06-27** (v1 = Q0 + Q1 + a pattern; Q2 thesis discipline / Q3 cross-month patterns deferred until data matures, `section_thesis` null in v1). `intelligence_report.py`; the entry-threshold value in the prompt is interpolated from `COMPOSITE_BUY` at call time (never hand-transcribed). The on-page report display is **visual-first**: a **decision-flow Sankey** (Q1 anchor — New Positions → acted/missed → win/loss, via the shared `signal_flow` + `_signal_flow_sankey`, scoped to the report window), an **alpha-by-composite-band bar** (Q0 — does higher conviction deliver higher alpha? via `by_composite_band`'s `avg_alpha`), and the **ranked missed-opportunity bar** (F-161 — named magnitudes a Sankey can't show). All three are built from **one snapshot** (`recommendations_history.report_viz_snapshot` — `signal_flow` + scoped `by_composite_band` + `distinct_missed`/`missed_split`), are fail-soft, and share the pure helpers so they can't drift from the narrative or the Recommendations History page. The snapshot is computed from the **full** (all-rec_type) enriched set so the "acted on via ANY surfacing" safeguard holds across rec_types (a name bought on a buy-candidate day is not mislabelled a missed New Position); the aggregate means are then scoped to `new_pick`. **Report freezing (immutable dated artifact):** the snapshot is **saved with the report** (`monthly_reports.viz_json`, jsonb) and re-rendered verbatim, so a report reads identically whenever it is opened rather than drifting as prices move and names mature; display prefers the frozen snapshot and falls back to a live recompute (same helper) only for rows saved before freezing. The page offers a **month-by-month period picker** (`load_monthly_reports(limit=12)`, deduped to the latest run per calendar month, newest first). Ships inert until the `monthly_reports` DDL (incl. the `viz_json` column) + service-role RLS are applied. |
| F-153a | **F-153 hard boundary — surfaces patterns, never tunes gates.** The monthly report MAY observe that a composite band underperforms (e.g. "the 65–74 band trailed the ≥75 band by X — you may want to review the entry threshold") but MUST NOT change `COMPOSITE_BUY` or any threshold, move a gate, or auto-issue a buy/sell. Threshold changes remain an **investment-policy decision** made in conversation with the user (CLAUDE.md hard rule #1; `constants.py`). |
| F-153b | **F-153 data-maturity guards.** A section is **suppressed** (not fabricated) when its data is too thin: entry-quality (Q0) needs ≥ `MONTHLY_REPORT_MIN_GRADED` (5) matured graded entries; thesis discipline (Q2) needs ≥ M thesis reviews with an outcome window; cross-month patterns (Q3) need ≥ 2 monthly reports. These floors are **measurement windows, not gates** — they never affect what the engine recommends. Q1 (signal discipline) is available from month 1. |
| F-154 | **Analyst Coverage — Ideas Inbox (F-6, Phase 1).** Captures professional analyst research (CNBC Pro, broker notes) into structured, queryable intelligence that both **surfaces new tickers** as watchlist candidates and **enriches** names already held/watchlisted. **Input = paste raw article text** (the source is paywalled/bot-protected so the app cannot fetch it; the user's subscription lives in their browser). The LLM (`analyst_intel.extract_report`, Sonnet) extracts **only atomic per-firm facts** — firm, analyst, rating, price_target, upside_pct — plus ticker/company/date/report_type/thesis/catalysts/risks, and produces **one record per covered stock**: each analyst's rating/target attaches ONLY to the stock they discuss, so a firm's multi-stock "top picks" roundup yields one record per stock rather than a merge (fixing a bug where a BofA roundup mislabeled Visa's/Walmart's targets as Spotify's); names merely *listed* without a rating/target/write-up are skipped, sidebar/consensus widgets for other tickers are ignored, and it never fabricates a number (`null` for anything not stated). `extract_report` returns `list[dict] | None`. **All aggregates (avg/high/low PT, consensus label) are computed in pure Python** (`analyst_intel.derive_consensus`), never by the LLM, so no arithmetic is hallucinated. The extraction is shown in an **editable preview** — **one card per extracted stock** (include/exclude toggle; the human safety net — the user corrects any misread before save), "Save selected" writes each as its own row (partial-save failures are surfaced and kept for retry, never silently dropped) — and the **raw text is persisted** (`analyst_coverage.raw_text`) for re-processing. Saved rows render as an **Ideas Inbox** card library (recent = within `ANALYST_COVERAGE_FRESH_DAYS`, 30) with held/watchlist badges and per-card **▶ Analyze** (routes the ticker to the 📈 Analysis page → existing 5-gate scoring) + **➕ Add to Watchlist**. Awareness-only: analyst PTs/ratings **never modify a composite score, gate, or verdict** — the value is the **"Wall Street vs. your engine" tension** (e.g. five firms rate a one-month-old IPO Buy while the engine scores it below `COMPOSITE_BUY` (65) on thin history). Strictly additive / zero runtime dependency: extraction returns None offline; ships **inert** until the additive `analyst_coverage` DDL + service-role RLS are applied (load returns empty). Consensus **label** boundaries (`ANALYST_CONSENSUS_STRONG_BUY_FRAC` 0.80 / `ANALYST_CONSENSUS_BUY_FRAC` 0.50 / `ANALYST_CONSENSUS_SELL_FRAC` 0.50) are **display-only classifications, not decision thresholds**. `ANALYST_MIN_UPSIDE_PCT` (15) reserved for the Phase-2 Brief chip (unused in Phase 1). **Large-roundup robustness:** a CNBC "biggest analyst calls" roundup (20-30 separate calls) extracts in one pass — the call is budgeted `ANALYST_EXTRACT_MAX_TOKENS` (8000 output tokens) so the JSON array doesn't truncate mid-record, and `ANALYST_EXTRACT_TIMEOUT_SEC` (90s, vs the shared 30s) so the multi-thousand-token generation doesn't time out; both failure modes had surfaced as an identical blind "extraction failed". On any failure the real reason is captured (`analyst_intel.LAST_EXTRACT_ERROR`) and shown as a "Details:" caption, with guidance to paste a section at a time for an unusually large article. Plan: [docs/plans/analyst-coverage.md](plans/analyst-coverage.md). |
| F-154a | **Analyst Coverage Phase 2 — enrichment (SHIPPED, parts ①+②).** ① **Analysis drill-down "🏦 Analyst Coverage" tab** reconciles the two analyst-target sources so they never contradict: the **data-provider consensus** (`analyst_target`=`targetMeanPrice` mean, `num_analyst_opinions` breadth, `target_low`–`target_high` range, implied upside vs current price — the same mean the Base scenario in `targets.py` already uses) shown alongside **your saved Ideas Inbox research** (named firms + PTs + `consensus_rating` + thesis/catalysts/risks from `analyst_coverage`), with a factual reconciliation caption when both exist. ② **F-151 thesis-review evidence injection:** the newest saved coverage row for a held ticker is threaded into the reviewer's evidence (a new optional `analyst_consensus` key in `thesis_advisor.build_review_inputs`, rendered as an "External analyst coverage" prompt line) at BOTH call sites (on-demand + weekly Sunday cron), and flows into `inputs_hash` so fresh coverage can trigger a re-review. **Hard boundary:** `_SYSTEM_PROMPT` amended so provided consensus is **real, citable CONTEXT ONLY — it never upgrades a verdict** (a thesis whose stated rationale is contradicted stays WEAKENING/BROKEN even if analysts remain bullish); the reviewer still grades the user's specific claim against evidence, and analyst data never touches a composite/gate/verdict. Both parts awareness-only, fail-soft, no new constant. **Part ③ (general per-card Brief awareness chip) still NOT built** — a chip on every Brief card runs against the calm-advisor / anti-noise persona (§2B). **Narrow exception (2026-07-09, F-22c):** the awareness-only analyst note IS now shown on the **redeploy candidates inside the opt-in, button-gated Hard-Cap-Breach rebalance plan** — a rare, user-invoked surface (not the always-on Brief), where the user is actively choosing what to buy and the "Wall Street vs. your engine" context is directly decision-relevant. It reuses the F-154b render pattern (`_cached_analyst_coverage_recent` → per-candidate caption) and holds the same invariant: **display-only, never ranks/gates** (the engine composite remains the sole ranker/gate). |
| F-154b | **Analyst Coverage Phase 3 — Grow Today annotation (SHIPPED, display-only).** When a name the engine surfaces under "🆕 New Positions to Initiate" (`_grow_today` → `new_picks`) ALSO has recent saved coverage in the Ideas Inbox, its card carries a small awareness caption (`🏦 Your saved research: <consensus_rating> · avg PT $X (N firms) — awareness only`) — the corroboration moment where the user's paid research and the engine independently flag the same name. Render-layer only (`app.py`): an hourly `@st.cache_data` snapshot of recent coverage (one `load_analyst_coverage(days=ANALYST_COVERAGE_FRESH_DAYS)` query, newest row per ticker) + a per-card caption; renders only when coverage exists, fail-soft. **`daily_briefing.py` untouched — pick order, filtering, scores, and gates are unchanged.** The originally-optional **"soft tie-breaker" (rank picks by analyst conviction) was deliberately NOT built:** reordering recommendations by analyst data would influence the recommendation surface, violating the feature's locked awareness-only invariant and CLAUDE.md's "AI surfaces patterns but never tunes a gate." No new constant. **All Analyst Coverage phases now complete except the deferred Brief chip (③).** |
| F-154c | **Analyst Research Accountability — Research Scorecard (SHIPPED, display-only).** A dedicated 📊 Research Scorecard tab on 🧠 AI Insights tracks whether pasted analyst calls were directionally correct and whether price targets were hit — pure awareness, never feeds scoring or a gate. **Four blocks:** (A) Summary KPI row: Directional Accuracy %, PT Hit Rate %, Evaluable Calls count. (B) Per-call accuracy table: sortable columns (Ticker, Article Date, Consensus, Avg PT, Price @ Article, Exit Price, Return %, PT Proximity %, Status, Window); color-coded rows (green hit, red miss, grey pending). (C) Firm Leaderboard: per-firm aggregates (Calls, Dir. Accuracy %, PT Hit Rate %, Avg Return %) for firms with ≥`ANALYST_ACCURACY_LEADERBOARD_MIN_CALLS` (2) calls. (D) Best & Worst Calls highlight cards (top/bottom 3 by return %, gated on ≥`ANALYST_ACCURACY_HIGHLIGHTS_MIN_EVALUABLE` (5) calls). **Directional accuracy = endpoint close** (where the call ended up); **PT hit = intra-window HIGH** (≥75% of avg_pt, `ANALYST_ACCURACY_PT_HIT_PCT`, accommodates the short `ANALYST_ACCURACY_DIRECTION_DAYS` (30) window). Measurement endpoint is the earlier of (a) the sale date (if ticker sold after article), or (b) article date + 30 days. Pending calls (< 30 days), no-anchor rows (missing price_at_article_date), and rows with no consensus rating are excluded. Two new analyst_coverage columns: `price_at_article_date` (populated at save-time or via `scripts/backfill_analyst_prices.py`) and `composite_score_at_save` (reserved for Phase 3 engine-vs-analyst calibration, deferred). Rows with `no_anchor` status can be fixed on-demand via a per-row "Fetch" button (calls `analyst_intel.fetch_anchor_price()` live with yfinance→FMP failover) rather than waiting for the periodic backfill workflow. `classify_call()` and `is_bullish_rating()` in `stock_analyzer/analyst_intel.py`; `update_analyst_coverage_price()` in `stock_analyzer/db.py`. Awareness-only invariant locked: never touches `valuation_score()`, scoring gates, or any recommendation. |
| F-174 | **Pre-Earnings Playbook — CNBC enrichment (Earnings Playbook Phase 1, SHIPPED 2026-07-13; watchlist auto-add added 2026-07-13).** A "📅 Pre-Earnings" mode on the Ideas Inbox (alongside F-154's "📰 Stock Research" and F-175's Post-Earnings mode) lets the user paste a CNBC Pro earnings-preview article; `earnings_intel.extract_playbook()` (Sonnet 4.6) extracts, per covered stock, `beat_rate_pct` (ONLY from explicit Bespoke-style "beats estimates X% of the time" phrasing, never inferred from qualitative language like "strong history"), `recent_reaction_direction` (bullish/bearish/mixed/unknown, only from an explicit stated pattern), `consensus_growth_pct`, and `what_to_watch_cnbc` — an editable per-stock card preview saves selected records to `earnings_context` (§6.18). **"💾 Save earnings preview" also adds every saved ticker to the watchlist** (skipping tickers already present, `db.save_watchlist`) — a CNBC pre-earnings mention is a research signal in its own right, and without this the ticker would never enter `scan_sectors()`'s universe (curated `SECTOR_UNIVERSE` + watchlist `extra_tickers`) and so could never surface under Grow Today regardless of its composite score, mirroring F-154's Stock Research mode (which already treats Ideas Inbox saves as watchlist candidates). `earnings_advisor.build_earnings_playbook()` takes an optional `earnings_context` batch and threads `beat_rate`/`reaction` into `_recommend()`, which gains two new REDUCE conditions (weight ≥ 5% required for both): poor beat history (`beat_rate < EARNINGS_BEAT_RATE_REDUCE_THRESHOLD` (60.0) AND composite < `COMPOSITE_BUY` (65)) and bearish reaction history (`reaction == "bearish"` AND composite < `EARNINGS_BEARISH_REACTION_COMPOSITE_GATE` (75)) — and annotates the pre-existing HOLD_OR_ADD condition with the CNBC context when beat rate ≥ `EARNINGS_BEAT_RATE_STRONG_THRESHOLD` (75.0) or reaction is bullish. **CNBC data is evaluated strictly after `_recommend()`'s pre-existing EXIT check and can never override an already-determined EXIT.** Playbook cards on 🔔 Catalyst Watch show a "📰 CNBC" badge when context exists, plus beat-rate/consensus-growth lines, color-coded reaction-history text, and the CNBC "what to watch" note as the first bullet. `_earnings_posture_alerts_cache` (EXIT+REDUCE count) badges the Catalyst Watch nav entry. Ships inert until the `earnings_context` DDL is applied. Extends F-11. Plan: [docs/plans/earnings-playbook.md](plans/earnings-playbook.md). |
| F-175 | **Post-Earnings results capture — Finnhub auto-fetch (Earnings Playbook Phase 2a, SHIPPED 2026-07-13).** A "📥 Post-Earnings Results" mode on the Ideas Inbox: the user multi-selects held tickers that just reported (from `_last_held_tickers`), sets a report date, and clicks "⬇️ Fetch EPS from Finnhub" — `earnings_intel.fetch_recent_results()` calls Finnhub `/stock/earnings?limit=1` per ticker (90-day lookback, **no LLM call**) and returns `actual_eps`/`estimated_eps`/`eps_beat`/`eps_surprise_pct`/`quarter_period`. Revenue and guidance are NOT available on Finnhub's free tier, so each result card shows a Beat/Miss/Unknown chip with editable EPS fields plus a guidance-direction selectbox (raised/maintained/lowered/withdrawn/unknown) the user always sets manually, and an optional note. An "Enter manually" path shows blank cards for tickers Finnhub doesn't cover. Saves to `earnings_results` (§6.19). **Design note:** this replaces an original paste-based design — CNBC does not publish consolidated post-earnings roundups, making a per-ticker paste flow impractical; the LLM paste path (`earnings_intel.extract_results()`) remains in the module but is not wired to the UI. Ships inert until the `earnings_results` DDL is applied. Plan: [docs/plans/earnings-playbook.md](plans/earnings-playbook.md). |
| F-176 | **F-1 Earnings Thesis Checkpoint (Earnings Playbook Phase 2b, SHIPPED 2026-07-13).** When a held position has both a saved `user_thesis` (F-150) and an `earnings_results` row within the last 14 days (`db.load_earnings_result(ticker, lookback_days=14)`), an "Earnings checkpoint" prompt appears on 🧠 AI Insights → Positions. "Review earnings checkpoint" calls `thesis_advisor.generate_earnings_thesis_update()` (Sonnet 4.6), which derives a simple `earnings_signal` (beat/miss/mixed/unknown) from `eps_beat`/`rev_beat`/`guidance_direction` and returns a suggested `INTACT`/`WEAKENING`/`BROKEN` status + a 2–3 sentence rationale. **Suggestion-only — it never auto-saves.** Confirm writes a `thesis_reviews` row (F-151's table) tagged `summary="[Earnings checkpoint <date>] <rationale>"`; Dismiss clears the pending suggestion unsaved. Returns `None` on any failure (no API key, timeout, missing thesis/result) — the checkpoint prompt simply doesn't appear, matching the rest of the AI layer's graceful degradation. Plan: [docs/plans/earnings-playbook.md](plans/earnings-playbook.md). |
| F-196 | **Thesis Red Team Agent — Phase 1 (SHIPPED 2026-07-23, display-only).** New "⚠️ Red Team" tab on 🧠 AI Insights. Every trading day, each held position is scored 0–100 on four adversarial signals: (1) deterioration tier from `exit_signals` (WATCH=10pts, TRIM=22pts, EXIT=30pts); (2) 20-session relative strength vs SPY — negative RS contributes linearly up to 30pts via `compute_relative_strength()` in `exit_advisor.py`; (3) 5-session composite score delta from `thesis_erosion_cache.signals_snapshot` — falling composite contributes up to 25pts (self-referential bootstrap, inert for first 5 trading days); (4) analyst PT revision direction from `analyst_target_snapshots` — cut=15pts, flat=7pts, up=0pts. Total 0–100; display bands: 0–24 Intact · 25–49 Softening · 50–74 Eroding · 75–100 Breaking. Each signal renders with a 🔴/🟢 direction indicator and a plain-English interpretation. Cache: `thesis_erosion_cache` table, one row per (ticker, ET date). **Phase 2 — SHIPPED 2026-07-27, display-only.** Adds a Haiku-generated **counter-evidence** narrative (2–3 grounded counter-arguments, `stock_analyzer/thesis_red_team.py::generate_counter_evidence()`; UI-labelled "Counter-evidence," deliberately not "Bear case" — that term is reserved for the ⚔️ Debate feature's distinct `bear_case_score` mechanism, and the two sit near each other on Act Today cards) once a position's erosion score crosses `THESIS_EROSION_HAIKU_MIN` (30) **and** a `user_thesis` is on record, plus the **Pre-Mortem loop**: the investor's own `trades.premortem_commitment` (F-187) is read back as LLM context, and the model is instructed to quote it explicitly when current evidence supports it — closing the loop between "what would make me wrong" (ex-ante) and "here's what's actually going wrong" (continuous). **Deliberately excludes the erosion score/label and the still-inert PT-revision placeholder from the prompt** — an earlier review round caught that the aggregate score always launders a hardcoded PT placeholder back in, so the narrative is built only from primitive, individually-real signals (tier, RS vs SPY, composite delta, price/entry/age), each independently coerced through a finite-value check before ever reaching the prompt (a `None`/`NaN`/`inf` producer failure must never surface as a fabricated-looking citation). Output is `[{claim, severity, signal_basis}]`, 0–3 items — an empty list is a valid "no grounded counter-evidence found today" result, explicitly distinct from `None` (the call failed); this distinction is load-bearing for the day-cache (`counter_evidence` persists even when empty) and is tested via regression tests in `tests/test_thesis_red_team.py`. **Second surface:** a read-only "⚠️ Red Team — since you bought" expander on 🏠 Home's Act Today deterioration TRIM/EXIT cards (`_render_act_card`), sitting alongside the existing "⚔️ Challenge This Exit" debate button — a pure `thesis_erosion_cache` read, triggers no compute. **6 Opus design-review rounds** (plan `docs/plans/thesis-red-team-phase2.md`) — most catching a variant of the same bug class (a value assumed clean that could actually be `None`, `NaN`, or a truthy-but-garbage string) on a different signal each round; Opus pre-ship code review: SHIP, 0 blocking. **Phase 3 — SHIPPED 2026-07-28, commit `0aa1349`, display-only.** A "⚠️ Thesis Under Pressure" annotation at the end of 🏠 Home's Daily Brief (after the Buy Candidates overflow section, before Evening Debrief), for any held ticker where today's erosion score meets `THESIS_EROSION_BRIEF_MIN` (50) and its baseline (the most recent PRIOR trading day with an actual scored row — walked back up to `THESIS_EROSION_BASELINE_LOOKBACK_DAYS`=10 calendar days, since a `thesis_erosion_cache` row exists only on a day the user visited the Red Team tab, so literal calendar-yesterday is frequently blank) was below that floor, OR the score jumped `THESIS_EROSION_BRIEF_JUMP` (15) points or more since that baseline. No baseline found within the lookback window ⇒ treated as a first-ever observation, gated on the absolute floor alone. **Pure `thesis_erosion_cache` read — never triggers a compute** (compute only happens when AI Insights → Red Team is visited), so this annotation is usually empty on the day's first Home load and only populates once that tab has been visited today. **Deduped against tickers already shown as a deterioration/Act card in the same Brief run** via `decision_bucket._ticker()`'s canonical resolver (not a raw `ticker` key read — macro `PROTECTIVE_TRIM`/`WATCH` items carry `ticker=None` with the real subject in `action["trim_ticker"]`) — erosion score's tier component is sourced from the same `exit_signals` detector that fires those cards, so re-flagging an already WATCH/TRIM/EXIT ticker here would restate, not add, information. **Opus plan review: FIX-FIRST (2 blocking) → fixed → SHIP.** Round 1 caught that a literal calendar-yesterday baseline would silently re-fire every Monday for every stable ≥50 name (Sunday never has a cache row, so the code fell into the first-observation branch every week — a stable-state-noise regression against the app's calm-advisor posture) and the deterioration-card double-surface above; both fixed. Round 2 (re-review of the fix) caught the fix's own new gap — the dedup set read the raw `ticker` key, missing macro trim/watch items' `ticker=None`/`trim_ticker` shape — and a hardcoded `range(10)` lookback that should be a named policy constant per Hard Rule #1; both fixed (new constant `THESIS_EROSION_BASELINE_LOOKBACK_DAYS`, canonical `_ticker()` resolver). Final Opus review: SHIP, 0 blocking. **Neither the erosion score nor the counter-evidence narrative nor this annotation modifies composite score, any gate, or any recommendation — display-only, permanently. Closes the Thesis Red Team Agent feature — all 3 phases now shipped.** Plan: [docs/plans/thesis-red-team-agent.md](plans/thesis-red-team-agent.md) (Phases 1 & 3), [docs/plans/thesis-red-team-phase2.md](plans/thesis-red-team-phase2.md) (Phase 2). |
| F-195 | **Lessons Learned Library (Trade Journal Decision Context).** On every SELL / TRIM trade write in the Trade Journal, a structured `lesson_category` dropdown appears in the Decision Context expander — 10 fixed taxonomy options (Followed the signal, Thesis broke, Held too long, Cut too early, Ignored the exit signal, Position sized beyond conviction, Macro/sector headwind, Earnings surprise, Panic/fear sell, Pre-mortem call was right) plus "— (skip)" — complemented by the existing free-text `lesson` field (relabeled "Details (optional)"). `decision_journal.compute_patterns()` returns `lesson_category_counts` (frequency dict per category) and `lesson_category_by_outcome` (wins/losses/avg_pnl cross-tab per category). Trade Journal Decision Journal section upgraded to "Pattern Library": displays each category's frequency count + W/L outcome summary in a KPI grid, then the full entry log with category chips on each trade. Awareness only — zero gates, zero investment recommendations. |
| F-197 | **Multi-Agent Debate Agent — Phase 1: entry candidate debates (SHIPPED 2026-07-23, display-only).** A "⚔️ Debate" button on every 📈 Grow Today entry candidate card runs a structured 4-round Bull vs Bear debate followed by an impartial Judge verdict — 5 sequential Haiku calls (`claude-haiku-4-5-20251001`): Bull opens → Bear responds → Bull rebuts → Bear closes → Judge scores both sides 0–100 and issues `verdict` (`bull_wins` when the score gap ≥ `DEBATE_WIN_MARGIN` (20, module-level in `stock_analyzer/debate_agent.py`, not a gate/policy threshold) / `bear_wins` / `contested`), a one-sentence `key_dispute` (the specific claim Bull and Bear most disagree on), and a `grounded` flag (false when either side argued generically instead of citing supplied evidence). Both agents debate the identical evidence corpus (`build_entry_corpus()`: composite score + 4 pillar scores, sector, 5-/20-day momentum, 20-session relative strength vs SPY via `compute_relative_strength()`) — no information asymmetry between agents in Phase 1. **User-triggered only** — no cron, no page-load compute. Day-cached to `debate_cache` (one row per `ticker` × `debate_type` × ET date); a cache hit renders instantly with no new LLM calls. Capped at `DEBATE_SESSION_CEILING` (3) new debates per session — cache hits don't count against the cap. The button is hidden unless the ticker has a fetched composite score, and a failed/empty debate run is never cached and never consumes a session slot, so a transient API failure can be retried immediately rather than showing a false "Contested" verdict until the next day. **Debate verdict never modifies the composite score, reorders Grow Today candidates, or feeds any gate — purely a deliberation aid the user reads before deciding.** **Phase 2 — "Challenge This Exit" (SHIPPED 2026-07-24, D2 of Agentic Intelligence Roadmap v2):** a "⚔️ Challenge This Exit" button on single-name deterioration TRIM/EXIT cards (Today's Brief 🔴 Act Today lane; gated on the card's `kind` field, never WATCH) runs the same 5-Haiku debate in `debate_type="exit"` mode — the Bull defends continuing to hold (anchored on the saved `user_thesis` when present, and explicitly barred from sunk-cost "hold because we're underwater" reasoning), the Bear argues the fired deterioration signal citing the real `exit_advisor.assess_holding` numbers (drawdown-from-peak, sessions below trend, relative strength) via `build_exit_corpus()`. The 4 round prompts branch entry/exit; the Judge is unchanged (`bull_wins`=hold defensible, `bear_wins`=exit supported, `contested`). Shares the `debate_cache` table (`debate_type="exit"`) and the `DEBATE_SESSION_CEILING` per-session cap with entry debates. **A second opinion only — never suppresses the card, changes the deterioration tier, or touches the composite.** **Phase 3 — Debate Log (SHIPPED 2026-07-27):** a new "⚔️ Debate Log" 6th tab on 🧠 AI Insights, read-only and diagnostic, showing every stored `debate_cache` row (both `entry` and `exit` types) chronologically via new `db.load_all_debates()` (most-recent-first, `debate_date` then `created_at` as a same-day tiebreak; excludes `corpus_snapshot`, not needed for display). Built **forward-only** per an explicit user decision — `debate_cache` held only ~4 rows (all entry-type) when this shipped, and the feature renders an honest "no debates yet" state at any volume rather than waiting for more data to accumulate (same precedent as Behavioral Fingerprint, F-193). Each row expands to show the full transcript, scores, key dispute, and grounded warning via two functions extracted from the two existing render sites: `_debate_verdict_icon(verdict, debate_type)` (pure — icon/label only, used by the caller for the expander title, entry/exit-only tabs, and the log alike) and `_render_debate_result(row, debate_type)` (body rendering, including the exit-only "does not change the recommendation" disclaimer). This extraction closed a real risk caught in Opus design review: the original refactor sketch silently dropped that exit-only disclaimer and used a circular return-value contract that was unusable at either existing call site — both fixed before any code was written. No filter/search UI or aggregate chart in this phase (deliberately deferred — the original plan's own "What NOT to build" section defers a historical chart to "Phase 3+," and a filter UI has nothing to filter at today's volume). No new `constants.py` entries, no new cache table, no new LLM call — the cheapest class of addition on this roadmap. **This closes the entire Multi-Agent Debate feature and the last of the three originally-tracked Agentic Intelligence Roadmap gated phases.** Plans: [docs/plans/multi-agent-debate.md](plans/multi-agent-debate.md), [docs/plans/exit-red-team.md](plans/exit-red-team.md), [docs/plans/multi-agent-debate-phase3.md](plans/multi-agent-debate-phase3.md). |
| F-225 | **Portfolio Q&A (💬 Ask) — SHIPPED 2026-08-01, display-only.** New 7th tab "💬 Ask" on 🧠 AI Insights, sibling to Debate Log. A retrospective natural-language Q&A surface over trade history and past recommendations — **deliberately not a live `session_state` reader**: the two supported question shapes are "how many trades did I make in a date range, and what was the gain/loss on each" and "why did a past recommendation lose/gain money over N trading days." Two-step LLM pattern (`stock_analyzer/portfolio_qa.py`), not an agentic tool-loop: (1) `parse_question()` (Haiku) turns free text into a structured `{intent, ticker, start_date, end_date, horizon_days}` query — today's date is passed in explicitly so relative phrases ("last week") resolve against the app's actual calendar, not the model's guess; a question that doesn't fit either shape, or a `rec_outcome` question with no resolvable ticker, returns `intent: "unsupported"` rather than guessing; (2) a plain-Python deterministic lookup runs (`trades_in_range()` — SELLs report the already-stored `realized_pnl`, BUYs report unrealized P&L against the ticker's current price only if still held, else labelled "position_closed" rather than attempting new FIFO-lot matching; `recommendation_outcome()` — exact ticker/date match against `recommendations`, **never guesses the nearest one**, returns `found: False` explicitly when no row matches); (3) `narrate_answer()` (Haiku) turns the result into plain English, instructed to use ONLY the given facts and state plainly when a pillar sub-score wasn't recorded rather than inferring a reason. **The parsed query is shown back to the user as a caption before the answer** (e.g. "Looking at: AAPL, surfaced 2026-07-20, +5 trading days") so a misparse is visible, not silently wrong. Fails open throughout (`None` on any API error/missing key) — shows the same explicit "AI layer offline" state as every other AI Insights tab. **Companion change (same date):** `recommendations` gained three nullable columns (`t_score`, `bq_score`, `val_score`, alongside the existing `s_score`) so recommendations surfaced from this date forward carry the full 4-pillar breakdown that explains *why* a composite score landed where it did — before this, only the bare `composite_score` was persisted even though the pillar breakdown was already computed in memory at brief-build time and simply discarded. **Forward-only — existing rows are not backfilled**, so `rec_outcome` answers for pre-2026-08-01 recommendations are capped at composite score + the auto-generated thesis line, and the narration says so explicitly rather than fabricating a pillar reason. New constants: `QA_REC_OUTCOME_DEFAULT_HORIZON_DAYS` (5), `QA_MAX_RANGE_DAYS` (365) — query-scoping parameters, not investment-policy thresholds. **Never gates, scores, or modifies any recommendation — display-only, same class as the rest of AI Insights.** **v1.1 (same day):** live use surfaced two real UX gaps. (1) A third question shape was added — `trade_lookup` ("what was my trade on AAPL?", no date range needed) via a new `trades_for_ticker()` function (shares the same `_row_outcome()` P&L logic `trades_in_range()` uses, refactored out to avoid duplicating it). (2) An `unsupported` decline used to give zero indication of *why* — now every `unsupported` result carries a `reason` string (Python-determined for the two structural gaps — "needs a date range" / "couldn't confidently identify a ticker" — or the model's own short explanation for anything else), shown directly in the UI instead of a generic three-bullet fallback. The strict "never guess a ticker" rule itself was deliberately kept (a real live decline: "Robin Hood" wasn't confidently mapped to `HOOD`) — the fix is telling the user why it declined, not loosening the anti-hallucination guard. (A separate, prior same-day fix already closed a `parse_parsed_query` JSON-parsing bug where Haiku's trailing prose after a `` ```json `` fence broke `json.loads` — see `docs/plans/portfolio-qa.md` for both incidents.) **v2 (2026-08-02), three "complete the circle" additions, all retrospective narration only — no new advice, gate, or recommendation:** (1) Answers now surface a trade's own recorded `user_thesis`/`notes`/`lesson` (whichever are non-empty, joined into one short "reasoning" line) inline in `trades_in_range`/`trade_lookup` answers and in the "Show the underlying trades" table's new Notes column. (2) The Ask tab is now conversational (`st.chat_message`/`st.chat_input`, first use of this Streamlit pattern in the app) — `st.session_state["_qa_history"]` holds the turn list; `parse_question()` gains an optional bounded `history` param (last `QA_HISTORY_TURNS` exchanges) so a referential follow-up ("what about MSFT instead?") resolves, but `narrate_answer()` deliberately still sees ONLY the current query's facts, never conversation history — multi-turn context helps interpret what's being asked, never what's true. (3) A `rec_outcome` answer now cross-references the matching BUY trade (found via a new `_find_buy_trade_for_rec()`, matching on ticker + a `QA_PREMORTEM_TRADE_MATCH_WINDOW_DAYS`-day window after the recommendation date — never guessing further out) and narrates its recorded Pre-Mortem risk case (`premortem_case_against`) and exit commitment (`premortem_commitment`) against what the price data actually shows, retrospectively only; when no matching BUY exists at all, the answer says so plainly rather than staying silent. **A fourth idea — a forward-looking "what's the current call on X" intent — was explicitly considered and declined**: the user chose to keep the "AI narrates, never originates" redline closed for this pass; recorded as a parked, not-yet-approved provision in `docs/plans/portfolio-qa.md` rather than built. New constants: `QA_HISTORY_TURNS` (3), `QA_PREMORTEM_TRADE_MATCH_WINDOW_DAYS` (3). **v2.1, same day — closed-lot P&L bug fix (a live user cross-check of an AAPL trade history caught this):** the "still held → unrealized" check above was ticker-level only (is this ticker held ANYWHERE right now), not per-lot — so a ticker with a full sell-then-rebuy round trip had **every** historical BUY row marked "Unrealized (still held)" against today's price, including lots already fully closed weeks earlier, double-reporting P&L already shown on the matching SELL row. Fixed via a new `_closed_shares_by_buy()` that runs the existing, already-tested `investor_mirror.build_closed_lots()` FIFO matcher over the ticker's full trade history (not just the queried range) to tell a fully-closed lot apart from a genuinely open one; only the fully-closed case was corrected — a partially-sold lot still marks against the full original share count, which the UI already caveats separately. Plan: [docs/plans/portfolio-qa.md](plans/portfolio-qa.md). |

### 3.13 Recommendations History (retrospective scorecard)

The 📊 Recommendations History page is the **rule-based** retrospective over every pick Today's Brief has surfaced (no LLM): it cross-references the `recommendations` table with `trades` to grade acted vs. missed outcomes on **alpha** (return minus SPY over the same window). It is the data substrate the F-4 monthly report (F-153) narrates. Logic lives in `recommendations_history.py` (pure — no Streamlit / DB / API). (2026-07-15: restructured into three internal tabs — **📊 Summary**, **📈 Trends**, **📋 Full Table** — for navigability; no content added or removed.)

| ID | Requirement |
|----|-------------|
| F-160 | **Scorecard rollups.** Action rate, avg acted vs. missed outcome + **alpha**, best/worst named outcomes, daily acted-vs-missed volume chart, and breakdowns by composite band (`by_composite_band`), cross-check verdict (`by_verdict`), and rec type. Outcome aggregates exclude recs younger than `REC_SCORE_MIN_DAYS` (5 days) — shown but flagged ⏳ maturing. "Total recs" counts *surfacings* (a name recurs each day it qualifies), not distinct tickers. **Known historical gap (fixed going forward 2026-07-26, commit `51b2441`):** `buy_candidate` recs surfaced *before* this fix never captured a price snapshot, so the Conflicted verdict bucket and the Hold-zone/Sell-zone composite bands show blank missed-alpha for those older rows — not a data-maturity issue, a permanent gap for that historical slice. New `buy_candidate` recs resolve correctly. |
| F-160a | **Signal-flow Sankey.** A Plotly Sankey funnels New Positions to Initiate → acted / not-acted → win / loss / flat-or-open (`signal_flow`). **Distinct by ticker** (not surfacings); scoped to `new_pick` (awareness feed excluded, same as F-161); win/loss count **matured** outcomes only (⏳ maturing → Flat / Open) so it can never disagree with the F-160 aggregates. Built from the pre-status-filter snapshot so both branches always render regardless of the acted/missed filter. |
| F-161 | **Missed Opportunity analysis.** Collapses **actionable** recommendations the user **never acted on** to **distinct tickers** (`distinct_missed`, default `rec_types=("new_pick",)`), split into **missed winners** (rose) vs **dodged losers** (fell) with named outliers (biggest miss / biggest dodge, `missed_split`) and a ranked green/red horizontal bar of skipped-name outcomes. **Scope = New Positions to Initiate only** (`new_pick` — names that cleared all gates); the awareness-only **More Buy Candidates** feed (`buy_candidate` — Conflicted / Unverified / sub-`COMPOSITE_BUY` names the App flags to skip) is **excluded**, because skipping those is the *correct* call, not a miss. The acted-on check spans **all** of a ticker's surfacings (a name bought on a buy-candidate day is not wrongly flagged), but the representative outcome + count come from its `new_pick` surfacings only. Magnitude is framed **per-$1k notional** (a read on *selection* quality) — **never** a fabricated portfolio-level "you would have gained X%" counterfactual, because capital, the 15%/35% concentration caps, and the entry gates all bind. Same actionable scope governs the F-153 monthly report (`build_report_package(rec_types=("new_pick",))`). |
| F-201 | **Missed-Opportunity Pattern (O1 of Agentic Intelligence Roadmap v2) — SHIPPED 2026-07-24, commit `b11e34a`.** New "🔍 Missed-Opportunity Pattern" section on 📊 Recommendations History → Summary tab, sibling to (not nested inside) F-161's flat missed-opportunities list — built from the **unfiltered** `_rh_enriched_all` snapshot, deliberately not the page's status-filtered `_rh_enriched`, so `distinct_missed()`'s "acted via ANY surfacing" safeguard can't silently break under the Acted-only/Missed-only dropdown. Deliberately the "P6 done right" idea (P6/Autonomous Pattern Discovery was shelved the same day — its only untapped dimension, `decision_context`, had 17 rows); this points the same unsupervised-discovery instinct at real, already-accumulating data instead. A naive statistical rebuild (sector/price-band bucketing with a `min_n` gate, mirroring `predictive_analytics.py`'s `by_sector_alpha`/`by_conviction`) was ruled out during planning — only 44 distinct tickers have ever surfaced as `new_pick`, so most buckets would sit at 0-1 members, the same wall that sank P6. Instead, one Haiku call/day (`stock_analyzer/missed_opportunity.py::generate_missed_opportunity_patterns()`) finds a **descriptive** pattern (never causal — "what these skipped names have in common," not "why you skipped them," to avoid inviting an unverifiable psychology claim that reads as forward-looking advice) across F-161's missed-ticker corpus, enriched with 3 pre-computed **closed-category** fields — `sector` (empty→"Other"), `price_band` (fixed bands), `composite_band` (reuses `by_composite_band`'s exact `COMPOSITE_STRONG_BUY`/`COMPOSITE_BUY`/`COMPOSITE_HOLD` thresholds, no new breakpoints) — alongside the already-present `verdict`/`outcome_label`. A **two-layer fabrication guard** (pure Python): every cited ticker must exist in the real corpus, AND the claimed `shared_dimension`/`shared_value` must mechanically match that specific ticker's real value for that specific dimension — a non-conforming ticker is dropped from its pattern, not the whole pattern discarded. Every field here is a closed, checkable category, so this guard is **stricter** than free-text quote-matching, not a weaker analog of it. Includes ALL outcomes (not winners-only — a posture safeguard against becoming a FOMO amplifier) and renders each pattern's real win/dodge/flat mix, so a pattern can never read as "these were all winners, buy the next one like it." **Never cross-references a found pattern against current Grow Today candidates, the watchlist, or held positions** — the one design choice that would turn a retrospective pattern into a disguised forward-looking buy signal; any future proposal to add such a join needs its own Opus review. Day-cached to new `missed_opportunity_cache` table (RLS included). No `constants.py` entries — sample-size floors (`_MIN_MISSED_TICKERS=3`, matching `predictive_analytics.py`'s `min_n=3` convention; `_MIN_PATTERN_TICKERS=2`) are module-level tuning, not policy thresholds. **Diagnostic only — never gates, scores, or modifies `distinct_missed()`/`missed_split()` themselves.** Opus design review (FIX-FIRST → resolved): the original "no verification layer needed" reasoning was backwards (structured fields are *easier* to mechanically verify than free prose, not harder) — fixed by adding the predicate-verification guard; also fixed a data-source bug where the naive implementation would have read the page's filtered snapshot instead of the unfiltered one. Opus pre-ship code review: SHIP, 0 blocking, 2 of 4 non-blocking fixed (a constant-drift risk between `app.py` and the module; a cosmetic composite-band label mismatch vs `by_composite_band`). Plan: [docs/plans/missed-opportunity-pattern.md](plans/missed-opportunity-pattern.md). |

---

## 4. Non-Functional Requirements

### 4.1 Performance

| ID | Requirement |
|----|-------------|
| NF-01 | Portfolio data (held tickers) must load within 15 seconds on app open |
| NF-02 | Individual stock analysis (load_all) must complete within 10 seconds for a single ticker |
| NF-03 | Market Scanner full run must complete within 30 seconds for the 73-ticker universe |
| NF-04 | Cached data (load_all, sector returns) must be served from cache within 1 second on repeat calls |

### 4.2 Data Freshness

| ID | Requirement |
|----|-------------|
| NF-10 | Live prices refresh every 60 seconds during market hours via Streamlit fragment. During market hours the live-price primary (Finnhub) returns real-time US quotes (vs yfinance's ~15-min delay); when the market is closed all sources report the last regular-session close. |
| NF-10a | The held-position price cross-check is cached 5 minutes (`_cached_price_xcheck`) — a periodic integrity check, not a per-rerun live call — to bound keyed-provider quota usage. |
| NF-11 | load_all() cache TTL: 30 minutes (1800 seconds) |
| NF-12 | Sector ETF returns cache TTL: 60 minutes (3600 seconds) |
| NF-13 | Market indices cache TTL: not cached — fetched on each Daily Briefing load |
| NF-14 | When market is closed, the app must display a context note indicating data reflects last close |
| NF-15 | Pre-market intel cache TTL: 5 minutes (300 seconds); keyed on held tickers + watchlist to ensure correct data after holdings change |
| NF-16 | Risk-free rate (^IRX) cache TTL: 24 hours (86400 seconds); fallback to 4.5% if Yahoo Finance unavailable |
| NF-17 | All date comparisons must use America/New_York (ET) timezone via pytz to prevent midnight UTC rollover producing wrong calendar dates — Streamlit Cloud's worker runs UTC; the Railway pilot container's system clock has not been independently verified, so the pytz conversion must not be assumed unnecessary there |

### 4.3 Reliability

| ID | Requirement |
|----|-------------|
| NF-20 | Yahoo Finance API failures must be caught and displayed as warnings, not crash the app |
| NF-21 | Rate-limit (HTTP 429) responses from Yahoo Finance must be retried with linear backoff (3 attempts, 3s base) |
| NF-22 | API health events (`success`, `rate_limit`, `error`, `empty`, `auth`, `quota`, `parse`) must be recorded via the `api_health` module per provider (`yahoo_finance`, `finnhub`, `fmp`, `fred`, `supabase`). Event routing goes through `classify_error()` (`providers/_util.py`): `auth` = 4xx credential/plan errors, `quota` = 429 with quota-exceeded wording, `rate_limit` = generic transient throttling, `parse` = JSON-decode or empty-body failures, `error` = everything else. Health thresholds: RED on `auth_errors ≥ 1` or `rate_limits ≥ 3` or `consecutive_errors ≥ 5`; YELLOW on `quotas ≥ 1` or `parse_errors ≥ 3` or error-rate > 20%. The provider circuit-breaker cooldown trips on `auth_errors ≥ 3` (matches `rate_limits` threshold). |
| NF-23 | Supabase connectivity failures must display a user-facing error without exposing credentials |
| NF-24 | No single market-data source is a hard dependency for live prices or the analysis bundle. A provider hard-failure (error, rate-limit after retries, or empty) must fail over to the next configured provider in the chain; the request fails only if every capable provider fails. |
| NF-25 | Keyed-provider errors must never leak the API key — error text surfaced or logged must have the key redacted (e.g. FMP's `?apikey=` in request URLs). |
| NF-26 | The headless alert cron (§3.11) must be **fail-safe and non-blocking**: it always exits 0 (a failure never auto-disables the workflow), is inert when `RESEND_API_KEY` is absent (computes/emails nothing rather than erroring), and uses per-ET-day idempotency guards (Supabase `alert_state`) so a job firing on both DST-straddling UTC slots delivers at most one email per lane per day. |
| NF-27 | The cron and the app must share one data + credential path: `bundle_loader.load_bundle` (extracted from the app's `load_all`) and env-first credential resolution (`SUPABASE_URL`/`SUPABASE_KEY` → `st.secrets`). The cron's Supabase client is a module-global singleton (`db._CLIENT`), not `st.cache_resource`, so it runs without a Streamlit context. |
| NF-28 | **FMP daily quota counter (rate-limit-resilience Phase 3).** `db.increment_daily_quota("fmp")` must fire after every successful FMP request; `db.get_daily_quota("fmp")` reads today's count from `api_quota_log` (§6.17 schema). When count ≥ `FMP_DAILY_SOFT_CAP` (220), `orchestrator._providers_for()` must drop FMP from the capable list; it falls through to the full list if all providers are suppressed — never a hard-block. **DDL applied in Supabase (2026-07-10) — feature is active.** `api_health.get_fmp_daily_quota()` caches the Supabase read (5-min TTL) so the orchestrator hot-path is never a per-call DB hit. |

### 4.4 Security

| ID | Requirement |
|----|-------------|
| NF-30 | All secrets (Supabase URL/key, LLM API keys, market-data keys `FINNHUB_API_KEY` / `FMP_API_KEY`, `[fred] api_key`) must be stored in the hosting platform's secret store, never in code or committed files. On Streamlit Cloud that's the Secrets dashboard (native TOML); on the Railway pilot it's flat Service Variables, materialized into a real `.streamlit/secrets.toml` at container startup by `railway_start.sh` (docs/architecture.md §9.1b) so the ~50 direct `st.secrets.get(...)` call sites work unmodified on either platform. A missing market-data key degrades gracefully (provider skipped), never crashes. The headless cron (§3.11) reads the same secrets from **GitHub repo secrets** (incl. `RESEND_API_KEY` / `ALERT_EMAIL_TO` / `ALERT_EMAIL_FROM`) independent of either UI host; its Supabase key is the same service-role key class as Streamlit (RLS stays on per NF-32). |
| NF-31 | Single-user personal app, gated by a password screen (`app.py::_check_password()`) — not multi-user/role-based auth. `[app] password` / `APP_PASSWORD` grants full (owner) access; an optional `[app] readonly_password` / `APP_READONLY_PASSWORD` grants a read-only viewer role (§ readonly viewer, `project_readonly_viewer`). A failed-attempt counter adds a 2s delay after 3+ fails and a 5-minute lockout after 10 fails (`_login_fails`/`_login_locked_until` session_state) — added for the Railway pilot, which has no Streamlit-native "Private app" OAuth layer on top of the password gate. |
| NF-32 | Row Level Security is **enabled** on all public-schema tables with `FOR ALL TO service_role` policies. The Streamlit secret `[supabase] key` must be the service-role / secret key (bypasses RLS); the publishable/anon key has no matching policy and is denied. This is defense-in-depth: a leaked publishable key cannot access portfolio data. |

### 4.5 Usability

| ID | Requirement |
|----|-------------|
| NF-40 | All navigation between pages must use session-state-based routing (nav_page) without page reload |
| NF-41 | Analyze buttons must navigate to Stock Analysis and set a Back button to return to the origin page |
| NF-42 | The app must be usable on a standard desktop browser; no mobile-specific layout required |
| NF-43 | Urgent action count must be visible on Today's Brief without any click (2026-07-12: Today's Brief is a promoted full-width section, not a tab, so this is now always-on rather than a tab-badge) |
| NF-44 | Discovery movers surface under "New Positions to Initiate" with the context label **"Breakout today"** (not the scanner momentum score). The momentum score is not applicable to movers — they enter via day-change breakout, bypassing the momentum gate, and the score does not appear in Analysis Deep Dive. `reconcile_signals()` in `signal_reconciliation.py` accepts `is_mover: bool = False`; when `True`, `mom_str = "Breakout today"`. The "Momentum X/100" prefix is omitted from the mover header chip in `app.py`. |

### 4.6 Maintainability

| ID | Requirement |
|----|-------------|
| NF-50 | All domain logic (scoring, signals, risk, briefing) must live in the `stock_analyzer/` package, not in `app.py` |
| NF-51 | `app.py` is responsible for UI rendering and orchestration only |
| NF-52 | New database columns must be backward-compatible: `load_trades()` must backfill None for missing columns in older rows |
| NF-53 | Every decision threshold (beta tiers, sector limits, single-name ceiling, composite boundaries, risk-per-trade, imminence windows) must be defined in `stock_analyzer/constants.py`. Features import from this module — they never hardcode threshold values. Changes are policy decisions, not code tuning. |
| NF-54 | Cross-feature coordination must use `st.session_state` caches. One feature publishes; downstream features read and gate. When a producer fails, the cache is set to `None` (not an empty container) so consumers can render an explicit "offline" state rather than silently disabling the gate. |
| NF-55 | Data integrity failures (missing stop, missing composite, missing daily briefing) must surface visibly to the user rather than being masked with fabricated fallback values. |
| NF-56 | Date-aware temporal chips on Grow Today cards. Every Grow Today card displays two pill chips at the bottom: the existing grey **⏱ First surfaced** chip and a new blue **🕐 Score as of** chip (color `#7dd3fc`, border `#1d4ed8`). The Score-as-of chip shows when the composite was last evaluated; the First-surfaced chip shows when the pick first appeared in Grow Today. Both chips apply the same date-aware formatting rule: if the timestamp's ET date equals today (`_et.date() == _today_et()`), show time only (e.g. `11:25 AM ET`); if it is a prior day, prepend the date (e.g. `Jul 14 · 4:58 PM ET`). This prevents overnight-cached or Supabase-fallback bundles from appearing to be today's evaluation. `_fmt_first_seen()` in `app.py` (covers all four First-surfaced call sites: new picks, add candidates, Evening Debrief ×2); `_score_as_of_str` in the Grow Today card render block. The `fetched_at` field is added to `load_bundle()`'s return dict in `stock_analyzer/bundle_loader.py` and flows through `composite_fetched_at` in the pick dict (`stock_analyzer/daily_briefing.py:845`) to the chip render. |

---

## 5. Out of Scope

- Order execution or brokerage integration
- Multi-user / multi-portfolio support
- Mobile app
- Real-time streaming data (WebSocket feeds)
- International or non-US equity markets
- Options, ETFs, or fixed income instruments
- Automated trading or algorithmic execution
- Day-trading / intraday support (Level 2, order flow, tick charts, momentum-only signals) — counter to the intended persona; see §2B
