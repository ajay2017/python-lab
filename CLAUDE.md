# Claude Project Guidance — DRISHTA · Beyond Noise

A directive distillation for any Claude session working in this repo. Read first; obey throughout. Detail lives in [DEVELOPMENT.md](DEVELOPMENT.md) and [docs/](docs/).

---

## Project orientation

Personal portfolio intelligence app for a single user. **Primary deploy is Railway Hobby at `drishta.up.railway.app`**, auto-deploying from `main` — cut over 2026-08-15 after a 3-week parallel pilot that ran clean (see [docs/plans/railway-migration.md](docs/plans/railway-migration.md) for the full phase log and [DEVELOPMENT.md](DEVELOPMENT.md) for the secrets architecture). The Streamlit Community Cloud app is **kept dormant as a cold fallback** — it still auto-deploys from `main` against the same Supabase DB, but it is no longer the deploy you verify against, and its free-tier throttling notices are expected noise. Use it only if Railway itself is down. As of 2026-08-07 the same Railway project also owns all scheduled cron lanes — 7 (premarket/scan/intraday/eod/weekly thesis/weekly maintenance/daily broker sync — the `broker` lane was added 2026-08-18 with F-244; `system_health._LANES` is authoritative) — via dedicated native Cron Job services, migrated off GitHub Actions after a platform-wide GitHub Actions incident exposed its `schedule` trigger's best-effort delivery — see memory `project_cron_railway_migration`; `.github/workflows/alerts.yml` is now manual-dispatch-only. Never run locally — both deploys assume their respective hosted secrets delivery, not a local `.env`/`secrets.toml` dev loop.

## Operating posture

**The app decides, it does not inform.** Recommendations are issued as actionable calls; gates are hard suppressions with visible banners, not soft warnings. When in doubt, recommend nothing rather than recommend wrongly. See [docs/requirements.md §2A](docs/requirements.md).

---

## Hard rules

1. **Never hardcode decision thresholds.** Every gate / threshold / boundary value lives in [`stock_analyzer/constants.py`](stock_analyzer/constants.py). Import from there. Changing a value there is an investment-policy decision — discuss with the user before changing.

2. **Never disable RLS.** Supabase tables are protected by `FOR ALL TO service_role` policies. The `[supabase] key` secret must be the service-role / secret key (not publishable). If you see "row-level security blocking" errors, the fix is to correct `SUPABASE_KEY` in Railway → Variables and redeploy the service — not to disable RLS.

3. **Never run the app locally to test changes.** Push to `main`, wait ~2 min for the Railway auto-redeploy, hard-refresh the browser (Ctrl+F5) at `drishta.up.railway.app`. (The dormant Streamlit Cloud app redeploys off the same push; it is not the surface you verify against.)

4. **Any commit touching `stock_analyzer/constants.py`, a gate, a scoring/recommendation formula, or a DB-write / data-integrity path requires an Opus review before it ships, cited in the commit body.** The citation format is mechanically enforced (see below) and must carry three facts you only have after actually reading a reviewer's output:

   ```
   Review = Opus reviewer (<resolved model>): SHIP|FIX-FIRST, N blocking; <notes>
   ```

   The `reviewer` subagent's `model: opus` pin ([`.claude/agents/reviewer.md`](.claude/agents/reviewer.md)) is a generic alias, not a fixed version — it auto-follows whatever Opus release the account currently resolves it to, so the reviewer's own output states which specific version it ran as (its `MODEL:` line); copy that into the citation rather than assuming a number from memory. This applies **regardless of which model is running the main session** — invoke the `reviewer` subagent explicitly; don't rely on the main session's own judgment as a substitute. A commit in this category with no review citation is itself a defect — flag it. (Two 2026-07-15 commits shipped without this citation and needed a retroactive review to close the gap — see `docs/cost-routing.md`.)

   **Invoke the reviewer automatically — do not ask first** (user decision, 2026-08-15). When a change lands in a review-required category, run the `reviewer` subagent and report the verdict; don't pause to request permission. Waiting to be asked is how a required review gets skipped, because it depends on the lead remembering to raise it. If a session-level instruction says not to call agents unprompted, this rule is the standing request that satisfies it for the `reviewer` lane.

5. **A `feat(` commit must state who designed it and who built it**, via two trailers the hook enforces:

   ```
   Design = planner (<model>): <verdict>     OR   Design = lead — <why no planner>
   Build  = implementer (<model>)            OR   Build  = lead — <why no implementer>
   ```

   New user-facing features route design through `planner` (Opus) and the build through `implementer` (Sonnet), so the agent that wrote the code is never the agent that reviews it. `lead` is an accepted answer — the requirement is a **deliberate, permanently-recorded statement**, not a forced handoff. Both trailers are self-attested (see the honesty caveat below).

---

## Review & test economy (quality at least cost)

The deterministic gates are the safety net; the **paid agents are for judgment only.** Spend tokens where a wrong call moves money — not on re-checking what a hook already guarantees for free. (Codified 2026-08-04 as a cost/quality-balance pass; see memory `feedback_recurring_defect_gate`.)

> **What the hook can and cannot prove (be honest about this).** `pre_tool_checks.py` verifies that a *correctly-formatted citation string is present* — it cannot verify that a `reviewer` subagent actually ran. **The commit hook cannot** — though that is not a permanent ceiling: a `PreToolUse` matcher on `Task`, or a `SubagentStop` hook, could append a reviewer-invocation record that the commit gate then requires, which would prove the subagent *ran* (still not that the quoted verdict is faithful). Not built; noted so the ceiling isn't mistaken for a law. The hardened format (resolved model + verdict + blocking count) raises the bar from "a habit phrase" to "three facts you only get from reading real reviewer output", which defeats lazy and accidental citations but not a deliberately fabricated one. Same caveat for the `Design =` / `Build =` trailers. **Treat these as honesty mechanisms with a permanent git-history audit trail, not as guarantees** — the real protection is that the reviewer runs in a *separate agent instance with its own context*, so it re-derives from the diff instead of inheriting the author's reasoning. That independence is what caught the 2026-08-15 green-heartbeat bug the author had read past four times.

- **Deterministic gates (free, always-on) are the real pre-deploy safety net.** The `pre_tool_checks.py` commit/push hook runs the full `pytest` suite and blocks on failure; the same hook + CI run `check_antipatterns.py`; and the suite itself asserts `check_constants_documented.py` is green and that `app.py`/`cron_runner.py` byte-compile (`tests/test_repo_hygiene.py`). Streamlit auto-deploys from `main` regardless of CI, so **the local hook is what actually blocks a broken push.** Trust it — do not re-run the suite through an agent "to be sure"; that's cost, not rigor.
- **`test-runner` (Haiku) is NOT a mandatory per-change stage.** The pytest hook already runs the suite deterministically for $0 and covers everything the agent's old checklist did (full suite + `py_compile` of the un-imported entrypoints + constants-doc). Invoke `test-runner` only when the hook can't be relied on (a session that started before the hook loaded — see `feedback_hook_enforcement`; or work done outside Claude Code's own tools), or as a cheap pre-filter before an expensive Opus review on a large change. Don't pay Haiku tokens to duplicate a deterministic gate.
- **Tier the work by the model pins, which hold regardless of the session's default model** (the `.claude/agents/*.md` `model:` frontmatter is authoritative unless a per-call override is passed, or the `fork` agent type is used): **design of money-moving logic → `planner` (opus)**; scoped decided edit → `implementer` (sonnet); verification → the free gates (or `test-runner` haiku for gap cases); docs → `doc-writer` (haiku); post-code review → `reviewer` (opus). Because these are pins, a cheap session default does NOT silently downgrade a gate/threshold design or its review — route policy design to `planner` and policy code to `reviewer`. (The built-in `Plan` agent inherits the session model and has no pin — use it only for non-policy structural scaffolding, not for gate/threshold design.) **Pin the EXACT model ID in agent frontmatter when a specific version matters** — corrected 2026-08-28 after the first version of this note got it wrong. An alias (`sonnet`/`opus`/`haiku`) resolves to "the recommended version **for your provider**", which differs by provider AND can be redefined by `ANTHROPIC_DEFAULT_SONNET_MODEL` / `ANTHROPIC_DEFAULT_OPUS_MODEL`. **This machine sets `ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-6`**, so every `sonnet` alias — frontmatter, per-call override, and the main session — resolves to 4.6. Passing `model: "sonnet"` per call therefore guarantees the TIER, never the version; the note here previously claimed it guaranteed the latter, which was false. `implementer` is now pinned `model: claude-sonnet-5` (a full ID bypasses alias resolution). **Precedence, first match wins:** `CLAUDE_CODE_SUBAGENT_MODEL` env var → per-call `model` → frontmatter → inherited. **That first entry is a live footgun: if ever set, it overrides the `reviewer`'s Opus pin and would silently downgrade the mandatory Opus review Hard Rule #4 depends on. It is unset today — verify before trusting a review citation on a machine you have not checked.** No hook can verify which model a subagent actually ran as (`SubagentStop` receives no `model` field), so the reviewer's self-reported `MODEL:` line remains the only observable check — which is exactly why Hard Rule #4 requires copying it rather than assuming.
- **`reviewer` (Opus) is gated on change type, not "every commit":**
  - **Review REQUIRED:** `stock_analyzer/constants.py`, any gate, scoring/recommendation formula, cross-feature coordination, DB-write / data-integrity, or a new user-facing decision surface. (Hard Rule #4's surface, plus coordination/data — where a wrong call costs real money.) **Mechanically enforced:** the `pre_tool_checks.py` hook blocks a `git commit` staging any decision-engine-core file (`constants.py`, `risk_advisor`, `exit_advisor`, `daily_briefing`, `portfolio`, `scoring`, `valuation`, `technicals`, `fundamentals`, `ranking`, `targets`, `risk`, `bundle_loader`, `watchlist_advisor`, `home_risk_synthesis` — added 2026-08-31, F-260 Phase 3's extraction of Home's risk/fragility/correlation producer orchestration, which selects the offline sentinels a downstream gate like Watchlist's ENTER_NOW check keys on for safety; `beta_repair` — added 2026-09-15, the beta card's lever arithmetic (trim/swap/add dollar targets, relief-per-dollar ranking, margin/leverage side-effect) — a recommendation-formula module from day one, gated mechanically rather than left to the prose rule alone) **or any DB-write / data-integrity file** (`db.py`, `cron_runner.py`, `system_health.py` — added 2026-08-15 so the hook finally implements the "DB-write / data-integrity" clause this same bullet already claimed; `broker_sync.py` — added 2026-09-12 after three genuinely distinct data-integrity bugs shipped in it within 24 hours, all in the SnapTrade income-event cross-path dedup, which classifies every transaction before it reaches `db.py`'s writers) without an Opus-review citation in the body — for these files there is no "it's just a comment" self-exemption, because a change to them can move a call regardless of how the diff looks. The hook **also** blocks any `feat(` commit missing its `Design =` / `Build =` provenance trailers (Hard Rule #5). `_GATE_FILES` in the hook is the authoritative list; if you edit it, update this enumeration in the same commit.
  - **Skip (deterministic gates suffice):** docs-only, comments, tests-only, pure-additive code not yet wired into a decision path, and mechanical refactors in **non-gate files** — **provided** the full suite + antipattern + constants-doc gates are green. When skipping, say so and why in one line, rather than invoking Opus reflexively.
- **Audit cadence:** run **incremental** `/audit` (diff + blast radius) after a feature ships — cheap, and it catches cross-cutting drift while it's still one feature. Reserve a **full** `/audit --full` (whole-codebase multi-agent sweep) for milestones, not every few days — its value/cost ratio collapses when run too often (2026-07-29 → 2026-08-04 was too close together).

---

## Coordination pattern

Features that own state publish to `st.session_state`; downstream features read and gate. When a producer fails, set the cache to `None` (not an empty container) so consumers can detect "offline" rather than silently disabling gates. Existing cache keys (refreshed 2026-07-13 against actual code, prior list had drifted): `_last_port_df`/`_port_df_enriched` (both hold the same enriched `port_df` object), `_last_held_data`, `_last_held_tickers`, `_portfolio_value`, `_port_risk_cache`, `_fragility_cache`, `_highbeta_share`, `_risk_high_alerts_cache`, `_risk_advisor_recs_cache` (full recommendation list), `_alert_list_cache`, `_actions_cache`, `_div_recs_cache`, `_corr_df_cache`, `_div_score_cache`, `_avg_corr_cache`, `_risk_pairs_cache`, `_div_label_cache` (these 8 feed the standalone Signals & Advice / Risk Analysis pages), `_corr_coverage_cache` (F-246 — how many daily returns `_corr_df_cache`'s matrix was built on, produced beside it and carried in the `_home_synth_cache` bundle so it always describes the matrix actually on screen; `None` = unknown, never "sample is fine". Computing it at render time instead would describe a *different* `held_data` whenever the memo serves an older matrix), `_grow_today_sectors_cache`, `_grow_composites`, `_grow_composites_coverage`, `_daily_brief_offline`, `_acct_gate_cache` (concentration gate basis — now equity; see account-baseline), `_leverage_cache` (margin/leverage awareness — read-only, never gates), `_reduce_calls` (published after Daily Brief build — `{ticker: reduce_call_item}` for tickers under an active Reduce/Exit call; consumed by Analysis, My Edge/Investor Mirror, Opportunity Signals, and Rebalancer to suppress/annotate conflicting ADD suggestions — **and by 🧑‍⚖️ The Judge**, which is a different KIND of consumer and was missing from this list until 2026-08-28: it passes the key set to `audit_coherence` as the reduce set every active protective veto is cross-checked against, so staleness here does not merely annotate an ADD, it makes the audit report "no coherence gap" against yesterday's calls — **and, missing from this list until the 2026-09-01 audit, by 🧾 Summary's Active-Vetoes banner and Portfolio Health card**, which finally SAY the suppression is happening rather than just enforcing it silently: until this pair shipped, a session could read a clean Watchlist with no explanation for why certain names were absent from it; both correctly preserve the `None`-means-not-checked sentinel via `.get()` + explicit `is None`, never `or {}`), `_holdings_sig_at_home_build` (published at Home's `build_portfolio_df` call; a `(ticker, shares)` signature consumed by `_portfolio_snapshot_stale()`/`_render_portfolio_stale_banner()` on 7 other pages to warn — never gate — when a trade logged elsewhere this session has made their cached `_port_df_enriched` stale), `_day_shock_cache` (Home, after the price strip — held tickers moving ≥`DAY_SHOCK_PCT` same-day, awareness only, never gates), `_structural_alert_cache` (Home, after the `_home_synth_cache` hit/miss block converges — `None` when correlation data is offline this session, `[]` when checked and no new cluster formed, else the list of newly-formed correlation clusters since the last 🧬 Structural Scan; awareness only, never gates; nothing consumes it downstream yet), `_dpnl_cache` (Home, after the Tier-B `compute_positions_day_pnl` try/except — `{dpnl, baseline_date, is_current, date}`; 🧾 Summary consumes it so its "Today's P&L" tile matches Home's instead of independently recomputing the cheaper held-only mark, dated so a stale cross-day value is never reused, falls back to its own held-mark calc labeled "(held)" when absent/stale), `_leading_sectors_cache` (Home, leading-sector names from Grow Today; consumed by the standalone Catalyst Watch page for its 🔥 leading-sector flag without recomputing the brief), `_market_tone_cache` (Home; consumed by Compare), `_mirror_orphans`/`_mirror_overexp`/`_mirror_overhangs` (My Edge/Investor Mirror; consumed by the Analysis page's Mirror banner), `_pi_factor_tilt_cache` (**produced by 🧩 Intelligence** — the sole write is `app.py` ~15527, inside that page's range; **consumed by 🔗 Risk Analysis** and re-read within Intelligence itself. **Corrected 2026-08-27: this row previously named Risk Analysis as the PRODUCER and AI Insights as a consumer — both wrong, and the producer error mattered.** Because Risk Analysis is a *consumer*, it can be visited with this cache never populated (Intelligence never opened), so its unguarded read is the COMMON case, not an edge case. **FIXED 2026-08-28** — and the window was even wider than this row said: the cache is written only by 🧩 Intelligence's "📡 Load factor exposure" BUTTON, so a session that visits that page without clicking it still reaches both consumers with `None`. Both LLM consumers (🔗 Risk Analysis's regime scenario, and Intelligence's own 🧬 Structural Scan) silently omitted the factor line when absent, so the model received evidence indistinguishable from "measured, nothing notable" and the app **persisted** the resulting narrative as the day's reading. Now every prompt carries an explicit line from `util.factor_tilt_evidence_line`, which distinguishes THREE states — not measured / measured-but-unusable / measured — and forbids inferring low concentration from absence; `util.factor_tilt_state` is the single classifier both the prompt and the on-screen caption read, so they cannot disagree. Opus-reviewed SHIP. **Still open, deliberately:** the persisted row cannot record that it was built without factor evidence — `regime_scenario_cache`/`structural_scan_cache` have snapshot columns for blast-radius/regime/cross-asset but none for factor tilt, so tomorrow's read of today's narrative has no provenance. That needs a DDL column and is therefore its own `db.py` reviewed commit; the disclosure fires at generation time, when you can still act on it), `_broker_drift_cache` (Home, filled just before the Tier-B day-P&L block — the `broker_sync.decide_drift_banner` verdict for app-vs-broker position drift. `None` or state `"unknown"` means NOT CHECKED and must never read as "no drift"; awareness only, never gates. Consumed by the F-250 day-P&L `qty_drift` caption, which DEFERS to it for any ticker the broker already explains — broker drift is EXTERNAL ground truth while the day-P&L check is internal and inferential, so the authoritative one keeps the full explanation and the other becomes a cross-reference), `_coord_epoch` / `_coord_stamps` (F-260 Phase 1 — the portfolio GENERATION and a `{cache_key: epoch_at_publish}` map. `_coord_epoch` bumps only when the BOOK changes (inside `_refresh_portfolio_cache_after_trade`), never on a plain rerun, so freshness here means "describes the current book", not "was computed recently". `stock_analyzer/coord_freshness.py` owns the registry of portfolio-dependent keys, their gate/decision/decorative tier, and the per-surface narrowing map; a key MISSING from that registry reads as permanently fresh, so add new portfolio-derived caches there in the same commit that creates them), `_refresh_error` (Home/Trade Journal — the post-trade republisher's failure string, `None` on success; rendered by `_render_portfolio_stale_banner` so a crashed refresh is distinguishable from a successful no-op), `_home_synth_cache` (Home's synthesis memoization signature — new Home inputs MUST join it or ship stale; consumed by Summary), `_rh_prices_cache_{start}_{end}` (dynamically-named per date range, e.g. `_rh_prices_cache_2020-01-01_2026-08-24` — 2026-08-24 review finding, added to this list late; live prices for all-time `new_pick` rec tickers, shared by 🧾 Summary's Engine Track Record card and the 📜 Recommendations History page's "All time" window: whichever renders first populates it, the other reuses it, guaranteeing the two surfaces agree on a cold session with no prior Rec History visit needed — `None` until first computed, never an empty dict on failure), `_xc_bad_prev_tickers` (F-260/prev_close-incident follow-on, 2026-08-31 — Home's price cross-check compute is hoisted early in its render specifically to publish this: a `set()` of tickers whose `prev_close` is CURRENTLY failing the strict cross-check leg, i.e. `prev_ok is False` specifically, not the broader `not ok` which also includes the loosely-tolerated live-price leg. Consumed by 6 sites across Home and Summary — Day Shock, the live price strip, both pages' Today's P&L Tier-A fallback + captions, `_render_holdings_table`'s Day Δ% column, Top Positions' day-% column, and the day-direction footer — each either excludes the flagged ticker from a sum/ranking or shows "⚠ unverified" instead of a number built on data the app has already flagged as unreliable. Always a `set()`, never `None` — the producer publishes unconditionally even when `held_tickers` is empty — so every consumer's `or set()` default is "not yet published this session" (Home hasn't run), not an offline sentinel to preserve).

When adding a new advisor or recommendation feature, **always** check whether its decision overlaps with another feature's. If yes, wire coordination via the same publish/consume pattern.

---

## Navigation safety

`st.session_state.nav_page` is bound to the sidebar navigation widget. **Setting it directly raises `StreamlitAPIException`.** Use the `_pending_page` indirection (button sets `_pending_page`; consumed at top of next run; then assigned to `nav_page`). The `_pending_page` consumption logic lives near the top of `app.py`.

---

## Coding conventions

- Pure logic lives in `stock_analyzer/`; UI rendering and orchestration in `app.py`. Don't move domain logic into `app.py`.
- **Extract the DECISION, not just the helper — this is the default, not an occasional refactor.** `app.py` is **43,729 lines and no test imports it** (measured 2026-09-21, up from 38,197 on 2026-08-28 — refreshed as part of that day's app-review doc-drift cleanup; re-verify the count before citing it again rather than trusting this number indefinitely). So any conditional that decides *what the user is told* — a threshold comparison, a breach determination, a staleness call, a tone classification — is unverifiable there; its only check is a screenshot. That is why **decision** logic there is the class most likely to reach production unverified, while the pure-logic package catches its own. **Be precise about what this does NOT cover:** the two defects that actually reached the owner (a `**bold**` printing literally inside raw HTML, `$…$` rendering as LaTeX) were **renderer-mismatch** bugs, and extraction does not help there at all — per `feedback_streamlit_renderer_mismatch` that class is invisible to tests AND review, and only a screenshot finds it. This convention is a bet on the decision half, justified structurally rather than by those two incidents. **The shape:** put the decision in a pure function in `stock_analyzer/`, unit-test it at its boundaries, and leave `app.py` as render-only wiring that calls it. Recent examples worth copying: `portfolio.refresh_outcome`, `portfolio.gate_basis`, `coord_freshness.decide_stale_banner`, `outage_gate.decide`, `util.factor_tilt_state`. Each became testable the moment it left `app.py`, and several immediately caught a defect that had been invisible.
- **Mechanically ratcheted:** `check_antipatterns.py`'s `POLICY_DECISION_IN_RENDER` rule fails when `app.py`/`cron_runner.py` gains a **new** comparison against a `constants.py` value. The existing ~90 are baselined — nothing must be fixed retroactively, and many are probably fine. The point is that the next one is easier to extract than to baseline. Comparing a threshold inside `stock_analyzer/` is correct and never flagged; that is where decisions belong. **Prefer a NEW module over growing a `_GATE_FILES` one** (`outage_gate.py`, `coord_freshness.py` are the pattern): extracting into `portfolio.py`/`risk.py`/`scoring.py` pulls the commit into the mandatory-Opus-review set, so for the highest-value extractions the correct path can cost more than the `--init` escape hatch. Keep the gradient pointing toward extraction — a ratchet only ratchets while the right move is the cheap one.
- New database columns must be backward-compatible: `db.load_trades()` backfills `None` for legacy rows missing columns.
- Date comparisons use America/New_York timezone via `pytz` (the hosted containers run UTC).
- For UI suppressions, render a visible banner explaining what was suppressed and why — never silently filter.

---

## Commit messages

**Conventional Commits**: `type(scope): summary` — imperative, lowercase, ≤72 chars, no trailing period; a body explaining **why**; trailers in the footer. Types: `feat fix docs refactor perf test build ci chore revert`. Full spec + the one-time `git config commit.template .gitmessage.txt` setup live in [DEVELOPMENT.md](DEVELOPMENT.md); the template is [`.gitmessage.txt`](.gitmessage.txt).

- **Threshold/gate changes** (`stock_analyzer/constants.py`) are investment-policy decisions — call them out in the body and name the constant + old→new value.
- **Feature commits must sync the docs that describe behaviour:** a user-facing feature or gate touches `docs/requirements.md` (the functional spec), not just `docs/architecture.md`. requirements.md silently drifted ~3 weeks once because per-feature docs commits hit architecture but skipped requirements — don't repeat that.
- **Claude-authored commits** end with the trailer `Co-Authored-By: Ajay with <model>` — **no email address** (changed 2026-09-02; the email previously here made GitHub link it as a structured co-author, which was more than intended for a personal solo repo — plain attribution text is enough). Written via `.git/COMMIT_MSG.txt` + `git commit -F` (dodges PowerShell here-string mangling). Use the **actual model name and version from the session context** (e.g. `Claude Sonnet 5`, `Claude Opus 4.8`, `Claude Haiku 4.5`) — never hardcode a model name or drop the version. **Not retroactive** — commits before 2026-09-02 keep the old `<model> <email>` trailer; only new commits use this format.

## Documentation integrity (zero-hallucination)

This is a decision-making app: a **wrong value in a doc erodes trust in the app itself**, so docs are held to the same bar as code.

- **Transcribe every threshold / constant / file:line from the source, never from memory.** Before writing a number into any doc, open `stock_analyzer/constants.py` (or the actual code) and copy it. Recalled values in `MEMORY.md`/summaries reflect what was true *when written* — verify against HEAD.
- **Never invent** function names, constant names, session keys, UI text, or dates. If you can't confirm it in code, don't write it.
- **Doc edits that carry policy values stay on the Opus lead** — do not delegate constants-table / gate / requirements writes to the Haiku doc-writer (it has hallucinated exactly these: invented function names, a wrong constant value, a fabricated date). The doc-writer is for prose/comments after the facts are pinned.

---

## Definition of Done — sync docs in the SAME session (don't defer to a periodic audit)

A user-facing feature or a new/changed decision value is **not done** until its docs are synced in the same work session. Three shipped features (Phase 3 Catalyst Scanner, Phase 2b sentiment, the 44/45 gate) drifted undocumented for days before the 2026-07-16 sweep caught them — the fix is a per-feature checkpoint, not a rare cleanup. Run this every time:

1. **New/changed constant in `stock_analyzer/constants.py`** → add/update its row in the `docs/architecture.md` constants table (or, if genuinely internal plumbing, add it to `scripts/constants_doc_allowlist.txt`). **Mechanically enforced:** `.github/workflows/docs-check.yml` runs `scripts/check_constants_documented.py` and fails when a constant is neither documented nor allowlisted. Run it locally before committing: `python scripts/check_constants_documented.py`.
2. **New user-facing surface** (page, tab, card, gate, section) → add/update an F-ID in `docs/requirements.md`. *(Judgment item — CI can't detect "this is user-facing"; it's on the author.)*
3. **Shipped a queued item** → move it out of "What's queued → Genuinely not yet done" and add an entry to [docs/shipped-log.md](docs/shipped-log.md), and fix any memory that still calls it parked. At the start of any doc-sync, grep the queue's named functions/features against the code (and against `docs/shipped-log.md`) to catch items that already shipped.
4. **User-visible behaviour changed** → update the in-app User Guide (`app.py`, `elif page == "📖 User Guide":`).
5. **Non-obvious decision or rationale** → capture it in a memory file.
6. **Shipped a phased feature with a future Phase 2/3 gated on a trigger date/condition** → add that gate to "What's queued" below immediately, not just the roadmap/plan memory file. Memory only surfaces when a session's retrieval happens to match it; CLAUDE.md is loaded into *every* session. A gate that lives only in memory is invisible to any session that doesn't happen to ask about that specific feature — this is exactly how three Agentic Intelligence Roadmap Phase-2/3 gates (Thesis Red Team, Structural Scanner, Multi-Agent Debate) went untracked here until a 2026-07-26 user question surfaced them.

7. **Touched a `docs/plans/*.md` feature** → bump **that plan's own `**Status:**` line**. This was recorded on 2026-08-06 as "consider this a 7th checklist item if this class of drift recurs." **It recurred** (2026-08-17: `docs/plans/app-settings.md` needed a status edit that steps 1-6 don't ask for), so it is now a real step. The drift is fast and one-directional: a plan doc written the same session a feature ships still says "pre-decision" or "mock pending approval" days later, because steps 1-6 name requirements.md, shipped-log.md and memory — never the plan's own header.

Only #1 is mechanically guarded; #2–#7 are the author's checklist. When wrapping up a feature or doing a "sync the docs" pass, run all seven.

**Second mechanical guard (added 2026-08-04): the recurring-defect gate.** `scripts/check_antipatterns.py` (CI: `.github/workflows/antipatterns-check.yml`; local: the `pre_tool_checks.py` commit/push hook) fails when a change introduces a NEW instance of a bug-class the 2026-07-29 / 2026-08-04 audits kept re-finding: offline-sentinel collapse (`.get(...) or []`/`or {}` defeating the `None`-on-failure contract), dynamic `unsafe_allow_html` (the XSS class, patched 7×), and naive `datetime.utcnow()`/`date.today()` date math. It's baseline-gated (`scripts/antipattern_baseline.json`) so only new instances fail. **Prefer fixing at the source over growing the baseline:** read caches via `stock_analyzer.util.get_or_offline`, escape interpolated HTML via `stock_analyzer.util.safe_html`, and take "now"/"today" from `stock_analyzer.market_time.now_et`/`today_et`. Migrating an existing call site onto these helpers is a live behaviour change — if it touches a decision/gate path, it still needs its own review.

---

## Pointers

| Need | Where |
|---|---|
| Single portable map of the whole app (usage + mechanics) | [docs/user-manual.md](docs/user-manual.md) |
| Full dev context after time away | [DEVELOPMENT.md](DEVELOPMENT.md) |
| All decision thresholds | [stock_analyzer/constants.py](stock_analyzer/constants.py) |
| Functional requirements + operating policy | [docs/requirements.md](docs/requirements.md) |
| Architecture, data flow, scoring model, db schema, known behaviours | [docs/architecture.md](docs/architecture.md) |
| What's automated (pytest) vs. what needs manual testing, and when | [docs/testing-strategy.md](docs/testing-strategy.md) |
| Latest pytest pass/fail counts + coverage, and run history | [docs/test-results.md](docs/test-results.md) |
| Auto-memory index (durable feedback, threshold rationale, etc.) | `MEMORY.md` (Claude auto-memory, outside repo) |
| Full shipped-feature changelog (history, not needed every session) | [docs/shipped-log.md](docs/shipped-log.md) |

---

## What's queued

Last reconciled **2026-08-17** — a full SDLC/doc/memory sweep after the three reference-roster refreshes (F-240 scan universe + two live macro-gate fail-opens, F-241 weekly ticker-liveness sweep, F-242 diversification roster) and the rate-sensitivity fabricated-neutral fix. 🩺 check ⑤ went **3 amber → 1** (only the macro calendar remains, blocked externally on BEA 2027 GDP dates). Synced this pass: `docs/requirements.md` F-240/241/242, `docs/architecture.md` (+2 module sections for `reference_shelf.py` and `ticker_liveness.py`, constants table, System-Trust check count), `docs/user-manual.md` (scanner counts, 5 checks not 4, the maintenance cron lane, and a **false** "always exits 0" claim that F-239 had invalidated), `docs/test-results.md` (**3657 passed / 77%** — closing a 13-day, 508-test logging gap), `docs/shipped-log.md`, and memory. 

**The reconciliation HISTORY that used to live here has moved to [docs/shipped-log.md](docs/shipped-log.md)** — it had grown to ~7KB on one line and was injected into every session for no per-session value, which is the same reason the shipped-feature list was extracted on 2026-07-22. Every entry in that chain already pointed at shipped-log.md or a memory file; nothing is lost. Keep this paragraph SHORT: state the current state, not the path taken to it. Older context still worth knowing: earlier items were last audited against code 2026-07-13 (nav Phase A/B/C all shipped — Home is fully de-tabbed), and the macro/regime Phase-4 cluster is **done** (CPI NSA swap, drift detection, FRED `actual`) — don't re-chase it.

**Genuinely not yet done** (verify against code before starting — statuses live in the named plan/memory):
- **Data Foundation Strategy — Phase 0/1 + recommendations-table pagination SHIPPED 2026-09-22 (commits `6c60656`, `eea121d`), same day as the assessment itself.** A full data-quality audit (`docs/plans/data-foundation-strategy.md`) found the app's biggest weak-trend cause wasn't missing history — it was a live aggregation bug: Engine Track Record, the Gate Suppression Ledger, and The Judge's per-ticker dimensions all counted one real signal episode once per day it persisted, instead of once per episode (two sibling features, `protective_track_record`/`rec_events_readout`, already had the correct fix). Live Phase-0 queries confirmed `recommendations` (1473 rows) was the one table already over PostgREST's 1000-row cap, and confirmed both known duplicate-row issues were already clean. Both Opus-reviewed SHIP/0-blocking, full suite 6116 passed. **The live 🎯 Engine Track Record headline moved, confirmed via a real owner screenshot the same day: Offense 21→30 matured / +6.3pp→+8.3pp; Defense (untouched by this fix) unchanged at 21/-18.7pp, a useful negative control; "Calls you skipped" flipped sign, +1.5pp→-4.5pp, with its shared since-date moving ~7 weeks earlier (Jul 15→May 28) consistent with the pagination fix restoring previously-truncated rows.** **Genuinely remaining, sized against real data:** pagination for 7 more tables (Phase 2); `debrief_advisor.py`'s date-filter bug, the income-event subtype gap, and a sector-taxonomy sweep (Phase 3 — A4 already shipped inside Phase 1, don't re-scope it); `COMPOSITE_WEIGHTS` durable versioning (Phase 4 — real urgency, 27% of `recommendations` predates the 2026-07-09 formula change, but needs its own `planner`+`reviewer` pass); an "effective N" discipline for The Judge's portfolio-wide dimensions (Phase 5 — confirmed zero data exists for this yet, correctly deferred). Memory `project_data_foundation_strategy`.
- **🔎 Portfolio Investigator (F-275) v1.1 — `forward_alpha_at_horizon` deliberately deferred, not started, no trigger date set.** F-275 shipped 2026-09-18 (all 7 chunks, production-verified same day on both `claude-sonnet-4-6` and `claude-opus-5`) with a fixed 8-function toolbox. A single ticker/date/entry-price alpha lookup (e.g. "AAPL's alpha after the March 3rd BUY") was implemented during the build then deliberately removed before ship: the plan step's `{"fn_id", "why"}` JSON shape has no per-step argument slot for a scalar like a ticker, so registering it would let the plan pick a function that can only ever fail today. **v1.1 path:** extend the plan schema with a per-step `"params"` field (+ matching `validate_plan` support) paired with a question-side ticker/date resolver, similar to `portfolio_qa.py`'s own parse step for Ask. **Trigger to revisit: a real COMPOUND question surfaces that needs this lookup as one step among several** — the narrow single-instance case alone is already answered today by 💬 Ask's own `rec_outcome` intent (confirmed via the two tabs' 2026-09-18 cross-link work), so building this speculatively for that case alone was explicitly declined. Toolbox itself already grew once post-ship on the same principle (`sector_exposure`, added the same day after a real question exposed a real gap — re-verified 34/34 on both models after) — same "one deliberate, reviewed addition once a real question shape needs it" discipline applies here. Full detail: `docs/plans/portfolio-investigator.md`; memory `project_portfolio_investigator`.
- ~~Measure whether past Rebalancer/Diversification-ADD/Tax-Harvest recommendations actually worked~~ — **BOTH PHASES SHIPPED. Phase 1a as F-273 2026-09-15; Phase 1b/2 as F-273b 2026-09-17.** Phase 1a: a daily `portfolio_risk_snapshots` EOD capture + a "📊 Portfolio Risk Trend" chart on 💰 Account, three new `trigger_type` values wired into the trade journal/Rebalancer quick-log, a non-banded tax-harvest running total, a wash-sale AFTER-side compliance check. Phase 1b/2, picked up once 🩺 System Trust check ② confirmed Phase 1a was writing clean rows: a fresh `planner` pass found the original design had conflated the Rebalancer's concentration-driven trim with the risk_advisor beta-card's portfolio-beta trim under one label — owner ratified capturing both as distinct `rec_type`s (`rebal_trim`, `beta_trim`, plus `diversify_add`), a new owner-only "🎯 Recommendation Outcomes" page (not folded into an existing one), and a 10-trading-day action window for attribution. New `rec_events` table + `rec_events_capture.py`/`rec_events_readout.py`; attribution requires an EXACT named-ticker match (a different ticker never credits a call); trim outcomes are portfolio-level proxies, never per-ticker-causal; ADD outcomes keep the candidate's-own-return leg separate from the correlation/diversification-score leg; banding requires both an 8-call and a 5-distinct-ticker floor. Opus `reviewer`: SHIP, 0 blocking both phases. **Nothing left queued from this feature's original scope** (the never-acted ADD counterfactual and opportunity-cost legs remain deliberately phase-3-or-never). Full detail: `docs/requirements.md` F-273/F-273b, memory `project_recommendation_outcomes_measurement`. **Production verification CONFIRMED 2026-09-20** via a direct Supabase SQL check (row counts + per-day/per-type breakdown + raw-row spot-check, not just a screenshot): 20 rows spanning both trading days since DDL apply (2026-09-17, 2026-09-18), `beta_trim`+`diversify_add` firing correctly each day (1+9 rows, same 10 tickers both days), correct NULL-preservation (`metric_predicted_after` null for every `diversify_add` row as designed, `price_at_rec` null for `beta_trim` as designed). `rebal_trim` has not fired yet — expected, not a gap, no oversized+profitable trim has occurred since DDL apply. **Anchor dates resolved:** first real `fired_date` = 2026-09-17 (same as DDL-apply date), so §11's pre-registered dates stand as originally computed — 3-month sanity-check checkpoint ~2026-12-16/17 (accumulation-only, no verdict), 12-month retirement review ~2027-09-17. Nothing left open on this feature. Memory `project_recommendation_outcomes_measurement`.
- **Investor Maturity Roadmap — Tier 0 (A1/A2/A4) run against real data, Tier 1 (B1/B2) SHIPPED, Tier 2 (C1/C2) SHIPPED 2026-09-14, D1/E1 SHIPPED 2026-09-14. Plan: `docs/plans/investor-maturity-roadmap.md`; memory `project_investor_maturity_roadmap`.** Grew out of a trust/confidence review of the app's Offense/Defense/Restraint lanes, run against real production data via manual Supabase SQL pulls. **Real-data findings, load-bearing for what comes next:** **A1** — the Defense facet's first-ever verdict (`protective_track_record`), now past both `PROTECT_TRACK_MIN_CALLS`/`PROTECT_TRACK_FIRM_CALLS`, is **NEGATIVE** (`protect_alpha = -15.7%`; only 4 of 17 mature EXIT/TRIM calls validated positively — robust to the single largest outlier, still -9.1% without it). **A2 — REVISED 2026-09-22, do not cite the original verdict below as settled.** The original run found alpha "not monotonic with composite band," concluding the engine is a pure candidate generator, not a ranker. That run was computed over the SAME uncollapsed, one-row-per-surfacing population the episode-collapse fix corrected the same day (commit `6c60656`; see the Data Foundation Strategy entry above). A same-day re-run (`scripts/offense_attribution.py`, extended with a collapsed-by-ticker lens, commit `97f31c6`) on the identical live population (N=266 raw / 76 distinct tickers — so the collapse, not additional data, is the only variable) shows alpha **rising monotonically** with band once collapsed (0.08 → 0.85 → 4.64) — the specific "not monotonic" claim does not survive. **This is not a clean reversal, though:** acted alpha still beats skipped alpha substantially in every band (+16.7/+6.9/+13.4pp) — the script's own pre-registered criterion correctly returns MIXED. Both the composite's own ranking AND the owner's selection appear to add real value simultaneously; neither a "pure selection" nor a "pure ranking" story fits. Sample remains thin (76 tickers, as few as 6 in the 75-79 band) — evidence only, no retuning, same posture as every other single-data-point moment in this roadmap. Memory `project_investor_maturity_roadmap`'s "A2 RE-RUN, 2026-09-22" section has the full table and caveats. **A4** — real median holding period across 139 closed round trips is **~7 calendar days**, far shorter than the deterioration ladder's weeks-scale confirmation window — the position-size cut is flat across terciles, arguing this is a genuine strategy/design mismatch, not an artefact of small/short trades. **Shipped:** **B1** — score history capture (F-269; **supersedes the old standalone "score history capture" entry below**), capture-only, no readout/card/gate yet — DDL applied 2026-09-13 (Sunday, a non-trading day; `_run_premarket` skips entirely on non-trading days before it ever reaches the capture code), so the first real cron opportunity is Monday 2026-09-14's `premarket` run — check then, not before. **B2** — Gate Suppression Ledger expansion to 5 more gates, G-02/G-05/G-06/G-13/G-18 (F-270) — architecturally bigger than scoped (none of the 5 flow through `grow_today`, so this shipped as 3 new pure builders + 3 new write sites, the original 9-site builder untouched); UI-verified live via screenshot (5 new "building" cards on 🛑 The Road Not Taken, correct order) and via 🩺 System Trust check ⑥ (5 of 6 write paths confirmed "ok" same day; the 6th, Analysis/G-18, correctly stays "unknown" until a viewed ticker's stop actually breaches). **C1 SHIPPED 2026-09-14 as F-271** (make the risk-off blindness visible — a real, live bug: two UI sites rendered a missing VIX/SPY read as an affirmative "the market regime is calm," including a confirmed `except: False, []` silent collapse on 🔗 Risk Analysis; fixed via a new `risk_off_state()` three-state classifier, wired into exactly 2 sites, redline independently verified 3 times — absent data still never arms anything). **Owner scope decision, 2026-09-14:** extending the same disclosure to Home's de-risk cards, the Forward Simulator, and the premarket email was explicitly considered and DEFERRED, not rejected — none of those 3 makes a false claim today (they already fail silently, which is correct), and Home specifically has a declined 2026-08-29 proposal against adding more stacking banner disclosures (`project_home_redesign`) — queued separately in the roadmap doc §5. **C2 SHIPPED 2026-09-14 as F-272** (capital-equivalent risk disclosure). Reconciliation finding: of the "five F-255 sizing surfaces," only 2 (Analysis, Watchlist) actually display a risk-per-trade figure to disclose against — scoped to those 2; extending a NEW risk-per-trade metric to the other 3 surfaces is a bigger, different change, also deferred and queued separately. **Return-contract decision, reversed once fully reasoned through:** the owner first picked the roadmap's literal "return `None` when unknown," then agreed to an explicit-state dict instead once shown that a bare `None` reintroduces a bug class this project has already shipped and fixed twice (`feedback_sentinel_is_present`, `feedback_none_sentinel_meets_pandas`) — worth remembering as a case where restating a concern with the full picture changed a decision that was made reasonably the first time. ~~**D1** (extend the `_TIPS` glossary past its valuation/fundamentals-only MVP boundary — no beta, ATR stop, R:R, drawdown, correlation, margin, or leverage entries exist today)~~ — **SHIPPED 2026-09-14, and this bullet's own premise was stale before it was even acted on:** beta/ATR stop/R:R/drawdown/correlation already had entries (35 total in `_TIPS`, not an MVP boundary) — verified against the live dict before writing anything, not assumed from this queue text. The genuinely missing cluster was **Leverage / Margin Cushion / Margin Call Distance / Entry Zone**, now added and wired via `help=_tip(...)` into every render site (also de-duplicating two pre-existing, mutually-inconsistent ad-hoc inline tooltips on Summary's Book Safety card and Account's margin panel). Pure display text, no reviewer needed. ~~**E1** (version §2B's investor-persona assumption with a dated note + an explicit re-read trigger, rather than silently correct it — it was written 2026-06-02 for an unlevered book, `margin.py` landed 2026-08-24, nobody re-read it since)~~ — **SHIPPED 2026-09-14**: dated investor-state note + explicit re-read trigger added to `docs/requirements.md` §2B (both dates re-verified against `git log`, not recalled). **Diary dates:** **~mid-Oct 2026** the original 9-gate ledger's first evaluable verdicts; **Dec 2026–Feb 2027** the score-history decay readout becomes designable (gated on A4 — must open by confronting the ~7-day real holding period, not assume the original weeks-scale framing still fits); **~2027-08-27** the ledger's pre-registered retirement review (retiring is the SUCCESS condition, do not soften it later). **IMPORTANT — read the 2026-09-17 regime-confound result before treating A1 as a broad ladder problem.** A1's -15.7% headline is NOT broad-based: a same-session follow-up cross-tabbed it by sector and found it's concentrated in exactly 5 tickers (TEAM/NOW/PLTR/APP/CRWD, an AI/growth/cybersecurity cluster) averaging **-51.3%**, while the other 15 tracked tickers across 8 sectors average **-1.2% to -4.5%** — essentially neutral, and this survives excluding the single largest outlier (TEAM). A further "completing piece" the same session found the cluster itself is bimodal: CRWD was a genuine winner worth its risk (+$427 combined realized+unrealized), but TEAM/NOW — the two names driving nearly all of the negative alpha — were **also realized DOLLAR LOSSES at the time of exit** (-$242, -$33), meaning the app's caution on them was dollar-correct even though it scores badly on the alpha-vs-SPY metric (a stock recovering hard after you correctly cut a loss still shows as "ran early" by that metric — the two are different questions). **Standing conclusion from that session, not overturned by anything since: this reads as a regime/concentration artifact in one high-beta pocket, not a broadly miscalibrated ladder — "explicitly does NOT support broadly retuning the deterioration ladder's thresholds," and any lever here is a personal position-sizing/concentration decision, not a software fix.** Full detail: memory `project_investor_maturity_roadmap`'s "A1's regime-confound check" and "The completing piece" sections.

**2026-09-20 follow-up — re-confirms the SAME underlying finding from new angles, does not add independent weight, and a real process gap surfaced along the way.** A live-screenshot session found `self_track_record`'s sell-side Self-vs-Engine (engine-called exits +14.4pp vs self-initiated +5.6pp, n=19/44 — near-identical population to A1's n=17) and a leverage-adjacent signal (`benchmark_mirror`: portfolio -40.3%/ann. vs SPY +17.8%/ann. over 87 days, beta-adjusted alpha -141.2pp, real $1,113 behind the shadow-SPY counterfactual). An Opus `planner` correctly identified the sell-side number as non-independent (same episodes as A1, different angle) and correctly declined a threshold change today — but **that planner pass was not given the 2026-09-17 regime-confound/completing-piece result and so reasoned about A1 as if it were still an open, broad, unexplained finding**, when it had already been substantially explained and scoped down two days earlier. The two pieces built today (disclosure caption + diagnostic script, both shipped, see below) are still reasonable — the disclosure states only the measured number without claiming a cause, and the diagnostic generalizes what the 2026-09-17 completing-piece did by hand for 5 tickers into a re-runnable tool — but **the standing conclusion remains the 2026-09-17 one** (concentration/regime, not mechanism, not a software fix) until the diagnostic script is actually run and shows something that contradicts it. **Lesson for future sessions:** before reasoning about a project trust/confidence question from a `planner` pass, hand it the relevant memory file's own prior conclusions directly (not just a CLAUDE.md queue summary, which can lag what memory actually contains) — CLAUDE.md's own queue bullet here didn't carry the regime-confound result forward, which is exactly how this gap happened. **Shipped, both awareness-only, no `_GATE_FILES` touch, voluntary Opus `reviewer`: SHIP, 0 blocking:** (1) a one-line disclosure on 🧾 Summary's Act Today card, shown only when a live EXIT/TRIM item is present AND the Defense facet is `band="firm"` with negative `protect_alpha` — reads the already-published `_etr_cache`, never recomputes, skips silently (with an explicit same-day freshness check) rather than show a stale verdict; (2) `scripts/exit_early_cost_analysis.py`, a new read-only diagnostic (sibling to W6's `exit_ladder_replay.py`) that puts a real dollar figure on "cut short vs self-corrected vs avoided-loss vs ignored-call" per protective call, across the FULL tracked population (not just the 5-ticker cluster) — needs the owner's own Supabase credentials to run, same constraint as its sibling; produces evidence only, never a recommendation. **CLOSED 2026-09-20 — the owner ran the script for real, it did surface cut-short dollars outside the known cluster, and a second Opus `planner` reconciliation pass (not a reflex reaction to the literal number) found this extends the 2026-09-17 story rather than overturning it: a SECOND high-beta pocket (semiconductors: AMD/MU/MRVL, $921.80 of the $1,502.38 "new" total) — the exact three names 2026-09-17's own cross-tab already flagged as the sector's worst laggards, now showing dollar weight after 3 more weeks of price drift, not a new signal. Real SPY closes pulled live confirmed the market was essentially FLAT over every signal window, so this is idiosyncratic high-beta/momentum strength, not "everything rallied" — which sharpens the concentration read rather than weakening it.** Still fails all three of the earlier-set conditions for a threshold conversation, decisively on condition (c): W6's opposite failure mode (52% of real closed losers get no signal at all) is completely untouched by this data, so loosening the ladder to stop cutting these names short would only worsen the majority failure mode. **Do not retune the deterioration ladder from this evidence. If ever revisited, the only defensible framing is narrow — a beta/momentum-aware qualifier on the ladder's classification logic, not a constant change — gated on BOTH the mid-Oct larger-N ledger verdicts AND a W6 re-run showing the two failure-mode populations are actually separable; neither is met today.** Full reasoning + the exact bucket breakdown: memory `project_investor_maturity_roadmap`.
- **Analyst-weight-audit follow-ons — item (1) SHIPPED 2026-09-22, commit `38c50e3`. Item (2) still PARKED, unchanged.** Two non-blocking notes from the 2026-09-13 Phase B `reviewer` passes (`docs/plans/analyst-weight-audit.md` §8c). **(1) CLOSED:** the originally-cited location (`app.py`'s pillar-detail renderer, then ~22701-22703) turned out to already be fixed independently (now `app.py:23705-23749`, via `util.pillar_tile()` — this entry's own line citation had drifted stale). But the SAME bug shape (a fabricated neutral 50 from `val_available`/`bq_available`=False rendering/deciding indistinguishably from a real measurement) survived at 3 other genuinely separate sites, found in the same pickup: `premortem_advisor.py::driving_pillar_from_bundle()` (could name a withheld pillar as "driving" a Pre-Mortem counterargument — now excluded from its `max()` candidate pool), 🥧 Portfolio Overview's score-breakdown tiles (now show "Withheld" instead of a fabricated score + fabricated points-contribution), and — the highest-stakes of the three — 📡 Signals & Advice's "Recommended Action" text, which could assert "fundamentals are intact" as an AFFIRMATIVE FALSE CLAIM on a ticker whose fundamentals were never measured (decision-adjacent, not cosmetic; now falls through to the existing safe generic fallback with a disclosure line). 3 more cosmetic sibling sites of the identical shape (Home's Rebalancer trim-basis line, Signals & Advice's score-breakdown bars + raw-fundamentals header, Market Scanner's Signal Evidence table) were found in the same sweep and deliberately left unfixed — scoped down with the owner to the decision-adjacent site plus the two originally-intended ones, per the established precedent against blind bulk-patching an untested render file (`feedback_recurring_defect_gate`). 5 new tests, Opus reviewer: SHIP, 0 blocking. **(2) still open, unchanged:** `tests/test_valuation.py`'s footprint test still hardcodes the other three legs' point values (`25+20+25`) instead of deriving them — low stakes, accepted as a pin. **Trigger to revisit the 3 deferred cosmetic sites or item (2): either becomes actually confusing in practice, or picked up as a small polish pass — not on its own priority.**
- ~~Score history capture~~ — **SHIPPED 2026-09-13 as F-269.** Superseded by the "Investor Maturity Roadmap" entry above (B1) — full detail there, `docs/plans/score-history-capture.md`, memory `project_investor_maturity_roadmap`. Kept as a struck-through pointer rather than deleted so a stale reference to "DESIGNED, NOT STARTED" doesn't linger anywhere else in this file.
- ~~`snaptrade_income_events` cross-path dedup gap~~ — **SHIPPED 2026-09-11.** F-268's documented root cause ("SnapTrade's Robinhood connector never returns income activities") had been falsified: 12 duplicate rows (3 MINT margin-interest, 9 CDIV/MDIV dividends) were found and manually cleaned up as a one-time data fix. The recurring code gap is now closed: a new canonical `broker_sync.income_event_subtype(raw_code, event_type)` merges the two ingestion paths' different vocabularies onto shared subtypes (CDIV↔DIVIDEND, MDIV↔SUBSTITUTE_DIVIDEND — kept distinct from each other since a manufactured dividend is a genuinely different real event; MINT kept its own subtype, not merged with plain interest, pending a confirmed live-sync equivalent), a new `raw_code` column persists the source code for this mapping, write-time consume-on-match dedup (`classify_transactions`, keyed on ticker/canonical-subtype/**signed** cents within `INCOME_EVENT_DEDUP_DATE_TOL_DAYS`=3, wired into the `broker` cron lane), and a read-time backstop (`dedupe_income_events`, CSV-wins tie-break) wired into both Cash Activity and every F-267 Capital vs Margin summing consumer. Two Opus reviewer rounds — first FIX-FIRST/1 blocking (the read-time fix reached the Cash Activity chart and cash-reconstruction chart but missed three other Capital vs Margin consumers, risking a correct chart next to an inflated interest verdict on the same screen), then SHIP/0 blocking. Memory `project_snaptrade_broker_integration`; `docs/requirements.md` F-268.
- ~~F-267 Capital vs Margin — real date-range mismatch bug~~ — **SHIPPED 2026-09-11.** "Net Value Margin Added" = `extra_exposure_pnl − interest_paid`, but the two terms had covered different windows: `extra_exposure_pnl` scoped to the reconstruction period (`golive` → the anchor date), while the headline "Interest since go-live" total (`interest_partition(_cvm_income)`) had NO date filter and summed every income event ever loaded, including MINT margin-interest charges from January 2026 — confirmed live via a $277-vs-$106 discrepancy against the Up/Down-weeks split. New pure `capital_vs_margin.window_income_events(income_events, golive, end)` filters to `[golive, end]` inclusive both ends (the end bound is the reconstruction's own anchor date, not "today" — using "today" would reintroduce the same bug on a stale anchor), wired into `app.py` once, right after the income-event dedup call and before every summing consumer. The weekly Up/Down split turned out to already be correctly windowed, but by accident (it only loops over weeks that already fall in range) rather than by design — the fix makes this a guarantee instead of a coincidence. `drawdown_decomposition` confirmed genuinely unaffected (self-windows to a strict subset range already). Opus reviewer: SHIP, 0 blocking. No new constant. Memory `project_capital_vs_margin_analysis`.
- **F-266 Leverage & Margin Cushion history — SHIPPED 2026-09-10 (commit `a6cc226`), core capture done, three deliberately-deferred follow-ups queued.** Grew out of a capital-protection brainstorm after a ~9-11% weekly capital loss while running ~2.85x margin: a backtest attempt found the app had ZERO historical day-by-day account-level data (leverage/cushion/net-equity were pure live computations, `account_cash` is a single overwritten row). Shipped a new `account_daily_snapshots` table (DDL applied by the owner, not yet auto-migrated) written by the EOD cron, reusing `margin.call_distance()` verbatim (never re-derived), stale/missing cash → NULL rather than a fabricated cushion, plus a new "🛡️ Leverage & Margin Cushion" chart on 💰 Account with the same Weekly/Monthly/All-data toggle as the existing Capital Trend chart. Opus reviewer SHIP, 0 blocking. Forward-only — no backfill possible, the daily inputs were never recorded before this shipped. **Deliberately deferred, not forgotten:** (1) buying power — doesn't exist as a tracked concept in the code at all, would need new SnapTrade balance-payload work; (2) a real per-security margin maintenance rate — still the flat `MARGIN_MAINTENANCE_RATE` assumption, same as the live panel already used; (3) upgrading the existing Capital Trend chart's single-point "Net account value" marker into a real historical line — technically enabled now that `net_equity` has a history column, but not built this pass. **Also still open from the same brainstorm, unscoped:** the original "Option A" market-regime nowcast and "Option B" leverage-linked de-risk trigger themselves were never built — this feature is the prerequisite data foundation only. A backtest during the brainstorm found the market backdrop itself (SPY/VIX/breadth) stayed mild that week (never confirmed risk-off), and that the owner's own signal-driven exits netted positive (+$62.80) while manual "sell on dip" discretionary trades netted negative (-$180.92) — worth revisiting before assuming "more signals" is the fix. Memory `project_leverage_history_capture`.
- ~~F-265 Watchlist "Ready to Enter" proactive email~~ — **ALL THREE verification items CLOSED 2026-09-14; nothing left open.** Both this and its F-264 grading predecessor are code-complete, tested, and Opus-reviewed SHIP. The first real firing (2026-09-10 09:49 ET) sent 21 tickers at once — a **cold-start artifact, not a bug**: F-264/F-265 shipped that same day, so there was no 2026-09-09 `enter_now` baseline for the D-B transition diff to compare against, so every currently-qualifying not-held ticker read as "newly" ready on day one. **Item (3) CLOSED 2026-09-12:** a live DB query confirmed a held ticker (`COF`/`MSFT`/`NFLX`, `already_held=true`) gets captured for grading but stays unemailed, exactly as designed. **Item (2) CLOSED 2026-09-12 (data-confirmed):** the table holds the full raw qualifying set per day, held + not-held. **Item (1) — the transition-cadence question — CLOSED 2026-09-14:** Monday's email fired at 09:46 ET with exactly **one** new name (TMUS) — the small/quiet-delta shape a correctly-working D-B diff should produce on an ordinary day, confirming 2026-09-10's 21-name batch really was just the one-time cold-start effect and 2026-09-11's non-fire really was just the unrelated deploy error, not a recurring cadence problem. Memory `project_watchlist_entries_verification`.
- **Watchlist email decision context (commit `7ee6011`, 2026-09-22) — PENDING LIVE VALIDATION: do the email's capital-basis numbers match 💰 Account?** Shipped and pushed same day; Opus reviewer SHIP/0-blocking traced the dollar-risk figure as **bit-identical** to `risk.py`'s own `actual_risk` (same operands, same order, same rounding, unrounded `risk_per_share`), and the email was rendered against the owner's real book ($24,502 gross / $7,802 net capital) with real CVX/PAY numbers before commit. **But both of those are code-side checks — no one has yet put the live email next to the live 💰 Account page.** Owner will supply an Account screenshot alongside the next real alert. **What to compare:** (1) the card's `⚖️ That risk is ~X% of your net capital (vs Y% of gross book)` line against Account's own margin/net-capital panel for the same moment; (2) the `📐 Size also capped at 25% of net capital — now ~Z%` figure against Account's capital-basis concentration table; (3) that the suggested share count is the one 📋 Watchlist's own ENTER_NOW sizing panel shows for the same ticker. **A mismatch most likely means a freshness gap, not an arithmetic bug** — the email resolves net capital once at ~09:45 ET from `db.load_account_cash`, while Account recomputes live, so an account-cash row updated mid-day will legitimately diverge; check the cash record's `updated_at` before treating a delta as a defect. **Do not close this item on a render-test or a code trace — it needs the two surfaces side by side.** Note the email only fires on a NEW transition, so a quiet morning is the D-B diff working, not a failed deploy. Memory `project_watchlist_entries_verification`.
- **Watchlist email — measured ENTER_NOW base rate. GATED ON DATA, not on work; earliest ~Nov 2026.** The 2026-09-22 decision-context pass (commit `7ee6011`, F-265 addendum in `docs/requirements.md`) gave the email honest arithmetic — dollar risk, dollar target, breakeven win rate, sizing + capital-basis parity with the interactive page — after a requested "confidence this will make money" metric was **refused as un-buildable**: A2 (N=239) measured the composite does NOT rank forward alpha within 65-79, so any confidence figure would be fabricated. **The genuinely honest version of that number is a measured base rate** ("of N matured ENTER_NOW calls, X% beat SPY"), and F-264 has been capturing the rows since 2026-09-09 — ~2 weeks as of the 09-22 pass, needing roughly 1-2 more months to clear the min-call floor at a 30-trading-day horizon. **When built it must be a SINGLE headline, never a per-composite-band breakdown** — a band breakdown would re-imply exactly the ranking A2 disproved. **Do not build a confidence/probability/odds figure on any recommendation surface without a new measurement that overturns A2.** Also surfaced by that pass and genuinely unmade: F-255's net-capital cap is enforced **per-name**, so acting on several same-day alerts can commit far more than 25% of net capital (the real 2026-09-22 CVX+PAY pair would have been ~48%) with nothing saying so — an aggregate same-day view/cap is a policy question, not a defect. Memory `project_watchlist_entries_verification`.
- **🏠 Home priority-zone redesign — DECLINED 2026-08-29, mockup built and reviewed but not on visual grounds.** A code-confirmed problem is real (Act Today sits behind ~13 preamble sections despite the user-manual calling Home "look here first, every day"; 5 independent banner types — Day Shock, price cross-check, stock split, structural alert, broker drift — can all stack the same morning with no cross-type ranking). A mockup proposed a top-of-page Act Today pointer + a consolidated "needs attention" tray. **Declined once the risk read was done, not because the idea is wrong:** the tray would be a *second consumer* of caches (broker drift chief among them) whose state vocabulary was fitted to their one existing consumer only — the exact bug class that broke 🧾 Summary's Book Safety cell just two days earlier (`feedback_overloaded_producer_state`) — and several of the 5 caches are 3-state (`None`=offline vs `[]`=checked-clean vs populated=firing), which a naive tray would collapse into a false "all clear" (`feedback_sentinel_is_present`). Also flagged: a compute-order dependency risk, a second Act Today count that must stay in sync with the real bucket forever (this app has already had that exact drift once), and zero test coverage on `app.py` combined with multi-banner days being rare enough that a bug here could ship unnoticed for weeks. **Do not re-propose from a fresh read of the mockup alone** — start from the risks section in `docs/plans/home-redesign.md`. A narrower version limited to only the already-known-2-state caches would address the load-bearing objection and is the more promising angle if revisited. Memory `project_home_redesign`.
- **Analyst research email ingestion — REDESIGNED 2026-09-01 after live testing, then DEPRIORITIZED same day (user call — not a must-have, no urgency). Opus `planner` verdict: PROCEED WITH CHANGES, zero code written.** Original Gmail/IMAP + auto-forward-filter + 5-vendor-allowlist design is superseded: live testing this session proved (1) Resend's Inbound feature accepts real external mail at a `<id>.resend.app` address with zero DNS/domain setup, and its Received Emails API is poll-safe without a webhook — fits the existing cron-lane batch-job pattern far better than IMAP or a webhook receiver would have; (2) a manual Yahoo "Forward" rewrites the MIME `From:` to the user's own address and inlines the original as quoted text (proven with a real forwarded newsletter, not assumed) — so the original per-vendor allowlist is unworkable against manual forwarding. **New design:** the user manually forwards newsletters (CNBC Pro, Goldman, JPMorgan, BofA, Morgan Stanley) from Yahoo to a Resend inbound address; a new Railway cron lane polls Resend's API (not IMAP), runs the existing `analyst_intel.extract_report()`, and lands results in `analyst_inbox_pending` (dedup key now Resend's own `email_id`) plus a new `research_inbox_config` cursor table, surfacing through the *existing* editable preview-card UI before promotion into `analyst_coverage` — still awareness-only, no gate/score/threshold touched. **Security model changed accordingly:** allowlist collapses to one trusted sender (the user's own Yahoo address) plus a DMARC/DKIM header check exploiting Yahoo's strict `p=reject` policy — honestly scoped as proving only "this left the user's mailbox," never "this is really from Goldman," since Yahoo's inlining destroys the original's DKIM signature; the publisher name is a display-only best-effort label, human-reviewed like everything else. Two policy decisions made 2026-09-01: an unparseable auth verdict is accepted into `pending` with a visible "unverified" flag rather than silently dropped, and the DMARC/DKIM check is not a hard Phase-1 blocker (ship string-only-with-disclosure if Resend doesn't expose a usable signal). Full design: `docs/plans/analyst-email-ingestion.md` (fully rewritten). **The original blockers (mailbox creation, auto-forward filter, capturing a vendor `From:`) are gone** — the manual-forward model needs none of them. **Remaining before Phase 1 build:** set `RESEARCH_TRUSTED_SENDER` env, apply both DDLs, and confirm at build time that Resend's headers actually expose a parseable DMARC/DKIM verdict (§6a in the plan). `cron_runner.py`/`db.py`/`system_health.py` changes will need the mandatory Opus `reviewer` pass when built (bundle `research_inbox.py` into that review too — it's the security-critical decision surface); `constants.py` is deliberately untouched. Memory `project_analyst_email_ingestion`.
- **F-204a 🧾 Summary cockpit — ONE unverified surface left: Act Today's ROW layout. Gated on a trading day with an active protective call; earliest 2026-08-31 (Mon).** Every other zone was verified on live renders 2026-08-28/29. This one cannot be: the book has been clear (`Nothing to act on today`, and Active Vetoes correctly agrees `None active`), so the per-item rows have **never rendered in production**. They replaced a single derived line (`REDUCE — DETERIORATION EXIT APP` from the FIRST item plus "+N more"), so the code path is genuinely new. **What to check when a call fires:** (1) ONE ROW PER ITEM, not one summary line; (2) rows ordered **EXIT → TRIM → WATCH** (they iterate `bucket_act_by_type`'s buckets, not producer order, so the worst leads); (3) each row = colour bar · badge · ticker · two-line rationale · right-aligned composite; (4) **no literal `**` in the rationale** — `risk_advisor` bolds dollar figures and these strings go through `_md_bold`, but per `feedback_streamlit_renderer_mismatch` that class is invisible to tests AND review, so a screenshot is the only check; (5) the header chip counts equal the row count; (6) the purple veto banner appears iff `_reduce_calls` is non-empty. **Best cross-check:** a ticker appearing in BOTH Act Today and Top Positions must show the SAME composite — both read `port_df["Score"]`. A mismatch means one of them regressed to the act item's `momentum_score`, which is misleadingly named (it carries the composite for held names) and was deliberately not used. **Note this may not resolve on Monday** — it needs an actual EXIT/TRIM/WATCH call, not merely an open market. Keep this item until the rows have been seen. **Checked 2026-08-31 (Mon): still `Nothing to act on today`, still unverified** — despite two real sells that day (NEM, BKNG), both were the owner acting on their own **manual stop** price levels, not the app's own `risk_advisor`/`exit_advisor` composite-based advisory firing an EXIT/TRIM/WATCH call. A manual-stop breach and an active app-side protective call are different things and don't necessarily coincide — don't read a manual-stop trading day as satisfying this item's trigger. **First real firing observed 2026-09-21 — 3 of 6 checks now PASS on a live screenshot.** A single-item SPCX `WATCH` (Critical News: 2 tier-2 headlines, sentiment −0.32) rendered as (1) one row, not a summary line ✅; (3) the exact 5-part shape — colour bar · badge · ticker · two-line rationale · right-aligned composite (`58`) ✅; (4) no literal `**` leaking into the rationale ✅; (5) header chip ("1 item needs attention" / "1 WATCH") matched the 1-row count ✅. The Home card for the same ticker showed identical rationale wording, cross-surface-consistent. **Still open, deliberately not closed:** (2) EXIT→TRIM→WATCH ordering is unverified — this was a single-item day, nothing to sort; (7) the Top-Positions composite cross-check (does SPCX also show `58` there, or has it regressed to the misleadingly-named `momentum_score`?) was raised but not yet confirmed by the owner. Keep this item open until both are seen. Spec: `docs/requirements.md` F-204.
- ~~F-261's cold-path refusal~~ — **SHIPPED 2026-08-27, PRODUCTION-VERIFIED 2026-08-29.** The fix stops the app sizing trades against a fabricated $50,000 book when a session has **not** visited 🏠 Home. **Cold-path PASS**, owner screenshot: 📋 Watchlist → ONON's ENTER NOW row expanded on a fresh session shows *"Position sizing unavailable — your portfolio value isn't loaded in this session, so any share count would be a guess. Open 🏠 Home to load it, then come back"* — no blue *POSITION SIZING* panel, no fabricated share count. The Recommended Action copy also held up: "Open the position, sized to your risk rules" (the F-255-era fix that removed the dangling "use the position sizing below" pointer on a panel that isn't there). **The WARM path was already verified 2026-08-27** (2 shares @ $935.39, F-255 net-capital cap binding correctly). Both halves now confirmed — nothing left to check on this item.
- **~~Streamlit `use_container_width` deprecation~~ — MIGRATED + PRODUCTION-VERIFIED 2026-08-27. All 140 mentions in `app.py` are gone; the pin stays at `streamlit==1.57.0` deliberately.** Verified on screen, not just shipped: 🥧 Portfolio Overview's Sector Exposure chart renders at roughly full page width (~1515px) where plotly's DEFAULT is 700px, so `width="stretch"` is provably honoured by the deployed 1.57.0 — a silently-ignored kwarg would have collapsed every chart to 700px. Kept as a struck-through entry rather than deleted because two facts here are worth not re-deriving. **(1) The pin is what gives runway, not the version number** — both 1.57.0 and 1.60.0 are frozen until someone bumps them, and the stated removal date (`2025-12-31`) was already ~8 months past while 1.60.0 still accepted the kwarg, so upgrading would have moved *closer* to the release that finally drops it, not further away. Migrating removed the deadline outright, which is why it was done first. **(2) The venv/pin divergence — CLOSED 2026-08-29.** Decided with the user: align the venv down (not bump the pin) — production has been stable on 1.57.0, there was no identified feature need for 1.60.0, and the local venv's job is to validate what's actually deployed, not run ahead of it (memory `feedback_venv_resolve_over_force`). `.venv` downgraded via `pip install streamlit==1.57.0` (resolved clean, no dep conflicts, `1.60.0` cleanly uninstalled first). Full suite re-run on the realigned venv: **4665 passed**, no regression. Local and production now match exactly.
- **~~Markdown bold leaks as literal `**` on advisor text~~ — FIXED 2026-08-28, and the scope was WIDER than this entry said.** `util.md_bold_to_html()` now converts at the RENDER boundary (escape first, then `**x**` → `<b>x</b>`), applied at **9** sites. The advisor still emits markdown and knows nothing about its renderer, as this entry required. **Three things worth keeping.** **(1) This entry named 📋 Watchlist; the class also lived on 🔗 Risk Analysis and 🥧 Portfolio Overview**, where `risk_advisor` bolds **dollar loss figures** on cards issuing TRIM calls — higher stakes than the Watchlist instance that prompted it. Found by an Opus review sweeping all 43 dynamic-HTML sites, after my own grep returned a confident wrong answer: I searched for the two variables I had already found (`_wr['detail']`, `_wr['summary']`) rather than sweeping the render sites, so the search could only confirm what I knew. The screenshot's own strings (`Portfolio beta is already **1.44**`, `β **3.66**`) reach the screen via `portfolio_caution` and `conditions_missing`, neither of which my first fix touched — I would have closed this entry while the reported symptom still rendered. See `feedback_confident_negative_from_weak_check`. **(2) It also closed a real escaping gap** — every one of those sites interpolated advisor strings RAW into `unsafe_allow_html`. **(3) A second defect surfaced from the same screenshot and is also fixed:** the ENTER NOW copy said *"Use the position sizing below to calibrate shares"* unconditionally, but F-261 WITHHOLDS that panel when the portfolio value is unknown — so a cold session was issued a BUY imperative plus an instruction to use something not on screen. The advisor now says *"sized to your risk rules"*; the concrete pointer was restored at the RENDER layer, gated on the panel having actually rendered. ~~Still open, deliberately deferred to its own commit + review~~ — **CLOSED same day, commit `62a61c2` ("fix(gate): let the XSS rule burn down, and bind exemptions to a binding"). This paragraph was simply never updated after the fix shipped — caught 2026-09-14 while triaging a "what's next" list, before any duplicate work started.** `_is_dynamic_html` now credits an f-string whose every interpolation is a direct call to `safe_html`/`md_bold_to_html` (296 → 286 baselined instances), with the exemption bound to the actual import BINDING rather than a bare name (closing a same-day Opus-reviewed FIX-FIRST finding: 4 bypasses could otherwise forge credit via shadowing/rebinding). A nested format-spec is still treated as dynamic (its fill character is attacker-arbitrary), and the attribute form `_html.escape(...)` is deliberately left uncredited. Test hardened from `assert live > 250` (caught 1 of 3 unsafe mutations) to an exact `live == baseline` equality, mutation-verified on every axis. Review = Opus reviewer (Opus 5): FIX-FIRST, 1 blocking (fixed same commit); full suite 4578 passed.

- **Gate Suppression Ledger — BOTH halves now shipped (F-259 capture 2026-08-27, F-259b readout 2026-08-30).** "🛑 The Road Not Taken" (owner-only page) reads the capture half's data and grades each gate's historical suppressions vs SPY at a 30-trading-day horizon. Full build detail, the two Opus review rounds (FIX-FIRST/1 blocking — an import-isolation test missed `risk.py`, the live sizing engine — then SHIP/0 blocking), and the four new `GATE_LEDGER_*` constants: `docs/requirements.md` F-259b; `docs/plans/gate-suppression-ledger.md` (top status line + §5/§5a); `docs/architecture.md`'s `gate_ledger_readout.py` module section. **Expect "building" on every gate for ~2 months** — the ledger is only ~3 days old and the N=8-calls/K=5-distinct-tickers floors are far beyond current data; this is the designed primary state, not a bug to chase or re-investigate. **Two things still genuinely open, both dated:** (1) **G-20's `counterfactual=true` (the "A2 in-place upgrade") is still unobserved in production** as of 2026-08-30 — needs a held WATCH name with composite ≥ 65 and gap ≥ `ADD_WINNER_MIN_GAP_PCT` (8.0) on a bull day, which cannot be forced; the readout's G-20 row will correctly show "building" until one appears, and its footnote already discloses why a low count there must not be read as "rarely binds." (2) **The retirement review, 12 months after the first row lands (~2027-08-27).** Pre-registered in §5 *before any data existed*: if by then no `gate_id` has produced an evaluable verdict distinguishable from zero, **the readout is retired** — retiring is the SUCCESS condition of that criterion, not a failure. Do not soften it later to save the feature. **The observability gap for the CAPTURE half's own write success/failure is still unfixed** (a separate, smaller item from the readout): `_gate_ledger_save_result` is written to `st.session_state` and rendered nowhere, so a successful "nothing to record" is indistinguishable from a failed write on the app path. **CLOSED 2026-09-01** via a 6th 🩺 System Trust check, `system_health.check_write_outcomes()` (commit `f8a1155`, `feat(system-health): grade interactive write-outcome dead-diagnostics`) — grades both this gap and the same pre-existing shape in `_rec_log_save_result` as one defect class. **Scope note, 2026-09-13:** this bullet describes the ORIGINAL 9 `grow_today` sites only — the ledger now covers 5 more (G-02/G-05/G-06/G-13/G-18, shipped as F-270; see the "Investor Maturity Roadmap" entry above) via 3 additional pure builders, with the original 9-site capture function completely untouched by that expansion. Memory `project_gate_suppression_ledger`.
- **Margin-maintenance distance-to-a-call — Slice 1 SHIPPED as F-253, Slice 3's awareness half SHIPPED as F-254 (both 2026-08-24), Slice 3's policy half SHIPPED as F-255 (2026-08-25, option (b) below).** MEASURED 2026-08-23: the owner runs **3.15x leverage by deliberate policy** ($24,503 of stock on $7,802 of capital, $16,701 debit) — every risk surface measured the book, none measured the loan. F-253 closed that: 💰 Account now shows margin cushion, call-trigger %, leverage ×, and a distance-to-call bar with the `FRAGILITY_PULLBACK_PCT` marker — awareness only, `MARGIN_MAINTENANCE_RATE` gate-isolation-tested. F-254 closed the second half of the gap — **the gates are gross-book-basis, not "equity-basis" as previously documented** (verified in code: `portfolio.py` sums ALL holdings, margin-bought shares included; there is no equity/gross split in the code today) — by adding a `stock_analyzer.margin.capital_basis_weight()` dual-basis readout next to the leverage banner's sector line and a new sector-level table in 💰 Account's concentration expander, so every gate-relevant number now shows its capital-basis twin. Awareness only, zero gate/denominator/threshold changes, no reviewer required (not a `_GATE_FILES` touch). **The policy decision was walked through directly with the user (not delegated to planner alone) with three options: (a) leave gross-basis gates as-is, capital-basis stays a display-only mirror; (b) add a SEPARATE, additive capital-basis cap alongside the existing gross-basis one, touching nothing about its math/denominator; (c) recalibrate the existing gate's own input to `net_capital` (same threshold numbers, different meaning).** A `planner` pass had scoped (c) and explicitly recommended against it without the user, since it would flip "leverage is awareness, never a gate" into an actual gating change across ~10 modules (`portfolio.py`'s shared weight denominator, the Judge's concentration opinion, rebalancer trim math) and reintroduce the transient-cash instability equity-basis was chosen to avoid (net capital moves with cash balance day to day even when no position changed). **The user chose (b).** Shipped as F-255: `NET_CAPITAL_POSITION_CAP_PCT = 25.0`, additive to `SINGLE_NAME_CEILING`, wired into every new-position sizing surface (Grow Today, Analysis, Watchlist, the pullback-add flow, the cron/email buy-list) plus an awareness-only 💰 Account banner for holdings already over the cap (ALB/OXY today) that deliberately never feeds `risk_advisor`/`exit_advisor`. Two Opus-reviewed commits, both SHIP/0-blocking (`af5d4ca`, `a9e920a`). **Option (c) — recalibrating the existing gross-basis gate's own denominator — remains genuinely unbuilt and undecided**; it is a materially bigger, riskier change than (b) and was not chosen. Don't confuse it with F-255. The emailed buy-list gap is now **partially** closed: `notify.py`'s `_sizing_cap_note` shows a capital-basis disclosure whenever the cap actually binds (capped or infeasible), but does not show a capital-basis `%` on every pick unconditionally the way `port_pct` (gross) always does — that fuller parity, if wanted, is still unstarted. **Option (c) — PARKED 2026-08-25, explicitly declined, not a build queued for later "eventually":** the user is planning to exit COF/CRM/MSFT soon and/or whenever the Exit Advisor's own WATCH/TRIM/EXIT signals fire on them (unrelated to concentration), so the concentration exposure is expected to resolve through normal trading, not through a new gate. Weighed directly: (c)'s cost is real (~10 modules, a fresh `planner` + mandatory `reviewer` pass, and it reintroduces day-to-day cash-driven noise into gates that are today stable) against a risk that is already bounded, visible (the F-255 Account banner), and finite (3 named tickers, not a growing category) — building a structural gate change to guard against a risk the user is already positioned to close manually was judged not worth it. **Trigger to revisit:** the three positions are NOT exited/trimmed over a meaningfully long period AND the over-cap list keeps growing rather than resolving — i.e. if this stops looking like "a temporary legacy set working itself out" and starts looking like "an accumulating blind spot." An explicit user re-ask is also sufficient; simply having (c) sit on this list is not itself a green light. Memory `project_margin_maintenance_blindness`.
- **F-252 follow-ups — three of four now closed.** **(1) CLOSED 2026-08-23:** DDL applied, table live and verified, `broker` cron confirmed scheduled and running (fires on weekends too). **(2) CLOSED — first capture VALIDATED 2026-08-24:** `all_accounts_ok = true` and a `positions` map matching real Robinhood holdings, confirmed via direct query. **(3) SHIPPED 2026-08-24:** F-249's suggested share size is annotated when the book disagrees with the broker — both the morning "New Positions to Initiate" email and the intraday pullback-entry email now carry a book-wide drift disclosure (`notify._book_drift_banner`), computed headlessly via a new `compute_morning_picks` call site that reuses the existing pure `broker_sync.decide_drift_banner`. Never alters the suggested size — annotation only. Full detail: `docs/requirements.md` F-252 entry. **(4) Still deferred, judged not a gap:** an Account-page in-session override that writes the fresher live diff back into `_broker_drift_cache`. The only divergence it closes is a stale-positive (Account computes clean while Home still shows dated drift), and Home already dates its own claim. The inverse cannot happen, because the book side is always live.
- **`daily_snapshots` coverage — MEASURED 2026-08-24, and it's healthy. Earlier "48% missing" figure stays RETRACTED.** Read directly off F-247's `attribution_readiness.snapshot_coverage()` panel (📊 Predictive Analytics → ⏱️ Entry Timing tab → "Alpha Attribution" expander): **93% of expected NYSE sessions captured** (50 of 54 sessions, 2026-06-09 to 2026-08-24), only 4 sessions missing with a longest unbroken gap of 2 — no long outage, just scattered small misses. **Not a data-quality gap anymore.** The only real number is a countdown: **50 of the 180 captured days** E2 Alpha Attribution needs (27% there, ~130 more trading days — roughly mid-March 2027 at this pace), which is time passing, not something to fix. Memory `project_alpha_attribution_readiness`.
- **F-249 Phase 2 — VALIDATE THE FIRST LIVE CAPTURE. CLOSED 2026-08-24 — PASS.** DDL applied 2026-08-23 (Sunday); first trading day 2026-08-24 produced two qualifying `new_pick` rows: **ALB** (25 sh × $143.25 = $3,581.25 ÷ $24,502.62 = 14.6% of book, `rec_sizing_version=2`) and **OXY** (59 sh × $60.10 = $3,545.90 ÷ $23,860.46 = 14.9% of book, `rec_sizing_version=2`) — both under the 15% ceiling with the version stamp set, confirming Phase 1's cap and Phase 2's capture are both live and correct in production. (An earlier same-day partial pull of this query returned only `buy_candidate` rows — those never carry sizing by design and were mistakenly read as inconclusive before the fuller pull surfaced the two `new_pick` rows above; corrected same session.) Cross-checking the Grow Today card / buy-list email text for ALB and OXY against these numbers is optional confirmation, not required to close this. Plan `docs/plans/sizing-calibration.md`; memory `project_sizing_calibration`.
- **Cron DST schedules — ALL FOUR LANES RESOLVED 2026-08-23. Three residuals only.** The 2026-08-07 GitHub-Actions -> Railway migration collapsed each lane's two `schedule:` entries into one Cron Job service and kept the **winter** expression, so every affected lane ran exactly one hour late in EDT. Proven from the `cron-premarket` deploy logs (exactly ONE `Starting Container` in a 14-hour window), not inferred. Fixed by extending the HOUR field to a comma list per service — the existing gates discard the wrong slot. **`cron-premarket` `0 12,13 * * *`**, **`cron-scan` `45 13,14 * * *`**, **`cron-intraday` `30 15,16 * * 1-5`** (all applied and confirmed on the Railway dashboard). **`cron-eod` `30 21 * * 1-5` was already correct and was NOT changed** — 17:30 ET EDT / 16:30 ET EST, both post-close, both clearing `ALERT_EOD_HOUR_ET = 16`; its gate boundary sits far enough from the schedule that a DST shift cannot cross it, which is exactly why it survived when the others did not. **Do not 'fix' it.** Full detail in [docs/shipped-log.md](docs/shipped-log.md). **Residual 1 — intraday winter floor — RESOLVED 2026-08-25.** Unlike premarket/scan, intraday's gate was a *floor* (`hour < 10`), not a boundary the two slots straddle, so in EST **both** slots cleared it: 10:30 ran and 11:30 deduped, i.e. winter shifted an hour *earlier* than the docstring's "~11:30 ET" rather than self-skipping. Harmless (10:30 satisfied the gate's stated "opening volatility has settled" intent) but undecided — decided directly with the user before the 2026-11-01 DST flip: lock 11:30 ET year-round. Shipped as a new named constant `CRON_INTRADAY_START_HOUR_ET = 11` in `constants.py` (`cron_runner.py::_run_intraday`'s floor now reads it instead of a bare `10`), Opus-reviewed (SHIP, 0 blocking), 8 new boundary tests in `tests/test_cron_intraday_gate.py`. The Railway schedule itself (`30 15,16 * * 1-5`) is unchanged — only which of the two daily UTC firings does the real work changed. ~~Residual 2 — verify the fixes on the first trading day (2026-08-24+)~~ — **RESOLVED, verification already happened.** Memory `project_cron_railway_migration`'s 2026-08-25 entry cites real dual-slot EDT/EST firing timestamps observed post-2026-08-24 — that data is what justified shipping `CRON_INTRADAY_START_HOUR_ET=11` the same week, so the verification this bullet was asking for already occurred; the bullet just never got marked done. **Residual 3 — log noise, observability not correctness.** ~20 `missing ScriptRunContext!` lines render as RED errors per premarket run, so a real failure would be hard to spot; `docs/architecture.md` claims the lane runs "headlessly (NO Streamlit)" but something in the import chain pulls Streamlit in. **Already closed by the fix, do not re-chase:** the buy-list email footer's "Exit signals shown above are from the 8am premarket run" (`notify.py:566`) is TRUE again now that premarket lands at 08:00 ET year-round; and `alert_state` row 4 is the **intraday** lane (`_INTRADAY_ROW`, `cron_runner.py:93`), alive, last fired 08-19 — the "lane unidentified" note is closed.
- **Correlation matrix: listwise → pairwise-complete deletion. OPEN DECISION, gated on reading one number (2026-08-21).** F-246 shipped the *measurement* (`portfolio.correlation_coverage`, memory `project_correlation_sample_size`); the strategy itself is unchanged. `portfolio.correlation_matrix()` drops rows **listwise across every ticker**, so the matrix runs on the intersection of all histories and **one short/degraded fetch silently thins the sample for every pair** — a thin sample pushes correlations toward zero, which renders as diversification the user does not have. Live trigger: the app reported avg correlation **0.12** and classified an 18-name tech/semis book **"Well Diversified"** while flagging CRM×SAP at 0.77 and omitting NVDA/MU/MRVL. **MEASURED 2026-08-21, same day: `n_obs` = 125 daily returns shared by all 18 of 18 holdings — a full 6 months, nothing capping the sample. The matrix is SOUND on this book and the 0.12 reading is a genuine measurement, not a thin-sample artifact.** So the listwise fragility is real but **not currently firing**, and the suspicion that motivated F-246 is closed. **Do not treat this as an open bug.** It remains queued only as a latent risk: a single degraded fetch would still thin every pair at once, and F-246's coverage caption is now the tripwire that would show it. **Revisit only if that caption ever reports a materially short sample.** If it does, this is NOT a quick fix — the same matrix feeds the diversification score and classification, the 🧩 Intelligence correlation clusters, F-230 Correlation Under Stress, the Diversification Advisor's ADD recommendations and F-245, so it needs `planner` + `reviewer` and a decision on whether varying per-pair sample sizes are acceptable. Do not bundle it into an unrelated change. ~~Second, smaller follow-up from the same review~~ — **CLOSED 2026-09-01.** The Dashboard caption used to read "weighted avg pairwise **6-month** return correlation" unconditionally, which could mismatch the real sample size (e.g. 40 observations) shown immediately below it. Fixed at `app.py:14288` — the caption no longer asserts a specific window; it now reads "...weighted avg pairwise return correlation (actual sample window shown below)" and defers the real number entirely to the existing coverage caption directly beneath it, so there's only one place the window is ever stated. Display-only text change, no `_GATE_FILES` touch, no reviewer required.
- ~~Forward Portfolio Simulator Phase 2~~ — **CLOSED 2026-08-29, per its own pre-registered falsifiable criterion.** Phase 1 SHIPPED 2026-08-21 as F-245 (🧯 After My Rules — see `docs/shipped-log.md`, plan `docs/plans/forward-portfolio-simulator.md`, memory `project_forward_portfolio_simulator`) and found the mechanism SHAPE was real: a Rate-Spike scenario (SPY −12%) breached 12 of 18 protective stops, and the driver is **stop width vs shock magnitude** (`ATR_STOP_MULT = 2.0` puts stops ~6% below price while a −12% SPY at β 1.22 moves names −15% to −25% — the stops sit inside the shock by construction), not correlation (measured mean pairwise 0.12, F-246). Phase 1 explicitly left the COST unmeasured, deferring to `PROTECT_TRACK_MIN_CALLS` (8 matured protective calls, expected ~late Oct 2026) as slow evidence about whether exits are systematically early. **That slow path is now moot.** The 2026-08-26 app review's Innovation #2 (`scripts/exit_ladder_replay.py`, "W6") answered the same question directly and faster: it replays BOTH exit mechanisms (the deterioration ladder AND the `ATR_STOP_MULT` stop) day-by-day against the owner's own real closed losing round trips, with a falsifiable criterion pre-registered *before any run*: "if the engine's would-have-fired date is NOT systematically earlier than the actual sell date... the exit ladder is not the lever, and the whole Forward-Simulator Phase-2 branch should be closed rather than waited on." **Result, converged across three independent methodology tightenings** (ladder-only → +ATR stop → +intraday-low breach check — all landing within 22–24%, i.e. added rigor did not move the number): of 54 evaluable closed losing round trips, only **13 (24%)** got ANY protective signal before the actual sale; **13 (24%)** fired same-day only (too late); **28 (52%)** got NO signal at all, from either mechanism, for the entire hold. The criterion is met — closing per its own pre-registered rule, not a judgment call made after seeing the number. **What this means for the underlying worry:** Phase 1's premise was that the app de-risks too aggressively/early and strands the owner in cash, needing a re-entry counterpart — this data shows the opposite emphasis (realized exits are frequently late or absent, not early), so a Phase 2 built to aid re-entry after an over-eager exit would solve a problem this measurement doesn't support. **A separate, unscoped observation, not grounds to reopen this:** most of the 54 round trips were held only 0–10 days, far shorter than either mechanism needs to act — that points toward entry discipline/position sizing as a possible *future, different* idea, not a reason to revive re-entry/redeployment work. **Don't re-propose without new evidence** — an explicit user re-ask citing a different measurement, not a repeat of the Phase 1 mechanism argument this entry already engaged. Full detail: memory `project_forward_portfolio_simulator`.
- **Two follow-ups from the F-240 scan-universe refresh (2026-08-16) — BOTH CLOSED 2026-09-02.** (1) **Materials / Utilities / Real Estate buckets — CLOSED, commit `2333beb`.** All three GICS sectors had zero taxonomy coverage anywhere in the app; now have user-confirmed `_SECTOR_IMPACT` severities, `SECTOR_ETF` benchmarks, and `_SECTOR_PROFILES` correlation entries (Materials corr 0.30/XLB, Utilities 0.15/XLU, Real Estate 0.22/XLRE), plus 15 seeded `TICKER_SECTORS` entries (LIN/DOW/APD/FCX/NUE, DUK/SO/D/AEP/EXC, AMT/PLD/EQIX/O/SPG). Opus reviewer SHIP, 0 blocking. **Deliberately NOT added to `_DIVERSIFYING_SECTORS`** (whether the app actively recommends these as ADD suggestions is a separate, unmade policy call) or to `RATE_SENSITIVITY`/`stress_test._SECTOR_SHOCKS`/`earnings_advisor._SECTOR_DEFAULTS` (all separate, larger, not-yet-decided items). **Scope nuance:** this closes the taxonomy gap only — the scan/discovery universe ROSTER itself is Supabase-backed since F-262 and unaffected, so none of the 15 seeded tickers are automatically scanned; adding them to the scan roster is a separate ⚙️ App Settings data step, not yet done. (2) **`_DIVERSIFY_TO_DISCOVERY` coherence — CLOSED, commit `ee0e0f6`.** The real blocker turned out to be bigger than this bullet used to say: it's not just a missing `_SECTOR_PROFILES` corr value, it's that Industrials was missing from `_DIVERSIFYING_SECTORS` entirely (the actual iteration gate in `diversification_recommendations()`) AND had no `sector_candidates` roster bucket in Supabase at all — adding a `_DIVERSIFY_TO_DISCOVERY` key alone would have stayed inert. Fixed all three: `_SECTOR_PROFILES["Industrials"]` (corr 0.35, pattern-matched to Financials/EV & Auto), added to `_DIVERSIFYING_SECTORS`, `_DIVERSIFY_TO_DISCOVERY["Industrials"] = "Industrials & Defense"` (intentional many-to-one reuse of Defense's existing bucket). New `sector_candidates` "Industrials" bucket (CAT/GE/GEV) seeded directly via SQL against `reference_tables` (bypassing the App Settings editor's structure lock, which only governs UI-driven edits — this is the "reviewed code change" path that lock's own test docstring anticipates), then a no-op Save through the UI to let `save_reference_table()` recompute `payload_hash`/`as_of` correctly. Opus reviewer SHIP, 0 blocking. **One non-blocking finding tracked as a follow-up, not bundled here:** Industrials and Defense now share the same "Industrials & Defense" discovery bucket with no cross-card dedup in `diversifying_candidate_pool()`, so the same non-held name (e.g. RTX/GD/CAT) can surface under both the "Add Defense" and "Add Industrials" ADD cards, and Defense names can appear mislabeled under the Industrials card (the reverse already existed for Defense before this change). Awareness-surface only (Risk Analysis / Signals, not Act Today) — cannot suppress a protective call or break a gate. Fix: filter each card's discovery-bucket slice to names whose `TICKER_SECTORS` value equals that card's own sector, or give Industrials a dedicated bucket — its own review since it touches shared `diversifying_candidate_pool`/Defense behavior. Memory `project_scan_universe_refresh`.
- ~~A `Utilities` sector~~ (2026-08-17 RECOMMENDATION: WAIT) — **SHIPPED 2026-09-15, as part of F-274's beta-repair work.** The trigger that reversed the earlier WAIT call: F-274's new defensive-beta candidate ranker needed a real low-beta lever, and Utilities has the best defensive corr profile of any sector in `_SECTOR_PROFILES` (0.15, tied with Healthcare) — leaving it un-recommendable structurally weakened that ranker, a stronger reason than the original "fourth option in a crowded band" framing anticipated. Of the 13 policy numbers this entry originally priced: 5 already existed (`_SECTOR_PROFILES` corr, `SECTOR_ETF`→XLU, `TICKER_SECTORS`, `_SECTOR_IMPACT` ×4 — landed 2026-09-02 under F-240). This commit closed the remaining ones: `macro.RATE_SENSITIVITY["Utilities"] = -0.50` (user-confirmed), `stress_test._SECTOR_SHOCKS` × 6 scenarios (user-confirmed, landed as F-274's own prerequisite commit, `23252ff`), `earnings_advisor._SECTOR_DEFAULTS["Utilities"] = 3.0` (user-confirmed — below every other sector's value, reflecting genuinely lower earnings-day volatility for regulated, rate-case-driven utility earnings), the `_DIVERSIFYING_SECTORS` policy call (yes — it now actively recommends utilities), and `_DIVERSIFY_TO_DISCOVERY["Utilities"] = "Clean Energy & Utilities"` (reuses the existing bucket, same many-to-one pattern as Industrials/Defense — safe because `diversifying_candidate_pool`'s discovery-bucket slice is `TICKER_SECTORS`-filtered, confirmed by Opus review). **The Supabase `sector_candidates` roster bucket (DUK/SO/D/AEP/EXC) — CLOSED 2026-09-15, same day.** Seeded by the owner via a direct SQL merge against the live `reference_tables` row (`payload || '{"Utilities": [...]}'::jsonb`, verified by a read-back before and after) — a coding session has no live DB credentials to run this itself, so the owner executed it directly, then did the required no-op Save through ⚙️ App Settings to re-stamp `payload_hash`/`as_of` correctly (same two-step sequence the Industrials precedent, `ee0e0f6`, used). Utilities is now fully live: `diversifying_candidate_pool`'s roster slice returns DUK/SO/D/AEP/EXC immediately, no redeploy needed. `docs/plans/beta-repair.md`; memory `project_beta_repair`.
- **`VST` reachable in the Clean Energy candidate pool (2026-08-17, accepted and documented, NOT a bug to chase).** Removing three micro-caps from the discovery bucket advanced later names past `DIVERSIFY_SCAN_CAP`, so `VST` — previously cut — is now a pool candidate. Accepted: it is a pool candidate, not a roster name the app *asserts* as a representative, and `_SECTOR_PROFILES["Clean Energy"]["corr"]` is unchanged, so no claim moved. **A genuine exclusion needs a per-sector deny-list inside `diversifying_candidate_pool`** — NOT an ordering trick (a first attempt at exactly that promoted CEG/VST *into* the pool) and NOT a pool-position test (both consuming surfaces re-rank by composite before display, so such a test guards nothing while looking like it does). **Trigger:** VST or CEG actually observed surfacing as a suggestion. Largely subsumed by the live-correlation item above.
- ~~Ragged `_SECTOR_IMPACT` category coverage — the INERT half~~ (2026-08-16, found by the F-240 review; the live half was fixed then) — **CLOSED 2026-09-09.** `Consumer` gained AI & Cloud (2) / Cybersecurity (1); `Activity` gained AI & Cloud (2) / Consumer Tech (1) / Cybersecurity (2) / Healthcare (1) — all 6 severities set directly with the user, each derived by matching the closest existing analog already in the table (e.g. AI & Cloud mirrors AI & Data's value, since those two carry identical severity everywhere else in the map). **Still inert today** — `Consumer`'s own `_STATIC` event (Retail Sales) remains MEDIUM and `Activity` carries no `_STATIC` rows at all, so neither gate site reads these cells live yet — but the coverage gap itself (the thing that actually held the gate closed, per the Employment/Cybersecurity lesson below) is closed ahead of the trigger rather than after it fires. `test_every_keyed_macro_category_covers_the_same_sector_set`'s allowlist is now empty; both categories cover the same sector set as Inflation/Employment. No reviewer required — inert data-completeness fix, not a live behavior change. **The cautionary half, still worth keeping:** the same 2026-08-16 review initially had `Employment × Cybersecurity` filed here as inert debt too — it was not. `Employment` carries Non-Farm Payrolls at HIGH ×24, so that one gap left all 7 cyber names unsuppressible ahead of every payrolls print; it was fixed in the same commit at severity 2. Check a category's actual `_STATIC` impact levels before ever calling one of these inert.
- **Semiconductor concentration in the scan universe (2026-08-16, noted not queued as work). Caveat added 2026-09-02: this is about the F-240 "scan universe" roster specifically, not the wider Movers-scan net.** LRCX $416B, ARM $298B, KLAC $266B, TXN $255B, MRVL $199B and ADI $190B were absent from that roster and all large — but all are **semis**, which is the concentration F-240 was correcting; adding them would undo the 53%→44% tech rebalance. Recorded so a future "refresh" doesn't read the absence as an oversight and re-add them without engaging this reasoning. **Since confirmed distinct from `discovery_universe`'s "Semiconductors" bucket** (commit `6a7ef61`, 2026-09-01), which already contains ADI/KLAC/ARM/NXPI/MCHP/ON/TER/SWKS/MPWR/SMCI/WOLF as discovery candidates — a different, wider roster serving a different feature. **Also note:** F-262 Commit 3 deleted the hardcoded `_SECTOR_CANDIDATES`/`SECTOR_UNIVERSE`/`DISCOVERY_UNIVERSE` dicts from code entirely — both rosters are now Supabase-backed via `reference_data.resolve_universe`, so confirming the F-240 scan roster's exact current membership needs a live DB check, not a code grep.
- ~~Raw-GICS-vs-curated-sector shadow defect~~ — **ALL FOUR discovery buckets CLOSED 2026-09-01** (Semiconductors `6a7ef61`, Financials `934413d`, Software & Cloud `ddc7697`, Internet & Media `3cb77b7`, Consumer & Retail `3b34cb5` — 46 tickers total, every commit Opus-reviewed SHIP/0-blocking, `TICKER_SECTORS` now 173 entries with zero collisions). A discovery-mover pick in an unmapped ticker could silently bypass the macro-suppression gate; every genuinely ambiguous classification was confirmed with the user before implementation, not decided solo. Full detail: `docs/shipped-log.md`. **One structural follow-up left, not part of this closure:** `reference_data.validate_payload`'s `TICKER_SECTORS`-coverage check is scoped to `sector_candidates` only, not `discovery_universe` — so a future ⚙️ App Settings edit adding an unmapped ticker to any discovery bucket can silently reopen this exact class of hole. No trigger to build yet; noted so it isn't rediscovered from scratch.

~~Related gap found 2026-09-01 (F-262 App Settings Commit 3 review)~~ — **CLOSED same day, commit `9431f17`.** `reference_data.validate_payload` now runs a presence-only `TICKER_SECTORS`-coverage check for `sector_universe`/`discovery_universe` too, scoped to newly-added/moved tickers via `changed_tickers()` so it grandfathers the ~39 pre-existing not-yet-reconciled names instead of hard-blocking the next unrelated save. Opus-reviewed SHIP, 0 blocking; production-verified on the live Railway deploy same day. Memory `project_app_settings_design`.
- ~~Outage-gate decision logic extracted into testable pure functions~~ — **SHIPPED 2026-08-25 (commit `1b12779`), and all three cosmetic follow-ups CLOSED 2026-09-01 (Opus reviewer: SHIP, 0 blocking).** `db.classify_load_result(h, w, t, now) -> dict | None` and `stock_analyzer/outage_gate.py::decide(fail_rec, page, safe_pages) -> ("stop"|"warn"|"info"|"none", message)` own the allowlist application, scope branching and message text — `app.py`'s call site is render-only wiring. **The three 2026-08-17 cosmetic findings are now fixed:** (1) `classify_load_result`'s `h is None` branch no longer calls `unavailable_detail()` (which re-read holdings a second time purely to word the message, even though the caller's own read already confirmed the failure) — it now uses a cheap `has_db()`-only check, making the function's own "no I/O of its own" docstring claim actually true; (2) a safe page (📖 User Guide / 🩺 System Trust) rendering during an active holdings outage now shows a `st.info()` banner instead of looking identical to a healthy session (new `"info"` verdict, distinct from `"none"`); (3) the `safe_pages` tuple passed to `decide()` is now pre-filtered by `db.is_readonly()` before use, so a read-only viewer's "stop" banner no longer names 🩺 System Trust, a page already hidden from their nav. Full suite 4981 passed; antipattern/constants-doc gates green; `tests/test_db_outage_gate.py` updated (2 changed, 2 new, 31 total). Memory `project_db_outage_gate`.
- **🧾 Prior Trades — ALL carry-overs now closed (F-237).** Phase 1 (tab) and Phase 2 (Trade Plan pointer, F-237c) both SHIPPED 2026-08-14, along with the vs-SPY window guard (F-237d) — see `docs/shipped-log.md` and memory `project_prior_trades_tab`. **The `_m()` privacy mask gap SHIPPED 2026-08-25** — `decision_context.portfolio_value` in the conditions-at-entry line now wraps through `_m()`, matching Home/Summary; deliberately scoped to just that one ambient-total figure, not the round trip's own entry/exit/realized/unrealized dollar figures (widening it there would be a UX decision, not a bug fix). **The last two open items CLOSED 2026-09-02, commit `627b3ee`:** (1) **cropped-chart caption** — new pure `ticker_history.chart_start_gap()` detects when the widen-fetch didn't reach back to the trade history's first entry, and the existing span caption now discloses it instead of unconditionally claiming the chart shows "a wider window." (2) **per-rerun recompute cost** — new pure `ticker_history.trades_fingerprint()` keys a single-slot `st.session_state["_prior_trades_render_cache"]` memo (bounded, not a growing per-ticker dict) around the widen/SPY-rebuild/`build_pnl_series`/figure-construction chain, keyed on `(ticker, trades_fingerprint, price)` — price included deliberately so unrealized figures on an open position keep updating as the price cache refreshes. Neither touches a `_GATE_FILES` member or a decision/gate/score — `app.py` isn't in that list and Prior Trades is awareness-only, so no Opus review was required; both closed via the free deterministic gates (full suite + antipatterns). **The situational-category tag SHIPPED 2026-08-25 as F-237e** — the "What you wrote at the time" expander now shows the entry's `situational_category` (F-257's locked vocabulary) as a badge, read from the opening BUY row. Purely additive, non-gate, no reviewer required. Still deliberately unbuilt: a **SELL-side** analogue (n/a for now — `situational_category` is a BUY-only field by F-257's own design). Full detail: `docs/plans/prior-trades-tab.md` §7.
- **System Proprioception — Phase 2 (suppression meta-gate). CLOSED 2026-08-07 — not warranted.** Phase 1 SHIPPED same day (F-235). A `planner` (Opus) provable-input audit ran same session and found **no qualifying decision surface**: every high-value decision (EXIT/TRIM/WATCH, risk advisor, composite, watchlist) recomputes live at render time from the resilient multi-source provider layer — none reads a cron-written store as its decision input (`exit_signals` is a history capture, not the live EXIT decision source). The one surface with a single cron input (`scanner_cache` → cold-load buy list) already fails safe to an empty list. The live-recompute architecture *is* the resilience Phase 2 would have bolted on. **Do not re-propose** without a structural change to the decision layer (e.g. a new surface that explicitly reads a single cron-written store as its gating input). Full audit + reasoning: `docs/plans/system-proprioception.md` Phase 2 section; memory `project_system_proprioception`.
- ~~Predictive Modeling Shadow Layer — Phase 2 (F-234)~~ — **SHIPPED 2026-09-07.** Extends Phase 1 (SHIPPED 2026-08-06, 20-day forward-volatility EWMA) with a second, orthogonal measurement: unsigned earnings-move magnitude, scored against the app's own already-live `earnings_advisor._estimate_move` heuristic (no new model) and a trailing-6-print median baseline drawn from this same ledger's own prior matured rows (live-only, no backfill). Before build, a SECOND `planner` pass (same day as pickup) resolved three implementation gaps a code-level investigation surfaced that the original 2026-09-04 design didn't cover — unknown BMO/AMC earnings timing (exclude, never guess), exactly-one-row-per-event idempotency across a missed cron day (window trigger + existence check, no upsert-key change), and reschedule invalidation within the existing schema (new `db.withdraw_unmatured_model_prediction()`, race-safe by construction against the maturation cron). Verdict PROCEED, no DDL change needed — matching per the user's own explicit instruction going in ("if any gap can't be resolved fully, don't hand this to implementer, rethink the approach"). `implementer` built to the resolved spec. **First Opus reviewer pass caught a real blocking bug the build's own 87 new tests didn't exercise:** the maturation cron's reschedule backstop ran BEFORE its own maturity gate, so a still-upcoming pending row's fresh lookup legitimately returned its own frozen date (gap=0) — misread as a reschedule and withdrawn, the SAME cron run it was written, every day, which would have kept the ledger permanently empty while logs showed plausible "written / withdrawn" churn. The reviewer reproduced this by direct execution, not inference. Fixed by reordering the maturity gate first + tightening the reschedule bound as defense-in-depth, with a new regression test (a not-yet-due row is left completely untouched) that the original 87 tests had no equivalent of — every existing maturation test happened to use an already-past frozen date. Second reviewer pass: SHIP, 0 blocking. Full suite 5098 passed. Memory `project_predictive_shadow_modeling`; plan `docs/plans/predictive-modeling-shadow-layer.md`. Phase 3 (gated, months out, full `planner`+`reviewer`, fresh explicit approval) = wire ONE validated signal into ONE protective gate as **tightening-only**, only if skill score > 0 across a real window incl. ≥1 stress episode — never loosens a gate, never manufactures a buy. Untouched by any of this.
- **~~Self Track Record — SELL-side~~ — SHIPPED 2026-08-25 as F-256, plus F-257's situational-category tag. REMOVED FROM THIS LIST 2026-08-27 after a queue-vs-code sweep found it still described as "explicitly deferred, not started" two days after it shipped** — the exact Definition-of-Done step 3 drift that step exists to catch, so it is recorded here rather than silently deleted. Full spec: `docs/requirements.md` F-256/F-257; memory `project_self_track_record`. Nothing left to build.
- **Self Track Record — decision-moment mirror.** **REJECTED 2026-08-06** (same day as ship), don't re-propose without engaging the reasoning: a `planner` (Opus) pass rejected porting F-231's live-mirror pattern here because F-231's own design already drew a line between action-rate mirrors (safe live — a fact about how you behave) and outcome/alpha mirrors (unsafe live — F-231 excluded its own opening-window pattern for exactly this reason). Self Track Record's entire signal is an alpha number, zero action-rate content — it's the excluded class, not the shipped class; showing it live would editorialize on the whole pending decision at the worst possible moment. A stripped-down "just show the scope fact, no number" fallback was also considered and rejected (fires as noise if broad, singles out exactly the buys most likely to feel surveilled if narrowed). User confirmed leave as My-Edge-only, declined even a Summary pointer-card alternative. **Trigger to revisit:** an explicit user re-ask with new reasoning — not "F-231 did it so this should too," which is the exact reasoning that led here and was rejected. Memory `project_self_track_record`.
- **Today's-P&L cash/flows + broker reconciliation** — **PARKED 2026-06-24 (Tier B is sufficient).** Robinhood Agentic Trading / MCP path analyzed (read access to all accounts via `agent.robinhood.com/mcp/trading`; Claude is a supported agent; auth is interactive/desktop-only so the app/cron can't be the MCP client → Model C = Claude bridges RH→Supabase snapshot→app renders). **Decision: HOLD until beta matures** (user chose). Full analysis + revisit triggers in memory `project_today_pnl_scope`. Don't re-propose unless asked.
- **NYSE calendar — checked 2026-09-01, genuinely blocked on NYSE, not an oversight.** `NYSE_HOLIDAYS`/`NYSE_EARLY_CLOSES` (`constants.py`) run through 2028 (`MARKET_CALENDAR_LAST_YEAR = 2028`). Checked NYSE's own official calendar (nyse.com/markets/hours-calendars) directly: it explicitly states coverage for "2026, 2027, and 2028" only — NYSE has not yet published 2029 dates. Third-party sites showing a "2026-2029" table are computing 2029 from the standard federal-holiday rules (fixed dates + Nth-weekday rules + a Good Friday/Easter calculation), not citing an NYSE announcement — writing those into `constants.py` as if confirmed would violate this file's own zero-hallucination doc-integrity standard applied to code, not just docs. **Trigger to revisit:** NYSE actually publishes a 2029 calendar (check periodically, or when `reference_shelf.py`'s staleness warning starts firing as 2028 approaches).
- ~~`SP500_SECTOR_WEIGHTS` refresh~~ — **REFRESHED 2026-09-07, ahead of the 2026-09-29 tripwire.** Re-fetched the English Wikipedia "S&P 500" GICS sector-weighting table (now dated "as of 2026-08-12" per the source article) and transcribed all 11 sector values directly from the fetch — none recalled from memory, per this file's own doc-integrity standard. All 11 sectors moved slightly (e.g. Info Tech unchanged at 37.4%, Financials 12.0→12.2%, Energy 2.97→3.27%, Real Estate 1.84→1.79%); no sector added/removed. Updated `portfolio.py`'s `SP500_SECTOR_WEIGHTS` dict + its source comment, `reference_shelf.py`'s `as_of` date (now 2026-08-12, next tripwire ~2026-11-10 per the 90-day shelf life), and the `docs/architecture.md` F-223 row's as-of citation. Display-only reference data (never gates) — but `portfolio.py` sits in `_GATE_FILES`, so the commit hook required an Opus review regardless; the reviewer caught a real miss (FIX-FIRST/1) — I'd updated three doc/comment locations but missed the one date the user actually sees on screen, the F-223 tilt-chart caption at `app.py:19161`, which still said "as of 2026-07-01." Fixed same pass; confirmation pass SHIP/0-blocking.
- **Deterioration-card hysteresis** — **PARKED until a flicker is actually observed** (user had NOT noticed any toggling as of 2026-06-28; the existing 2-of-3 below-MA entry confirmation + settling grace already damp most noise). **NOT the "small UX polish" it was once labelled:** it changes WATCH/TRIM/EXIT *recommendation* behaviour (decision-logic → Opus-review-worthy), needs a **new policy constant** (the asymmetric clear-band buffer → `constants.py`, a policy decision to set with the user). **Data gap closed 2026-07-21:** per-ticker day-over-day tier state now exists via `exit_signals` (shipped 2026-07-18, commit `f86147d`; cron-covered as of 2026-07-21 — see docs/shipped-log.md), so this is purely a decision-logic + policy-constant item now, not a missing-data problem. Trigger to revisit: a deterioration card seen toggling on/off across days. `exit_advisor.classify_deterioration_tier`; memory project_exit_discipline.
- ~~Broker CSV v2 — cash events~~ — **SHIPPED 2026-09-11 as F-268, NOT moot.** The 2026-09-01 closure claimed F-244 SnapTrade's broker cron "already renders a monthly dividends/interest/fees trend sourced from `db.load_snaptrade_income_events`, populated automatically by the broker cron — no manual CSV upload needed." **This was incorrect.** Live testing on 2026-09-10 confirmed: **SnapTrade's Robinhood connector never returns DIVIDEND/INTEREST/FEE type activities via `get_account_activities`, despite these types being documented valid in the SnapTrade API schema** (23 consecutive days of cron logs show `0 income events, ignored={}`). This is a Robinhood-connector-specific gap, not an API design gap. F-268 shipped a CSV import path: user downloads their Robinhood statement (Account → Statements → Export as CSV) and uploads via a new **"📥 Import from Robinhood Statement"** expander below the Cash Activity chart (owner-only). New pure function `stock_analyzer/broker_sync.py::parse_robinhood_csv_income()` maps Robinhood transaction codes to event types and generates deterministic dedup keys; new `stock_analyzer/db.py::save_income_events_from_csv()` upserts into `snaptrade_income_events`. **Production bug fixed during testing:** the unique index was partial (`WHERE snaptrade_txn_id IS NOT NULL`), so upserts without the predicate threw "no matching constraint" and failed silently (false "0 new rows"). DDL fix applied in production 2026-09-11; re-upload of YTD CSV imported all 30 real rows correctly. Awareness only — never feeds Modified Dietz return. Opus reviewer: SHIP, 0 blocking. See `docs/requirements.md` F-268; memory `project_snaptrade_broker_integration`.
- ~~Research Scorecard Phase 3 — Engine vs Analyst Calibration~~ — **SHIPPED 2026-08-29, production-verified same day.** Trigger (`composite_score_at_save` populated on ≥20 `analyst_coverage` rows) measured live and met (26 of 547). A `planner` scoping pass corrected the plan doc's stale spec before build — reuses the Scorecard's existing 30-day window (not the doc's stale "60 days"), excludes Hold/Mixed consensus rows from the 2×2 entirely rather than forcing them onto Buy/Sell, and "who was right" reuses each row's own `directional_hit` rather than a second metric. `analyst_intel.consensus_side()` + `calibration_matrix()`, new constant `ANALYST_CALIBRATION_MIN_CASES` (5), 16 tests, Opus reviewer SHIP/0-blocking, commit `e31060b`. **Owner-verified on screen 2026-08-29:** the two "Engine < 65" cells render empty ("No cases yet") on real data. **That was mis-diagnosed as statistical selection bias and was actually STRUCTURAL — found and FIXED 2026-09-07.** `composite_score_at_save` was resolved only from `_port_df_enriched`, i.e. HELD names, so those cells were structurally STARVED — not strictly impossible (a held name that had deteriorated below the floor could still land there; `calibration_matrix` splits purely on `cs >= COMPOSITE_BUY` with no held-status condition) but missing the one case the feature exists for: an engine-skeptical read on a name the owner does NOT own, which resolved to `None` and was excluded outright. The resolver now validates the held bundle through a new pure `analyst_intel.trustworthy_composite()` (all three of `daily_briefing`'s trust legs — `stale_as_of`, `fund_cache_age_days` vs `GROW_TODAY_MAX_FUND_AGE_DAYS`, `fundamentals_available`/`val_available` — plus a 0-100 sanity bound) and FALLS THROUGH on rejection to a Save-click-only `_parallel_load_all` backfill, so a non-held ticker gets a real score; `None` is still recorded on failure rather than any fallback number, and unresolved tickers are disclosed by name. **Why this mattered beyond the feature:** the `buy_disagree` cell is the instrument the owner uses to decide whether a ~$299/yr CNBC Pro subscription is worth renewing, so a blind cell meant that decision had no evidence behind it. Opus reviewer (claude-opus-5) ran three passes — FIX-FIRST/5, then FIX-FIRST/1, then SHIP. Worth knowing what independent review caught that the author had not: the first fix's own tier 1 read the UNVALIDATED `port_df["Score"]` column (same quantity as `total`, but no trust guard, and `float(nan)` passes it) and PREEMPTED the validated path; the disclosure caption was discarded unseen by the handler's own `st.rerun()`; and the second pass caught that this very CLAUDE.md paragraph overstated the defect as "could never populate", which is the doc-integrity standard applied to a post-mortem rather than to a constant. Full spec: `docs/requirements.md` F-154c; memory `project_analyst_coverage`.
- ~~`_ac_resolve_price_at_save` NaN path AND back-dated article accuracy bug~~ — **BOTH CLOSED.** NaN half closed 2026-09-07 (`analyst_intel.valid_anchor_price()`, Opus SHIP/0-blocking). The back-dated half — a Monday catch-up paste of a Friday article stamping *today's* live price as `price_at_article_date` instead of the article's own date, unrecoverably since the backfill script only revisits `NULL` prices — was **fixed 2026-09-09 in commit `89fe79a`** (Opus reviewer SHIP, 0 blocking): `_ac_resolve_price_at_save` now returns `None` unless `article_date == today_et()`, deferring to the date-correct "Fetch" button or the weekly backfill. **This queue entry itself was stale for 3 days** — the code fix shipped on schedule but its doc-sync (this file, `docs/requirements.md`, `docs/shipped-log.md`) was never done, so it sat listed here as "still open" after it was actually done. Caught 2026-09-12 while about to re-build already-shipped work — `git log -S` on the guard text surfaced the existing commit before any duplicate code was written. Full detail: `docs/shipped-log.md`; memory `project_analyst_coverage`.
- **Portfolio Q&A — forward-looking "current status" intent** — **PARKED 2026-08-02, explicitly declined, NOT started.** A `current_status` Ask-tab intent narrating the engine's already-decided live state for a ticker (composite tier, active WATCH/TRIM/EXIT, an active reduce call) was proposed alongside the shipped v2 "complete the circle" enhancements (reasoning surfacing, multi-turn chat, Pre-Mortem cross-reference — see F-225 in requirements.md) and the user chose to keep the "AI narrates, never originates" redline closed for this pass rather than build it. **Trigger to revisit: an explicit user re-ask — a pure policy call, not a data/engineering milestone.** If ever picked up, treat it as needing a fresh explicit approval and its own Opus design review, not an implied green light from this entry. Full writeup in `docs/plans/portfolio-qa.md` (Parked section) and memory `project_portfolio_qa`.

**Recently shipped:** moved to [docs/shipped-log.md](docs/shipped-log.md) on 2026-07-22 — same content, verbatim, just relocated so it isn't injected into every session's context (it had grown to ~76KB / 86% of this file). Grep/Read it for the "did we already ship this?" check in Definition-of-Done step 3, or for full feature history.
